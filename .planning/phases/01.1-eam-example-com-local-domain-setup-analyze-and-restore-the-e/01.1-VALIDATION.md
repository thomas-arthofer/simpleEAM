---
phase: 01.1
slug: eam-example-com-local-domain-setup-analyze-and-restore-the-e
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-27
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — infra/config/documentation phase, no unit test framework applies |
| **Config file** | none |
| **Quick run command** | `docker exec nextgen-eam-server-1 getent hosts auth.example.com` (container-side DNS sanity check) |
| **Full suite command** | End-to-end sequence: `docker ps --filter name=traefik`, `docker exec nextgen-eam-server-1 getent hosts auth.example.com`, `curl -fsSk https://api.example.com/health`, manual browser login walkthrough via `https://eam.example.com` |
| **Estimated runtime** | ~2 minutes (automated checks) + ~2 minutes manual browser login |

---

## Sampling Rate

- **After every task commit:** Run `docker exec nextgen-eam-server-1 getent hosts auth.example.com` — must resolve to the local Traefik container's IP on the `traefik` network, not a public IP
- **After every plan wave:** Run the full suite command sequence above
- **Before `/gsd-verify-work`:** Manual end-to-end login via `https://eam.example.com` must succeed
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 0 | GOAL-01.1 | — | `traefik.local.yml` snippet created, valid compose syntax | smoke | `docker compose -f traefik.local.yml config` | ❌ W0 | ⬜ pending |
| 01.1-01-02 | 01 | 1 | GOAL-01.1 | — | `.env` cert-resolver values empty for local use | smoke | `grep -E '^(TRAEFIK|TEMPORAL_UI)_CERTRESOLVER=$' .env` | ✅ | ⬜ pending |
| 01.1-01-03 | 01 | 1 | GOAL-01.1 | — | `/etc/hosts` entries present for all 6 subdomains | manual | `getent hosts eam.example.com` (host-side) | ✅ | ⬜ pending |
| 01.1-01-04 | 01 | 2 | GOAL-01.1 | — | Local Traefik running with network aliases; container-side DNS resolves correctly | smoke | `docker exec nextgen-eam-server-1 getent hosts auth.example.com` | ✅ | ⬜ pending |
| 01.1-01-05 | 01 | 2 | GOAL-01.1 | — | End-to-end Keycloak login succeeds via `https://eam.example.com` | manual | Browser walkthrough (see Manual-Only Verifications) | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Note: No formal `REQ-XX` IDs are tied to Phase 01.1 in REQUIREMENTS.md (it is an urgent inserted phase, "Requirements: TBD" in ROADMAP.md). `GOAL-01.1` is used as a placeholder requirement tag referring to the phase goal itself; the planner may refine Task IDs/wave numbers to match the actual PLAN.md structure.*

---

## Wave 0 Requirements

- [ ] `traefik.local.yml` — new standalone Compose snippet (Pattern 2 from RESEARCH.md) providing the local Traefik instance with network aliases; does not exist yet.

*No test framework installation needed — this phase has no code-level test framework gap (docs/config phase, per RESEARCH.md Validation Architecture section).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end Keycloak SSO login via `https://eam.example.com` | GOAL-01.1 | Requires a real browser, accepting a self-signed TLS warning (D-06), and interactive Keycloak login form submission — cannot be scripted without a browser automation harness, which is out of scope for this narrow infra-restoration phase | 1. Ensure `/etc/hosts` and `.env` changes are applied and local Traefik is running. 2. Open `https://eam.example.com` in a browser. 3. Accept the self-signed certificate warning. 4. Confirm the client app loads and redirects to Keycloak login. 5. Log in with valid credentials. 6. Confirm redirect back to the client and that an authenticated GraphQL call succeeds (e.g., a page that loads data). |
| Browser-side hostname resolution for all 6 subdomains | GOAL-01.1 | Host OS DNS resolution (`/etc/hosts`) is outside any automated test harness for this repo | `getent hosts eam.example.com api.example.com auth.example.com neo4j.example.com room.example.com temporal.example.com` — each must resolve to `127.0.0.1`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`traefik.local.yml`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
