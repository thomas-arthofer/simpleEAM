---
phase: 01.1
slug: eam-example-com-local-domain-setup-analyze-and-restore-the-e
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-27
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — infra/config/documentation phase, no unit test framework applies |
| **Config file** | none |
| **Quick run command** | `docker exec nextgen-eam-server-1 getent hosts auth.example.com` (container-side DNS sanity check) |
| **Full suite command** | End-to-end sequence: `docker compose config --quiet`, `docker ps --filter name=traefik`, `docker exec nextgen-eam-server-1 getent hosts auth.example.com`, `docker compose logs server \| grep -i UNABLE_TO_VERIFY_LEAF_SIGNATURE` (expect no matches), `curl -fsSk https://api.example.com/health`, manual browser login walkthrough via `https://eam.example.com` |
| **Estimated runtime** | ~2 minutes (automated checks) + ~2 minutes manual browser login |

---

## Sampling Rate

- **After every task commit:** Run `docker exec nextgen-eam-server-1 getent hosts auth.example.com` — must resolve to the local Traefik container's IP on the `eam-network`, not a public IP
- **After every plan wave:** Run the full suite command sequence above
- **Before `/gsd-verify-work`:** Manual end-to-end login via `https://eam.example.com` must succeed
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 0 | GOAL-01.1 | T-01.1-01 | `docker-compose.override.yml` + `local/traefik/*` + `local/make-local-ca.sh` + `local/set-admin-password.sh` ported from verified prior art, valid compose syntax, auto-merged with `compose.yml` | smoke | `docker compose config --quiet` | ✅ (ported) | ✅ done |
| 01.1-01-02 | 01 | 1 | GOAL-01.1 | — | `.env` cert-resolver values empty for local use | smoke | `grep -E '^(TRAEFIK|TEMPORAL_UI)_CERTRESOLVER=$' .env` | ✅ | ⬜ pending |
| 01.1-01-03 | 01 | 1 | GOAL-01.1 | — | DNS resolution present for all 6 subdomains (Option A `/etc/hosts` or Option B firewall DNS) | manual | `getent hosts eam.example.com` (host-side) | ✅ | ⬜ pending |
| 01.1-01-04 | 01 | 2 | GOAL-01.1 | T-01.1-01 | Local Traefik running with network aliases on `eam-network`; container-side DNS resolves correctly; local CA generated and trusted | smoke | `docker exec nextgen-eam-server-1 getent hosts auth.example.com` | ✅ | ⬜ pending |
| 01.1-01-05 | 01 | 2 | GOAL-01.1 | T-01.1-01 | `server` container trusts the local CA via `NODE_EXTRA_CA_CERTS`; no JWKS TLS verification failures | smoke | `docker compose logs server \| grep -i UNABLE_TO_VERIFY_LEAF_SIGNATURE` (expect no output) | ✅ | ⬜ pending |
| 01.1-01-06 | 01 | 2 | GOAL-01.1 | — | Keycloak realm `admin` user has a password set (`realm-export.json` ships none) | smoke | `./local/set-admin-password.sh` exits 0 | ✅ | ⬜ pending |
| 01.1-01-07 | 01 | 2 | GOAL-01.1 | — | End-to-end Keycloak login succeeds via `https://eam.example.com` | manual | Browser walkthrough (see Manual-Only Verifications) | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Note: No formal `REQ-XX` IDs are tied to Phase 01.1 in REQUIREMENTS.md (it is an urgent inserted phase, "Requirements: TBD" in ROADMAP.md). `GOAL-01.1` is used as a placeholder requirement tag referring to the phase goal itself; the planner may refine Task IDs/wave numbers to match the actual PLAN.md structure.*

---

## Wave 0 Requirements

- [x] `docker-compose.override.yml`, `local/traefik/traefik.yml`, `local/traefik/dynamic/tls.yml`, `local/make-local-ca.sh`, `local/set-admin-password.sh`, `docs/lokale-https-domains.md` — ported verbatim from the verified working reference checkout (RESEARCH.md "Verified Prior Art"); `docker compose config --quiet` confirms a clean merge with `compose.yml` (exit 0).

*No test framework installation needed — this phase has no code-level test framework gap (docs/config phase, per RESEARCH.md Validation Architecture section).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end Keycloak SSO login via `https://eam.example.com` | GOAL-01.1 | Requires a real browser, importing/trusting the local root CA (per-origin in Chrome, see `docs/lokale-https-domains.md`), and interactive Keycloak login form submission — cannot be scripted without a browser automation harness, which is out of scope for this narrow infra-restoration phase | 1. Ensure the local CA is trusted, DNS (Option A or B) and `.env` changes are applied, local Traefik is running (`docker compose up -d`), and `./local/set-admin-password.sh` has been run. 2. Open `https://eam.example.com` in a browser — no certificate warning expected once the CA is trusted. 3. Confirm the client app loads and redirects to Keycloak login. 4. Log in with valid credentials (admin password from step 1, or another realm user). 5. Confirm redirect back to the client and that an authenticated GraphQL call succeeds (e.g., a page that loads data). |
| DNS resolution for all 6 subdomains (host + container side) | GOAL-01.1 | Host OS DNS resolution (`/etc/hosts` or firewall DNS) and Docker embedded DNS are outside any automated test harness for this repo | `getent hosts eam.example.com api.example.com auth.example.com neo4j.example.com room.example.com temporal.example.com` on the host (Option A: must resolve to `127.0.0.1`; Option B: must resolve to this host's LAN IP); `docker exec nextgen-eam-server-1 getent hosts auth.example.com` inside the `server` container (must resolve to a private Docker network IP, never a public IP). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (`docker-compose.override.yml`, `local/traefik/*`, `local/make-local-ca.sh`, `local/set-admin-password.sh` — all ported and verified via `docker compose config --quiet`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
