---
phase: 01.1
slug: eam-example-com-local-domain-setup-analyze-and-restore-the-e
verified: 2026-07-30T00:00:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: false
note: "Retroactive verification. All functional work (Tasks 1-3) and the developer's end-to-end browser login sign-off were already completed and confirmed on 2026-07-27 (see 01.1-SUMMARY.md, 01.1-VALIDATION.md, STATE.md). This artifact closes a tooling gap — no VERIFICATION.md was generated at the time — by independently re-confirming the codebase evidence for each must-have without re-executing any tasks."
---

# Phase 01.1: eam.example.com local domain setup — Verification Report

**Phase Goal:** Restore `https://eam.example.com` local routing, most importantly Keycloak-backed SSO login, so it works end-to-end again. Narrow restoration only — Phase 1's localhost-first Supported Path stays the unchanged default.
**Verified:** 2026-07-30 (retroactive)
**Status:** passed
**Re-verification:** No — initial verification (retroactive, closing a tooling gap; the phase itself was completed and developer-confirmed 2026-07-27)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Ein Entwickler kann den lokalen Traefik starten und `https://eam.example.com` ohne Zertifikatswarnung erreichen, nachdem der lokale Root-CA importiert wurde. | ✓ VERIFIED | `docker-compose.override.yml` defines a `traefik:3.7.8` service mounting `./local/certs:/etc/traefik/certs:ro` and `./local/traefik/{traefik.yml,dynamic}`; `local/make-local-ca.sh` (executable, present) generates the wildcard/root CA; SUMMARY.md documents `openssl s_client` against `127.0.0.1:443` returning `Verify return code: 0 (ok)` and `curl -sk https://eam.example.com/` returning 200. Developer confirmed browser login worked 2026-07-27 (STATE.md, SUMMARY.md). |
| 2 | Container-zu-Container-Aufloesung von `auth.example.com` zeigt auf den lokalen Traefik-Container, UND `server` vertraut dem lokalen CA via `NODE_EXTRA_CA_CERTS`. | ✓ VERIFIED | `docker-compose.override.yml` (read directly): `traefik` service carries `eam-network` aliases for all 6 hostnames including `auth.example.com`; `server`/`analytics-worker`/`client` services mount `./local/certs/rootCA.pem` and set `NODE_EXTRA_CA_CERTS=/etc/ssl/local/rootCA.pem`. SUMMARY.md records `docker exec nextgen-eam-server-1 getent hosts auth.example.com` resolving to the private `eam-network` IP and `docker compose logs server` showing no `UNABLE_TO_VERIFY_LEAF_SIGNATURE` matches. |
| 3 | Ein Endnutzer kann sich über `https://eam.example.com` per Keycloak SSO anmelden (inkl. gesetztem Admin-Passwort via `set-admin-password.sh`) und danach einen authentifizierten GraphQL-Aufruf ausführen. | ✓ VERIFIED | `local/set-admin-password.sh` present and executable. SUMMARY.md ("human_judgment: true, rationale: Confirmed by the developer on 2026-07-27 ('pass - user login funktioniert')") and STATE.md ("Phase 01.1 complete: developer confirmed E2E Keycloak login via eam.example.com") both independently attest to a completed, developer-signed-off browser walkthrough. A real follow-up defect (Chrome per-origin cert trust blocking `api.example.com`/`room.example.com` GraphQL/WS calls until each origin is visited once) was found and is now documented as a Troubleshooting section in `docs/lokale-https-domains.md` (confirmed present at line 60: "Troubleshooting: Login klappt, aber GraphQL-Requests schlagen fehl"). |
| 4 | Phase 1's Supported Path (localhost-first) bleibt unverändert Standard; README dokumentiert den SSO-Login-Caveat für den Optional Path. | ✓ VERIFIED | `README.md` line 102 still labels the section "Optional Path: Traefik/HTTPS parity (not required)"; line 107 contains the explicit caveat: "SSO login via the Supported (localhost) Path above does not currently work — ... The Optional Path below is the one that supports an end-to-end login today." Guardrail line and Supported Path sections (§3-4) untouched. |
| 5 | `compose.yml`, `auth/src/realm-export.json`, `k8s/realm-export.json` bleiben unverändert. | ✓ VERIFIED | `git log` shows the most recent commits touching these three files are dated 2026-06-09 (`839860e`, `2913143`, `35be1fc`), all well before Phase 01.1 started (2026-07-27) — no phase-01.1-era commit touches them. |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docker-compose.override.yml` | Additive override (compose.yml unchanged), network aliases for 6 subdomains, CA mounts/`NODE_EXTRA_CA_CERTS` for server/analytics-worker/client | ✓ VERIFIED | Present at repo root; content directly inspected — matches all described elements plus a documented `neo4j mem_limit: 4g` regression fix and the `temporal-ui` certresolver label override for the `${VAR:-default}` fallback quirk. |
| `local/traefik/traefik.yml`, `local/traefik/dynamic/tls.yml` | Static Traefik config + dynamic default-cert declaration | ✓ VERIFIED | Both files exist (`local/traefik/traefik.yml`, `local/traefik/dynamic/tls.yml`). |
| `local/make-local-ca.sh`, `local/set-admin-password.sh` | Executable helper scripts | ✓ VERIFIED | Both present with `rwxrwxr-x` permissions (executable). |
| `local/certs/` excluded via `.gitignore` | Generated certs/keys never committed | ✓ VERIFIED | `.gitignore` line 95: `local/certs/`. |
| `README.md` §5 "Optional Path" | Full restoration steps, 6-hostname list, D-09 login caveat, `docs/lokale-https-domains.md` reference | ✓ VERIFIED | grep confirms all 6 hostnames, `make-local-ca.sh`, `set-admin-password.sh`, `docker-compose.override.yml`, `getent hosts`, `lokale-https-domains.md`, and the login caveat text are present. |
| `env.template` updated comment qualifiers | Comment guidance above `TRAEFIK_CERTRESOLVER`/`TEMPORAL_UI_CERTRESOLVER`, default values unchanged | ✓ VERIFIED | Lines 46/83 still show default placeholder `my-traefik-cert-resolver`; line 80/82 carry the added qualifier comment referencing the `docker-compose.override.yml` labels override. |
| `.planning/.../01.1-VALIDATION.md` | Updated to `status: validated`, `nyquist_compliant: true` | ✓ VERIFIED | Frontmatter confirms both fields; all 7 task rows marked done, sign-off checklist fully checked. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `docker-compose.override.yml` network aliases (`eam-network`) | `compose.yml` Traefik `Host()` router labels | Same alias hostnames resolved by both browser and container DNS | ✓ WIRED | Aliases (`eam.example.com`, `api.example.com`, `auth.example.com`, `room.example.com`, `neo4j.example.com`, `temporal.example.com`) declared on the `traefik` service's `eam-network` attachment; matches the existing label-driven routing pattern noted in CONTEXT.md. |
| `local/make-local-ca.sh` rootCA.pem | `NODE_EXTRA_CA_CERTS` in server/analytics-worker/client | Volume mount + env var | ✓ WIRED | All three services mount `./local/certs/rootCA.pem:/etc/ssl/local/rootCA.pem:ro` and set `NODE_EXTRA_CA_CERTS=/etc/ssl/local/rootCA.pem`. |
| `local/set-admin-password.sh` | Keycloak admin user | `kcadm.sh` password set | ✓ WIRED | Script present/executable; SUMMARY.md documents it was run and the admin login subsequently succeeded. |
| README Optional Path steps | `env.template` certresolver qualifier comments | Documentation cross-reference | ✓ WIRED | Both sets of guidance present and consistent (empty local override vs. versioned default placeholder). |
| `auth.example.com` container DNS | `server/src/auth/auth-jwks.ts` JWKS fetch | Alias resolution + trusted TLS chain | ✓ WIRED | SUMMARY.md confirms no `UNABLE_TO_VERIFY_LEAF_SIGNATURE` in `server` logs after the fix, and the developer-confirmed login proves the JWKS fetch/token validation path works end-to-end. |

### Requirements Coverage

No formal `REQ-XX` IDs are assigned to this urgent inserted phase (ROADMAP.md: "Requirements: TBD"); `GOAL-01.1` is used as the placeholder tag across PLAN/VALIDATION/SUMMARY and is covered by all 5 truths above (no orphaned requirements).

### Anti-Patterns Found

None. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across all phase-modified files (`docker-compose.override.yml`, `local/make-local-ca.sh`, `local/set-admin-password.sh`, `local/traefik/traefik.yml`, `local/traefik/dynamic/tls.yml`, `docs/lokale-https-domains.md`) returned no matches.

### Human Verification Required

None outstanding. The one inherently human-only check (Task 3, end-to-end browser SSO login) was already performed and explicitly signed off by the developer on 2026-07-27 ("pass - user login funktioniert" — 01.1-SUMMARY.md; corroborated by STATE.md). The follow-up Chrome per-origin cert-trust gotcha this surfaced (api/room GraphQL/WS calls needing their own cert exception) is documented as a Troubleshooting section in `docs/lokale-https-domains.md`, not an open gap.

### Gaps Summary

No gaps. All 5 must-have truths, all 7 required artifacts, and all 5 key links are verified present, substantive, and wired. This VERIFICATION.md is a retroactive artifact only — it documents evidence for work that was already completed, validated (`01.1-VALIDATION.md`: `status: validated`, `nyquist_compliant: true`), and developer-confirmed on 2026-07-27; no task re-execution or file modification (beyond this report) was performed.

---

*Verified: 2026-07-30 (retroactive)*
*Verifier: the agent (gsd-verifier)*
