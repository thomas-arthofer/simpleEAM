---
phase: 01.1
slug: eam-example-com-local-domain-setup-analyze-and-restore-the-e
verified: 2026-08-07T08:48:19Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 5/5
  gaps_closed: []
  gaps_remaining: []
  regressions: []
note: "Re-verification triggered by a staleness flag: 01.1-SUMMARY.md and 01.1-VALIDATION.md showed as dirty (newer mtime than the prior VERIFICATION.md's commit) in the working tree. Independently confirmed via `git diff` that the dirty changes are purely cosmetic (auto-formatter converted double-quoted YAML/markdown scalars to single-quoted, reflowed the `coverage:` array and several markdown tables onto wrapped/padded lines, added blank lines around code fences and swapped `*em*` for `_em_`) — no substantive content changed in either file, nor in README.md/docs/lokale-https-domains.md (also dirty, also confirmed cosmetic-only). A full conversational UAT (01.1-UAT.md, completed 2026-08-07) re-ran all 4 checkpoints — including a live Cold Start Smoke Test and a manually re-confirmed end-to-end Keycloak SSO login — with 0 issues. This report independently re-verified all 5 must-have truths, all 7 artifacts, and all 5 key links against the current codebase and a live running stack; no regressions found."
---

# Phase 01.1: eam.example.com local domain setup — Verification Report

**Phase Goal:** Restore `https://eam.example.com` local routing, most importantly Keycloak-backed SSO login, so it works end-to-end again. Narrow restoration only — Phase 1's localhost-first Supported Path stays the unchanged default.
**Verified:** 2026-08-07
**Status:** passed
**Re-verification:** Yes — regression check after a staleness flag caused by cosmetic-only dirty changes to 01.1-SUMMARY.md/01.1-VALIDATION.md (and README.md/docs/lokale-https-domains.md). Original verification: 2026-07-30, status passed, 5/5.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Ein Entwickler kann den lokalen Traefik starten und `https://eam.example.com` ohne Zertifikatswarnung erreichen, nachdem der lokale Root-CA importiert wurde. | ✓ VERIFIED | Re-read `docker-compose.override.yml` directly (current file, clean/committed): `traefik:3.7.8` service still mounts `./local/certs:/etc/traefik/certs:ro` and `./local/traefik/{traefik.yml,dynamic}`. `local/make-local-ca.sh` still present, `rwxrwxr-x`. Live check: `nextgen-eam-traefik-1` container is `Up 10 days`. `docker compose config --quiet` re-run today — clean merge, exit 0. UAT (01.1-UAT.md, 2026-08-07) re-confirms this via the passing Cold Start Smoke Test and the manually re-confirmed T3 login checkpoint. |
| 2 | Container-zu-Container-Aufloesung von `auth.example.com` zeigt auf den lokalen Traefik-Container, UND `server` vertraut dem lokalen CA via `NODE_EXTRA_CA_CERTS`. | ✓ VERIFIED | Re-read `docker-compose.override.yml`: `traefik` service still carries `eam-network` aliases for all 6 hostnames including `auth.example.com`; `server`/`analytics-worker`/`client` still mount `./local/certs/rootCA.pem` and set `NODE_EXTRA_CA_CERTS=/etc/ssl/local/rootCA.pem`. Live re-check today: `docker exec nextgen-eam-server-1 getent hosts auth.example.com` → `172.19.0.7 auth.example.com` (private `eam-network` IP, not public); `docker compose logs server \| grep -i UNABLE_TO_VERIFY_LEAF_SIGNATURE` → no matches. |
| 3 | Ein Endnutzer kann sich über `https://eam.example.com` per Keycloak SSO anmelden (inkl. gesetztem Admin-Passwort via `set-admin-password.sh`) und danach einen authentifizierten GraphQL-Aufruf ausführen. | ✓ VERIFIED | `local/set-admin-password.sh` still present/executable. 01.1-UAT.md (completed 2026-08-07, `status: testing` with `passed: 4/4, issues: 0`) records test #2 "End-to-end Keycloak SSO login via https://eam.example.com" → `result: pass`, manually confirmed by the developer just now (not merely re-quoting the 2026-07-27 sign-off — a fresh independent confirmation). `docs/lokale-https-domains.md` line 60 still documents the known Chrome per-origin cert-trust Troubleshooting section for `api.example.com`/`room.example.com`; diff-confirmed this file's dirty working-tree state is cosmetic-only (emphasis-marker/table-padding reformatting), not a content regression. |
| 4 | Phase 1's Supported Path (localhost-first) bleibt unverändert Standard; README dokumentiert den SSO-Login-Caveat für den Optional Path. | ✓ VERIFIED | `README.md` (current, dirty-but-cosmetic-only per `git diff`) line 102 still labels the section "Optional Path: Traefik/HTTPS parity (not required)"; line 107 still contains the explicit caveat: "SSO login via the Supported (localhost) Path above does not currently work — ... The Optional Path below is the one that supports an end-to-end login today." `git diff -- README.md` today shows only blank-line-around-code-fence additions in the numbered restoration steps — no wording/content changed. |
| 5 | `compose.yml`, `auth/src/realm-export.json`, `k8s/realm-export.json` bleiben unverändert (im Scope von Phase 01.1). | ✓ VERIFIED | Re-checked `git log` today: `auth/src/realm-export.json` and `k8s/realm-export.json` last touched 2026-04-20 (`811718f`), well before Phase 01.1. `compose.yml` **was** touched by a later, unrelated commit (`eb5414e`, 2026-08-03, "cap json-file log growth on all 16 compose.yml services") — inspected the diff: it only adds a shared `x-logging` anchor + `logging: *default-logging` reference to every service (log-rotation hardening from a separate quick-task, unrelated to routing/TLS/network-alias/Keycloak concerns). This does not touch any Traefik labels, network aliases, or auth config Phase 01.1 depends on, so the phase-01.1-scoped boundary (D-02/D-04/D-05: no *routing-relevant* change to `compose.yml` during this phase) still holds; it is not a regression of this phase's goal. |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docker-compose.override.yml` | Additive override (compose.yml unchanged), network aliases for 6 subdomains, CA mounts/`NODE_EXTRA_CA_CERTS` for server/analytics-worker/client | ✓ VERIFIED | Re-read in full today — clean/committed (no dirty diff). Still matches all described elements plus the documented `neo4j mem_limit: 4g` regression fix and the `temporal-ui` certresolver label override for the `${VAR:-default}` fallback quirk. `docker compose config --quiet` re-run today: exit 0. |
| `local/traefik/traefik.yml`, `local/traefik/dynamic/tls.yml` | Static Traefik config + dynamic default-cert declaration | ✓ VERIFIED | Both files re-read today, unchanged, clean/committed. |
| `local/make-local-ca.sh`, `local/set-admin-password.sh` | Executable helper scripts | ✓ VERIFIED | Both still present with `rwxrwxr-x` permissions (`ls -la` re-run today, mtime 2026-07-27, unchanged). |
| `local/certs/` excluded via `.gitignore` | Generated certs/keys never committed | ✓ VERIFIED | `.gitignore` line 95 re-confirmed today: `local/certs/`. |
| `README.md` §5 "Optional Path" | Full restoration steps, 6-hostname list, D-09 login caveat, `docs/lokale-https-domains.md` reference | ✓ VERIFIED | Fresh grep today re-confirms all 6 hostnames, `make-local-ca.sh`, `set-admin-password.sh`, `docker-compose.override.yml`, `getent hosts`, `lokale-https-domains.md`, and the login caveat text are present. File is dirty in the working tree but `git diff` confirms cosmetic-only (blank lines around code fences). |
| `env.template` updated comment qualifiers | Comment guidance above `TRAEFIK_CERTRESOLVER`/`TEMPORAL_UI_CERTRESOLVER`, default values unchanged | ✓ VERIFIED | Re-grepped today: lines 46/83 still show default placeholder `my-traefik-cert-resolver`; lines 80/82 still carry the qualifier comment. File is clean (not in the dirty set). |
| `.planning/.../01.1-VALIDATION.md` | Updated to `status: validated`, `nyquist_compliant: true` | ✓ VERIFIED | Re-read today — frontmatter still confirms both fields; all 7 task rows still marked done, sign-off checklist still fully checked. Working-tree-dirty but `git diff` confirms cosmetic-only (quote style + table padding), not a content change. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `docker-compose.override.yml` network aliases (`eam-network`) | `compose.yml` Traefik `Host()` router labels | Same alias hostnames resolved by both browser and container DNS | ✓ WIRED | Aliases (`eam.example.com`, `api.example.com`, `auth.example.com`, `room.example.com`, `neo4j.example.com`, `temporal.example.com`) still declared on the `traefik` service's `eam-network` attachment. Live-verified today: `nextgen-eam-traefik-1` container `Up 10 days`, `docker exec nextgen-eam-server-1 getent hosts auth.example.com` resolves to `172.19.0.7` (eam-network IP). |
| `local/make-local-ca.sh` rootCA.pem | `NODE_EXTRA_CA_CERTS` in server/analytics-worker/client | Volume mount + env var | ✓ WIRED | All three services still mount `./local/certs/rootCA.pem:/etc/ssl/local/rootCA.pem:ro` and set `NODE_EXTRA_CA_CERTS=/etc/ssl/local/rootCA.pem` (re-read today). |
| `local/set-admin-password.sh` | Keycloak admin user | `kcadm.sh` password set | ✓ WIRED | Script still present/executable. UAT (2026-08-07) re-confirms the resulting login still succeeds (test #2, `result: pass`). |
| README Optional Path steps | `env.template` certresolver qualifier comments | Documentation cross-reference | ✓ WIRED | Both sets of guidance still present and consistent today (empty local override vs. versioned default placeholder). |
| `auth.example.com` container DNS | `server/src/auth/auth-jwks.ts` JWKS fetch | Alias resolution + trusted TLS chain | ✓ WIRED | Live-verified today: `docker compose logs server \| grep -i UNABLE_TO_VERIFY_LEAF_SIGNATURE` → no matches on the currently-running `server` container (up 3 days), and the UAT-reconfirmed login proves the JWKS fetch/token validation path still works end-to-end. |

### Requirements Coverage

No formal `REQ-XX` IDs are assigned to this urgent inserted phase (ROADMAP.md: "Requirements: TBD"); `GOAL-01.1` is used as the placeholder tag across PLAN/VALIDATION/SUMMARY and is covered by all 5 truths above (no orphaned requirements). Unchanged since 2026-07-30.

### Anti-Patterns Found

None. Re-ran `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` today across all phase-modified files (`docker-compose.override.yml`, `local/make-local-ca.sh`, `local/set-admin-password.sh`, `local/traefik/traefik.yml`, `local/traefik/dynamic/tls.yml`, `docs/lokale-https-domains.md`) — no matches, same as the original verification.

### Dirty Working-Tree Diff Audit (this re-verification's specific trigger)

| File | Dirty? | `git diff` content | Verdict |
|------|--------|---------------------|---------|
| `01.1-SUMMARY.md` | Yes | YAML scalars re-quoted (double → single), `coverage:` block reflowed, one blank line added before a list item | Cosmetic only — no wording/status/evidence changed |
| `01.1-VALIDATION.md` | Yes | YAML scalars re-quoted, markdown tables reflowed/padded, `*text*` → `_text_` emphasis markers | Cosmetic only — no wording/status/evidence changed |
| `README.md` | Yes | Blank lines added around code fences in the numbered restoration steps | Cosmetic only — no wording/content changed |
| `docs/lokale-https-domains.md` | Yes | Markdown table column padding reflowed, `*text*` → `_text_` emphasis markers | Cosmetic only — no wording/content changed |
| `docker-compose.override.yml`, `local/*`, `env.template`, `.gitignore` | No | — | Clean/committed, no diff at all |

Conclusion: the tool-reported "stale" status was a false positive driven by mtime-based staleness heuristics picking up formatter-only reflow, not a substantive regression. No content in any phase-01.1 artifact changed since the 2026-07-30 verification.

### Human Verification Required

None outstanding. The one inherently human-only check (end-to-end browser SSO login) has now been independently reconfirmed twice: by the developer on 2026-07-27, and again today (2026-08-07) via 01.1-UAT.md test #2 (`result: pass`) plus a fresh Cold Start Smoke Test (test #1, `result: pass`). The known Chrome per-origin cert-trust gotcha (api/room GraphQL/WS calls needing their own cert exception) remains documented as a Troubleshooting section in `docs/lokale-https-domains.md`, not an open gap.

### Gaps Summary

No gaps, no regressions. All 5 must-have truths, all 7 required artifacts, and all 5 key links independently re-verified against the current codebase and a live running stack (not just re-quoted from the prior report). The only change since the 2026-07-30 verification was cosmetic auto-formatter reflow in 4 files (confirmed via `git diff`, table above), plus one unrelated later commit (`eb5414e`, log-rotation hardening) touching `compose.yml` outside this phase's routing/auth scope. A fresh conversational UAT (01.1-UAT.md, 2026-08-07) re-ran all 4 checkpoints with 0 issues, including a manually re-confirmed live browser SSO login.

---

*Verified: 2026-08-07*
*Verifier: the agent (gsd-verifier)*
