---
phase: 01.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - traefik.local.yml
  - README.md
  - env.template
  - .planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-VALIDATION.md
autonomous: false
requirements:
  - GOAL-01.1
user_setup:
  - service: local-etc-hosts-and-port-443
    why: "Adding /etc/hosts entries requires interactive sudo on the developer's machine, and starting a local Traefik instance requires host port 443 to be free; neither can be guaranteed or performed non-interactively by the executing agent."
    dashboard_config:
      - task: "Add a new /etc/hosts line with all 6 *.example.com FQDNs (do not edit the existing PVE-managed eam.wien.arthofer.dev block); confirm no other process/container already publishes host port 443 before starting traefik.local.yml."
        location: "Developer's local machine (manual sudo step)"
must_haves:
  truths:
    - "Ein Entwickler kann den lokalen Traefik starten und https://eam.example.com im Browser erreichen (nach Akzeptieren der selbstsignierten Zertifikatswarnung, D-06)."
    - "Container-zu-Container-Aufloesung von auth.example.com zeigt auf den lokalen Traefik-Container im traefik-Netzwerk, nicht auf eine oeffentliche IP, sodass JWKS/Token-Validierung im server-Container funktioniert."
    - "Ein Endnutzer kann sich ueber https://eam.example.com vollstaendig per Keycloak SSO anmelden und danach einen authentifizierten GraphQL-Aufruf erfolgreich ausfuehren."
    - "Phase 1's Supported Path (localhost-first) bleibt unveraendert Standard; README dokumentiert explizit, dass SSO-Login ueber den Supported Path aktuell nicht funktioniert und nur der Optional Path (eam.example.com) End-to-End-Login unterstuetzt (D-01, D-02, D-09)."
    - "compose.yml sowie auth/src/realm-export.json und k8s/realm-export.json bleiben unveraendert (D-02, D-04, D-05)."
  artifacts:
    - "traefik.local.yml existiert im Repo-Root, ist ein eigenstaendiges, gueltiges Compose-File, wird nicht von compose.yml referenziert, und traegt Netzwerk-Aliasse fuer alle 6 Subdomains im externen traefik-Netzwerk."
    - "README.md Abschnitt 5 'Optional Path: Traefik/HTTPS parity' enthaelt lauffaehige Wiederherstellungsschritte (.env-Overrides, /etc/hosts-Zeile, Startbefehl, Alternative fuer bereits laufenden geteilten Traefik, Container-DNS-Verifikationsbefehl) sowie den D-09-Login-Caveat."
    - "env.template enthaelt aktualisierte Kommentar-Qualifier oberhalb von TRAEFIK_CERTRESOLVER/TEMPORAL_UI_CERTRESOLVER, die fuer lokale Nutzung auf leere Werte hinweisen, ohne die Default-Platzhalterwerte selbst zu aendern."
    - ".planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-VALIDATION.md ist nach Abschluss auf status: validated und nyquist_compliant: true aktualisiert."
  key_links:
    - "traefik.local.yml Netzwerk-Aliasse -> compose.yml bestehende Traefik-Labels (Host()-Regeln) -> Browser- und Container-DNS-Aufloesung."
    - "README Optional Path Restaurierungsschritte -> env.template TRAEFIK_CERTRESOLVER/TEMPORAL_UI_CERTRESOLVER-Hinweis -> tatsaechlicher lokaler .env-Override (nicht versioniert)."
    - "auth.example.com Container-DNS -> server/src/auth/auth-jwks.ts KEYCLOAK_URL JWKS-Fetch -> erfolgreicher Keycloak-Login."
---

<objective>
**As a** Developer,
**I want to** die lokale `eam.example.com`-Hostname-Routing wiederherstellen, insbesondere Keycloak-basierten SSO-Login,
**so that** der Docker-Compose-Stack wieder End-to-End unter `https://eam.example.com` funktioniert, ohne Phase 1's localhost-first Supported Path zu veraendern.

Purpose: Dies ist eine enge, dringliche Restaurierung eines zuvor funktionierenden lokalen Routing-Pfads (Root Cause: fehlender lokaler Traefik-Container plus fehlende Container-seitige DNS-Aufloesung fuer `auth.example.com`), kein Redesign des Deployment-Modells und keine Wiederaufnahme der Phase-1-Entscheidung zum Supported Path.
Output: `traefik.local.yml` (neu, eigenstaendig, nicht von compose.yml referenziert), erweiterte README.md/env.template-Dokumentation im bestehenden Optional-Path-Abschnitt, sowie ein verifizierter End-to-End-Login ueber `https://eam.example.com`.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-CONTEXT.md
@.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-RESEARCH.md
@.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-PATTERNS.md
@.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-VALIDATION.md
@.planning/phases/01-setup-stabilization-deployment-clarity/01-CONTEXT.md
@compose.yml
@README.md
@env.template
</context>

<source_audit>
## Multi-Source Coverage Audit

GOAL (ROADMAP Phase 01.1 — "restore eam.example.com hostname routing... Keycloak-backed login"): vollstaendig abgedeckt durch Task 1-3 (technische Restaurierung, Dokumentation, verifizierter Login).

REQ-Coverage: Keine formalen REQ-IDs zugewiesen (ROADMAP.md: "Requirements: TBD"). Placeholder `GOAL-01.1` (konsistent mit 01.1-VALIDATION.md) wird von allen 3 Tasks referenziert.

RESEARCH-Coverage:
- Pattern 2 (lokaler Traefik mit Netzwerk-Aliassen, der eigentliche Root-Cause-Fix) -> Task 1
- `.env` TRAEFIK_CERTRESOLVER/TEMPORAL_UI_CERTRESOLVER leeren (D-06) -> Task 1
- `/etc/hosts`-Eintraege (Pitfall 3) -> Task 1
- Pitfall 4 (Port-443-Konflikt / bereits laufender geteilter Traefik) -> Task 1 (Precondition-Check) und Task 2 (dokumentierte Alternative)
- README-Restaurierungsschritte + vollstaendige Hostname-Liste + Login-Caveat -> Task 2
- End-to-End-Login-Verifikation (Validation Architecture, manuell) -> Task 3

CONTEXT Decision Coverage:
- D-01, D-02, D-03 (Root Cause, Realm-Export unveraendert, deferred localhost:3000-Redirect) -> Task 1 (Nicht-Aenderung), Task 2 (Login-Caveat referenziert D-01/D-02, Deferred Idea referenziert D-03)
- D-04, D-05 (kein Traefik-Service in compose.yml, eigenstaendiges Snippet) -> Task 1
- D-06, D-07 (selbstsigniertes Zertifikat, kein mkcert) -> Task 1, Task 2 (Dokumentation), Task 3 (erwartete Browser-Warnung)
- D-08, D-09 (Supported Path bleibt Standard, Login-Caveat) -> Task 2
- Deferred Ideas (localhost:3000-Redirect, mkcert, automatisiertes /etc/hosts-Scripting) -> Task 2 (explizit als out-of-scope referenziert, nicht implementiert)

Result: Keine unplanned items, keine Deferred-Ideen in Scope umgesetzt.
</source_audit>

<tasks>

<task type="tracer" id="T1">
  <name>Task 1 (Tracer): Lokalen Traefik mit Netzwerk-Aliassen wiederherstellen</name>
  <files>traefik.local.yml</files>
  <precondition>Docker Engine und Docker Compose sind installiert, das externe `traefik`-Docker-Netzwerk existiert bereits (`docker network ls | grep traefik`), und kein anderer Prozess/Container publiziert bereits Host-Port 443 (`docker ps --filter publish=443` liefert keine Treffer) — siehe RESEARCH.md Pitfall 4.</precondition>
  <reversibility rating="reversible">Neue eigenstaendige Datei, lokale `.env`-Wertaenderung und eine zusaetzliche `/etc/hosts`-Zeile sind vollstaendig und ohne Datenmigration rueckgaengig zu machen; kein Effekt auf den Supported Path oder das Datenmodell (per D-05).</reversibility>
  <action>Erstelle `traefik.local.yml` als eigenstaendiges, optionales Compose-File nach RESEARCH.md Pattern 2: Image `traefik:v3.7.8`, Docker-Provider mit `exposedbydefault=false` und `network=traefik`, Entrypoint `websecure` auf `:443`, Docker-Socket read-only gemountet, und `networks.traefik.aliases` fuer alle 6 Hostnamen, die sich aus `env.template`'s `*_SUBDOMAIN`-Werten und `BASE_DOMAIN=example.com` ergeben (`eam.example.com`, `api.example.com`, `auth.example.com`, `neo4j.example.com`, `room.example.com`, `temporal.example.com`). Deklariere das `traefik`-Netzwerk als `external: true` — erstelle kein neues Netzwerk. Aendere `compose.yml` NICHT (D-04/D-05) und `auth/src/realm-export.json`/`k8s/realm-export.json` NICHT (D-02/D-03) — diese sind Read-only-Evidenz fuer diese Phase. Setze anschliessend lokal (nicht versioniert, `.env` ist per `.gitignore` ausgeschlossen) `TRAEFIK_CERTRESOLVER=` und `TEMPORAL_UI_CERTRESOLVER=` auf leer (D-06, vermeidet Log-Rauschen durch einen nicht registrierten Certresolver-Namen, RESEARCH.md Pitfall 2) — die Default-Platzhalterwerte in `env.template` selbst bleiben unveraendert (siehe Task 2). Fuege eine neue `/etc/hosts`-Zeile mit allen 6 FQDNs hinzu (`127.0.0.1 eam.example.com api.example.com auth.example.com neo4j.example.com room.example.com temporal.example.com`), ohne den bestehenden PVE-verwalteten Block (`127.0.1.1 eam.wien.arthofer.dev eam`) zu beruehren (Pitfall 3). Starte `docker compose -f traefik.local.yml up -d`, danach `docker compose up -d`, damit Traefik's Docker-Provider die bestehenden Labels aller 6 bereits angebundenen Services (`client`, `server`, `keycloak`, `neo4j`, `excalidraw-room`, `temporal-ui`) erneut einliest. Verifiziere die eigentliche Root-Cause-Fixierung (Container-seitige DNS-Aufloesung, nicht nur Browser-Erreichbarkeit): `auth.example.com` muss innerhalb des `server`-Containers auf die lokale Traefik-Container-IP im `traefik`-Netzwerk aufloesen, nicht auf eine oeffentliche Internet-IP.</action>
  <verify>
    <automated>docker compose -f traefik.local.yml config >/dev/null && docker compose -f traefik.local.yml up -d && docker compose up -d && docker exec nextgen-eam-server-1 getent hosts auth.example.com | grep -qE '^(172\.|10\.|192\.168\.)'</automated>
  </verify>
  <done>`traefik.local.yml` existiert mit gueltiger Compose-Syntax und Netzwerk-Aliassen fuer alle 6 Subdomains; lokaler Traefik-Container laeuft, angebunden an das externe `traefik`-Netzwerk; lokale `/etc/hosts` enthaelt die neue Zeile mit allen 6 FQDNs (PVE-Block unveraendert); lokale `TRAEFIK_CERTRESOLVER`/`TEMPORAL_UI_CERTRESOLVER`-Werte sind leer; `docker exec nextgen-eam-server-1 getent hosts auth.example.com` loest auf eine private Docker-Netzwerk-IP auf (nicht auf eine oeffentliche IP); `compose.yml`, `auth/src/realm-export.json` und `k8s/realm-export.json` sind unveraendert.</done>
</task>

<task type="auto" id="T2" tdd="false">
  <name>Task 2: Optional-Path-Dokumentation restaurieren und Login-Caveat ergaenzen</name>
  <files>README.md, env.template</files>
  <reversibility rating="reversible">Reine Text-/Kommentaraenderung, versionierbar und risikoarm rueckziehbar.</reversibility>
  <action>Erweitere README.md Abschnitt 5 "Optional Path: Traefik/HTTPS parity (not required)": erhalte die bestehende bilinguale Guardrail-Zeile "Nicht erforderlich fuer Supported Path." woertlich (D-08) und den englischen Einleitungssatz unveraendert. Vervollstaendige die bestehende, unvollstaendige Hostname-Bullet-Liste (aktuell fehlen `neo4j`/`room`/`temporal`) im identischen Format `` `https://<sub>.<BASE_DOMAIN>>` (Service) ``. Ergaenze lauffaehige Wiederherstellungsschritte im bestehenden Bullet-/Fenced-Code-Stil: die `.env`-Overrides fuer lokale Nutzung (`TRAEFIK_CERTRESOLVER=`, `TEMPORAL_UI_CERTRESOLVER=` leer, D-06/D-07), die neue `/etc/hosts`-Zeile (mit dem Pitfall-3-Hinweis, den bestehenden PVE-Block nicht zu editieren), den Startbefehl `docker compose -f traefik.local.yml up -d`, die Alternative fuer Entwickler mit bereits laufendem geteiltem Traefik (`docker network connect --alias auth.example.com --alias ... traefik <bestehender-traefik-container>`, RESEARCH.md Open Question 1/Pitfall 4), sowie den Container-DNS-Verifikationsbefehl `docker exec nextgen-eam-server-1 getent hosts auth.example.com`. Ergaenze den expliziten D-09-Login-Caveat direkt im Abschnitt: SSO-Login ueber den Supported (localhost) Path funktioniert aktuell nicht, weil Keycloak's `redirectUris`/`webOrigins` ausschliesslich `https://eam.example.com/*` referenzieren (D-01/D-02); der Optional Path ist der Pfad, der End-to-End-Login heute unterstuetzt. Referenziere die Deferred Ideas (localhost:3000-Redirect-URI, mkcert, automatisiertes /etc/hosts-Scripting) explizit als bewusst nicht Teil dieser Restaurierung, damit Beitragende sie nicht versehentlich mit-implementieren. In `env.template` ergaenze oberhalb von `TRAEFIK_CERTRESOLVER` und `TEMPORAL_UI_CERTRESOLVER` je eine zusaetzliche Kommentarzeile im bestehenden Zwei-Zeilen-Qualifier-Stil (Beschreibung + "Optional Traefik/HTTPS parity variable." bzw. Supported/Optional-Path-Formulierung wie bei `KEYCLOAK_URL`), die auf "fuer lokale Nutzung mit selbstsigniertem Zertifikat leer lassen" hinweist — die Default-Platzhalterwerte selbst (`my-traefik-cert-resolver`) bleiben unveraendert (RESEARCH.md Open Question 2).</action>
  <verify>
    <automated>grep -c "Nicht erforderlich fuer Supported Path" README.md | grep -qx 1 && grep -q "neo4j.example.com" README.md && grep -q "room.example.com" README.md && grep -q "temporal.example.com" README.md && grep -q "traefik.local.yml" README.md && grep -q "getent hosts" README.md && grep -qi "Supported.*Path.*login\|login.*nicht\|SSO.*nicht" README.md && grep -c "TRAEFIK_CERTRESOLVER=my-traefik-cert-resolver" env.template | grep -qx 1</automated>
  </verify>
  <done>README.md Abschnitt 5 enthaelt die vollstaendige 6-Hostnamen-Liste, lauffaehige Restaurierungsschritte (Env-Overrides, Hosts-Zeile, Startbefehl, Alternative fuer geteilten Traefik, DNS-Verifikationsbefehl) und den expliziten D-09-Login-Caveat; die Guardrail-Zeile und der Supported-Path-Abschnitt (§3-4) bleiben unveraendert; `env.template`-Defaultwerte sind unveraendert, nur Qualifier-Kommentare wurden ergaenzt.</done>
</task>

<task type="checkpoint:human-verify" id="T3" gate="blocking">
  <name>Task 3: End-to-End Keycloak SSO Login ueber https://eam.example.com verifizieren</name>
  <what-built>Task 1 hat einen lokalen Traefik-Reverse-Proxy mit Netzwerk-Aliassen restauriert (Browser- UND Container-seitige DNS-Aufloesung fuer alle `*.example.com`-Hostnamen); Task 2 hat die zugehoerige README/env.template-Dokumentation inklusive Login-Caveat aktualisiert.</what-built>
  <how-to-verify>
    1. Stelle sicher, dass lokal `TRAEFIK_CERTRESOLVER=` und `TEMPORAL_UI_CERTRESOLVER=` leer sind und die neue `/etc/hosts`-Zeile aktiv ist.
    2. `docker compose -f traefik.local.yml up -d` (oder Netzwerk-Aliasse an einen bereits laufenden geteilten Traefik anhaengen).
    3. `docker compose up -d`, damit Traefik alle Labels neu einliest.
    4. Oeffne `https://eam.example.com` im Browser und akzeptiere die selbstsignierte Zertifikatswarnung (erwartet, D-06).
    5. Bestaetige, dass die Client-App laedt und zur Keycloak-Login-Seite unter `https://auth.example.com` weiterleitet.
    6. Melde dich mit gueltigen Zugangsdaten an.
    7. Bestaetige, dass die Weiterleitung zurueck zum Client erfolgt und ein authentifizierter GraphQL-Aufruf (z.B. eine Seite mit geladenen Daten) erfolgreich ist.
  </how-to-verify>
  <resume-signal>Antworte "approved", wenn der Login End-to-End erfolgreich war, oder beschreibe den Fehlerpunkt (Browser-Netzwerktab, `docker logs`-Ausgabe des Traefik-/Server-Containers, oder das Ergebnis von `docker exec nextgen-eam-server-1 getent hosts auth.example.com`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> lokaler Traefik (TLS-Terminierung) | Selbstsigniertes Zertifikat wird vom Entwickler akzeptiert; explizit auf lokale Entwicklung beschraenkt (D-06). |
| Docker-Host -> lokaler Traefik-Container (docker.sock-Mount) | Gewaehrt Traefik Lesezugriff auf die Docker-API zur Service-Discovery. |
| `traefik`-Docker-Netzwerk -> Keycloak-/server-Container | Netzwerk-Aliasse bestimmen, welcher Container fuer `auth.example.com` antwortet — die eigentliche Root-Cause-Grenze dieser Phase. |
| Entwicklerrechner `/etc/hosts` -> andere lokale Projekte | Geteilte Datei; Risiko einer unbeabsichtigten Kollision mit bestehenden Eintraegen (z.B. PVE-Block). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-01 | Spoofing | Selbstsigniertes Zertifikat des lokalen Traefik fuer `*.example.com` | low | accept | Explizit auf lokale Entwicklung beschraenkt (D-06) und im Optional-Path-Abschnitt dokumentiert; Browser-Warnung ist erwartet und akzeptiert; `mkcert` als optionales zukuenftiges Upgrade referenziert (D-07, deferred), falls die Warnung im Workflow stoert. |
| T-01.1-02 | Elevation of Privilege | Docker-Socket-Mount (`/var/run/docker.sock:ro`) am lokalen Traefik-Container | medium | accept | Standard-Minimal-Risiko-Muster fuer eine rein lokale, entwicklergesteuerte Traefik-Instanz; Read-only-Mount begrenzt (eliminiert aber nicht) das Risiko; keine weitere Einschraenkung sinnvoll fuer diesen engen, lokalen Optional-Path-Restaurierungs-Scope (RESEARCH.md Security Domain). |
| T-01.1-03 | Tampering | Kollision mit bestehendem `/etc/hosts`-Eintrag anderer Projekte (Pitfall 3) | low | mitigate | Neue Eintraege werden als separate Zeile hinzugefuegt; der bestehende PVE-verwaltete Block wird nie editiert; explizit in Task 1 (Ausfuehrung) und Task 2 (Dokumentation) verankert. |
| T-01.1-04 | Denial of Service | Host-Port-443-Konflikt mit einer bereits laufenden geteilten Traefik-Instanz (Pitfall 4) | low | mitigate | Task 1 prueft per Precondition vor dem Start, ob Port 443 bereits gebunden ist; Task 2 dokumentiert die Alternative, Netzwerk-Aliasse an eine bestehende geteilte Instanz anzuhaengen statt eine zweite zu starten. |

Keine "high"- oder "critical"-Severity-Blocking-Threats identifiziert — alle vier Eintraege sind explizit auf lokale Entwicklung beschraenkt (D-06/D-07/CONTEXT.md) und betreffen keine produktive oder geteilte Infrastruktur. Package Legitimacy Gate: nicht anwendbar — diese Phase installiert keine neuen npm/pip/cargo-Pakete, sondern konfiguriert ein bereits lokal gecachtes Docker-Image (`traefik:v3.7.8`).
</threat_model>

<verification>
- `docker compose -f traefik.local.yml config` validiert die Syntax des neuen Compose-Snippets (Wave-0-Anforderung aus 01.1-VALIDATION.md).
- `docker exec nextgen-eam-server-1 getent hosts auth.example.com` beweist die eigentliche Root-Cause-Fixierung (Container-seitige DNS-Aufloesung, nicht nur Browser-Erreichbarkeit) und muss auf eine private Docker-Netzwerk-IP zeigen.
- `grep`/`rg`-basierte Pruefungen bestaetigen, dass README.md/env.template die vollstaendige Hostname-Liste, Restaurierungsschritte und den D-09-Login-Caveat enthalten, ohne den bestehenden Supported-Path-Abschnitt oder die Guardrail-Zeile zu veraendern.
- Manueller Browser-Login-Walkthrough (Task 3, blocking checkpoint) beweist den End-to-End-Erfolg: Login via `https://eam.example.com` gefolgt von einem erfolgreichen authentifizierten GraphQL-Aufruf.
- Nach Abschluss aller Tasks: `.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-VALIDATION.md` Frontmatter auf `status: validated` und `nyquist_compliant: true` aktualisieren, inklusive Task-Status-Spalte (⬜ -> ✅) fuer alle 3 Tasks.
</verification>

<success_criteria>
1. `https://eam.example.com` ist im Browser erreichbar (nach Akzeptieren der selbstsignierten Zertifikatswarnung) und Keycloak-SSO-Login funktioniert End-to-End, inklusive eines erfolgreichen authentifizierten GraphQL-Aufrufs.
2. Container-seitige DNS-Aufloesung fuer `auth.example.com` zeigt nachweisbar auf den lokalen Traefik-Container, nicht auf eine oeffentliche IP.
3. `compose.yml`, `auth/src/realm-export.json` und `k8s/realm-export.json` sind unveraendert; `traefik.local.yml` ist eigenstaendig und wird von `compose.yml` nicht referenziert.
4. README.md/env.template dokumentieren den restaurierten Optional Path vollstaendig, inklusive des expliziten Login-Caveats fuer den Supported Path, ohne Phase 1's Supported-Path-Standard zu veraendern.
5. Keine der drei Deferred Ideas (localhost:3000-Redirect, mkcert, automatisiertes /etc/hosts-Scripting) wurde implementiert.
</success_criteria>

<output>
Create `.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-SUMMARY.md` when done.
Update `.planning/phases/01.1-eam-example-com-local-domain-setup-analyze-and-restore-the-e/01.1-VALIDATION.md` frontmatter (`status: validated`, `nyquist_compliant: true`) and task-status column during/after execution.
</output>
