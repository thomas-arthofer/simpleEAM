---
phase: 02.2
plan: 02
subsystem: ui
tags: [excalidraw, react, sovereignty-markers, diagrams, undo-history]

requires:
  - phase: 02.2
    plan: 01
    provides: "locked:true on marker ellipses, diff-based auto-add-on-drop, removeOrphanedMarkersLive() live orphan-cleanup helper (used verbatim here)"
provides:
  - "Atomic marker cleanup in FullCustomContextMenu.handleDelete — single updateScene call, single Ctrl+Z restore for the context-menu delete pathway (contrasting Plan 01's two-tier native-keyboard-delete path)"
  - "D-01 lifecycle-gap triage discharged: duplicate/copy/paste-of-main-element marker auto-coverage confirmed free via Plan 01's diff-based detection (pending manual QA); one confirmed non-trivial gap (handleDuplicate id/groupIds remapping) logged to STATE.md Deferred Items as accepted backlog, not fixed"
affects: [sovereignty-markers, diagram-lifecycle]

tech-stack:
  added: []
  patterns:
    - "Reuse (not reimplementation) of a Plan 01 live helper (removeOrphanedMarkersLive) across a second, independent delete pathway (context-menu vs native-keyboard), each tagged with its own explicit captureUpdate for an intentionally separate atomicity/undo-granularity choice"

key-files:
  created: []
  modified:
    - client/src/components/diagrams/components/FullCustomContextMenu.tsx
    - .planning/STATE.md

key-decisions:
  - "handleDelete's pre-existing filter-based removal of the main element itself was left unchanged (per plan scope) — only the elements array fed into updateScene was swapped from the raw filtered array to removeOrphanedMarkersLive's cleaned output, plus an explicit captureUpdate: IMMEDIATELY added to the same call"
  - "handleCut, handleCopy, handlePaste, handleDuplicate left byte-for-byte unmodified, per plan's explicit out-of-scope instruction"
  - "handleDuplicate's id/groupIds/boundElements/containerId remapping gap (RESEARCH.md Pitfall 4) logged as a single new STATE.md Deferred Items row rather than fixed — confirmed non-trivial, independent of sovereignty markers"

patterns-established: []

requirements-completed: [SOV-MARKER-DELETE]

coverage:
  - id: D1
    description: "Context-menu Delete removes a main element and its orphaned markers in one atomic updateScene call tagged captureUpdate: CaptureUpdateAction.IMMEDIATELY; a single Ctrl+Z restores both together"
    requirement: SOV-MARKER-DELETE
    verification:
      - kind: other
        ref: "cd client && yarn type-check"
        status: pass
    human_judgment: true
    rationale: "Requires live keyboard/mouse interaction with a running Excalidraw canvas (right-click Delete, observe marker removal, then Ctrl+Z to count restore keystrokes) — cannot be exercised in this sandboxed, non-browser environment. Manual QA steps documented below; VALIDATION.md row 02.2-01-02/03 remain ⬜ pending."
  - id: D2
    description: "D-01 lifecycle-gap triage: duplicate/copy/paste-of-main-element auto-marking confirmed free via Plan 01's diff-based detection (no new code); one confirmed non-trivial gap (handleDuplicate id/groupIds remapping) logged as accepted backlog in STATE.md Deferred Items, not fixed in this phase"
    verification:
      - kind: other
        ref: "grep -c \"Diagram Lifecycle\" .planning/STATE.md"
        status: pass
    human_judgment: true
    rationale: "The 'duplicate/copy/paste auto-marks via diff detection' and 'Ctrl+A never co-selects markers' claims require live pointer interaction with a running Excalidraw canvas — cannot be exercised in this sandboxed environment. The backlog-row addition itself is mechanically verified (grep count); the underlying manual behavior checks remain pending."

duration: ~10min
completed: 2026-07-31
status: complete
---

# Phase 02.2 Plan 02: Atomic context-menu delete cleanup + D-01 lifecycle-gap triage Summary

**`FullCustomContextMenu.handleDelete` now strips orphaned markers in the same atomic `updateScene` call as the element delete itself (single Ctrl+Z restore); `handleDuplicate`'s id/groupIds remapping gap logged to STATE.md as accepted backlog — type-checked clean, manual QA pending**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-31 (this session)
- **Completed:** 2026-07-31
- **Tasks:** 2/2 completed
- **Files modified:** 2

## Accomplishments

- `FullCustomContextMenu.tsx`'s `handleDelete` now calls `removeOrphanedMarkersLive(newElements)` and passes the resulting `cleanedElements` (not the raw filtered array) into its existing `excalidrawAPI.updateScene(...)` call, with an added explicit `captureUpdate: CaptureUpdateAction.IMMEDIATELY` on that same call — making the main-element removal and marker tombstone one single, self-contained history entry.
- `handleCut`, `handleCopy`, `handlePaste`, and `handleDuplicate` are byte-for-byte unchanged, per the plan's explicit out-of-scope instruction.
- D-01's case-by-case lifecycle triage was applied: duplicate/copy/paste-of-main-element auto-marking is confirmed (per RESEARCH.md Pitfall 3's reasoning — locked markers are never in `selectedElementIds`, so they can't be duplicated/copied/pasted alongside the main element, and the new main element is picked up for free by Plan 01's diff-based `previouslySeenMainElementIdsRef` detection) to require zero new marker-specific code.
- The one confirmed non-trivial gap — `handleDuplicate`'s string-concatenation id scheme (`duplicate_${el.id}_${Date.now()}_${index}`) with no `groupIds`/`boundElements`/`containerId` remapping — was logged as a new row in `.planning/STATE.md`'s `## Deferred Items` table (`Diagram Lifecycle` category, `Accepted backlog` status), not fixed in this phase.

## Task Commits

Each task was committed atomically:

1. **Task 1: Atomic marker cleanup in `handleDelete`** - `4be613f` (fix)
2. **Task 2: D-01 lifecycle-gap triage — STATE.md backlog row** - `f0eb68b` (docs)

## Files Created/Modified

- `client/src/components/diagrams/components/FullCustomContextMenu.tsx` - New imports: `CaptureUpdateAction` (`@excalidraw/excalidraw`), `removeOrphanedMarkersLive` (`../utils/sovereigntyMarkers`). `handleDelete`'s `updateScene` call now passes `cleanedElements` (output of `removeOrphanedMarkersLive(newElements)`) and sets `captureUpdate: CaptureUpdateAction.IMMEDIATELY`.
- `.planning/STATE.md` - One new row appended to the existing `## Deferred Items` table: `Diagram Lifecycle` category, documenting the confirmed non-trivial `handleDuplicate` id/`groupIds` remapping gap (RESEARCH.md Pitfall 4) as `Accepted backlog`, dated 2026-07-31. No other rows added or removed.

## Decisions Made

- `handleDelete`'s pre-existing filter-based removal of the main element itself was left unchanged — only the array fed to `updateScene` and the added `captureUpdate` field were touched, per the plan's explicit scope boundary (generically fixing `handleDelete`'s filter-vs-tombstone pattern for the main element is out of this phase's scope).
- No new row was added for duplicate/copy/paste-of-main-element marker coverage or group-operation co-selection — both are confirmed FREE (no gap) per RESEARCH.md Pitfall 3's reasoning, requiring only the verification record here, not a backlog entry.

## Deviations from Plan

None — plan executed exactly as written. Both tasks matched their `<action>` and `<acceptance_criteria>` verbatim; no Rule 1-4 triggers encountered.

## Verification

- **Automated:** `cd client && yarn type-check` — **PASSED** (exit code 0, zero errors) after Task 1.
- **Automated:** `grep -c "Diagram Lifecycle" .planning/STATE.md` — **1** (exactly one new row, as required) after Task 2.
- **Manual:** NOT performed in this session — see below.

## Manual Verification Deferred

**This execution ran in a sandboxed, non-browser environment with no access to a running dev server or live Excalidraw canvas.** Per this plan's explicit instruction and the user's request, manual verification is **not fabricated as passing**. The following steps must be performed by a developer running `yarn dev` under `client/` before this plan's `must_haves.truths` and the corresponding 02.2-VALIDATION.md rows can be marked green:

1. **Task 1 — atomic context-menu delete (single Ctrl+Z restore):**
   - Open a diagram with a main element that already has fill/ring sovereignty markers.
   - Right-click the main element and choose **Delete** from the custom context menu (not native keyboard Delete/Backspace).
   - Confirm both the main element AND its markers vanish immediately, in the same action — zero orphaned marker ellipses left behind.
   - Press **Ctrl+Z exactly once**.
   - Confirm BOTH the main element and its markers reappear together in that single undo press.
   - **Contrast and document:** this is a *single*-press restore, unlike Plan 01's native-keyboard-delete path (`ExcalidrawWrapper.handleChange`'s `removeOrphanedMarkersLive` wiring), which is documented as requiring up to **two** Ctrl+Z presses (first restores the marker-cleanup entry, second restores the delete itself) because that path reacts to the delete via a separate, subsequent `onChange`-driven `updateScene` call rather than folding the cleanup into the original delete's own call. Record the actual keystroke counts observed for both paths side-by-side.

2. **Task 2 — D-01 lifecycle-gap triage, free-coverage confirmation:**
   - Duplicate a main element with existing markers via the context menu's **Duplicate** action. Confirm the duplicate receives its own fill/ring marker pair automatically within one fetch round-trip (no visible delay beyond normal network latency), with no marker-specific code involved.
   - Copy a main element with markers via the context menu, then Paste it. Confirm the same auto-marking behavior.
   - Select a main element with markers and press **Ctrl+A** (select-all) or perform a group-select. Confirm marker ellipses are never included in the resulting selection, consistent with Plan 01 Task 1's non-selectability check (`locked: true`).

**02.2-VALIDATION.md status:** Rows `02.2-01-02` ("delete a main element (keyboard + context menu)") and `02.2-01-03` ("Ctrl+Z after delete; observe markers reappear") in the Per-Task Verification Map cover both this plan's and Plan 01's delete pathways jointly and remain `⬜ pending`, unchanged, correctly reflecting that no manual pass has been recorded for either delete path yet. No new VALIDATION.md rows were fabricated as passing.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Type-check is clean; both tasks structurally complete and committed atomically.
- This is the last plan in Phase 02.2 — the phase's structural work (locking, auto-add, delete-cleanup for both pathways, lifecycle-gap triage) is complete. Manual QA (Plan 01's 5 steps + this plan's 2 verification blocks above) remains outstanding before the phase's `must_haves.truths` can be marked fully verified end-to-end.
- Blocker: none structural; a developer with a running `yarn dev` instance must perform the manual checks above (especially the side-by-side Ctrl+Z keystroke-count comparison between the two delete pathways) to close out Phase 02.2's validation.

---
*Phase: 02.2*
*Completed: 2026-07-31*

## Self-Check: PASSED

- FOUND: `client/src/components/diagrams/components/FullCustomContextMenu.tsx`
- FOUND: `.planning/STATE.md`
- FOUND: commit `4be613f`
- FOUND: commit `f0eb68b`
