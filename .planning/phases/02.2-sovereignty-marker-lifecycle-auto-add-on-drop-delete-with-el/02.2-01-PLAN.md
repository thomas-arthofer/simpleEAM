---
phase: 02.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/diagrams/utils/sovereigntyMarkers.ts
  - client/src/components/diagrams/components/ExcalidrawWrapper.tsx
autonomous: true
requirements: [SOV-MARKER-AUTOADD, SOV-MARKER-DELETE]

must_haves:
  truths:
    - "Clicking directly on a marker ellipse, dragging it, or selecting it via Ctrl+A never selects/moves/deletes it independently of its main element."
    - "Dragging a main element with existing markers still live-repositions the marker pair (Phase 02.1 regression not broken by locked: true)."
    - "Dragging a new main element from the library onto the canvas shows its fill/ring markers without any manual Ctrl+R resync."
    - "Deleting a main element via native keyboard Delete/Backspace removes its marker ellipses in the same onChange pass; no orphaned markers remain on canvas."
    - "Pressing Ctrl+Z after a native-keyboard delete restores the markers (documented as up to two presses per the two-tier undo strategy — see Pitfall 1 in 02.2-RESEARCH.md)."
  artifacts:
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (createMarkerEllipses: locked: true; new export removeOrphanedMarkersLive)
    - client/src/components/diagrams/components/ExcalidrawWrapper.tsx (handleChange: new-main-element diff detection + live orphan cleanup wiring)
  key_links:
    - "ExcalidrawWrapper.handleChange -> syncSovereigntyMarkers/fetchSovereigntyMarkersForDiagram/applySovereigntyMarkers (existing fetch+apply pipeline reused verbatim for auto-add, per D-02)"
    - "ExcalidrawWrapper.handleChange -> removeOrphanedMarkersLive (new) -> apiRef.current.updateScene({ captureUpdate: CaptureUpdateAction.IMMEDIATELY })"
    - "createMarkerEllipses locked: true -> Excalidraw's own selection.ts shouldIgnoreElementFromSelection (external, unmodified, per D-04)"
---

<objective>
Wire the three core sovereignty-marker lifecycle fixes into their single existing integration point: `createMarkerEllipses` gets `locked: true` (D-04), and `ExcalidrawWrapper.tsx`'s `handleChange` gains diff-based auto-add-on-drop detection (D-02) and live, `isDeleted`-aware orphan-marker cleanup for the native-keyboard-delete path (D-01/D-03).

Purpose: These are the two named bugs in the phase goal (auto-add on drop, delete with element) plus the non-selectability fix (D-04) that both other fixes depend on being verified first, since D-04 changes what `applySovereigntyMarkers`'s group-based co-selection assumptions can rely on (per RESEARCH.md: locked markers are excluded from group co-selection, so onChange-based cleanup is the *sole* cleanup mechanism — this plan builds that mechanism).
Output: Markers are excluded from direct/group selection, new main elements dropped from the library get markers without a manual resync, and native-keyboard-deleted main elements have their markers tombstoned live with `captureUpdate: IMMEDIATELY` (one dedicated undo entry, restorable via Ctrl+Z per the two-tier strategy in RESEARCH.md Pitfall 1).
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-CONTEXT.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-RESEARCH.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-VALIDATION.md
@client/src/components/diagrams/utils/sovereigntyMarkers.ts
@client/src/components/diagrams/components/ExcalidrawWrapper.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `locked: true` on marker ellipses — non-selectability, verified end-to-end (D-04)</name>
  <files>client/src/components/diagrams/utils/sovereigntyMarkers.ts</files>
  <read_first>
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (createMarkerEllipses's `base` object, currently `locked: false`; also read repositionMarkerEllipses/repositionAllMarkerEllipses immediately below it — these must keep working unmodified since they are programmatic `updateScene()` writes, not user pointer interactions)
    - .planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-RESEARCH.md (Architecture Patterns → Pattern 3 preamble and the "Don't Hand-Roll" table row on `locked: true`; the VERIFIED claim that `locked: true` is purely an interaction-layer guard and does not affect programmatic `updateScene()` writes)
  </read_first>
  <action>
    In `createMarkerEllipses`'s shared `base` object literal, change the `locked: false` field to `locked: true`. Do not touch any other field in `base` (angle, fillStyle, roughness, opacity, groupIds, frameId, roundness, seed, version, versionNonce, isDeleted, boundElements, updated, link all stay exactly as-is). Do not modify `repositionMarkerEllipses` or `repositionAllMarkerEllipses` in this task — they are verified separately as a regression check. This is a genuine one-line change but constitutes the full tracer slice for this plan: it touches marker creation (source), Excalidraw's native selection/rendering layer (external, unmodified), and is independently, fully verifiable end-to-end via manual QA before the more involved Task 2/Task 3 wiring begins.
  </action>
  <verify>
    <automated>cd client && yarn type-check</automated>
    <manual>Per 02.2-VALIDATION.md's Manual-Only Verifications table: (1) open a diagram with an existing main element carrying fill/ring markers, click directly on a marker ellipse and attempt to drag it — nothing should happen (no selection outline, no move); (2) press Ctrl+A to select-all on the same diagram and confirm marker ellipses are excluded from the resulting selection (only main elements get selection outlines); (3) drag the main element itself and confirm its fill/ring pair still live-repositions in place (Phase 02.1 regression check — repositionAllMarkerEllipses must be unaffected by locked: true).</manual>
  </verify>
  <done>`locked: true` is set in `createMarkerEllipses`'s `base` object; a marker ellipse cannot be clicked, dragged, or select-all'd independently; dragging its main element still live-repositions the pair.</done>
  <acceptance_criteria>
    - `grep -n "locked: true" client/src/components/diagrams/utils/sovereigntyMarkers.ts` finds the field inside `createMarkerEllipses`'s `base` object (not elsewhere).
    - `repositionMarkerEllipses`/`repositionAllMarkerEllipses` function bodies are byte-for-byte unchanged from before this task (only the `base.locked` line differs in the file diff).
    - Manual check: clicking a marker ellipse produces no Excalidraw selection outline; dragging it does not move it; Ctrl+A does not include it in `selectedElementIds`.
    - Manual check: dragging the main element still moves its fill/ring pair to the new top-right-anchor position (per 02.1's existing drag-test procedure).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Diff-based new-main-element detection wired into `handleChange` (D-02, auto-add on drop)</name>
  <files>client/src/components/diagrams/components/ExcalidrawWrapper.tsx</files>
  <read_first>
    - client/src/components/diagrams/components/ExcalidrawWrapper.tsx (the full `ExcalidrawComponent` function body — its existing `apiRef`/`isAPIReady` refs, its `handleChange` callback and dependency array, its existing `useCompanyContext()` call for `selectedCompany`, and the top-of-file import block)
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (`syncSovereigntyMarkers`'s exact signature: `(apolloClient, elements, { enabled, companyId })` returning `Promise<DiagramElement[]>` — reuse this verbatim, do not reimplement fetch logic)
    - client/src/contexts/CompanyContext.tsx (confirms `selectedCompanyId` is already exposed directly from `useCompanyContext()`, no need to derive from `selectedCompany.id`)
    - client/src/lib/feature-flags.ts (`useFeatureFlags()` hook shape: returns `{ featureFlags }`, matching the existing usage pattern in client/src/components/diagrams/handlers/DiagramHandlers.ts and client/src/components/diagrams/state/DiagramState.ts)
  </read_first>
  <action>
    Add two new imports to ExcalidrawWrapper.tsx: `useApolloClient` from `@apollo/client` and `useFeatureFlags` from `@/lib/feature-flags`, following the exact usage pattern already established in DiagramHandlers.ts/DiagramState.ts. Also import `syncSovereigntyMarkers` from `../utils/sovereigntyMarkers` alongside the existing `repositionAllMarkerEllipses` import. Inside `ExcalidrawComponent`, call `const apolloClient = useApolloClient()` and `const { featureFlags } = useFeatureFlags()`, and read `selectedCompanyId` from the existing `useCompanyContext()` call (add it to the existing destructure alongside `selectedCompany`). Add a new `const previouslySeenMainElementIdsRef = React.useRef<Set<string>>(new Set())` alongside the existing `apiRef`/`isAPIReady` refs. Inside `handleChange`, after the existing `repositionAllMarkerEllipses` block (using `effectiveElements` as the base) and before the `if (onChange)` call, compute `currentMainElementIds` by iterating `effectiveElements`, skipping any element with `el.isDeleted`, and collecting `el.id` for elements where `el.customData?.isMainElement && el.customData?.databaseId`. Diff this set against `previouslySeenMainElementIdsRef.current` to get `newMainElementIds` (ids present in `currentMainElementIds` but not in the ref's prior value). Unconditionally reassign `previouslySeenMainElementIdsRef.current = currentMainElementIds` regardless of whether any new ids were found (self-healing on fetch failure, per RESEARCH.md Pattern 1). If `newMainElementIds.length > 0 && featureFlags.Sovereignty && selectedCompanyId`, fire an async IIFE that calls `syncSovereigntyMarkers(apolloClient, effectiveElements, { enabled: true, companyId: selectedCompanyId })`; if the returned array is reference-unequal to `effectiveElements`, set `suppressOnChangeRef.current = true` (guarding against the async resolution racing a later synchronous onChange) and call `apiRef.current?.updateScene({ elements: syncedElements, captureUpdate: CaptureUpdateAction.EVENTUALLY })` — import `CaptureUpdateAction` from `@excalidraw/excalidraw` for this. Do not block or await this call inside `handleChange` itself (fire-and-forget, matching the async nature of the fetch). Add `apolloClient`, `featureFlags.Sovereignty`, `selectedCompanyId` to `handleChange`'s existing `useCallback` dependency array.
  </action>
  <verify>
    <automated>cd client && yarn type-check</automated>
    <manual>Per 02.2-VALIDATION.md's Manual-Only Verifications table: drag a main element (e.g. an Application) from the library sidebar onto a diagram that has sovereignty markers enabled (featureFlags.Sovereignty on, a company selected) and has at least one BusinessCapability/DataObject root; observe the fill/ring markers appear on the newly dropped element shortly after drop, with no manual Ctrl+R resync. Confirm dragging/panning/editing an unrelated existing element does NOT trigger a new GraphQL sovereigntyMarkers request (check Network tab — no new query fires unless a genuinely new main element id appeared).</manual>
  </verify>
  <done>Dropping a new main element from the library gets its fill/ring markers applied automatically within one fetch round-trip of the drop, with zero new GraphQL calls fired on unrelated onChange events (drag/pan/edit of existing elements).</done>
  <acceptance_criteria>
    - `ExcalidrawWrapper.tsx` imports `useApolloClient` from `@apollo/client`, `useFeatureFlags` from `@/lib/feature-flags`, `CaptureUpdateAction` from `@excalidraw/excalidraw`, and `syncSovereigntyMarkers` from `../utils/sovereigntyMarkers`.
    - `previouslySeenMainElementIdsRef` is a `React.useRef<Set<string>>(new Set())` declared alongside `apiRef`.
    - The new-main-element diff loop explicitly excludes elements where `el.isDeleted` is true before adding to `currentMainElementIds`.
    - `syncSovereigntyMarkers` is called with the exact existing signature `(apolloClient, effectiveElements, { enabled: true, companyId: selectedCompanyId })` — no reimplemented fetch logic.
    - The fetch is only triggered when `newMainElementIds.length > 0`; manual Network-tab check confirms no query fires on a plain drag/pan of an already-seen element.
    - `handleChange`'s `useCallback` dependency array includes `apolloClient`, `featureFlags.Sovereignty`, and `selectedCompanyId`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live, `isDeleted`-aware orphan-marker cleanup wired into `handleChange` with explicit `captureUpdate` (D-01/D-03)</name>
  <files>client/src/components/diagrams/utils/sovereigntyMarkers.ts, client/src/components/diagrams/components/ExcalidrawWrapper.tsx</files>
  <read_first>
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (`applySovereigntyMarkers`'s existing `presentMainElementIds`/orphan-detection loop — the logic to base the new live helper on, but NOT reuse verbatim: it does not check `element.isDeleted`, which is the exact gap this task fixes for the live/incremental context; `repositionMarkerEllipses`'s mutation shape for the `version`/`versionNonce`/`updated` bump convention to mirror)
    - client/src/components/diagrams/components/ExcalidrawWrapper.tsx (the `handleChange` callback as modified by Task 2 — this task's cleanup wiring lands in the same callback, after the auto-add diff block)
    - .planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-RESEARCH.md (Pattern 2 for the exact `removeOrphanedMarkersLive` reference implementation shape; Pitfall 1 in full — this is the section governing the `captureUpdate` choice below; Pitfall 2 for why `applySovereigntyMarkers` itself must NOT be modified in this task)
  </read_first>
  <behavior>
    - `removeOrphanedMarkersLive(elements)` returns `{ elements, changed: false }` (same array reference) when no marker ellipse is orphaned — mirrors `repositionAllMarkerEllipses`'s no-op contract exactly.
    - `removeOrphanedMarkersLive(elements)` builds `presentMainElementIds` only from elements where `!element.isDeleted && element.customData?.isMainElement && element.customData?.databaseId` — a main element with `isDeleted: true` (native-keyboard tombstone) must NOT count as present.
    - For each marker ellipse (`customData.sovereigntyMarker === 'fill' | 'ring'`) whose `customData.mainElementId` is not in `presentMainElementIds`, the function returns a new element with `isDeleted: true` (tombstone — never physically filtered out of the array) plus bumped `version`/`versionNonce`/`updated`, and sets `changed: true` on the returned result.
    - A marker whose main element is present and not deleted is returned completely untouched (same object reference) even when `changed: true` overall, mirroring `repositionAllMarkerEllipses`'s per-element untouched-unless-affected contract.
  </behavior>
  <action>
    In `sovereigntyMarkers.ts`, add a new exported function `removeOrphanedMarkersLive(elements: DiagramElement[]): { elements: DiagramElement[]; changed: boolean }` implementing exactly the behavior above (reference implementation: RESEARCH.md Pattern 2). Place it after `repositionAllMarkerEllipses` in the file, following the same JSDoc-comment convention used for the other exported functions in this file (explain: live/incremental context only, `applySovereigntyMarkers` unchanged and remains the sync-time full-rebuild path). In `ExcalidrawWrapper.tsx`, import `removeOrphanedMarkersLive` alongside the existing `repositionAllMarkerEllipses`/`syncSovereigntyMarkers` imports. Inside `handleChange`, immediately after the Task-2 auto-add diff block (using `effectiveElements` as input — chain, do not recompute from the raw `elements` argument), call `removeOrphanedMarkersLive(effectiveElements)`. If `changed`, set `suppressOnChangeRef.current = true` and call `apiRef.current?.updateScene({ elements: cleanedElements, captureUpdate: CaptureUpdateAction.IMMEDIATELY })` — `IMMEDIATELY` is the deliberate, explicit choice for this reactive native-keyboard-delete path per RESEARCH.md Pitfall 1 (own, self-contained undo entry; two Ctrl+Z presses restore both the delete and the markers — this is the documented, correct behavior, not a bug). Reassign `effectiveElements` to the cleaned array for the remainder of `handleChange` (so the subsequent `onChange`/broadcast calls see the tombstoned markers too). While in this file, also fix the existing Task-1-adjacent `repositionAllMarkerEllipses` `updateScene` call's stale two-argument form (`apiRef.current?.updateScene({ elements: repositionedElements }, false)`) to the single-object form with an explicit `captureUpdate: CaptureUpdateAction.EVENTUALLY` (unchanged behavior, just made explicit instead of relying on the silently-ignored second argument and the implicit default) — this is the same-file, low-risk cleanup RESEARCH.md's State of the Art section flags as optional-but-recommended while touching this code.
  </action>
  <verify>
    <automated>cd client && yarn type-check</automated>
    <manual>Per 02.2-VALIDATION.md's Manual-Only Verifications table: (1) select a main element with fill/ring markers and press keyboard Delete/Backspace — confirm the markers vanish from the canvas in the same change as the element (no lingering orphaned ellipses); (2) press Ctrl+Z once — confirm the main element reappears (per the two-tier strategy, markers may not reappear yet on this first press); (3) press Ctrl+Z a second time if needed — confirm the markers reappear, fully restoring the pre-delete state. Document the actual number of presses observed in the plan's SUMMARY.md.</manual>
  </verify>
  <done>Deleting a main element via native keyboard Delete/Backspace removes its fill/ring markers in the same onChange pass (no orphans left on canvas); Ctrl+Z restores both the element and its markers within the documented two-tier keystroke budget (1–2 presses).</done>
  <acceptance_criteria>
    - `removeOrphanedMarkersLive` is exported from `sovereigntyMarkers.ts`, checks `!element.isDeleted` when building `presentMainElementIds`, and tombstones (`isDeleted: true`) rather than array-filters orphaned markers.
    - `applySovereigntyMarkers`'s own function body is byte-for-byte unchanged by this task (diff shows only additions after it, per Pitfall 2's explicit "do not modify" instruction).
    - The `handleChange` cleanup call uses `apiRef.current?.updateScene({ elements: cleanedElements, captureUpdate: CaptureUpdateAction.IMMEDIATELY })` — not the omitted/default form.
    - The pre-existing `repositionAllMarkerEllipses` `updateScene` call no longer passes a second positional `false` argument; it explicitly sets `captureUpdate: CaptureUpdateAction.EVENTUALLY`.
    - Manual check: deleting a main element with keyboard Delete/Backspace leaves zero marker ellipses visible on canvas immediately after the keypress.
    - Manual check: Ctrl+Z (1 or 2 presses, whichever is observed) fully restores both the main element and its fill/ring pair.
  </acceptance_criteria>
  <reversibility rating="costly">Folding the marker-cleanup into Excalidraw's own `captureUpdate`/history sequencing (per D-03) is architecturally load-bearing for this plan's delete-undo behavior — reworking this later means re-deciding the capture strategy for the reactive onChange path, not editing one function in isolation. Flagged per D-03's RESEARCH.md "costly" rating; not one-way (no data loss, purely an in-session undo-stack sequencing choice), so no blocking checkpoint:decision is inserted, but the choice and its two-keystroke consequence must be visible in the plan's SUMMARY.md for future reference.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser canvas ↔ existing GraphQL `sovereigntyMarkers` query | Unchanged trust boundary — this plan only changes *when* the existing, already-hardened fetch fires (diff-triggered vs. sync-time), not its shape, auth, or validation. |
| User pointer input ↔ Excalidraw scene state | New trust boundary consideration for D-04: `locked: true` moves marker ellipses out of direct user pointer-interaction reach entirely, at Excalidraw's own selection layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-01 | Tampering | Marker ellipses (`createMarkerEllipses`) | low | accept | A user could already tamper with any scene element via devtools/direct localStorage edits regardless of `locked`; `locked: true` narrows the *supported UI* attack surface (no click/drag path) but is not a security control against a determined client-side attacker with devtools — accepted as out of scope, matches the existing trust model for all other diagram elements. |
| T-02.2-02 | Denial of Service (client) | Diff-based auto-add fetch (`ExcalidrawWrapper.handleChange`) | low | mitigate | Rapid successive library drops could each fire a `syncSovereigntyMarkers` GraphQL call; mitigated by the diff-based trigger itself (only fires for genuinely new main-element ids, never on every onChange) — no additional debounce required for this plan's scope per D-02's discretion note, but the existing 500-node cap in `fetchSovereigntyMarkersForDiagram` remains the hard backstop. |
| T-02.2-03 | Repudiation / data-loss (undo integrity) | Live orphan-cleanup `captureUpdate` sequencing | medium | mitigate | An incorrectly-chosen `captureUpdate` value could silently corrupt a user's undo history (e.g. markers reappearing on an unrelated later undo step) rather than exposing data to anyone — mitigated by the explicit `IMMEDIATELY` choice documented in Task 3, verified manually per the two-tier strategy rather than assumed. |
</threat_model>

<verification>
- `cd client && yarn type-check` passes with zero errors after all three tasks.
- All manual checks in each task's `<verify>` block pass in the running dev stack (`yarn dev` under `client/`), matching 02.2-VALIDATION.md's Manual-Only Verifications table rows for SOV-MARKER-AUTOADD, SOV-MARKER-DELETE, D-04 non-selectability, and D-04 reposition-regression.
- No new GraphQL query fires on a plain drag/pan/edit of an already-seen main element (Network tab spot check).
</verification>

<success_criteria>
- `locked: true` set on both fill and ring ellipses; markers cannot be clicked, dragged, or select-all'd independently.
- Dropping a new main element from the library gets markers automatically, no manual resync required.
- Deleting a main element via native keyboard Delete/Backspace removes its markers in the same change; Ctrl+Z (1–2 presses) restores both.
- Phase 02.1's drag-reposition behavior is unaffected by `locked: true`.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `client/src/components/diagrams/utils/sovereigntyMarkers.ts`:
  - `createMarkerEllipses`'s shared `base` object: `locked: false` → `locked: true` (D-04).
  - New exported function `removeOrphanedMarkersLive(elements: DiagramElement[]): { elements: DiagramElement[]; changed: boolean }` — live/incremental, `isDeleted`-aware orphan-marker tombstone helper (D-01/D-03). Additive only; `applySovereigntyMarkers`, `repositionMarkerEllipses`, `repositionAllMarkerEllipses` remain unmodified in behavior.
- `client/src/components/diagrams/components/ExcalidrawWrapper.tsx`:
  - New imports: `useApolloClient` (`@apollo/client`), `useFeatureFlags` (`@/lib/feature-flags`), `CaptureUpdateAction` (`@excalidraw/excalidraw`), `syncSovereigntyMarkers` and `removeOrphanedMarkersLive` (`../utils/sovereigntyMarkers`).
  - New ref: `previouslySeenMainElementIdsRef: React.RefObject<Set<string>>` inside `ExcalidrawComponent`.
  - Modified `handleChange` (`useCallback`) signature/behavior unchanged externally, but internal body extended with: (1) diff-based new-main-element detection invoking `syncSovereigntyMarkers` when new ids are found, (2) `removeOrphanedMarkersLive` live cleanup call with `captureUpdate: CaptureUpdateAction.IMMEDIATELY`, (3) the pre-existing `repositionAllMarkerEllipses` `updateScene` call's stale two-argument form replaced with the single-object form + explicit `captureUpdate: CaptureUpdateAction.EVENTUALLY`.
  - `handleChange`'s `useCallback` dependency array gains `apolloClient`, `featureFlags.Sovereignty`, `selectedCompanyId`.
</artifacts_produced>

<output>
Create `.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-01-SUMMARY.md` when done, including the observed Ctrl+Z keystroke count from Task 3's manual verification.
</output>
