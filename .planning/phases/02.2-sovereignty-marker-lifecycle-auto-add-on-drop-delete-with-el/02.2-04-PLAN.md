---
phase: 02.2
plan: 04
type: execute
wave: 2
depends_on: ['02.2-03']
files_modified:
  - client/src/components/diagrams/components/ExcalidrawWrapper.tsx
  - .planning/debug/sovereignty-marker-sync-not-live.md
autonomous: false
requirements: [SOV-MARKER-DELETE]
gap_closure: true
gap_ids: [G-02.2-4]

must_haves:
  truths:
    - "Temporary, clearly-marked diagnostic logging exists around removeOrphanedMarkersLive's invocation site and its updateScene call in handleChange, sufficient to determine whether handleChange runs at all on native keyboard Delete/Backspace, and with what elements/isDeleted state."
    - "A human has run yarn dev, reproduced native-keyboard delete of a marked main element, and reported back console/Network-tab findings, giving a confirmed (not hypothesized) root cause for G-02.2-4."
    - "G-02.2-4 is not falsely marked closed by this plan — it remains an open gap until the confirmed root cause and its fix are captured in a follow-up plan."
  artifacts:
    - client/src/components/diagrams/components/ExcalidrawWrapper.tsx (temporary, greppable TEMP-DEBUG G-02.2-4 console logging — zero behavioral change)
    - .planning/debug/sovereignty-marker-sync-not-live.md (new section appending the human's reported live-instrumentation findings)
  key_links:
    - "ExcalidrawWrapper.handleChange (instrumented, post Plan 02.2-03's retry-safe fix) -> human-run yarn dev session -> reported console/Network findings -> follow-up gap-closure plan implementing the confirmed G-02.2-4 fix and removing this plan's temporary instrumentation"
---

<objective>
Locate the actual break point for G-02.2-4 (native keyboard Delete/Backspace leaves marker orphans; only Ctrl+R cleans up), whose root cause could not be confirmed via static analysis alone (see `.planning/debug/sovereignty-marker-sync-not-live.md` Resolution, root cause #2). Add temporary, clearly-marked diagnostic logging around `removeOrphanedMarkersLive`'s invocation site and its `updateScene` call in `ExcalidrawWrapper.tsx`'s `handleChange`, then hand off to a human to run `yarn dev`, reproduce the bug, and report back console/Network findings — no browser is available to the planning/execution agent.

Purpose: `removeOrphanedMarkersLive`'s logic reads correct in isolation and was traced against Excalidraw v0.18.1's own internals with no bug found; live instrumentation is the only remaining way to pinpoint whether `handleChange` even runs on native delete, what `isDeleted`/`elements` state it sees, and whether `updateScene` is actually invoked. This plan is diagnose-only for G-02.2-4 — it deliberately does NOT contain the fix, since writing one against an unconfirmed root cause would risk masking the real bug (per `planner_authority_limits`: missing information is a legitimate reason to split work, not a reason to guess).
Output: Temporary instrumentation added (zero behavioral change); a blocking checkpoint capturing the human's live-browser findings; the debug session file updated with those findings for whoever plans the follow-up fix.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-UAT.md
@.planning/debug/sovereignty-marker-sync-not-live.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-03-SUMMARY.md
@client/src/components/diagrams/components/ExcalidrawWrapper.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add temporary diagnostic logging around handleChange's native-delete cleanup path</name>
  <files>client/src/components/diagrams/components/ExcalidrawWrapper.tsx</files>
  <read_first>
    - client/src/components/diagrams/components/ExcalidrawWrapper.tsx — `handleChange`'s current shape after Plan 02.2-03's Task 1: the top-of-function `suppressOnChangeRef` guard (`if (suppressOnChangeRef?.current) { suppressOnChangeRef.current = false; return }`); the `removeOrphanedMarkersLive(effectiveElements)` call and its `if (orphansRemoved) { ...suppressOnChangeRef...; apiRef.current?.updateScene({ elements: cleanedElements, captureUpdate: CaptureUpdateAction.IMMEDIATELY }) }` block.
    - .planning/debug/sovereignty-marker-sync-not-live.md — Resolution, root cause #2 (UNCONFIRMED) and the gap's `missing` item: "Add temporary console logging around removeOrphanedMarkersLive's invocation and its updateScene call to confirm whether handleChange even runs to that point on native Delete/Backspace."
  </read_first>
  <action>
    Add temporary, clearly-marked diagnostic logging at exactly four points in `handleChange`, each prefixed with the literal string `TEMP-DEBUG G-02.2-4` in both the log message and an adjacent single-line comment (so every insertion is trivially greppable and removable later): (1) at the very top of `handleChange`, after the `suppressOnChangeRef` guard, log `elements.length`, the count of elements where `isDeleted === true`, and the current value of `suppressOnChangeRef?.current` at entry; (2) immediately before the `removeOrphanedMarkersLive(effectiveElements)` call, log the count of elements whose `customData?.sovereigntyMarker` is `'fill'` or `'ring'` and their individual `isDeleted` values; (3) immediately after that call, log the returned `orphansRemoved` boolean and, if true, the `id`/`customData.mainElementId` of every marker element being tombstoned in `cleanedElements`; (4) immediately around the resulting `apiRef.current?.updateScene(...)` call inside the `if (orphansRemoved)` block, log a message confirming the call was reached and its `captureUpdate` value. Use `console.debug` (not `console.log`) for all four so they can be filtered easily. This task is purely additive logging — it must not alter any conditional, computation, or control flow; the resulting diff should show only new `console.debug(...)` statements and their adjacent one-line `// TEMP-DEBUG G-02.2-4` comments.
  </action>
  <verify>
    <automated>cd client && yarn type-check</automated>
    <verify_no_behavior_change>grep -n "TEMP-DEBUG G-02.2-4" client/src/components/diagrams/components/ExcalidrawWrapper.tsx | wc -l</verify_no_behavior_change>
  </verify>
  <done>Four TEMP-DEBUG G-02.2-4-tagged console.debug statements exist at the specified call sites in handleChange, with zero change to any existing conditional, computation, or control flow.</done>
  <acceptance_criteria>
    - `grep -c "TEMP-DEBUG G-02.2-4" client/src/components/diagrams/components/ExcalidrawWrapper.tsx` reports at least 4.
    - `cd client && yarn type-check` passes with zero errors.
    - The diff for this task touches only new `console.debug(...)` lines and their adjacent `// TEMP-DEBUG G-02.2-4` comments — no existing line is modified or removed.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live browser reproduction + findings report-back (G-02.2-4)</name>
  <what-built>Temporary diagnostic logging (Task 1) around handleChange's native-keyboard-delete marker cleanup path, tagged TEMP-DEBUG G-02.2-4.</what-built>
  <how-to-verify>
    1. `cd client && yarn dev` — enable `featureFlags.Sovereignty`, select a company, open a diagram containing a main element that already has a full fill/ring marker pair applied (sync via Ctrl+R first if needed).
    2. Open browser DevTools: Console tab (filter for `TEMP-DEBUG G-02.2-4`) and Network tab.
    3. Select the marked main element and press keyboard Delete (or Backspace) — do not use the context menu or Ctrl+X.
    4. Record and report back:
       - Whether any `TEMP-DEBUG G-02.2-4` log lines appeared at all (confirms/denies whether `handleChange` runs on native delete).
       - The logged `elements.length` / `isDeleted` counts from log point (1).
       - The logged marker `isDeleted` states from log point (2).
       - The logged `orphansRemoved` value from log point (3), and which marker ids (if any) were reported as being tombstoned.
       - Whether log point (4)'s "updateScene reached" message appeared.
       - Whether any GraphQL request fired in the Network tab during this action.
       - Whether the marker ellipse visually vanished immediately, after a delay, or not at all (until a manual Ctrl+R).
    5. Paste the console output (or a summary covering every point above) and your Network-tab observation back into the conversation.
  </how-to-verify>
  <resume-signal>Paste the console log output and Network-tab findings for all 4 log points, or type "no logs appeared" if handleChange never fires on native delete.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the human's findings in the debug session file</name>
  <files>.planning/debug/sovereignty-marker-sync-not-live.md</files>
  <read_first>
    - .planning/debug/sovereignty-marker-sync-not-live.md — the existing `## Evidence` and `## Resolution` sections' format and tone, to match when appending.
  </read_first>
  <action>
    Once the human has responded with findings from the checkpoint task, append a new dated entry under `## Evidence` in `.planning/debug/sovereignty-marker-sync-not-live.md` titled "Live browser instrumentation (G-02.2-4, Plan 02.2-04)" containing the human's reported console/Network findings verbatim (or a faithful summary), followed by an `implication:` line stating what this confirms or rules out about root cause #2. Do NOT edit the existing `## Resolution` section's root-cause #2 text in place — instead, add a new short paragraph immediately after it, e.g. "Update ({date}): live instrumentation confirmed/ruled out {X} — see new Evidence entry above. G-02.2-4 remains open; a follow-up gap-closure plan is required to implement the confirmed fix and remove this plan's temporary TEMP-DEBUG G-02.2-4 logging from ExcalidrawWrapper.tsx." Do not attempt to write the actual fix in this task — the root cause confirmation from the human's report is a prerequisite for that, and belongs in a subsequent plan.
  </action>
  <verify>
    <automated>grep -c "Live browser instrumentation (G-02.2-4" .planning/debug/sovereignty-marker-sync-not-live.md</automated>
  </verify>
  <done>The debug session file contains the human's reported live-instrumentation findings and an explicit note that G-02.2-4 remains open pending a follow-up fix plan.</done>
  <acceptance_criteria>
    - `.planning/debug/sovereignty-marker-sync-not-live.md`'s `## Evidence` section contains a new entry with the human's reported findings.
    - The `## Resolution` section's existing root-cause #2 text is unmodified; a new update paragraph is appended after it.
    - The new update paragraph explicitly states G-02.2-4 remains open and that a follow-up plan must implement the fix and remove the TEMP-DEBUG G-02.2-4 logging.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser console/DevTools ↔ diagnostic logging | New, purely observational trust boundary — temporary `console.debug` output only, no new user input path, no data written anywhere except the browser console. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-08 | Information Disclosure | Temporary `console.debug` logging (`ExcalidrawWrapper.handleChange`) | low | accept | Logs only element ids, counts, and boolean flags already visible to the authenticated client rendering the diagram — no secrets, tokens, or cross-tenant data. Explicitly temporary and greppable (`TEMP-DEBUG G-02.2-4`); this plan's Task 3 records an explicit requirement that a follow-up plan removes it, preventing indefinite accumulation of dev-only logging in production code. |

</threat_model>

<verification>
- `cd client && yarn type-check` passes with zero errors after Task 1.
- Task 1's diff is logging-only (no behavior change) per its acceptance criteria.
- The blocking checkpoint task receives a human response containing concrete console/Network findings before Task 3 runs.
- `.planning/debug/sovereignty-marker-sync-not-live.md` is updated per Task 3 without altering the existing Resolution text.
</verification>

<success_criteria>
- Temporary diagnostic logging is in place with zero functional change.
- A human has reproduced native-keyboard delete against the instrumented build and reported concrete findings.
- The debug session file captures those findings, and explicitly states G-02.2-4 remains open pending a follow-up gap-closure plan that implements the confirmed fix and removes the temporary logging.
</success_criteria>

<artifacts_produced>

## Artifacts this plan produces

- `client/src/components/diagrams/components/ExcalidrawWrapper.tsx`:
  - Four `TEMP-DEBUG G-02.2-4`-tagged `console.debug(...)` statements in `handleChange`, purely additive, zero behavioral change. **Must be removed by whichever follow-up plan implements the confirmed G-02.2-4 fix.**
- `.planning/debug/sovereignty-marker-sync-not-live.md`:
  - New `## Evidence` entry recording the human's live-instrumentation findings.
  - New update paragraph after the existing root-cause #2 text, explicitly noting G-02.2-4 remains open.

</artifacts_produced>

<output>
Create `.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-04-SUMMARY.md` when done. This SUMMARY must explicitly state that G-02.2-4 is NOT closed by this plan — it is diagnose-only — and must name the next required action: a follow-up gap-closure plan implementing the confirmed fix and removing the TEMP-DEBUG G-02.2-4 instrumentation.
</output>
