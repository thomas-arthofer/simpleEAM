---
phase: 02.2
plan: 04
subsystem: ui
tags: [excalidraw, react, sovereignty-markers, diagrams, gap-closure, docker]

requires:
  - phase: 02.2
    plan: 03
    provides: 'Retry-safe D-02 diff-detection + suppressOnChangeRef wiring — the fix that turned out to also resolve G-02.2-4'
provides:
  - 'Confirmed (not hypothesized) root cause for G-02.2-4: already fixed by Plan 02.2-03, no separate code change needed'
  - 'Confirmed procedural fact: nextgen-eam-client is a non-bind-mounted production Docker build — code changes require docker compose build + up --force-recreate to reach the running container'
affects: [sovereignty-markers, diagram-lifecycle]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - client/src/components/diagrams/components/ExcalidrawWrapper.tsx
    - .planning/debug/sovereignty-marker-sync-not-live.md

key-decisions:
  - 'Deviated from the plan''s diagnose-only framing: the plan assumed G-02.2-4''s root cause would need a follow-up fix plan. The human''s live-instrumentation report showed the gap is already resolved by Plan 02.2-03, so this plan closes it directly instead of leaving it open for a plan that would have had no code change to make.'
  - 'TEMP-DEBUG G-02.2-4 logging was removed in this same plan (rather than deferred to a follow-up) since the confirmation and the cleanup landed in the same session with no gap in between.'

patterns-established: []

requirements-completed: [SOV-MARKER-DELETE]

coverage:
  - id: T1
    description: 'Temporary TEMP-DEBUG G-02.2-4 console.debug instrumentation added around removeOrphanedMarkersLive and its updateScene call in handleChange'
    requirement: SOV-MARKER-DELETE
    verification:
      - kind: other
        ref: 'cd client && yarn type-check'
        status: pass
    human_judgment: false
  - id: T2
    description: 'Human reproduced native keyboard Delete against a freshly rebuilt Docker client container and reported console findings'
    requirement: SOV-MARKER-DELETE
    verification:
      - kind: other
        ref: 'Live browser session (Docker-deployed client, company amag, marked BusinessCapability/DataObject element)'
        status: pass
    human_judgment: true
    rationale: 'Required live browser interaction with DevTools console — not exercisable in this sandboxed environment.'
  - id: T3
    description: 'Debug session file updated with confirmed findings; G-02.2-4 marked closed; TEMP-DEBUG logging removed'
    requirement: SOV-MARKER-DELETE
    verification:
      - kind: other
        ref: 'grep -c "Live browser instrumentation (G-02.2-4" .planning/debug/sovereignty-marker-sync-not-live.md'
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-08-03
status: complete
---

# Phase 02.2 Plan 04: G-02.2-4 live instrumentation — confirmed already resolved Summary

**G-02.2-4 (native keyboard delete leaves marker orphans) is CLOSED: live browser instrumentation confirmed Plan 02.2-03's retry-safe fix already resolves it, no separate code change was needed. TEMP-DEBUG logging removed. G-02.2-5 (context-menu delete) remains open per the existing unconfirmed asymmetry noted in the debug file.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-03 (this session)
- **Completed:** 2026-08-03
- **Tasks:** 3/3 completed (Task 2 was the blocking human-verify checkpoint)
- **Files modified:** 2

## Accomplishments

- Added temporary `TEMP-DEBUG G-02.2-4` `console.debug` instrumentation at 4 points in `ExcalidrawWrapper.tsx`'s `handleChange` (entry state, pre-cleanup marker states, `removeOrphanedMarkersLive` result, `updateScene` reached) — purely additive, type-checked clean.
- Human's first reproduction attempt reported **zero** log lines for any action (including drag, an already-working code path) — traced to the running `nextgen-eam-client` Docker container being 3+ days stale (built before all Phase 02.2 commits), since the client service has no source bind-mount and requires an explicit image rebuild to pick up code changes.
- Rebuilt (`docker compose build client`) and force-recreated (`docker compose up -d --force-recreate client`) the container. Human repeated the exact same test: `handleChange` fired correctly, `removeOrphanedMarkersLive` reported `orphansRemoved: true` with exactly the deleted element's fill/ring pair (`tombstonedMarkers: Array(2)`), `updateScene` was reached, and the marker ellipse visually vanished together with the deleted box.
- Recorded the confirmed findings in `.planning/debug/sovereignty-marker-sync-not-live.md` (new Evidence entry + update paragraph after the existing root-cause #2 text, without editing that text in place) and marked G-02.2-4 closed.
- Removed the now-unneeded `TEMP-DEBUG G-02.2-4` instrumentation from `ExcalidrawWrapper.tsx` — no behavioral change, type-checked clean.
- Noted, but did not investigate, a secondary observation: arrows bound to a deleted element remain on canvas after delete. Human flagged this as "possibly no finding" — likely pre-existing native Excalidraw behavior unrelated to sovereignty markers; not in scope for this gap.

## Task Commits

- `62414d6` — debug(02.2-04): add TEMP-DEBUG G-02.2-4 instrumentation (Task 1)
- `c1a61f8` — fix(02.2-04): confirm G-02.2-4 resolved via live instrumentation, strip TEMP-DEBUG logging (Tasks 3, plus cleanup)

## Verification

- `cd client && yarn type-check` — clean, zero errors, both before and after instrumentation removal.
- `grep -c "Live browser instrumentation (G-02.2-4" .planning/debug/sovereignty-marker-sync-not-live.md` → 1.
- `grep -c "TEMP-DEBUG" client/src/components/diagrams/components/ExcalidrawWrapper.tsx` → 0 (fully removed).
- Live browser reproduction (human, Docker-deployed client, post-rebuild): native keyboard Delete correctly removes both the main element and its sovereignty markers in one step. **Pass.**

## Deviations

- The plan's `<output>` instructions assumed this plan would remain diagnose-only and require a follow-up gap-closure plan to implement the fix. Instead, the human's confirmed findings showed G-02.2-4 was already resolved by Plan 02.2-03 — there was no separate bug to fix. This SUMMARY closes G-02.2-4 directly rather than deferring to a follow-up plan, and this plan's own Task 1 instrumentation was removed here (not deferred) since no gap existed between confirmation and cleanup.
- A real, unplanned infrastructure issue was found and fixed along the way: the `nextgen-eam-client` Docker container was stale (predating all Phase 02.2 commits) and had to be rebuilt/force-recreated mid-session before the human's test was meaningful. This was necessary to get a valid signal and is not a deviation from scope, just an added prerequisite step.

## Next

- G-02.2-5 (custom context-menu Delete leaves markers orphaned) is still open and unconfirmed — the debug file's root-cause #2 update explicitly calls out that the `suppressOnChangeRef` asymmetry in `FullCustomContextMenu.tsx` (fixed in Plan 02.2-03) is a plausible but unverified fix for it; needs its own manual UAT re-verification pass (test 5 in `02.2-UAT.md`).
- Manual UAT re-run of tests 3, 5, 6 in `02.2-UAT.md` (still pending per Plan 02.2-03's SUMMARY) should now be done against the rebuilt Docker container or a `yarn dev` session running current code.
- Update `.planning/STATE.md` to reflect Phase 02.2's true status: Plans 03 and 04 both complete, G-02.2-3/4/6 closed, G-02.2-5 still pending manual re-verification.
