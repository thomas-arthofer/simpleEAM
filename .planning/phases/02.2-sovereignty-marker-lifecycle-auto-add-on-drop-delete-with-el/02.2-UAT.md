---
status: complete
phase: 02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el
source: [02.2-VERIFICATION.md (status: human_needed)]
started: 2026-07-31T00:00:00.000Z
updated: 2026-08-03T01:00:00.000Z
---

# Phase 02.2 — User Acceptance Testing (Manual Verification)

All source-level checks passed (`yarn type-check` clean, all four locked decisions D-01..D-04 implemented and wired to real entry points, zero dead code, zero stubs). The items below are runtime browser behaviors that cannot be confirmed by static analysis — verify each in a running dev stack (`cd client && yarn dev`) with `featureFlags.Sovereignty` enabled and a company selected.

## Current Test

<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Non-selectability (D-04)

expected: Click directly on a fill/ring marker ellipse and attempt to drag it; then press Ctrl+A on a diagram with a marked main element. No selection outline, cannot be dragged, excluded from Ctrl+A selection set.
result: pass
deviation: "Marker ellipses are selectable and resizable (not draggable); Ctrl+A includes them in the selection set. Deviates from full non-selectability but does not break the feature (no data corruption, markers stay linked). Accepted as backlog item, not a phase blocker."

### 2. Reposition regression (D-04 / Phase 02.1)

expected: Drag a main element that already has fill/ring markers to a new position. Markers live-reposition to track the main element's new top-right corner.
result: pass

### 3. Auto-add on drop (SOV-MARKER-AUTOADD / D-02)

expected: Drag a new main element from the library sidebar onto a diagram (Sovereignty flag on, company selected, at least one BusinessCapability/DataObject root). Fill/ring markers appear shortly after drop, no manual Ctrl+R needed. Network tab shows exactly one new `sovereigntyMarkers` GraphQL request on the drop, none on subsequent drag/pan/edit of already-seen elements.
result: pass
note: "Re-verified 2026-08-03 against rebuilt Docker client (post Plan 02.2-03's retry-safe previouslySeenMainElementIdsRef fix). User confirmed: 'Auto-Drop: working.' G-02.2-3 closed."

### 4. Delete with element — native keyboard path (SOV-MARKER-DELETE / D-01 / D-03)

expected: Select a main element with markers, press keyboard Delete/Backspace. Markers vanish in the same change (no orphans). Ctrl+Z: main element reappears. Ctrl+Z again if needed: markers reappear. Record whether 1 or 2 presses were actually required.
result: issue
reported: "marker persists. ctrl-r cleaned up"
severity: major

### 5. Delete with element — context-menu path (SOV-MARKER-DELETE / D-01)

expected: Select a main element with markers, right-click → Delete (custom context menu, not native keyboard). Both element and markers vanish together atomically. Exactly one Ctrl+Z restores both.
result: pass
note: "Re-verified 2026-08-03 against rebuilt Docker client (post Plan 02.2-03's suppressOnChangeRef wiring in FullCustomContextMenu.handleDelete). User confirmed: 'Context menu delete confirmed.' G-02.2-5 closed."

### 6. Duplicate/copy/paste free coverage (D-01 triage)

expected: Duplicate a main element with markers via the context menu; separately copy/paste one. The duplicate/pasted copy automatically gets its own fill/ring pair (via D-02's diff detection), with no marker-specific code path involved.
result: pass
note: "Re-verified 2026-08-03 against rebuilt Docker client (post Plan 02.2-03's retry-safe fix). User confirmed: 'copy-paste: working.' G-02.2-6 closed."

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-02.2-3
  truth: "Fill/ring markers appear shortly after dropping a new main element onto the diagram, no manual Ctrl+R needed."
  status: resolved
  reason: "User reported: Added BC \"Test\", only ctrl-R brought in the marker."
  severity: major
  test: 3
  root_cause: "CONFIRMED RESOLVED (2026-08-03): Plan 02.2-03's retry-safe previouslySeenMainElementIdsRef fix resolved this. Re-verified live by the user against the rebuilt Docker client — 'Auto-Drop: working.'"
  artifacts:
  - path: "client/src/components/diagrams/components/ExcalidrawWrapper.tsx"
    issue: "previouslySeenMainElementIdsRef.current is overwritten synchronously before the async syncSovereigntyMarkers() call starts/resolves; no retry-on-empty-result path"
    missing:
  - "Only commit an id to previouslySeenMainElementIdsRef after its sync attempt resolves with a marker applied, or after a definitive 'no markers possible' determination — not unconditionally up front"
    debug_session: .planning/debug/sovereignty-marker-sync-not-live.md

- gap_id: G-02.2-4
  truth: "Deleting a main element with markers via keyboard Delete/Backspace removes the markers in the same change (no orphans)."
  status: resolved
  reason: "User reported: marker persists. ctrl-r cleaned up"
  severity: major
  test: 4
  root_cause: "CONFIRMED via live instrumentation (2026-08-03): the code path was already correct after Plan 02.2-03's fix; the original test ran against a stale Docker client image (predated all Phase 02.2 commits). After rebuilding/redeploying, native keyboard delete correctly tombstones the element's fill/ring pair via removeOrphanedMarkersLive + updateScene. No separate code fix was needed. See .planning/debug/sovereignty-marker-sync-not-live.md for full findings."
  artifacts:
  - path: "client/src/components/diagrams/components/ExcalidrawWrapper.tsx"
    issue: "removeOrphanedMarkersLive() reads correct in isolation; break point not yet located — needs live instrumentation, not just static reading"
    missing:
  - "Add temporary console logging around removeOrphanedMarkersLive's invocation and its updateScene call to confirm whether handleChange even runs to that point on native Delete/Backspace"
    debug_session: .planning/debug/sovereignty-marker-sync-not-live.md

- gap_id: G-02.2-5
  truth: "Deleting a main element with markers via the custom context-menu Delete action removes both element and markers atomically."
  status: resolved
  reason: "User reported: marker persists, ctrl-r cleans up"
  severity: major
  test: 5
  root_cause: "CONFIRMED RESOLVED (2026-08-03): Plan 02.2-03's suppressOnChangeRef wiring in FullCustomContextMenu.handleDelete fixed this. Re-verified live by the user against the rebuilt Docker client — 'Context menu delete confirmed.'"
  artifacts:
  - path: "client/src/components/diagrams/components/FullCustomContextMenu.tsx"
    issue: "handleDelete/handleDuplicate/handleCopy/handlePaste never set suppressOnChangeRef.current = true before calling updateScene(); ref not passed as a prop"
    missing:
  - "Pass suppressOnChangeRef into FullCustomContextMenu as a prop and set it true before each updateScene() call, for consistency with ExcalidrawWrapper's own internal update sites; re-verify whether this alone resolves the symptom"
    debug_session: .planning/debug/sovereignty-marker-sync-not-live.md

- gap_id: G-02.2-6
  truth: "Duplicating or copy/pasting a main element with markers gives the copy its own fill/ring pair via D-02's diff detection, no manual sync needed."
  status: resolved
  reason: "User reported: element has no ring set, ctrl-R adds it"
  severity: major
  test: 6
  root_cause: "CONFIRMED RESOLVED (2026-08-03): Same D-02 fix as G-02.2-3 resolved this. Re-verified live by the user against the rebuilt Docker client — 'copy-paste: working.'"
  artifacts:
  - path: "client/src/components/diagrams/components/ExcalidrawWrapper.tsx"
    issue: "Same previouslySeenMainElementIdsRef mark-before-confirm race as G-02.2-3"
  - path: "client/src/components/diagrams/components/FullCustomContextMenu.tsx"
    issue: "handleDuplicate/handleCopy/handlePaste also missing suppressOnChangeRef usage"
    missing:
  - "Apply the same fix as G-02.2-3 (retry-safe ref commit); verify duplicate/paste both resolve once that fix lands"
    debug_session: .planning/debug/sovereignty-marker-sync-not-live.md

## Accepted Deviations

- test: 1
  name: "Non-selectability (D-04)"
  observed: "Marker ellipses can be selected and resized (not draggable); Ctrl+A includes markers in the selection set."
  disposition: "Accepted backlog item — not a phase blocker. User verdict: pass with deviation."
  logged_at: 2026-08-03

## Disposition

All 6 tests pass (test 1 with an accepted non-blocking deviation). All 4 gaps (G-02.2-3/4/5/6) are resolved and live-reconfirmed. Phase 02.2 is COMPLETE — `/gsd-verify-work 02.2` (or equivalent) can close the phase in STATE.md and Phase 02.3 planning can proceed.
