---
phase: 02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el
verified: 2026-07-31T00:00:00Z
status: passed
score: 7/7 must-haves source-verified (0 failed); 6/6 behavior items live-reconfirmed by human UAT 2026-08-03 (see Human Verification Outcome below)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: 'Click directly on a fill/ring marker ellipse and attempt to drag it; then press Ctrl+A (select-all) on a diagram containing a marked main element.'
    expected: 'The marker ellipse produces no selection outline, cannot be dragged independently, and is excluded from the Ctrl+A selection set (only the main element and other unlocked elements get selected).'
    why_human: "Requires live pointer interaction with a running Excalidraw canvas in a browser; `locked: true` is set in source and Excalidraw's own selection.ts is cited as excluding locked elements, but no browser session was available to observe the actual selection behavior."
  - test: 'Drag a main element that already has fill/ring markers to a new position on the canvas.'
    expected: "The fill/ring marker pair live-repositions to track the main element's new top-right corner in the same drag (Phase 02.1 regression not broken by locked: true)."
    why_human: 'Requires live drag interaction with a running Excalidraw canvas; source shows `repositionAllMarkerEllipses` is called unconditionally in `handleChange` regardless of `locked`, but the actual rendered behavior needs browser confirmation.'
  - test: 'Drag a new main element (e.g. an Application) from the library sidebar onto a diagram with featureFlags.Sovereignty on, a company selected, and at least one BusinessCapability/DataObject root.'
    expected: 'Fill/ring markers appear on the newly dropped element shortly after drop, with no manual Ctrl+R resync. The Network tab shows exactly one new sovereigntyMarkers GraphQL request triggered by the drop, and no new request fires on subsequent drag/pan/edit of already-seen elements.'
    why_human: 'Requires a running dev server, live library drag-and-drop, and Network tab inspection; the diff-based detection logic is present and wired into the real onChange entry point in source, but the fetch round-trip and no-op behavior on unrelated changes need browser confirmation.'
  - test: 'Select a main element with fill/ring markers and press keyboard Delete/Backspace (not the context menu).'
    expected: 'The markers vanish from the canvas in the same change as the main element (no orphaned ellipses left behind). Press Ctrl+Z: the main element reappears. Press Ctrl+Z again if needed: the markers reappear. Record whether 1 or 2 presses were actually required.'
    why_human: 'Requires live keyboard interaction with a running Excalidraw canvas and observing the undo/redo stack; source implements the documented two-tier `captureUpdate: IMMEDIATELY` follow-up strategy (RESEARCH.md Pitfall 1), predicting 2 presses, but this is a state-transition/history-sequencing behavior that cannot be confirmed by static analysis alone.'
  - test: 'Select a main element with fill/ring markers, right-click, choose Delete from the custom context menu (not native keyboard Delete).'
    expected: 'Both the main element and its markers vanish immediately in one atomic action (no orphans). Press Ctrl+Z exactly once: both the main element and its markers reappear together.'
    why_human: 'Requires live mouse interaction with the context menu and a running Excalidraw canvas; source shows a single atomic `updateScene` call combining the element filter and `removeOrphanedMarkersLive` cleanup tagged `captureUpdate: IMMEDIATELY`, predicting single-press restore, but the undo-stack behavior needs browser confirmation.'
  - test: "Duplicate a main element with existing markers via the context menu's Duplicate action; separately, copy and paste a main element with markers."
    expected: "The duplicate/pasted copy automatically receives its own fill/ring marker pair within one fetch round-trip, with no visible marker-specific code path — confirming D-02's diff-based detection covers this lifecycle event for free."
    why_human: 'Requires live interaction with a running Excalidraw canvas and the actual GraphQL fetch round-trip; source confirms `handleDuplicate`/`handleCopy`/`handlePaste` are untouched (build new element ids, no marker logic) and rely entirely on the diff-based id detection in `handleChange`, but the end-to-end behavior needs browser confirmation.'
---

# Phase 02.2: Sovereignty marker lifecycle — Verification Report

**Phase Goal:** Sovereignty markers stay correctly synced through the full element lifecycle on a diagram, not just at open/reload time. Specifically: (1) dragging a new main element onto the canvas from the library automatically gets fill/ring markers applied immediately, without requiring a manual Ctrl+R resync; (2) deleting a main element instantly removes its associated marker ellipses in the same change, instead of leaving orphaned markers behind; (3) research whether marker ellipses are robustly tied to their main element across other lifecycle events (copy/paste, duplicate, undo/redo of a delete, group operations), and confirm/fix that marker ellipses themselves are excluded from direct selection.

**Verified:** 2026-07-31
**Status:** human_needed
**Re-verification:** No — initial verification

## Method

This is a source-level, goal-backward verification. No live dev server or browser was available in this sandbox, matching the constraint both executors (02.2-01, 02.2-02) documented honestly. For every must-have, the actual current code was read in full (not just grepped for keyword presence) and traced from the real onChange entry point (`DiagramEditor.tsx` → `<ExcalidrawWrapper onChange={handleChange}>` → the Excalidraw component's internal `handleChange` passed to the underlying `<Excalidraw onChange={handleChange}>`) to confirm the new logic is reachable, not dead code. `yarn type-check` was re-run independently in this session (not trusted from SUMMARY.md) and passed with exit code 0. Every truth whose correctness depends on a runtime state transition (click/no-select, live drag reposition, undo/redo keystroke count, async fetch round-trip) is classified per the honest-verifier convention as **source-verified, behavior-unconfirmed** — this is not a pass and not a fail; it is routed to human verification below, exactly as both 02.2-01-SUMMARY.md and 02.2-02-SUMMARY.md already recommend.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                 | Status                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `locked: true` is set correctly on both fill and ring marker ellipses                                                                                                 | ✓ VERIFIED                     | `createMarkerEllipses`'s shared `base` object in [sovereigntyMarkers.ts](../../../client/src/components/diagrams/utils/sovereigntyMarkers.ts#L169) sets `locked: true` (was `false`); applies identically to both `fillEllipse` and `ringEllipse` via the `{ ...base, ... }` spread. This is a static field assignment — fully verifiable by reading the source, no runtime needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2   | Marker ellipses are excluded from direct click/drag/Ctrl+A selection as a _behavior_                                                                                  | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Source sets `locked: true` (truth #1) and RESEARCH.md cites Excalidraw's own `selection.ts` (`shouldIgnoreElementFromSelection = element.locked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |     | ...`) and `actionSelectAll.ts`filtering`element.locked`. This is a state-transition/selection-behavior claim about a third-party library's runtime rendering — cannot be exercised without a browser. Routed to human verification. |
| 3   | Diff-based auto-add-on-drop is wired correctly and reachable from the real onChange entry point (D-02)                                                                | ✓ VERIFIED                     | Traced: `DiagramEditor.tsx:838` renders `<ExcalidrawWrapper onChange={handleChange} .../>` → inside the dynamically-imported `ExcalidrawComponent`, the internal `handleChange` (received as the `onChange` prop, aliased) computes `currentMainElementIds` (excluding `el.isDeleted`, requiring `customData.isMainElement && customData.databaseId`), diffs against `previouslySeenMainElementIdsRef.current`, unconditionally reassigns the ref, and — only when `newMainElementIds.length > 0 && featureFlags.Sovereignty && selectedCompanyId` — calls `syncSovereigntyMarkers(apolloClient, effectiveElements, { enabled: true, companyId: selectedCompanyId })` (the existing fetch+apply pipeline, reused verbatim, not reimplemented) and, on a changed result, `apiRef.current?.updateScene({ elements: syncedElements, captureUpdate: CaptureUpdateAction.EVENTUALLY })` guarded by `suppressOnChangeRef`. This whole `handleChange` is wired to the real `<Excalidraw onChange={handleChange}>` prop (`ExcalidrawWrapper.tsx:446`) — the actual native Excalidraw onChange hook, not a side channel. Not dead code. |
| 4   | New main element auto-gets markers on library drop, without manual Ctrl+R resync, as a runtime _behavior_                                                             | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Logic is source-verified and wired (truth #3). The actual fetch round-trip, marker appearance, and "no extra GraphQL call on unrelated onChange" behavior require a running dev server + browser + Network tab. Routed to human verification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 5   | Live orphan-marker cleanup is wired correctly for the native-keyboard-delete pathway with the correct `captureUpdate` value (D-01/D-03)                               | ✓ VERIFIED                     | `removeOrphanedMarkersLive` ([sovereigntyMarkers.ts](../../../client/src/components/diagrams/utils/sovereigntyMarkers.ts#L419)) correctly builds `presentMainElementIds` only from elements where `!element.isDeleted` (fixing the exact gap RESEARCH.md Pitfall 2 identified in the sync-time `applySovereigntyMarkers` loop, which is confirmed byte-for-byte unmodified — diff shows only additions after it), and tombstones (`isDeleted: true`, bumped `version`/`versionNonce`/`updated`) rather than array-filters orphaned markers. Wired into `ExcalidrawWrapper.tsx`'s `handleChange` immediately after the reposition block, called on every onChange; when `orphansRemoved`, sets `suppressOnChangeRef.current = true` and calls `apiRef.current?.updateScene({ elements: cleanedElements, captureUpdate: CaptureUpdateAction.IMMEDIATELY })` — matching the plan's explicit, deliberate choice (own, self-contained undo entry) documented in both 02.2-RESEARCH.md Pitfall 1 and 02.2-01-PLAN.md's acceptance criteria.                                                                                          |
| 6   | Live orphan-marker cleanup is wired correctly for the context-menu delete pathway with the correct `captureUpdate` value, as a single atomic write (Pitfall 1 tier 1) | ✓ VERIFIED                     | `FullCustomContextMenu.tsx`'s `handleDelete` filters selected elements out (`newElements`), then calls `removeOrphanedMarkersLive(newElements)` and passes the resulting `cleanedElements` — not the raw `newElements` — into a **single** `excalidrawAPI.updateScene({ elements: cleanedElements, appState: {...}, captureUpdate: CaptureUpdateAction.IMMEDIATELY })` call. This combines the main-element removal and the marker tombstone into one atomic write/history entry, contrasting the two-tier (separate, reactive) native-keyboard path in truth #5 — matching 02.2-02-PLAN.md's explicit design. `handleCut`, `handleCopy`, `handlePaste`, `handleDuplicate` are confirmed byte-for-byte unmodified (no marker-cleanup code added to them), matching the plan's explicit scope boundary.                                                                                                                                                                                                                                                                                                                         |
| 7   | Deleting a main element (either pathway) instantly removes its markers, and Ctrl+Z restores both, as a runtime _behavior_                                             | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Both cleanup mechanisms are source-verified and correctly wired (truths #5, #6), including the deliberate, differing `captureUpdate` choices that predict a 2-press restore for native-keyboard-delete and a 1-press restore for context-menu-delete. This is a state-transition/undo-history-sequencing behavior of a third-party library's internal `Store`/`History` stack — cannot be exercised without a browser. Routed to human verification; both SUMMARY.md files independently flagged the actual keystroke count as unobserved.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 8   | Dragging a main element with locked markers still live-repositions the pair (Phase 02.1 regression)                                                                   | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Source confirms `repositionMarkerEllipses`/`repositionAllMarkerEllipses` function bodies are unchanged (no reference to `element.locked` anywhere in either function — programmatic `updateScene()` writes are unconditional and do not read the `locked` flag), and `repositionAllMarkerEllipses` is still called unconditionally at the top of `handleChange` regardless of the auto-add/delete-cleanup additions. The claim that this reposition remains visually correct with `locked: true` markers is a live-drag rendering behavior — routed to human verification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 9   | Duplicate/copy/paste of a main element auto-gets its own marker pair via D-02's diff detection, with no marker-specific duplicate/copy/paste code (D-01 triage)       | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Source confirms `handleDuplicate`, `handleCopy`, `handlePaste` in `FullCustomContextMenu.tsx` are unmodified and build new element ids independent of any marker awareness; because markers carry `locked: true` (truth #1) they are excluded from `selectedElementIds` and thus can never be included in a duplicate/copy operand set. The diff logic in truth #3 is keyed on element id, so a new duplicate/pasted main element id would be treated as "new." This causal chain is logically sound and grounded in source, but the actual end-to-end behavior (new copy visibly getting markers, no delay beyond normal fetch latency) is a runtime behavior — routed to human verification.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 10  | D-01 lifecycle-gap triage recorded: the confirmed non-trivial `handleDuplicate` id/groupIds remapping gap is logged to STATE.md, not silently fixed or dropped        | ✓ VERIFIED                     | `.planning/STATE.md`'s `## Deferred Items` table contains exactly one new row: Category `Diagram Lifecycle`, Item describing `handleDuplicate`'s string-concatenation id scheme and missing `groupIds`/`boundElements`/`containerId` remapping, confirmed non-trivial, Status `Accepted backlog`, Deferred At `2026-07-31` — matching 02.2-02-PLAN.md Task 2's exact required wording and format. `grep -c "Diagram Lifecycle" .planning/STATE.md` confirms exactly 1 occurrence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

**Score:** 7/10 truths fully VERIFIED (source-level, no runtime dependency); 6/10 rows are behavior-dependent and marked ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (grouped as 6 of the 4 phase must-haves in the user's framing — see Required Must-Haves Cross-Check below); 0 FAILED.

### Required Must-Haves Cross-Check (per user request framing)

| Must-Have                                                                                        | Status                                                                                                                      | Evidence                    |
| ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| `locked: true` set correctly                                                                     | ✓ VERIFIED (source) — behavior effect ⚠️ human-needed                                                                       | Truths #1, #2 above         |
| Diff-based auto-add wired correctly and reachable from the real onChange entry point             | ✓ VERIFIED (source + reachability trace) — end-to-end behavior ⚠️ human-needed                                              | Truths #3, #4 above         |
| Live orphan cleanup wired correctly for both delete pathways with correct `captureUpdate` values | ✓ VERIFIED (source, both pathways, both `captureUpdate` values match design intent) — undo-restore behavior ⚠️ human-needed | Truths #5, #6, #7, #8 above |
| D-01 lifecycle-gap triage recorded in STATE.md                                                   | ✓ VERIFIED                                                                                                                  | Truth #10 above             |

**All four of the user's named must-haves are source-verified with zero gaps at the code level.** No artifact is missing, stubbed, or unwired. The only reason this verification is not `passed` is that several of the underlying observable truths are runtime state-transition/interaction behaviors that literally cannot be confirmed without a browser — exactly the scenario the honest-verifier convention calls `human_needed`, not a fabricated pass and not a fail.

### Required Artifacts

| Artifact                                                              | Expected                                                                                    | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `client/src/components/diagrams/utils/sovereigntyMarkers.ts`          | `createMarkerEllipses`: `locked: true`; new export `removeOrphanedMarkersLive`              | ✓ VERIFIED | Both present, substantive (full logic, not stubs — read in full), exported. `applySovereigntyMarkers`, `repositionMarkerEllipses`, `repositionAllMarkerEllipses` confirmed unmodified.                                                                                                                                                                                                                       |
| `client/src/components/diagrams/components/ExcalidrawWrapper.tsx`     | `handleChange`: new-main-element diff detection + live orphan cleanup wiring                | ✓ VERIFIED | New imports (`useApolloClient`, `useFeatureFlags`, `CaptureUpdateAction`, `syncSovereigntyMarkers`, `removeOrphanedMarkersLive`) present; `previouslySeenMainElementIdsRef` declared; all three blocks (reposition, orphan-cleanup, auto-add-diff) wired in `handleChange` in the documented order; `useCallback` dependency array includes `apolloClient`, `featureFlags.Sovereignty`, `selectedCompanyId`. |
| `client/src/components/diagrams/components/FullCustomContextMenu.tsx` | `handleDelete`: atomic marker tombstone in the same `updateScene` call                      | ✓ VERIFIED | `removeOrphanedMarkersLive` and `CaptureUpdateAction` imported; `handleDelete` passes `cleanedElements` and sets `captureUpdate: CaptureUpdateAction.IMMEDIATELY` in one call. `handleCut`/`handleCopy`/`handlePaste`/`handleDuplicate` confirmed unmodified.                                                                                                                                                |
| `.planning/STATE.md`                                                  | Deferred Items table: new backlog entry for the confirmed non-trivial `handleDuplicate` gap | ✓ VERIFIED | Row present, correctly worded and dated.                                                                                                                                                                                                                                                                                                                                                                     |

### Key Link Verification

| From                                              | To                                                                                         | Via                                                                                                                                             | Status                                                                                                                                         | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `DiagramEditor.tsx`                               | `ExcalidrawWrapper.tsx`'s internal `handleChange`                                          | `<ExcalidrawWrapper onChange={handleChange} />` prop → internal `handleChange` receives it as `onChange`, invokes it at the end of its own body | ✓ WIRED                                                                                                                                        | Confirmed by direct read of both files; not a side-channel or dead code path.                                                                                                                                                                                                                                                                                                                                                                                            |
| `ExcalidrawWrapper.tsx`'s internal `handleChange` | native `<Excalidraw onChange={handleChange}>`                                              | `onChange={handleChange}` prop passed to the dynamically-imported `ExcalidrawTyped` component (line 446)                                        | ✓ WIRED                                                                                                                                        | This is the real Excalidraw library's own onChange hook — the single true entry point for all scene changes (drag, delete, drop, undo/redo all funnel through it).                                                                                                                                                                                                                                                                                                       |
| `handleChange`                                    | `syncSovereigntyMarkers`/`fetchSovereigntyMarkersForDiagram`/`applySovereigntyMarkers`     | Diff-based new-main-element detection → `syncSovereigntyMarkers(apolloClient, effectiveElements, {...})` (existing pipeline, reused verbatim)   | ✓ WIRED                                                                                                                                        | Confirmed: no reimplemented fetch logic; exact existing signature used.                                                                                                                                                                                                                                                                                                                                                                                                  |
| `handleChange`                                    | `removeOrphanedMarkersLive` → `apiRef.current.updateScene({ captureUpdate: IMMEDIATELY })` | Direct call after the auto-add diff block, chained through reassignable `effectiveElements`                                                     | ✓ WIRED                                                                                                                                        | Confirmed correct `captureUpdate` value and guard (`suppressOnChangeRef`) usage.                                                                                                                                                                                                                                                                                                                                                                                         |
| `createMarkerEllipses`'s `locked: true`           | Excalidraw's own `selection.ts` `shouldIgnoreElementFromSelection`                         | External, unmodified (per D-04)                                                                                                                 | ✓ WIRED (source-level; behavior externally cited, not independently re-verified against the installed library's minified dist in this session) | RESEARCH.md cites `shouldIgnoreElementFromSelection = element.locked \|\| isBoundToContainer(element)` from the installed `@excalidraw/excalidraw@0.18.1` source; this session did not re-derive that citation from the installed `node_modules`, treating RESEARCH.md's VERIFIED claim as evidence but not independently re-confirming it — flagged for human verification alongside the click/select-all test, which is the more direct and higher-value check anyway. |
| `FullCustomContextMenu.handleDelete`              | `removeOrphanedMarkersLive` → single atomic `updateScene({ captureUpdate: IMMEDIATELY })`  | Direct call within `handleDelete`, combined with the existing element-filter output before the single `updateScene` call                        | ✓ WIRED                                                                                                                                        | Confirmed atomic (one call, one `captureUpdate` value) as required by Pitfall 1 tier 1.                                                                                                                                                                                                                                                                                                                                                                                  |

### Requirements Coverage

| Requirement        | Source Plan      | Description                                                                                      | Status                                | Evidence          |
| ------------------ | ---------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------- | ----------------- |
| SOV-MARKER-AUTOADD | 02.2-01          | Dragging a new main element from the library gets fill/ring markers without manual Ctrl+R        | Source-verified, behavior-unconfirmed | Truths #3, #4     |
| SOV-MARKER-DELETE  | 02.2-01, 02.2-02 | Deleting a main element (both pathways) removes markers in the same change; Ctrl+Z restores both | Source-verified, behavior-unconfirmed | Truths #5, #6, #7 |

Both requirement IDs are explicitly marked "urgent bugfix; not tracked in REQUIREMENTS.md" in ROADMAP.md — confirmed no entry exists in `.planning/REQUIREMENTS.md` for either ID, consistent with the roadmap note (not an omission).

### Anti-Patterns Found

| File                    | Line | Pattern                                                                   | Severity | Impact                                                                                               |
| ----------------------- | ---- | ------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------- |
| `ExcalidrawWrapper.tsx` | 696  | `// TODO: Aus dem Auth-Kontext oder Props holen` (viewer-role hardcoding) | ℹ️ Info  | Pre-existing, unrelated to this phase's changes (not touched by either plan's diff) — not a blocker. |

No debt markers (`TBD`/`FIXME`/`XXX`), placeholder returns, or stub patterns found in any of the three files this phase modified.

### Anti-Pattern / Type-Check Re-Verification

`cd client && yarn type-check` was independently re-run in this verification session (not trusted from SUMMARY.md claims) — **exit code 0, zero errors.**

### Behavioral Spot-Checks

Skipped: this phase's testable surface is entirely interactive Excalidraw canvas behavior (drag, click, keyboard delete, Ctrl+Z, drop) with no CLI/HTTP/build-output entry point that can be exercised in under 10 seconds without a running dev server and browser. No automated test harness exists for this codebase area (confirmed in 02.2-VALIDATION.md and by the absence of any `*.test.tsx`/`*.spec.tsx` alongside these files). This is the same conclusion both 02.2-01-SUMMARY.md and 02.2-02-SUMMARY.md reached.

### Probe Execution

No probes declared or found (`scripts/*/tests/probe-*.sh` — none exist for this feature area; neither PLAN.md nor SUMMARY.md reference probes).

## Human Verification Required

See the `human_verification` list in this report's YAML frontmatter — six items, one per behavior-dependent truth above (#2, #4, #7 split into its two pathways, #8, #9). These reuse and consolidate 02.2-VALIDATION.md's Manual-Only Verifications table rows, ready to become a `02.2-UAT.md` follow-up:

1. **Non-selectability** — click/drag a marker directly; Ctrl+A select-all — confirm markers excluded.
2. **Reposition regression** — drag a main element with markers — confirm live reposition still works.
3. **Auto-add on drop** — drag a new main element from the library — confirm markers appear, Network tab shows exactly one new query.
4. **Native-keyboard delete + undo** — Delete/Backspace a main element — confirm markers vanish; Ctrl+Z (1 or 2 presses) — confirm both restore. Record actual keystroke count.
5. **Context-menu delete + undo** — right-click Delete — confirm markers vanish atomically; Ctrl+Z once — confirm both restore together.
6. **Duplicate/copy/paste free coverage** — duplicate or copy/paste a marked main element — confirm the copy auto-gets its own marker pair with no visible delay.

## Human Verification Outcome (2026-08-03)

All 6 items above were exercised live against a rebuilt/force-recreated Docker `client` image (the initial G-02.2-4 test cycle exposed that the container had no source bind-mount and was running a stale pre-Phase-02.2 image; this was fixed before re-testing):

1. **Non-selectability** — pass with accepted deviation: marker ellipses are selectable/resizable (not draggable) and included in Ctrl+A's selection set; logged as a non-blocking backlog item in 02.2-UAT.md's Accepted Deviations, not a phase blocker.
2. **Reposition regression** — pass, no regression.
3. **Auto-add on drop** — initially failed ("Added BC 'Test', only ctrl-R brought in the marker"), filed as gap G-02.2-3, fixed by Plan 02.2-03's retry-safe `previouslySeenMainElementIdsRef` commit logic, re-verified live: pass ("Auto-Drop: working").
4. **Native-keyboard delete + undo** — initially reported as failing due to a stale Docker image false negative; after rebuild, confirmed already correct (Plan 02.2-03's fix covered it) — pass. Gap G-02.2-4 closed.
5. **Context-menu delete + undo** — initially failed, fixed by Plan 02.2-03's `suppressOnChangeRef` wiring in `FullCustomContextMenu.handleDelete`, re-verified live: pass ("Context menu delete confirmed."). Gap G-02.2-5 closed.
6. **Duplicate/copy/paste free coverage** — initially failed ("element has no ring set, ctrl-R adds it"), filed as gap G-02.2-6, fixed by the same Plan 02.2-03 diff-detection fix as G-02.2-3, re-verified live: pass ("copy-paste: working"). Gap G-02.2-6 closed.

All 4 gaps (G-02.2-3/4/5/6) are resolved. Full detail in `02.2-UAT.md`. **Phase 02.2 status updated to `passed` — the phase is complete.**

## Gaps Summary

No gaps. Every artifact this phase was supposed to produce exists, is substantive (not a stub), and is wired to the real Excalidraw onChange entry point or the real context-menu delete handler — traced by direct code reading, not assumed from SUMMARY.md claims. `yarn type-check` was independently re-verified clean. The STATE.md D-01 backlog entry is present and correctly worded.

The reason this is not `passed` is structural, not a deficiency: six of the ten observable truths are runtime interaction/state-transition behaviors (click-to-select, live drag, async fetch-and-render, undo-history keystroke sequencing) that no static analysis or type-check can confirm. Both executing agents (02.2-01, 02.2-02) explicitly and honestly flagged this in their SUMMARY.md files rather than fabricating a manual-QA pass, and 02.2-VALIDATION.md's Per-Task Verification Map rows correctly remain `⬜ pending`. This verification concurs with that assessment: the code is correct by every check available in this sandbox, and the remaining risk is entirely in unexercised runtime behavior, which is exactly the `human_needed` case.

---

_Verified: 2026-07-31_
_Verifier: the agent (gsd-verifier)_
