---
phase: 02.2
plan: 02
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - client/src/components/diagrams/components/FullCustomContextMenu.tsx
  - .planning/STATE.md
autonomous: true
requirements: [SOV-MARKER-DELETE]

must_haves:
  truths:
    - "Deleting a main element via the right-click context-menu Delete action removes its marker ellipses in the same updateScene call as the element delete itself (single, atomic history entry — one Ctrl+Z restores both, unlike the two-tier native-keyboard path)."
    - "Duplicating or copy/pasting a main element via the context menu automatically produces its own fill/ring marker pair via Plan 01's auto-add mechanism, with no marker-specific duplicate/copy/paste code added."
    - "Confirmed non-trivial lifecycle gaps found during research (e.g. handleDuplicate's id/groupIds remapping) are recorded as a backlog/findings item, not silently dropped or silently fixed out-of-scope."
  artifacts:
    - client/src/components/diagrams/components/FullCustomContextMenu.tsx (handleDelete: atomic marker tombstone in the same updateScene call)
    - .planning/STATE.md (Deferred Items table: new backlog entry for the confirmed non-trivial handleDuplicate gap)
  key_links:
    - "FullCustomContextMenu.handleDelete -> removeOrphanedMarkersLive (from Plan 01, client/src/components/diagrams/utils/sovereigntyMarkers.ts) -> single atomic updateScene({ captureUpdate: CaptureUpdateAction.IMMEDIATELY })"
    - "FullCustomContextMenu.handleDuplicate/handleCopy/handlePaste -> ExcalidrawWrapper.handleChange's diff-based auto-add detection (Plan 01, Task 2) — no new wiring needed, verified manually only"
---

<objective>
Close the second delete pathway (`FullCustomContextMenu.tsx`'s app-controlled context-menu Delete) with a truly atomic, single-`updateScene()` marker cleanup — giving a single-Ctrl+Z restore for this path, unlike the two-tier native-keyboard-delete path from Plan 01. Then apply D-01's case-by-case triviality judgment to the remaining named lifecycle events (copy/paste, duplicate, undo/redo, group operations): verify duplicate/copy/paste are already covered for free by Plan 01's diff-based auto-add mechanism, and log the one confirmed non-trivial gap (`handleDuplicate`'s missing id/groupIds remapping, RESEARCH.md Pitfall 4) as a backlog item rather than fixing it in this phase.

Purpose: SOV-MARKER-DELETE requires "delete with element" cleanup for *every* delete pathway, not just the reactive onChange one; the app's own context-menu delete is the one pathway where a fully atomic (not two-tier) fix is achievable, per RESEARCH.md Pitfall 1's tier-1 recommendation. The lifecycle-gap triage discharges D-01's "research other lifecycle events... surface any additional gaps found" mandate from the ROADMAP goal without silently expanding scope.
Output: `handleDelete` strips orphaned markers atomically; STATE.md's Deferred Items table gains one new, precisely-scoped backlog entry; manual verification confirms duplicate/copy/paste auto-marking and non-selectable group co-selection require no new code.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-CONTEXT.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-RESEARCH.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-VALIDATION.md
@.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-01-SUMMARY.md
@client/src/components/diagrams/components/FullCustomContextMenu.tsx
@client/src/components/diagrams/utils/sovereigntyMarkers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic marker cleanup in `handleDelete` — single `updateScene` call, one Ctrl+Z restore (Pitfall 1 tier 1)</name>
  <files>client/src/components/diagrams/components/FullCustomContextMenu.tsx</files>
  <read_first>
    - client/src/components/diagrams/components/FullCustomContextMenu.tsx (the full `handleDelete` callback — currently `elements.filter((el) => !appState.selectedElementIds[el.id])` then a single `excalidrawAPI.updateScene({ elements: newElements, appState: {...} })` with no `captureUpdate`; also read `handleCut` immediately above it, which has the same array-filter shape but is explicitly NOT in scope for this task)
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (`removeOrphanedMarkersLive`, added in Plan 01 — reuse verbatim; its `presentMainElementIds` check already correctly excludes `isDeleted` elements, matching this task's need to treat the just-filtered-out selected elements as no-longer-present)
    - .planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-RESEARCH.md (Pitfall 1's tier-1 recommendation in full: "do not react after the fact... extend handleDelete's own filter predicate to also strip/tombstone orphaned markers in the same updateScene() call... producing one genuinely atomic write with one captureUpdate value")
  </read_first>
  <action>
    In `FullCustomContextMenu.tsx`, import `removeOrphanedMarkersLive` from `../utils/sovereigntyMarkers` and `CaptureUpdateAction` from `@excalidraw/excalidraw`. Inside `handleDelete`, after computing the existing `newElements` (the array with selected elements filtered out — leave this existing filter-based removal of the main element itself unchanged, per RESEARCH.md's note that generally fixing `handleDelete`'s pre-existing filter-vs-tombstone pattern is out of this phase's scope), call `removeOrphanedMarkersLive(newElements)` to get `{ elements: cleanedElements, changed }`. Pass `cleanedElements` (not `newElements`) as the `elements` field of the existing `excalidrawAPI.updateScene(...)` call regardless of `changed` (so the call shape stays simple — `removeOrphanedMarkersLive` already returns the identical array reference when nothing changed, per its no-op contract). Add `captureUpdate: CaptureUpdateAction.IMMEDIATELY` to this same `updateScene` call's options object, alongside the existing `elements`/`appState` fields — this makes the whole delete (main element removal + marker tombstone) one single, explicit, self-contained history entry, giving a single-Ctrl+Z restore for this pathway (contrast with Plan 01 Task 3's two-tier native-keyboard path). Do not modify `handleCut`, `handleCopy`, `handlePaste`, or `handleDuplicate` in this task.
  </action>
  <verify>
    <automated>cd client && yarn type-check</automated>
    <manual>Per 02.2-VALIDATION.md's Manual-Only Verifications table: select a main element with fill/ring markers, right-click and choose Delete from the custom context menu — confirm markers vanish in the same action as the element (no orphans). Press Ctrl+Z exactly once — confirm BOTH the main element AND its markers reappear together (single-press restore, contrasting with the two-press native-keyboard-delete path from Plan 01 Task 3 — document both behaviors side-by-side in this plan's SUMMARY.md).</manual>
  </verify>
  <done>Context-menu Delete removes a main element and its markers in one atomic `updateScene` call tagged `captureUpdate: CaptureUpdateAction.IMMEDIATELY`; a single Ctrl+Z fully restores both.</done>
  <acceptance_criteria>
    - `handleDelete`'s `updateScene` call passes `elements: cleanedElements` (the output of `removeOrphanedMarkersLive(newElements)`), not the raw `newElements`.
    - The same `updateScene` call explicitly sets `captureUpdate: CaptureUpdateAction.IMMEDIATELY`.
    - `handleCut`, `handleCopy`, `handlePaste`, `handleDuplicate` are byte-for-byte unchanged by this task's diff.
    - Manual check: context-menu Delete on a main element with markers leaves zero orphaned marker ellipses visible immediately after the action.
    - Manual check: exactly one Ctrl+Z press after a context-menu Delete restores both the main element and its markers together.
  </acceptance_criteria>
  <reversibility rating="costly">Same undo/history-sequencing category as Plan 01 Task 3 (D-03) — this is the atomic-delete-path half of that same two-tier strategy. Not one-way (in-session undo-stack sequencing only, no data loss), so no blocking checkpoint:decision, but flagged per D-03's RESEARCH.md "costly" rating.</reversibility>
</task>

<task type="auto">
  <name>Task 2: D-01 lifecycle-gap triage — verify free coverage, log the one confirmed non-trivial gap</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-RESEARCH.md (Pitfall 3 in full: why duplicate/copy/paste of a main element are already covered for free by Plan 01's diff-based detection, since locked markers are never in `selectedElementIds` to begin with; Pitfall 4 in full: the one confirmed, non-trivial gap — `handleDuplicate`'s string-concatenation id scheme and missing `groupIds`/`boundElements`/`containerId` remapping, explicitly recommended as "log as backlog/findings item, do not fix in this phase")
    - .planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-CONTEXT.md (D-01's exact case-by-case rule and its explicit requirement: "Planner/executor should record the triviality call and rationale for each gap encountered")
    - .planning/STATE.md (the existing `## Deferred Items` table format and its columns: Category | Item | Status | Deferred At — follow this exact format for the new row)
  </read_first>
  <action>
    Perform the manual verification steps below, then edit `.planning/STATE.md`'s existing `## Deferred Items` table (do not create a new table or section) to append exactly one new row: Category `Diagram Lifecycle`, Item describing the confirmed gap — `handleDuplicate` (FullCustomContextMenu.tsx) builds duplicate element ids via string concatenation instead of `generateElementId()` and does not remap `groupIds`/`boundElements`/`containerId`, a latent grouping bug independent of sovereignty markers, confirmed non-trivial (would require extracting/reusing `createLibraryItemFromDatabaseElement`'s id/group remapping logic) — Status `Accepted backlog`, Deferred At today's date matching the table's existing date format. Do not add rows for duplicate/copy/paste-of-main-element marker coverage or group-operation co-selection — both are confirmed FREE (no gap) by the manual checks below and require no backlog entry, only the verification record in this task's acceptance criteria / the plan's SUMMARY.md.
  </action>
  <verify>
    <automated>grep -c "Diagram Lifecycle" .planning/STATE.md</automated>
    <manual>Per 02.2-VALIDATION.md's Manual-Only Verifications table and RESEARCH.md Pitfall 3: (1) duplicate a main element with existing markers via the context menu — confirm the duplicate gets its own fill/ring pair automatically within one fetch round-trip, no visible delay beyond normal network latency; (2) copy a main element with markers, paste it — confirm the same; (3) select a main element (with markers) and confirm Ctrl+A / group-select never includes its markers in the selection, consistent with Plan 01 Task 1's non-selectability check.</manual>
  </verify>
  <done>One new, precisely-worded backlog row exists in STATE.md's Deferred Items table for the confirmed non-trivial `handleDuplicate` id/group remapping gap; duplicate/copy/paste-of-main-element marker auto-coverage is manually confirmed working with zero new code.</done>
  <acceptance_criteria>
    - `.planning/STATE.md`'s `## Deferred Items` table contains exactly one new row for the `handleDuplicate` id/groupIds remapping gap, using the table's existing column format.
    - No other rows are added or removed from the Deferred Items table by this task.
    - Manual check: duplicating a main element with markers produces a new element that automatically receives its own fill/ring pair (via Plan 01's diff-based detection) with no marker-specific code added anywhere.
    - Manual check: copy/paste of a main element with markers produces the same auto-marking behavior.
    - Manual check: Ctrl+A / group-select never co-selects a marker ellipse with its main element.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser canvas ↔ FullCustomContextMenu's app-controlled delete/duplicate/copy/paste actions | Unchanged trust boundary — this plan only adds marker-cleanup to `handleDelete`'s existing selection-filtering logic; no new user input surface, no new server call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-04 | Repudiation / data-loss (undo integrity) | `FullCustomContextMenu.handleDelete`'s new atomic `captureUpdate` | medium | mitigate | Same class of risk as T-02.2-03 (Plan 01) — an incorrect `captureUpdate` choice could desync the user's undo history rather than exposing data; mitigated by the explicit `IMMEDIATELY` choice, verified manually (single Ctrl+Z restores both element and markers). |
| T-02.2-05 | Tampering (backlog scope creep) | `.planning/STATE.md` Deferred Items table | low | accept | Risk that an unrelated, pre-existing `handleDelete`/`handleCut` undo-correctness gap (RESEARCH.md Open Question 2) gets silently expanded into this phase's fix scope; mitigated by Task 1's explicit instruction to leave `handleCut`/`handleCopy`/`handlePaste`/`handleDuplicate` untouched and Task 2's explicit single-row-only backlog entry. |
</threat_model>

<verification>
- `cd client && yarn type-check` passes with zero errors after Task 1.
- Manual checks in both tasks pass in the running dev stack, matching 02.2-VALIDATION.md's Manual-Only Verifications table.
- `.planning/STATE.md`'s Deferred Items table gains exactly one new, correctly-formatted row and no other changes.
</verification>

<success_criteria>
- Context-menu Delete removes a main element and its markers atomically; one Ctrl+Z restores both.
- Duplicate/copy/paste of a main element with markers is manually confirmed to auto-mark via Plan 01's mechanism, with zero new marker-duplication code.
- The one confirmed non-trivial lifecycle gap (`handleDuplicate` id/groupIds remapping) is logged as a precisely-scoped backlog item, not fixed and not silently dropped.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `client/src/components/diagrams/components/FullCustomContextMenu.tsx`:
  - New imports: `removeOrphanedMarkersLive` (`../utils/sovereigntyMarkers`, produced by Plan 01), `CaptureUpdateAction` (`@excalidraw/excalidraw`).
  - `handleDelete`'s `updateScene` call modified: `elements` field now passes `removeOrphanedMarkersLive(newElements).elements` instead of the raw filtered `newElements`; call gains an explicit `captureUpdate: CaptureUpdateAction.IMMEDIATELY` field. `handleCut`, `handleCopy`, `handlePaste`, `handleDuplicate` unmodified.
- `.planning/STATE.md`:
  - One new row appended to the existing `## Deferred Items` table: `Diagram Lifecycle` category, documenting the confirmed non-trivial `handleDuplicate` id/`groupIds` remapping gap (RESEARCH.md Pitfall 4) as accepted backlog.
</artifacts_produced>

<output>
Create `.planning/phases/02.2-sovereignty-marker-lifecycle-auto-add-on-drop-delete-with-el/02.2-02-SUMMARY.md` when done, including a side-by-side note of the single-Ctrl+Z (context-menu delete) vs. two-Ctrl+Z (native-keyboard delete, Plan 01) restore behaviors.
</output>
