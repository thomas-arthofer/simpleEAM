---
phase: 05-rework-chain-colors
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - server/src/sovereignty/types.ts
  - server/src/sovereignty/repository.ts
  - server/src/sovereignty/evaluator.ts
  - server/src/sovereignty/markers.ts
  - server/src/sovereignty/companyRollup.ts
  - server/src/sovereignty/graphql/resolvers.ts
  - server/src/sovereignty/__tests__/fixtures.ts
  - server/src/sovereignty/__tests__/evaluator.test.ts
  - server/src/sovereignty/__tests__/markers.test.ts
  - server/src/sovereignty/__tests__/companyRollup.test.ts
  - server/src/sovereignty/__tests__/evaluator.parity.test.ts
  - server/src/sovereignty/__tests__/evaluator.cycles.test.ts
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
user_setup: []
tags:
  - sovereignty
  - evaluator
  - markers
  - rollup
  - refactor

must_haves:
  truths:
    - "classifyNode emits YELLOW/RED/GREY findings per D-02 for all four sovereignty dimensions (strategicAutonomy, resilience, security, control), each dimension independently classified, and the deviation math uses maturityIndex only (no hard-coded 4)."
    - "For every element, the marker color is the worst-of-four-dimensions verdict via worseStatus (D-01), computed by the existing findings-fold pipeline in projectMarkers."
    - "worseStatus(GREY, YELLOW) = YELLOW and worseStatus(GREY, RED) = RED — provable violation on any dim dominates data gaps on other dims (D-02 precedence)."
    - "BusinessProcessChain carries effectiveRequiredLevels: RequirementLevels populated by an extended fetchBusinessProcessChain that walks HAS_PARENT_PROCESS*0.. with the same $companyIds ancestor filter and TS-side max fold (D-03 + D-05)."
    - "For DataObject, effectiveRequiredLevels = required (uniform shape via assignment in loadDataObjectSupportChain — no rootType dispatch inside classifyNode). (D-03)"
    - "projectMarkers no longer references selfViolatingIds, comparedIds, or the capabilityIds GREEN-backfill loop; every element (including nested BCs and requirement roots) resolves its fill from findings via a single unified worseStatus-fold. (D-04, RESEARCH §3.3)"
    - "Nested BusinessCapabilities receive their fill via synthesised per-BC premise findings emitted by analyzeCapabilitySubtree (Design A, RESEARCH §3.4). (D-04)"
    - "Downstream Application/Infrastructure/AIComponent fill = per-element deviation vs the reached root's effective-Req, produced natively by the reworked classifyNode; ring = worseStatus of findings whose chainPath contains this element below its position. (D-04, RESEARCH §2.4 + §3.5)"
    - "classifyCapabilityAgainstParent and its BusinessProcess counterpart are removed from the codebase; parentRequiredLevels is removed from BusinessCapabilityChain and BusinessProcessChain; comparedCapabilityIds is removed from SovereigntyAnalysis; selfViolatingIds is removed from projectMarkers. (D-06 Option a, RESEARCH §5)"
    - "companyRollup.ts pushAchievedScores handles YELLOW findings the same way it handles RED (contributes finding.actualLevel via MATURITY_SCORE) — the pre-existing silent-drop bug is fixed. (RESEARCH §8 + executive summary)"
    - "The Phase 2 D-03 per-branch visited-set contract for the downward childCapabilities walk in analyzeCapabilitySubtree is preserved unchanged."
    - "SOVEREIGNTY_STATUSES stays exactly ['RED', 'YELLOW', 'GREY', 'GREEN'] and the GraphQL schema (SovereigntyMarker, SovereigntyFinding, SovereigntyAnalysis) is unchanged in shape (RESEARCH §8)."
    - "verification: backstop — Effective-Req = max over ancestor chain for BusinessProcess via HAS_PARENT_PROCESS is exercised by a new fixture with a multi-level BP chain (RESEARCH Validation Architecture D-05 row)."
    - "verification: backstop — Data-gap dominance (any RED/YELLOW beats any GREY on the same element) is exercised by precedenceViolationOverGapFixture in markers.test.ts (RESEARCH Validation Architecture D-02 precedence row)."
    - "verification: backstop — YELLOW findings contribute to achievedSovereigntyScore in companyRollup (RESEARCH Validation Architecture Companyrollup row)."
    - "Cross-tenant ancestor rows are silently excluded from the max fold via the ancestorCompany.id IN $companyIds filter on both BC and BP fetches (T-05-03 mitigation)."
  prohibitions:
    - "MUST NOT introduce a new value in SOVEREIGNTY_STATUSES; the enum stays 4-valued."
    - "MUST NOT add a new field to SovereigntyMarker or SovereigntyFinding beyond what already exists (no schema-level change per RESEARCH §8)."
    - "MUST NOT add a per-element persistence/caching layer for premise verdicts; every request runs the evaluator (RESEARCH §8 storage-at-rest audit — no cache)."
    - "MUST NOT introduce parallel classifier logic in projectMarkers or companyRollup; both consume the same Finding[] pipeline (Anti-Pattern 3)."
    - "MUST NOT retain classifyCapabilityAgainstParent or parentRequiredLevels behind a flag; the retirement is a hard delete per D-06 Option (a)."
    - "MUST NOT touch client components or messages files; that is Plan C."
    - "MUST NOT hardcode the literal 4 anywhere in the new deviation code (scale-independence, D-02)."

threat_model_ref: shared with 05-01-PLAN.md (T-05-01..T-05-04); Plan B specifically closes T-05-04 (companyRollup YELLOW branch).
---

<objective>
Expansion wave. Take the Plan A tracer (BC, one dim, end-to-end) and finish the refactor: all four dimensions, BusinessProcess parity, DataObject uniform shape, retire the parent-consistency machinery per D-06 Option (a), rewrite projectMarkers to a single findings-fold, fix the companyRollup YELLOW-drop bug, and rewire the full sovereignty test suite per RESEARCH §6.

Purpose: land the chain-premise semantics uniformly across BC/DO/BP and downstream leaves, close the D-06 disposition, and make the full sovereignty test suite green under the new rule.
Output: complete, uniform chain-premise math; retired parent-consistency codepath; fixed rollup; rewritten test fixtures; all sovereignty tests green.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-rework-chain-colors/05-CONTEXT.md
@.planning/phases/05-rework-chain-colors/05-RESEARCH.md
@.planning/phases/05-rework-chain-colors/05-01-PLAN.md
@.planning/phases/05-rework-chain-colors/05-01-SUMMARY.md
@.planning/phases/03-business-capability-self-status-green-when-consistent-with-p/03-CONTEXT.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md
@server/src/sovereignty/types.ts
@server/src/sovereignty/repository.ts
@server/src/sovereignty/evaluator.ts
@server/src/sovereignty/markers.ts
@server/src/sovereignty/companyRollup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend deviation math to all 4 dimensions and thread effective-Req through nested-BC and downstream walks</name>
  <files>server/src/sovereignty/evaluator.ts, server/src/sovereignty/__tests__/fixtures.ts, server/src/sovereignty/__tests__/evaluator.test.ts</files>
  <read_first>
    - server/src/sovereignty/evaluator.ts — classifyNode, analyzeCapabilitySubtree, walkApplication/walkInfrastructure/walkAIComponent, maturityIndex.
    - Plan A's SUMMARY (this repo path: .planning/phases/05-rework-chain-colors/05-01-SUMMARY.md) — the tracer-dimension classifyNode branch is the template to generalise.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §2.1 (deviation math for all 4 dims), §2.3 (effective-Req threading via `parentEffectiveReq` parameter and maxByDimension), §2.4 (downstream leaf finding shape).
    - server/src/sovereignty/__tests__/fixtures.ts — existing BC / DO / BP fixtures for shape template.
  </read_first>
  <behavior>
    - classifyNode iterates all four dimensions (strategicAutonomy, resilience, security, control). Per dim: null effective-Req → no finding; null achieved (with non-null effective-Req) → GREY finding; deviation ≥ 2 → RED finding; deviation === 1 → YELLOW finding; deviation ≤ 0 → no finding.
    - analyzeCapabilitySubtree accepts a `parentEffectiveReq: RequirementLevels` parameter; nested BCs compute their own effectiveReq via `maxByDimension(chain.required, parentEffectiveReq)` before recursing and before passing into walkers.
    - For the analysis root, parentEffectiveReq = the repository-supplied `chain.effectiveRequiredLevels`; the walkers see the root's effective-Req.
    - The Phase 2 D-03 per-branch visited-set contract is preserved unchanged in the downward walk.
    - New fixtures: `deviationOneStepFixture`, `deviationTwoStepFixture`, `deviationBoundaryFixture` (scale extremes NONE ↔ VERY_HIGH), `precedenceViolationOverGapFixture` (one dim RED, one dim GREY on same element), `ancestorMaxWinsFixture` (root MEDIUM, grandparent HIGH — grandparent wins).
    - New evaluator-test describe: "Phase 5: deviation math (all dims)" with cases per dim and boundary case NONE→VERY_HIGH (deviation ≥ 2, RED).
    - Retire "parent-vs-child required-level contradiction (Test A–N)" describe block by rewrite into an "ancestor-stricter" describe block using `ancestorStricterOneStepFixture` (Plan A) plus the new fixtures.
  </behavior>
  <action>
    Generalise Plan A's classifyNode change from one dimension to all four. Introduce (or extend) a small pure helper `deviationStatus(effectiveReq: MaturityLevel | null, actual: MaturityLevel | null): 'RED'|'YELLOW'|'GREY'|null` that encodes RESEARCH §2.1's decision table via `maturityIndex`; classifyNode loops the four `sovereigntyReq*` / `sovereigntyAch*` pairs and emits at most one finding per dim with the corresponding status. The literal `4` MUST NOT appear.

    In analyzeCapabilitySubtree, add a `parentEffectiveReq: RequirementLevels` parameter defaulting to the null-quadruple; compute `effectiveReq = maxByDimension(chain.required, parentEffectiveReq)`; pass `effectiveReq` (not `chain.required`) into walkApplication/walkInfrastructure/walkAIComponent; recurse into `chain.childCapabilities` with `effectiveReq` as their parentEffectiveReq. Introduce `maxByDimension(a, b)` as a pure helper using `maturityIndex`.

    For BusinessProcess and DataObject entry points: analyzeBusinessProcess uses `chain.effectiveRequiredLevels` from the repository (populated by Task 2); analyzeDataObject uses `chain.required` copied to `chain.effectiveRequiredLevels` at load time (Task 2 sets this at load, so classifyNode reads the uniform field).

    Add the fixtures listed in `behavior` to `fixtures.ts`. Add "Phase 5: deviation math (all dims)" and "Phase 5: ancestor-stricter propagation" describe blocks to `evaluator.test.ts`. Rewrite (in place) the "parent-vs-child required-level contradiction" describe block per RESEARCH §6 into an ancestor-stricter block — do not delete the existing test file; refactor the affected describe. The retired `classifyCapabilityAgainstParent` output is not asserted anywhere post-refactor (Task 3 removes the code).
  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; yarn jest src/sovereignty/__tests__/evaluator.test.ts</automated>
    <automated>bash -c "awk '/function classifyNode|const classifyNode/,/^}/' server/src/sovereignty/evaluator.ts | grep -v '^\s*//' | grep -v '^\s*\*' | grep -c '\\b4\\b' | awk '{ if (\$1 &gt; 0) exit 1; else exit 0 }'"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && yarn jest src/sovereignty/__tests__/evaluator.test.ts` exits 0.
    - `grep -c 'deviationOneStepFixture\|deviationTwoStepFixture\|deviationBoundaryFixture\|precedenceViolationOverGapFixture\|ancestorMaxWinsFixture' server/src/sovereignty/__tests__/fixtures.ts` is ≥ 5.
    - `grep -c 'maxByDimension' server/src/sovereignty/evaluator.ts` is ≥ 2 (helper + at least one call site).
    - `grep -c 'parentEffectiveReq' server/src/sovereignty/evaluator.ts` is ≥ 2.
    - `awk '/function classifyNode|const classifyNode/,/^}/' server/src/sovereignty/evaluator.ts | grep -v '^\s*//' | grep -v '^\s*\*' | grep -c '\b4\b'` returns 0.
    - `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">All-dim generalisation and helper introduction are additive over Plan A's tracer; a revert restores single-dim tracer behavior.</reversibility>
  <done>
    classifyNode covers all four dimensions with correct YELLOW/RED/GREY/no-finding verdicts; the nested-BC walk threads effective-Req; new fixtures and test blocks pass; the retired parent-vs-child assertions have been refactored into ancestor-stricter cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend repository for BusinessProcess ancestor walk and set DataObjectChain.effectiveRequiredLevels := required (uniform shape)</name>
  <files>server/src/sovereignty/types.ts, server/src/sovereignty/repository.ts</files>
  <read_first>
    - server/src/sovereignty/types.ts — BusinessProcessChain, DataObjectChain shape.
    - server/src/sovereignty/repository.ts — fetchBusinessProcessChain (HAS_PARENT_PROCESS one-hop pattern from Phase 4 D-04), loadDataObjectSupportChain / fetchDataObjectChain shape.
    - Plan A's Task 1 in this phase — the BC precedent for the ancestor-walk + TS max fold; mirror the same pattern for BP.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §1.2 (BP mirror + DO no-change note), §1.5 (multi-tenancy).
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md D-04 (existing BP parent-consistency shape being replaced).
  </read_first>
  <action>
    Add `readonly effectiveRequiredLevels: RequirementLevels` to `BusinessProcessChain` and to `DataObjectChain` (both with docstrings referring to D-05). Do NOT retire `parentRequiredLevels` here — Task 3 handles the removal in a single retirement task guarded by the reversibility checkpoint.

    Extend `fetchBusinessProcessChain`: replace the one-hop `HAS_PARENT_PROCESS` with `OPTIONAL MATCH (proc)-[:HAS_PARENT_PROCESS*0..]->(ancestor:BusinessProcess)-[:OWNED_BY]->(ancestorCompany:Company) WHERE $isAdmin OR ancestorCompany.id IN $companyIds`. Return `ancestorRequiredRows` alongside the pre-existing `parentRequiredRows` (temporary coexistence). Fold TS-side via `maturityIndex` (import shared helper — do not duplicate the ordinal mapping). Cap ancestor rows at 100 with `console.warn` on overflow (T-05-02). Populate `effectiveRequiredLevels` on the root only.

    In `loadDataObjectSupportChain` (or the DO chain constructor), set `effectiveRequiredLevels: chain.required` on `DataObjectChain` at construction time so downstream code uses one uniform field with no rootType dispatch inside classifyNode (RESEARCH §2.1).

    Do not otherwise touch `parentRequiredLevels` — coexist until Task 3.
  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; grep -c 'HAS_PARENT_PROCESS\*0' server/src/sovereignty/repository.ts</automated>
    <automated>cd server &amp;&amp; grep -c 'ancestorCompany.id IN \$companyIds' server/src/sovereignty/repository.ts</automated>
    <automated>cd server &amp;&amp; grep -c 'effectiveRequiredLevels' server/src/sovereignty/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readonly effectiveRequiredLevels: RequirementLevels' server/src/sovereignty/types.ts` is ≥ 3 (BC + BP + DO).
    - `grep -c 'HAS_PARENT_PROCESS\*0' server/src/sovereignty/repository.ts` is ≥ 1.
    - `grep -c 'ancestorCompany.id IN \$companyIds' server/src/sovereignty/repository.ts` is ≥ 2 (BC from Plan A + BP from this task).
    - `grep -c 'effectiveRequiredLevels' server/src/sovereignty/repository.ts` is ≥ 3 (BC set, BP set, DO uniform assignment).
    - `cd server && yarn tsc --noEmit` exits 0.
    - `cd server && yarn jest src/sovereignty` exits 0 (existing tests still green with coexisting parentRequiredLevels).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive fields + additive Cypher; removable without touching consumers (parentRequiredLevels still populated).</reversibility>
  <done>
    BP has the same ancestor-walk shape as BC; DO carries a uniform `effectiveRequiredLevels`; all three root chain types expose the same field so classifyNode does no rootType dispatch. tsc + existing tests green.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>
    Confirm proceeding with D-06 Option (a): hard-delete `classifyCapabilityAgainstParent` and its BP counterpart, and remove `parentRequiredLevels` from `BusinessCapabilityChain` / `BusinessProcessChain` and `comparedCapabilityIds` from `SovereigntyAnalysis`. This is a one-way change to the pre-Phase-5 diagnostic surface.
  </decision>
  <context>
    CONTEXT.md D-05 is rated `costly` to reverse and CONTEXT.md D-06 is user-discretion. RESEARCH.md §5 recommends Option (a) (hard delete). Reversal would require reintroducing the classifier, restoring the retired fields on Chain and Analysis, restoring `selfViolatingIds` / `comparedIds` in projectMarkers, and re-adding the retired test blocks. The subsequent task (Task 4) executes this retirement.
  </context>
  <options>
    <option id="option-a">
      <name>Proceed with Option (a) — hard delete (RESEARCH.md recommendation)</name>
      <pros>Smallest post-Phase-5 surface; findings retain single meaning; fewer branches, less bug surface (Phase 4 04-02 regression class avoided).</pros>
      <cons>Loses parent-vs-own diagnostic surface from detail view. Reversal requires reintroducing three retired symbols and their tests.</cons>
    </option>
    <option id="option-c">
      <name>Fall back to Option (c) — retain as additive `effectiveRequirementSources` field</name>
      <pros>Preserves per-dim explanation of effective-Req provenance in the detail view.</pros>
      <cons>Additive scope creep: new field on SovereigntyAnalysis, new resolver, new i18n (would blow Plan C scope) — pushes Plan C or a new Plan D onto the roadmap.</cons>
    </option>
    <option id="pause">
      <name>Pause and re-discuss with user before removing any code</name>
      <pros>Zero risk of removing behavior the user wanted to keep.</pros>
      <cons>Blocks Plan B completion; Plan A's tracer coexists with old code paths in the interim.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-c, or pause</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Retire classifyCapabilityAgainstParent, parentRequiredLevels, comparedCapabilityIds (D-06 Option a)</name>
  <files>server/src/sovereignty/evaluator.ts, server/src/sovereignty/repository.ts, server/src/sovereignty/types.ts, server/src/sovereignty/graphql/resolvers.ts</files>
  <read_first>
    - Task 3 checkpoint outcome — MUST be `option-a`.
    - server/src/sovereignty/evaluator.ts — `classifyCapabilityAgainstParent` definition + call sites (in analyzeBusinessCapability, analyzeBusinessProcess, analyzeCapabilitySubtree per RESEARCH §5).
    - server/src/sovereignty/types.ts — `parentRequiredLevels` field on BusinessCapabilityChain and BusinessProcessChain; `comparedCapabilityIds` on SovereigntyAnalysis (docstring line 83 notes it is internal, not exposed via GraphQL).
    - server/src/sovereignty/repository.ts — the one-hop `parentRequiredRows` blocks that populate `parentRequiredLevels`.
    - server/src/sovereignty/graphql/resolvers.ts — `toGraphQLAnalysis` DTO shape.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §5 (Option a rationale + deprecation path).
  </read_first>
  <action>
    Delete `classifyCapabilityAgainstParent` (and its BP counterpart if separately named) from `evaluator.ts`. Remove all call sites in `analyzeBusinessCapability`, `analyzeBusinessProcess`, and `analyzeCapabilitySubtree`. Delete the `parentRequiredLevels` field from `BusinessCapabilityChain` and `BusinessProcessChain` in `types.ts` and drop the population blocks in `repository.ts` (the one-hop `parentRequiredRows` Cypher and its TS fold). Delete `comparedCapabilityIds` from `SovereigntyAnalysis` and any resolver / DTO reference in `graphql/resolvers.ts` (docstring line 83 confirms no GraphQL surface).

    Do not modify `SOVEREIGNTY_STATUSES` or `SovereigntyMarker` shape. Do not modify `SovereigntyFinding` shape.

    After deletion, verify the compiled tree is clean and no dangling references remain. Any test-only helpers that referenced these symbols are removed alongside them.
  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>bash -c "grep -rn 'classifyCapabilityAgainstParent' server/src 2&gt;/dev/null | wc -l | awk '{ if (\$1 == 0) exit 0; else exit 1 }'"</automated>
    <automated>bash -c "grep -rn 'parentRequiredLevels' server/src 2&gt;/dev/null | wc -l | awk '{ if (\$1 == 0) exit 0; else exit 1 }'"</automated>
    <automated>bash -c "grep -rn 'comparedCapabilityIds' server/src 2&gt;/dev/null | wc -l | awk '{ if (\$1 == 0) exit 0; else exit 1 }'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'classifyCapabilityAgainstParent' server/src` returns 0 matches.
    - `grep -rn 'parentRequiredLevels' server/src` returns 0 matches.
    - `grep -rn 'comparedCapabilityIds' server/src` returns 0 matches.
    - `cd server && yarn tsc --noEmit` exits 0.
    - `grep -c "'RED'\s*,\s*'YELLOW'\s*,\s*'GREY'\s*,\s*'GREEN'" server/src/sovereignty/types.ts` is ≥ 1 (enum unchanged).
  </acceptance_criteria>
  <reversibility rating="one-way">Hard delete of three symbols and their populated fields. Reversal requires reintroducing all three and rewiring three call sites plus repository fetches. Gated by the Task 3 checkpoint.</reversibility>
  <done>
    All three retired symbols are gone from `server/src`; tsc clean; enum and marker shapes untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Rewrite projectMarkers to a single findings-fold, synthesise per-BC premise findings for nested BC fill</name>
  <files>server/src/sovereignty/markers.ts, server/src/sovereignty/evaluator.ts, server/src/sovereignty/__tests__/markers.test.ts, server/src/sovereignty/__tests__/fixtures.ts</files>
  <read_first>
    - server/src/sovereignty/markers.ts — current STATUS_RANK, worseStatus, DEFAULT_MARKER, and the three chunks slated for deletion (RESEARCH §3.2: `selfViolatingIds` computation, `comparedIds` signal, `isCapabilitySelfViolation` ternary, `capabilityIds` GREEN-backfill loop).
    - server/src/sovereignty/evaluator.ts — `analyzeCapabilitySubtree` for the per-nested-BC premise synthesis (Design A, RESEARCH §3.4).
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §3.1 (STATUS_RANK unchanged), §3.2 (branches that DIE), §3.3 (unified fold shape), §3.4 (Design A rationale), §3.5 (downstream leaf fill).
    - server/src/sovereignty/__tests__/markers.test.ts — the ~10 tests that must be rewritten per RESEARCH §6 churn map.
  </read_first>
  <behavior>
    - projectMarkers contains a single loop over `analysis.findings` and folds `selfStatus`/`downstreamStatus` per `finding.chainPath` via `worseStatus`. No reference to `capabilityIds`, `selfViolatingIds`, `comparedIds`, or a GREEN-backfill loop.
    - Nested BCs receive their fill via findings synthesised by `analyzeCapabilitySubtree` when the subtree's leaves violate the BC's own effective-Req. The finding uses `violatingElementId = nestedBc.rootId`, `violatingElementType = 'businessCapability'`, `status = worst-of-dim over that subtree`.
    - Downstream leaves (Application/Infrastructure/AIComponent) receive their fill via the reworked classifyNode's per-leaf findings (RESEARCH §2.4/§3.5) — no additional lookup is required.
    - STATUS_RANK stays `GREEN < GREY < YELLOW < RED`; worseStatus unchanged.
    - markers.test.ts adds/updates: (a) "worst-of-dim aggregation" (D-01), (b) "precedence: RED/YELLOW beats GREY on same element" (D-02), (c) "downstream App inherits YELLOW fill at 1-step deviation" (D-04), (d) "downstream Infra inherits RED fill at ≥2-step deviation" (D-04), (e) "ring propagates worst-of-below unchanged".
    - The retired "GREY-self forever for nested BCs" invariant tests and Tests E / F / N (parent-consistency selfStatus) are deleted.
    - resolveMarker / DEFAULT_MARKER tests stay unchanged; the precedence regression test ("YELLOW beats GREY on same element") stays as a value-change only.
  </behavior>
  <action>
    Replace `projectMarkers` with the unified fold per RESEARCH §3.3: seed the map with `analysis.rootId → { selfStatus: analysis.selfStatus, downstreamStatus: analysis.downstreamStatus }`, then loop findings, updating each id in `finding.chainPath` — `selfStatus` picks up worseStatus only when the id equals the finding's `violatingElementId`, `downstreamStatus` picks up worseStatus unconditionally. Delete `selfViolatingIds`, `comparedIds`, `isCapabilitySelfViolation`, and the `capabilityIds` GREEN-backfill loop. `STATUS_RANK` / `worseStatus` / `DEFAULT_MARKER` are untouched.

    In `analyzeCapabilitySubtree`, after computing the subtree's findings, synthesise one additional Finding per nested BC whose subtree fails the BC's effective-Req: for each nested BC where the worst-of-dim deviation over the subtree > 0, emit a Finding with `violatingElementId = nestedBc.rootId`, `violatingElementType = 'businessCapability'`, `status = worst-of-dim status over that subtree`, `chainPath = [analysisRoot, ..., nestedBc.rootId]`. This ensures projectMarkers' `isViolatingElement` branch produces the correct fill for nested BCs without any special-case code.

    Rewrite markers.test.ts per RESEARCH §6 churn map: delete Test E, F, N (parent-consistency), delete "GREY-self forever for nested BCs", update the precedence test's value assertions, and add the five new cases in `behavior`.
  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; yarn jest src/sovereignty/__tests__/markers.test.ts</automated>
    <automated>bash -c "grep -c 'selfViolatingIds\\|comparedIds\\|comparedCapabilityIds\\|GREEN-backfill' server/src/sovereignty/markers.ts | awk '{ if (\$1 == 0) exit 0; else exit 1 }'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'selfViolatingIds' server/src/sovereignty/markers.ts` is 0.
    - `grep -c 'comparedIds\|comparedCapabilityIds' server/src/sovereignty/markers.ts` is 0.
    - `grep -c 'isCapabilitySelfViolation' server/src/sovereignty/markers.ts` is 0.
    - `grep -c 'STATUS_RANK' server/src/sovereignty/markers.ts` is ≥ 1 (unchanged).
    - `grep -c 'worseStatus' server/src/sovereignty/markers.ts` is ≥ 1 (unchanged).
    - `cd server && yarn jest src/sovereignty/__tests__/markers.test.ts` exits 0.
    - `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">Removes 3+ code branches in projectMarkers and rewrites ~10 tests. Reversing would require restoring the retired symbols from Task 4 as well.</reversibility>
  <done>
    projectMarkers is a single findings-fold; nested BCs get fill via synthesised premise findings; markers.test.ts is rewired per RESEARCH §6 and green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Fix companyRollup pushAchievedScores YELLOW branch (silent-drop bug from RESEARCH executive summary)</name>
  <files>server/src/sovereignty/companyRollup.ts, server/src/sovereignty/__tests__/companyRollup.test.ts</files>
  <read_first>
    - server/src/sovereignty/companyRollup.ts — `pushAchievedScores` (RESEARCH §8 references line 82–93), the current `RED`/`GREY`-only branches.
    - server/src/sovereignty/__tests__/companyRollup.test.ts — existing describe blocks; do NOT rewrite unrelated cases.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md executive summary + §8 (the silent-drop bug + required fix) + Validation Architecture Companyrollup row.
  </read_first>
  <behavior>
    - Given a company whose sovereignty analysis contains only YELLOW findings (1-step deviations), the rollup's `achievedSovereigntyScore` is a real degraded number (contributed via each YELLOW finding's `actualLevel` mapped through `MATURITY_SCORE`), NOT null.
    - RED findings continue to contribute `MATURITY_SCORE[finding.actualLevel]` as today.
    - GREY findings continue to contribute `MATURITY_SCORE.NONE` as today.
    - A new test asserts a YELLOW-only company yields a non-null degraded score; a mixed YELLOW + GREY company yields a score reflecting both contributions.
  </behavior>
  <action>
    Add a `YELLOW` branch to `pushAchievedScores` in `companyRollup.ts` mirroring the RED branch: `if (finding.status === 'YELLOW' && finding.actualLevel !== null) scores.push(MATURITY_SCORE[finding.actualLevel])`. Do not otherwise alter the function. Add a new test case in `companyRollup.test.ts`: "YELLOW findings contribute their actualLevel to achievedSovereigntyScore (Phase 5)" using a fixture built from `deviationOneStepFixture` (Task 1). Update any pre-existing tests whose assertion values shift because of the reclassification (per RESEARCH §6 companyRollup row).
  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; yarn jest src/sovereignty/__tests__/companyRollup.test.ts</automated>
    <automated>bash -c "awk '/function pushAchievedScores/,/^}/' server/src/sovereignty/companyRollup.ts | grep -c 'YELLOW'"</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/function pushAchievedScores/,/^}/' server/src/sovereignty/companyRollup.ts | grep -c 'YELLOW'` is ≥ 1.
    - `cd server && yarn jest src/sovereignty/__tests__/companyRollup.test.ts -t "YELLOW"` exits 0.
    - `cd server && yarn jest src/sovereignty/__tests__/companyRollup.test.ts` exits 0.
    - `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Single-branch addition + one new test. Trivially removable.</reversibility>
  <done>
    YELLOW findings contribute to achievedSovereigntyScore; regression test in place; full companyRollup suite green.
  </done>
</task>

<task type="auto">
  <name>Task 7: Rewire parity + cycles tests and final full-suite gate</name>
  <files>server/src/sovereignty/__tests__/evaluator.parity.test.ts, server/src/sovereignty/__tests__/evaluator.cycles.test.ts</files>
  <read_first>
    - server/src/sovereignty/__tests__/evaluator.parity.test.ts — `redChainFixture` expected-findings block; DTO shape assertions.
    - server/src/sovereignty/__tests__/evaluator.cycles.test.ts — the `bFindings` count logic that shifts when 1-step deviations become YELLOW instead of RED.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §6 (parity + cycles rows) — assertion-value updates only, no semantic behavior change.
  </read_first>
  <action>
    Update expected values in `evaluator.parity.test.ts` and `evaluator.cycles.test.ts` per RESEARCH §6: parity's `redChainFixture` YELLOW/RED counts shift where 1-step deviations reclassify; cycles' `app-cycle-b` security LOW-vs-MEDIUM (1 step) shifts from RED to YELLOW. If Task 4's DTO shape change (removal of `comparedCapabilityIds`) affects the parity assertion, update the parity assertion to ignore that dropped field. No new fixtures, no new describe blocks — value updates only.

    After updates, run the full sovereignty suite and confirm it is green end-to-end. This is the phase gate for Plan B.
  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; yarn jest src/sovereignty</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && yarn jest src/sovereignty` exits 0.
    - `cd server && yarn tsc --noEmit` exits 0.
    - `grep -rn 'classifyCapabilityAgainstParent\|parentRequiredLevels\|comparedCapabilityIds\|selfViolatingIds' server/src` returns 0 matches (retired symbols stay retired).
  </acceptance_criteria>
  <reversibility rating="reversible">Assertion-value updates only.</reversibility>
  <done>
    Full sovereignty suite green under the new chain-premise semantics with the retired symbols confirmed absent.
  </done>
</task>

</tasks>

<threat_model>
Shared with 05-01-PLAN.md; the same T-05-01..T-05-04 register applies. Plan B specifically closes T-05-04 via Task 6 (companyRollup YELLOW branch). T-05-03 (multi-tenancy) is re-verified for the BP ancestor walk in Task 2 via the same `ancestorCompany.id IN $companyIds` clause used for BC in Plan A.
</threat_model>

<verification>
- `cd server && yarn tsc --noEmit` exits 0.
- `cd server && yarn jest src/sovereignty` exits 0 (unit + parity + cycles + rollup, all green).
- `grep -rn 'classifyCapabilityAgainstParent\|parentRequiredLevels\|comparedCapabilityIds\|selfViolatingIds' server/src` returns 0 matches.
- `grep -c "'RED'\s*,\s*'YELLOW'\s*,\s*'GREY'\s*,\s*'GREEN'" server/src/sovereignty/types.ts` is ≥ 1 (enum unchanged).
- Manual GraphQL smoke: `POST /graphql { sovereigntyAnalyzeBusinessCapability(id: "<any real BC>"){ findings { status } selfStatus downstreamStatus } }` returns YELLOW/RED findings against effective-Req (not against own required in isolation).
</verification>

<success_criteria>
Chain-premise math is uniform across BC/DO/BP and downstream leaves; the parent-consistency codepath is retired; projectMarkers is a single findings-fold; companyRollup no longer silently drops YELLOW; the full sovereignty test suite is green.
</success_criteria>

<artifacts_produced>
**New symbols:**
- `BusinessProcessChain.effectiveRequiredLevels: RequirementLevels` (types.ts)
- `DataObjectChain.effectiveRequiredLevels: RequirementLevels` (types.ts, uniform-shape assignment)
- `maxByDimension(a: RequirementLevels, b: RequirementLevels): RequirementLevels` helper (evaluator.ts)
- `deviationStatus(effectiveReq, actual)` helper (evaluator.ts) — the RESEARCH §2.1 decision table
- `parentEffectiveReq: RequirementLevels` parameter on `analyzeCapabilitySubtree`
- Fixtures: `deviationOneStepFixture`, `deviationTwoStepFixture`, `deviationBoundaryFixture`, `precedenceViolationOverGapFixture`, `ancestorMaxWinsFixture` (+ BP counterpart if the BP path requires a distinct fixture)
- Synthesised per-nested-BC premise Findings emitted by `analyzeCapabilitySubtree`
- YELLOW branch in `pushAchievedScores` (companyRollup.ts)

**Retired symbols (D-06 Option a):**
- `classifyCapabilityAgainstParent` (BC + BP variants)
- `parentRequiredLevels` field on `BusinessCapabilityChain` and `BusinessProcessChain`
- `comparedCapabilityIds` field on `SovereigntyAnalysis`
- `selfViolatingIds`, `comparedIds`, `isCapabilitySelfViolation`, and the `capabilityIds` GREEN-backfill loop in `projectMarkers`

**Modified symbols:**
- `classifyNode` (all 4 dims)
- `analyzeCapabilitySubtree` (parentEffectiveReq threading + per-BC premise synthesis)
- `analyzeBusinessCapability`, `analyzeBusinessProcess`, `analyzeDataObject` (effective-Req entry)
- `fetchBusinessProcessChain` (variable-length ancestor walk)
- `loadDataObjectSupportChain` / DO chain constructor (uniform `effectiveRequiredLevels`)
- `projectMarkers` (single unified fold)
- `pushAchievedScores` (companyRollup.ts, YELLOW branch)
- `toGraphQLAnalysis` (resolvers.ts, drops `comparedCapabilityIds`)
- Multiple test files rewired per RESEARCH §6 churn map
</artifacts_produced>

<output>
Create `.planning/phases/05-rework-chain-colors/05-02-SUMMARY.md` when done.
</output>
