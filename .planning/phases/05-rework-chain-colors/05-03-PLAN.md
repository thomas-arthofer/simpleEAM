---
phase: 05-rework-chain-colors
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - client/src/components/sovereignty/SovereigntyCapabilityView.tsx
  - client/src/components/sovereignty/SovereigntyDataView.tsx
  - client/src/components/sovereignty/SovereigntyProcessView.tsx
  - client/messages/de.json
  - client/messages/en.json
autonomous: true
requirements:
  - D-04
user_setup: []
tags:
  - sovereignty
  - client
  - i18n

must_haves:
  truths:
    - "All three sovereignty detail views (Capability, Data, Process) dispatch per-finding row copy on `finding.status` — `findingRowYellow`, `findingRowRed`, `findingRowGrey` — so the YELLOW-vs-RED distinction is explained textually as '1-step deviation' vs '≥2-step deviation' (RESEARCH §7)."
    - "The `greyEmptyBody` copy in `sovereigntyDetail` reflects the D-05 semantics: 'no data available and no violation detected' (replacing the pre-Phase-5 'no comparison could be made' copy)."
    - 'Both German (`messages/de.json`) and English (`messages/en.json`) locales contain matching key sets under `sovereigntyDetail` (no locale drift).'
    - 'The three view components render the same `SovereigntyAnalysis` shape as before — no structural JSX change, no query change, no new UI channel or marker overlay (RESEARCH §7).'
    - '`client && yarn build` completes without type or i18n key errors.'
  prohibitions:
    - 'MUST NOT modify any GraphQL query, mutation, or codegen output.'
    - 'MUST NOT modify diagram marker overlay components or any file outside `client/src/components/sovereignty/` and the two locale files.'
    - 'MUST NOT introduce a new UI channel or a fifth `SOVEREIGNTY_STATUSES` value.'
    - 'MUST NOT add English-only or German-only keys — every new key exists in both locale files.'

threat_model_ref: none — client-side text and i18n only (no auth, no user input change, no new endpoint).
---

<objective>
Text-only polish for the Phase 5 chain-premise semantics. Update the three sovereignty detail-view renderers and the two locale files so the YELLOW / RED / GREY findings-list copy reflects the new deviation-based semantics ("1-step deviation" / "≥2-step deviation" / "no data available and no violation detected") instead of the retired parent-consistency wording.

Purpose: keep the detail-view UX consistent with the server semantics landed in Plan B.
Output: dispatched `findingRow*` rendering + updated `greyEmptyBody` copy in DE + EN.
</objective>

<execution_context>
@~~/.copilot/gsd-core/workflows/execute-plan.md
@~~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-rework-chain-colors/05-CONTEXT.md
@.planning/phases/05-rework-chain-colors/05-RESEARCH.md
@.planning/phases/05-rework-chain-colors/05-02-SUMMARY.md
@client/src/components/sovereignty/SovereigntyCapabilityView.tsx
@client/src/components/sovereignty/SovereigntyDataView.tsx
@client/src/components/sovereignty/SovereigntyProcessView.tsx
@client/messages/de.json
@client/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-status finding-row i18n keys in DE and EN and update greyEmptyBody copy</name>
  <files>client/messages/de.json, client/messages/en.json</files>
  <read_first>
    - client/messages/de.json — locate the `sovereigntyDetail` group and note the existing `findingRow`, `greyEmptyBody`, `greenEmptyBody` keys.
    - client/messages/en.json — the parity English group.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §7 (client detail-view impact — dispatch design, exact key names) and CONTEXT.md D-05 (semantic shift to "chain deviation of exactly 1 step" vs "≥2 steps").
    - nextgen-eam.instructions.md — the next-intl multi-locale rule.
  </read_first>
  <action>
    In both `de.json` and `en.json`, under the existing `sovereigntyDetail` group:
    - Add `findingRowYellow` — copy explains "1-step deviation on the sovereignty scale between required and achieved" per dimension. Reuse the existing `findingRow` placeholders (dimension label, required, actual) plus a new marker/word for the delta.
    - Add `findingRowRed` — copy explains "deviation of 2 or more steps".
    - Add `findingRowGrey` — copy explains "achieved value is unknown for this dimension" (data gap).
    - Update `greyEmptyBody` copy to reflect D-05 semantics: "no data available and no violation detected" (DE and EN equivalents). Do not rename the key.
    - Keep the pre-existing `findingRow` key in place unless the code no longer references it (Task 2 will decide).
    - Do NOT rename other keys, do NOT remove `greenEmptyBody`.

    Match key names exactly between DE and EN. Preserve JSON formatting (2-space indent, trailing newline) to avoid noisy diffs.

  </action>
  <verify>
    <automated>node -e "const de=require('./client/messages/de.json'), en=require('./client/messages/en.json'); const keys=['findingRowYellow','findingRowRed','findingRowGrey','greyEmptyBody']; for (const k of keys) { if (!de.sovereigntyDetail[k]) throw new Error('de missing '+k); if (!en.sovereigntyDetail[k]) throw new Error('en missing '+k); } console.log('OK');"</automated>
    <automated>node -e "const de=require('./client/messages/de.json'), en=require('./client/messages/en.json'); const dk=Object.keys(de.sovereigntyDetail).sort(), ek=Object.keys(en.sovereigntyDetail).sort(); if (JSON.stringify(dk)!==JSON.stringify(ek)) { throw new Error('locale drift: DE='+dk.join(',')+' EN='+ek.join(',')); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Both locale files contain `sovereigntyDetail.findingRowYellow`, `findingRowRed`, `findingRowGrey`.
    - Both locale files contain updated `sovereigntyDetail.greyEmptyBody` reflecting "no data / no violation".
    - The set of keys under `sovereigntyDetail` is identical between DE and EN.
    - `client/messages/de.json` and `client/messages/en.json` are valid JSON (parseable by `node -e "require(...)"`).
  </acceptance_criteria>
  <reversibility rating="reversible">Locale key additions and a copy update; trivially removable.</reversibility>
  <done>
    DE and EN carry matching new keys; `greyEmptyBody` copy updated; JSON parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dispatch per-status finding-row rendering in the three SovereigntyXView components</name>
  <files>client/src/components/sovereignty/SovereigntyCapabilityView.tsx, client/src/components/sovereignty/SovereigntyDataView.tsx, client/src/components/sovereignty/SovereigntyProcessView.tsx</files>
  <read_first>
    - client/src/components/sovereignty/SovereigntyCapabilityView.tsx (lines 88–103 referenced by RESEARCH §7 — the `analysis.downstreamStatus === 'GREEN'` / `'GREY' && findings.length === 0` alert branches and the findings row rendering).
    - client/src/components/sovereignty/SovereigntyDataView.tsx (parallel structure).
    - client/src/components/sovereignty/SovereigntyProcessView.tsx (parallel structure — added in Phase 4).
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §7 (dispatch design (a), no structural JSX change, no query change).
    - Existing `useTranslations('sovereigntyDetail')` call site in each view for the translation hook contract.
  </read_first>
  <action>
    In each of the three view components, at the finding-row render site, replace the single `findingRow` translation call with a dispatch on `finding.status`:
    - `finding.status === 'YELLOW'` → `t('findingRowYellow', { …existing placeholders })`
    - `finding.status === 'RED'` → `t('findingRowRed', { …existing placeholders })`
    - `finding.status === 'GREY'` → `t('findingRowGrey', { …existing placeholders })`
    - Any other value (including GREEN, which should not appear as a finding) → fall back to the pre-existing `t('findingRow', …)` OR drop the row if the pre-existing key is being removed. Choose whichever keeps the diff minimal; if `findingRow` is no longer referenced anywhere, delete it from BOTH locale files as a cleanup step (in that case, extend Task 1's key-set check accordingly).

    No structural JSX change (no new components, no query change). The `analysis.downstreamStatus === 'GREEN'` / `'GREY' && findings.length === 0` alert branches keep their existing translation keys.

  </action>
  <verify>
    <automated>cd client &amp;&amp; yarn build</automated>
    <automated>bash -c "for f in client/src/components/sovereignty/SovereigntyCapabilityView.tsx client/src/components/sovereignty/SovereigntyDataView.tsx client/src/components/sovereignty/SovereigntyProcessView.tsx; do grep -c 'findingRowYellow\\|findingRowRed\\|findingRowGrey' \"\$f\" | awk -v f=\"\$f\" '{ if (\$1 &lt; 3) { print \"missing dispatch in \" f; exit 1 } }'; done"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three view components references all three new keys (`findingRowYellow`, `findingRowRed`, `findingRowGrey`) at least once.
    - `cd client && yarn build` exits 0.
    - No new GraphQL query, mutation, or hook is introduced (structural diff limited to the finding-row render site).
    - If `findingRow` was removed from the components, both locale files also drop `findingRow` (Task 1 key-set parity check still passes).
  </acceptance_criteria>
  <reversibility rating="reversible">Text-render dispatch swap; reverting restores the single-key call site.</reversibility>
  <done>
    All three views dispatch on `finding.status` for the new copy; client build is clean.
  </done>
</task>

</tasks>

<verification>
- `cd client && yarn build` exits 0.
- Smoke check: `curl -sf http://localhost:3000/sovereignty` (or the corresponding running-app URL) renders without console errors when a YELLOW or RED finding is present — a manual browser check with the updated copy is expected but not gated.
- DE / EN key parity assertion (Task 1 verify) passes.
</verification>

<success_criteria>
Detail views reflect the Phase 5 chain-premise semantics in both locales; no structural UI change; no schema or GraphQL surface change.
</success_criteria>

<artifacts_produced>
**New keys (DE + EN):**

- `sovereigntyDetail.findingRowYellow`
- `sovereigntyDetail.findingRowRed`
- `sovereigntyDetail.findingRowGrey`

**Updated copy:**

- `sovereigntyDetail.greyEmptyBody` (DE + EN)

**Modified components:**

- `SovereigntyCapabilityView.tsx`, `SovereigntyDataView.tsx`, `SovereigntyProcessView.tsx` — finding-row render dispatched on `finding.status`.
  </artifacts_produced>

<output>
Create `.planning/phases/05-rework-chain-colors/05-03-SUMMARY.md` when done.
</output>
