---
phase: 05-rework-chain-colors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/sovereignty/types.ts
  - server/src/sovereignty/repository.ts
  - server/src/sovereignty/evaluator.ts
  - server/src/sovereignty/__tests__/fixtures.ts
  - server/src/sovereignty/__tests__/evaluator.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
user_setup: []
tags:
  - sovereignty
  - evaluator
  - repository
  - tracer

must_haves:
  truths:
    - 'BusinessCapabilityChain carries an effectiveRequiredLevels: RequirementLevels field populated by the repository (per D-05).'
    - 'fetchBusinessCapabilityChain walks HAS_PARENT*0.. from the analysis root, filtered by ancestorCompany.id IN $companyIds, and folds max-per-dimension in TypeScript via maturityIndex (per D-05, RESEARCH §1.2–§1.3, and multi-tenancy §1.5).'
    - 'classifyNode emits YELLOW when maturityIndex(effectiveReq) − maturityIndex(actual) === 1, RED when the delta is ≥ 2, and produces no finding when the delta is ≤ 0 (per D-02, RESEARCH §2.1).'
    - 'The deviation thresholds are computed via maturityIndex only; no literal 4 (or any hard-coded scale bound) appears in the new deviation code (per D-02 scale-independence).'
    - "For BusinessCapability chains, walkApplication/walkInfrastructure/walkAIComponent see the root's effectiveRequiredLevels (not the raw chain.required) as the requirement passed to classifyNode (per D-03 uniform application, tracer scope = BC only)."
    - 'The Phase 2 D-03 per-branch visited-set contract for the downward childCapabilities walk is preserved unchanged (per RESEARCH §1.4).'
    - 'The tracer test walks a real fixture end-to-end (repository → evaluator → fixture assertion) and passes on the ancestor-stricter one-step scenario, yielding a YELLOW finding for the single tracer dimension (RESEARCH §10, Plan A success signal).'
    - 'No Cypher fragment introduced by this plan concatenates user input into query strings; the ancestor filter uses $companyIds and $isAdmin only (T-01 mitigation).'
    - "The variable-length ancestor MATCH is protected by an explicit TypeScript-side defensive cap (max 100 ancestors) that log-warns if reached, in addition to Neo4j's per-edge uniqueness (T-02 mitigation)."
    - 'ancestorCompany.id IN $companyIds is enforced on every ancestor node returned by the extended fetch (T-03 multi-tenancy mitigation, RESEARCH §1.5).'
  prohibitions:
    - 'MUST NOT delete classifyCapabilityAgainstParent, parentRequiredLevels, comparedCapabilityIds, selfViolatingIds, or the projectMarkers three-valued selfStatus machinery in this plan (Plan B owns D-06 retirement).'
    - 'MUST NOT modify projectMarkers, companyRollup, chainLabels, or any client component in this plan.'
    - 'MUST NOT extend deviation math to more than one dimension in this plan; the remaining three dimensions are Plan B (per RESEARCH §10 Plan A scope).'
    - 'MUST NOT introduce ancestor-fold logic in Cypher via a hand-rolled CASE ordinal mapping; the fold lives in TypeScript against maturityIndex (per RESEARCH §1.3 Recommendation Option 2).'

threat_model_ref: shared with 05-02-PLAN.md and 05-03-PLAN.md (T-01..T-04 defined at end of this file)
---

<objective>
Tracer slice for Phase 5. Prove the chain-premise math end-to-end for BusinessCapability on one sovereignty dimension: repository walks the full HAS_PARENT ancestor chain, folds max-per-dim in TypeScript, materialises `effectiveRequiredLevels` on `BusinessCapabilityChain`, and the evaluator's `classifyNode` emits YELLOW at deviation = 1 and RED at deviation ≥ 2 against that effective requirement. No `projectMarkers`, marker-projection, or D-06 retirement work is done here — that is Plan B.

Purpose: de-risk the ancestor-walk Cypher, the TS-side max fold via `maturityIndex`, and the `classifyNode` deviation extension before the full expansion in Plan B.
Output: `effectiveRequiredLevels` field, extended Cypher fetch, extended `classifyNode`, one new fixture, one new passing test.
</objective>

<execution_context>
@~~/.copilot/gsd-core/workflows/execute-plan.md
@~~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-rework-chain-colors/05-CONTEXT.md
@.planning/phases/05-rework-chain-colors/05-RESEARCH.md
@.planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md
@.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-CONTEXT.md
@server/src/sovereignty/types.ts
@server/src/sovereignty/repository.ts
@server/src/sovereignty/evaluator.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add effectiveRequiredLevels field and extend fetchBusinessCapabilityChain with variable-length ancestor walk (single tracer dimension)</name>
  <files>server/src/sovereignty/types.ts, server/src/sovereignty/repository.ts</files>
  <read_first>
    - server/src/sovereignty/types.ts (existing BusinessCapabilityChain, RequirementLevels, SOVEREIGNTY_MATURITY_LEVELS shape)
    - server/src/sovereignty/repository.ts (existing fetchBusinessCapabilityChain, the isRoot-gated one-hop HAS_PARENT block that this task extends)
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §1.2 (recommended shape), §1.3 (Cypher extension + Option 2 TS-side fold), §1.4 (cycle-safety contract), §1.5 (multi-tenancy)
    - .planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md D-01 (existing ancestor-fetch pattern)
    - server/src/sovereignty/evaluator.ts — the maturityIndex helper location (RESEARCH §1.3 references line 27) that is the sole ordinal source of truth to reuse.
  </read_first>
  <action>
    Add a readonly `effectiveRequiredLevels: RequirementLevels` field to `BusinessCapabilityChain` in types.ts, with a docstring stating it is populated only on the analysis root and folds max-per-dimension over the root's own required plus every ancestor's required along HAS_PARENT (per D-05). Do NOT retire `parentRequiredLevels` yet — Plan B owns that.

    In repository.ts, extend the BC branch of `fetchBusinessCapabilityChain`: **ADD a NEW `OPTIONAL MATCH` block** `OPTIONAL MATCH (cap)-[:HAS_PARENT*0..]->(ancestor:BusinessCapability)-[:OWNED_BY]->(ancestorCompany:Company) WHERE $isAdmin OR ancestorCompany.id IN $companyIds`, returning `collect(DISTINCT ancestor { .id, .sovereigntyReqStrategicAutonomy, .sovereigntyReqResilience, .sovereigntyReqSecurity, .sovereigntyReqControl }) AS ancestorRequiredRows`. **LEAVE the existing one-hop `OPTIONAL MATCH (cap)-[:HAS_PARENT]->(parent)` block, its `parentRequiredRows` collect, and its TypeScript-side `parentRequiredLevels` fold intact and functioning — Plan B (Task 4) removes them.** Both fetches must coexist during Wave 1 so existing parent-consistency tests keep passing until Plan B rewires them.

    On the TypeScript side (still inside repository.ts), import the existing `maturityIndex` helper from evaluator.ts (or lift it to a shared module if a circular-import risk exists — but do NOT duplicate the ordinal mapping) and fold `ancestorRequiredRows` per dimension: for each of `sovereigntyReqStrategicAutonomy`, `sovereigntyReqResilience`, `sovereigntyReqSecurity`, `sovereigntyReqControl`, take the row (including the root cap itself) with the highest `maturityIndex(value)` and use that literal `MaturityLevel` string as `effectiveRequiredLevels.<dim>`. A null ancestor row contributes `-1` and never wins. Cap the ancestor-row count at 100 with a `console.warn` on overflow (T-02 defensive cap); log the root id when hit.

    Populate `effectiveRequiredLevels` on the returned `BusinessCapabilityChain` for the root only. For nested `childCapabilities`, set `effectiveRequiredLevels` to a null-quadruple `RequirementLevels` (per RESEARCH §1.2 note — the recursive evaluator walk in Plan A / Plan B threads the descendant effective-Req through analyzeCapabilitySubtree, not through the repository).

    Do NOT touch `fetchBusinessProcessChain` or `fetchDataObjectChain` — Plan B owns those.

  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; grep -n "effectiveRequiredLevels" src/sovereignty/types.ts src/sovereignty/repository.ts</automated>
    <automated>cd server &amp;&amp; grep -n "HAS_PARENT\*0" src/sovereignty/repository.ts</automated>
    <automated>cd server &amp;&amp; grep -c "ancestorCompany.id IN \$companyIds" src/sovereignty/repository.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "readonly effectiveRequiredLevels: RequirementLevels" server/src/sovereignty/types.ts` returns exactly one hit inside the `BusinessCapabilityChain` interface.
    - `grep -n "HAS_PARENT\*0\.\." server/src/sovereignty/repository.ts` returns at least one hit.
    - `grep -n "ancestorCompany.id IN \$companyIds" server/src/sovereignty/repository.ts` returns at least one hit (T-03 multi-tenancy).
    - `grep -c 'maturityIndex' server/src/sovereignty/repository.ts` is ≥ 1 (TS-side ordinal fold uses the shared helper).
    - `grep -c "parentRequiredLevels" server/src/sovereignty/types.ts` remains ≥ 1 (D-06 retirement is Plan B only).
    - `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive field + additive Cypher fragment; both are removable without touching downstream consumers.</reversibility>
  <done>
    `BusinessCapabilityChain.effectiveRequiredLevels` exists; repository fetches the ancestor chain, folds max-per-dim in TypeScript via `maturityIndex`, filters ancestor companies via `$companyIds`, caps the ancestor list at 100 with a warning, and populates the field on the root only. tsc clean. No behavior change to consumers yet.
  </done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Extend classifyNode with deviation math (one-step YELLOW / ≥2-step RED) and thread effectiveRequiredLevels into walkApplication for BusinessCapability</name>
  <files>server/src/sovereignty/evaluator.ts, server/src/sovereignty/__tests__/fixtures.ts, server/src/sovereignty/__tests__/evaluator.test.ts</files>
  <read_first>
    - server/src/sovereignty/evaluator.ts — `classifyNode` (RESEARCH §2.1 references line 44) and `analyzeCapabilitySubtree` (RESEARCH §2.3 references lines 300–370), including how `chain.required` currently flows into walkers.
    - server/src/sovereignty/__tests__/fixtures.ts — existing BC fixtures (e.g. `redChainFixture`, the parent-consistency fixtures) as the shape template for the new fixture.
    - server/src/sovereignty/__tests__/evaluator.test.ts — existing describe-block naming to slot the tracer test in without touching parent-consistency blocks.
    - .planning/phases/05-rework-chain-colors/05-RESEARCH.md §2.1 (deviation math + threshold table), §2.3 (effective-Req threading), §10 Plan A (tracer scope).
    - .planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-CONTEXT.md D-03 (per-branch visited-set contract to preserve unchanged).
  </read_first>
  <behavior>
    - Given a BC root with own `sovereigntyReqSecurity = MEDIUM` and an ancestor requiring `HIGH`, effectiveRequiredLevels.sovereigntyReqSecurity is `HIGH` (max-of-ancestors fold from Task 1).
    - Given a leaf Application with achieved `sovereigntyAchSecurity = MEDIUM` under that root, classifyNode emits exactly one YELLOW finding on the security dimension (deviation = maturityIndex(HIGH) − maturityIndex(MEDIUM) = 1).
    - Given the same shape but leaf achieved `LOW`, classifyNode emits one RED finding on security (deviation = 2).
    - Given leaf achieved `HIGH` (or higher), no finding is emitted for security.
    - Given effective-Req null and achieved non-null, no finding is emitted (nothing required).
    - Given effective-Req non-null and achieved null, a GREY finding is emitted (unchanged from today).
    - No literal `4` appears in the deviation code; the thresholds use maturityIndex only.
    - Parent-consistency describe blocks in `evaluator.test.ts` continue to pass as-is (this is a tracer — parent-consistency machinery stays in place until Plan B).
  </behavior>
  <action>
    In `classifyNode`, extend the branch that today emits an implicit-GREEN (achieved ≥ required, no finding) so that it computes `deviation = maturityIndex(effectiveReq) − maturityIndex(actual)` when both are non-null and emits a `YELLOW` finding when `deviation === 1`, a `RED` finding when `deviation >= 2`, and no finding when `deviation <= 0`. The GREY branch (achieved null) is unchanged. The "required null → no finding" branch is unchanged. The literal `4` MUST NOT appear anywhere in the new logic — the ordinal check goes through `maturityIndex`.

    Thread `effectiveRequiredLevels` into the BC path only: at the entry of `analyzeCapabilitySubtree`/`analyzeBusinessCapability` for the analysis root, prefer `chain.effectiveRequiredLevels` over `chain.required` when passing the requirement into `walkApplication` / `walkInfrastructure` / `walkAIComponent` for this tracer. For nested `childCapabilities`, keep the existing behavior (Plan B will fold effective-Req properly for descendants); nested-BC coverage is out of this plan's scope. Preserve the Phase 2 D-03 per-branch visited-set contract on the downward walk unchanged.

    In `fixtures.ts`, add `ancestorStricterOneStepFixture`: a BC root with `sovereigntyReqSecurity = MEDIUM`, one ancestor row with `sovereigntyReqSecurity = HIGH`, a single downstream Application with `sovereigntyAchSecurity = MEDIUM`, and null for all other dimensions on all rows. The fixture must feed both the repository shape (populate `effectiveRequiredLevels.sovereigntyReqSecurity = HIGH`) and the evaluator input path.

    In `evaluator.test.ts`, add a new describe block "Phase 5 tracer: BC ancestor-stricter deviation (security)" with the following it-cases against `ancestorStricterOneStepFixture` (or an inline mutant of it): (a) leaf MEDIUM → 1 YELLOW finding on security, (b) leaf LOW → 1 RED finding on security, (c) leaf HIGH → no finding, (d) leaf null → 1 GREY finding on security. Do NOT modify existing describe blocks — Plan B rewires the parent-consistency tests.

    The test is RED first (write assertions → run → confirm they fail against pre-Task-2 classifyNode), then GREEN after the classifyNode change.

  </action>
  <verify>
    <automated>cd server &amp;&amp; yarn tsc --noEmit</automated>
    <automated>cd server &amp;&amp; yarn jest src/sovereignty/__tests__/evaluator.test.ts -t "Phase 5 tracer"</automated>
    <automated>bash -c "grep -v '^\s*//' server/src/sovereignty/evaluator.ts | grep -v '^\s*\*' | grep -c '\\b4\\b' | awk '{ if (\$1 &gt; 0) exit 1; else exit 0 }'"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && yarn jest src/sovereignty/__tests__/evaluator.test.ts -t "Phase 5 tracer"` exits 0.
    - `cd server && yarn jest src/sovereignty` exits 0 (pre-existing tests still green — parent-consistency machinery intact).
    - `grep -c 'ancestorStricterOneStepFixture' server/src/sovereignty/__tests__/fixtures.ts` is ≥ 1.
    - `grep -c 'maturityIndex' server/src/sovereignty/evaluator.ts` is ≥ 2 (existing + new deviation check).
    - No literal `4` appears in non-comment code of the modified `classifyNode` region: `awk '/classifyNode/,/^}/' server/src/sovereignty/evaluator.ts | grep -v '^\s*//' | grep -v '^\s*\*' | grep -c '\b4\b'` returns 0.
    - `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">classifyNode extension is a single-branch change; reverting collapses back to today's implicit-GREEN behavior with no other consumer changes required.</reversibility>
  <done>
    classifyNode produces YELLOW/RED per the deviation math on the tracer dimension against effective-Req; the new fixture and describe block pass; the full existing sovereignty suite is still green. The end-to-end path (repository fold → evaluator classify → finding) is validated for BC on one dimension.
  </done>
</task>

</tasks>

<threat_model>
Shared with 05-02-PLAN.md and 05-03-PLAN.md (Plan C carries none of these).

## Trust Boundaries

| Boundary                                     | Description                                                       |
| -------------------------------------------- | ----------------------------------------------------------------- |
| GraphQL resolver → Neo4j Cypher              | user-controlled `$companyIds` and `$isAdmin` cross into the query |
| Ancestor walk (this plan's Cypher extension) | new traversal that could leak across tenants or loop              |

## STRIDE Threat Register

| Threat ID       | Category               | Component                                                                   | Severity | Disposition | Mitigation Plan                                                                                                                                                                                   |
| --------------- | ---------------------- | --------------------------------------------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01         | Tampering              | new Cypher fragment in repository.ts                                        | low      | mitigate    | All new params passed via `$` placeholders; existing pattern extended (no string concatenation of user input into new Cypher). Verified by inspection of the Task 1 diff.                         |
| T-05-02         | Denial of Service      | HAS_PARENT*0.. variable-length walk                                         | low      | mitigate    | Neo4j per-edge uniqueness bounds cyclic expansion; add defensive TS-side cap of 100 ancestors with a `console.warn` on hit (Task 1).                                                              |
| T-05-03         | Information Disclosure | multi-tenant ancestor fetch                                                 | low      | mitigate    | `ancestorCompany.id IN $companyIds` clause added to the extended `OPTIONAL MATCH` (Task 1). Cross-tenant ancestor rows silently excluded from the max fold; regression covered in Plan B fixture. |
| T-05-04         | Tampering              | companyRollup.ts `pushAchievedScores` silently dropping new YELLOW findings | low      | mitigate    | Explicit YELLOW branch in `pushAchievedScores` — implemented in Plan B (task-flagged, not this plan).                                                                                             |
| </threat_model> |

<verification>
- `cd server && yarn tsc --noEmit` exits 0.
- `cd server && yarn jest src/sovereignty` exits 0 (full sovereignty suite green — pre-existing parent-consistency tests intact plus new tracer describe block passes).
- Manual inspection confirms only the four files in `files_modified` were changed.
</verification>

<success_criteria>
Chain-premise math is proven end-to-end for BusinessCapability on the security dimension: an ancestor-stricter fixture yields YELLOW at deviation = 1, RED at ≥ 2, GREEN at ≤ 0, GREY at null-either-side. The repository ancestor fetch is cycle-safe, multi-tenant-safe, and scale-independent. No downstream consumer (`projectMarkers`, `companyRollup`, client) has been touched — Plan B and Plan C carry that work.
</success_criteria>

<artifacts_produced>
**New symbols this plan creates:**

- `BusinessCapabilityChain.effectiveRequiredLevels: RequirementLevels` (types.ts)
- `ancestorStricterOneStepFixture` (fixtures.ts)
- "Phase 5 tracer: BC ancestor-stricter deviation (security)" describe block (evaluator.test.ts)
- (Cypher fragment) variable-length `HAS_PARENT*0..` ancestor walk in `fetchBusinessCapabilityChain`

**Modified symbols:**

- `classifyNode` (evaluator.ts) — deviation-math extension for one dimension
- `fetchBusinessCapabilityChain` (repository.ts) — ancestor-walk + TS max fold
- `analyzeCapabilitySubtree` / `analyzeBusinessCapability` — root-only threading of `effectiveRequiredLevels` into walker requirement

**Not yet retired (Plan B owns retirement):**

- `parentRequiredLevels`, `classifyCapabilityAgainstParent`, `comparedCapabilityIds`, `selfViolatingIds`, projectMarkers three-valued `selfStatus` machinery.
  </artifacts_produced>

<output>
Create `.planning/phases/05-rework-chain-colors/05-01-SUMMARY.md` when done.
</output>
