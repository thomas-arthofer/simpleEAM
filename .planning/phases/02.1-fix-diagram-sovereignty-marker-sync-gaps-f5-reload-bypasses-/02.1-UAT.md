---
status: testing
phase: 02.1-fix-diagram-sovereignty-marker-sync-gaps-f5-reload-bypasses-
source: [02.1-VERIFICATION.md]
started: 2026-07-30T00:00:00.000Z
updated: 2026-07-30T00:00:00.000Z
---

## Current Test

number: 1
name: F5/full-page reload restores sovereignty markers via the full syncDiagramOnOpen pipeline
expected: |
  With featureFlags.Sovereignty enabled and a company selected, open a diagram with visible fill/ring
  sovereignty markers, let the scene persist to localStorage, then perform a raw browser F5 reload
  (not the in-app Open dialog, not a manual Ctrl+R resync). The reloaded diagram shows the same
  sovereignty markers as before the reload, and the browser's Network tab shows a sovereigntyMarkers
  GraphQL request fired during the reload. Then, with a simulated network/GraphQL failure, reload
  again and confirm the diagram still restores from localStorage (elements visible, no crash), just
  without markers.
awaiting: user response

## Tests

### 1. F5/full-page reload restores sovereignty markers via the full syncDiagramOnOpen pipeline

expected: |
  With featureFlags.Sovereignty enabled and a company selected, open a diagram with visible fill/ring
  sovereignty markers, let the scene persist to localStorage, then perform a raw browser F5 reload
  (not the in-app Open dialog, not a manual Ctrl+R resync). The reloaded diagram shows the same
  sovereignty markers as before the reload, and the browser's Network tab shows a sovereigntyMarkers
  GraphQL request fired during the reload. Then, with a simulated network/GraphQL failure, reload
  again and confirm the diagram still restores from localStorage (elements visible, no crash), just
  without markers.
result: [pending]

### 2. Marker ellipses live-track a dragged main element

expected: |
  With featureFlags.Sovereignty enabled, drag a single main element that has visible fill/ring markers
  across the canvas (not a multi-selection). Separately, drag an element with no markers and watch for
  console errors/lag. Then Ctrl+Z after the marked-element drag. If a second client is available,
  confirm it sees repositioned markers during collaboration. Both marker ellipses should visually
  follow the dragged element in real time during the drag (not just snapping into place after release);
  the unmarked-element drag should produce no errors/lag; exactly one undo step should revert the
  marked-element drag; a second collaborating client should see the repositioned markers.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

- Both items require a live Docker/Neo4j/GraphQL stack and a real browser session (Network-tab inspection, live drag interaction, undo-stack depth, multi-client collaboration) — not reproducible via static code analysis or headless `yarn type-check` in this session. `02.1-VERIFICATION.md` independently confirmed all 4 locked decisions (D-01 through D-04) are correctly implemented in the codebase (6/8 must-haves fully verified; these 2 are present-and-wired but behavior-unverified).
