---
phase: 02.1-fix-diagram-sovereignty-marker-sync-gaps-f5-reload-bypasses-
verified: 2026-07-30T00:00:00Z
status: passed
score: 8/8 must-haves verified (6 via static analysis, 2 via human UAT confirmation — see 02.1-UAT.md)
behavior_unverified: 0
overrides_applied: 0
behavior_unverified_items:

  - truth: "Reloading the browser (F5 / full-page reload) on a diagram that has sovereignty markers re-fetches and re-renders those markers through the same pipeline handleOpenDiagram uses."
    test: "With featureFlags.Sovereignty enabled and a diagram open with visible fill/ring markers, save the scene, then perform a raw browser F5 reload (not the Open dialog, not Ctrl+R)."
    expected: "Reloaded diagram shows the same sovereignty markers as before reload; Network tab shows a sovereigntyMarkers GraphQL request fired during the reload."
    why_human: "Requires a running Docker/Neo4j stack with a selected company and a marker-bearing diagram in a real browser session — a raw F5 reload and network trace can't be reproduced by static code inspection or a headless type-check."

  - truth: "Dragging a single main diagram element that currently has fill/ring sovereignty markers attached moves both marker ellipses with it on every onChange event fired during the drag, not just after drop."
    test: "With featureFlags.Sovereignty enabled, drag a single marked main element across the canvas."
    expected: "Both marker ellipses visually follow the element in real time during the drag (not just snapping into place after release); only one undo step reverts the drag; a second connected client sees the repositioned markers during collaboration."
    why_human: "Live visual tracking during an in-progress drag, undo-stack depth, and collaboration broadcast correctness are runtime/visual behaviors that require a real browser session against a live stack — not observable via static code or type-check."
human_verification:

  - test: "F5/full-page reload on a diagram with visible sovereignty markers (Sovereignty flag enabled, company selected)."
    expected: "Markers reappear identically to opening via the Open dialog; a sovereigntyMarkers GraphQL request fires in the Network tab; a simulated sync failure still restores the scene from localStorage without markers and without throwing."
    why_human: "Needs a live Docker/Neo4j stack, a selected company, and a marker-bearing diagram in a real browser — not reproducible headlessly."

  - test: "Drag a single main element with attached fill/ring markers across the canvas; separately drag an unmarked element; then Ctrl+Z after a marked-element drag; then (if testable) observe a second collaborating client during the drag."
    expected: "Markers live-track the dragged element on every intermediate onChange (not just at drop); dragging an unmarked element produces no console errors/lag; exactly one undo step reverts the drag; a second collaborating client sees the repositioned markers, not just the moved main element."
    why_human: "Real-time visual tracking, undo-stack depth, and multi-client collaboration behavior can only be confirmed in a live running app, not via static analysis."
---

# Phase 02.1: Fix diagram sovereignty marker sync gaps Verification Report

**Phase Goal:** Fix two bugs in the sovereignty-marker feature: (1) F5/full-page reload bypasses sovereignty sync entirely — the localStorage scene-restore path must run the full `syncDiagramOnOpen` pipeline, identical to `handleOpenDiagram`; (2) marker fill/ring ellipses do not live-track a dragged main element — they must reposition in-place on every `onChange` during drag, scoped only to elements that already carry marker ellipses.
**Verified:** 2026-07-30
**Status:** passed (human UAT confirmed both pending items on 2026-07-31 — see 02.1-UAT.md)
**Re-verification:** No — initial verification, closed out by human UAT

## Goal Achievement

### Observable Truths

Truths merged from both plans' `must_haves.truths` (no ROADMAP.md Success Criteria available — this urgent bugfix phase was inserted directly, per the request's traceability note).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | F5/full-page reload re-fetches and re-renders sovereignty markers via the same `syncDiagramOnOpen` pipeline `handleOpenDiagram` uses, without cross-page navigation or manual resync. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `DiagramState.ts` scene-restore effect (lines ~130-203) now calls `await syncDiagramOnOpen(apolloClient, currentScene, { enabled: featureFlags.Sovereignty, companyId: selectedCompanyId })` before `restoreSceneData`, with `useApolloClient`/`useFeatureFlags` imported and called identically to `DiagramHandlers.ts`'s `handleOpenDiagram` (line 42-44, 249-252). Code is present and wired correctly (byte-identical options shape), but actual runtime re-appearance of markers on a raw F5 reload requires a live browser/stack — see human_verification. |
| 2 | Scene-restore still restores the last-saved local scene when Sovereignty is disabled or no company is selected — degrades gracefully rather than breaking restoration. | ✓ VERIFIED | Traced the full call chain: `syncDiagramOnOpen` → `syncSovereigntyMarkers` (`sovereigntyMarkers.ts` line ~404-406): `if (!options.enabled || !options.companyId) { return elements }` — the D-09 gate is enforced inside `syncSovereigntyMarkers`, so no `sovereigntyMarkers` GraphQL query is ever issued when the flag is off or no company is selected. Note: `syncDiagramOnOpenSimple` (name/missing-element sync) still runs unconditionally in both the reload path and `handleOpenDiagram` — this is intentional per D-01 ("run the full syncDiagramOnOpen pipeline... identical to handleOpenDiagram") and matches `handleOpenDiagram`'s own behavior exactly (verified same call shape at `DiagramHandlers.ts` lines 249-252); the PLAN-01 frontmatter's gloss "no sync call made" is imprecise phrasing but the functional requirement (no sovereignty-marker query when disabled) is correctly met and does not regress restoration. |
| 3 | If the sync call fails during scene restore, the diagram still restores from localStorage without markers rather than failing to load or throwing an unhandled rejection. | ✓ VERIFIED | `DiagramState.ts` (~lines 152-161): `let syncedScene = currentScene; try { syncedScene = await syncDiagramOnOpen(...) } catch (syncError) { console.warn(...) }` — on failure `syncedScene` remains the original `currentScene`, and execution falls through to `restoreSceneData(syncedScene)` unchanged. Deterministic control-flow, verifiable by static reading; matches `handleOpenDiagram`'s identical try/catch convention (`DiagramHandlers.ts` lines 248-253). |
| 4 | Scene-restore effect still only fires once per persisted scene (`sceneRestoredRef` guard) and is still skipped when `openDiagramId` is present. | ✓ VERIFIED | `DiagramState.ts`: `if (excalidrawAPI && isClient && currentScene && !sceneRestoredRef.current)` guard unchanged; `if (openDiagramId) { return }` early-exit unchanged; `sceneRestoredRef.current = true` still set at the same point after restoration. No changes to this gating logic — only the data feeding `restoreSceneData` changed, exactly as the plan specified. |
| 5 | Dragging a single main element with attached fill/ring markers moves both marker ellipses with it on every `onChange` event during the drag, not just after drop. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `ExcalidrawWrapper.tsx`'s `handleChange` (lines 174-224) calls `repositionAllMarkerEllipses(elements)` on every invocation (i.e., every `onChange`, which Excalidraw fires continuously during a drag) and applies the repositioned elements via `updateScene({ elements: repositionedElements }, false)` when `changed`. Code is present, wired, and geometrically correct (see Truth 7), but actual real-time visual tracking during a live drag requires a running browser — see human_verification. |
| 6 | Dragging/editing/adding any element that does NOT currently carry markers triggers zero marker-recomputation work (D-04). | ✓ VERIFIED | `repositionAllMarkerEllipses` (`sovereigntyMarkers.ts` lines 355-390): builds a `Map` of only fill/ring marker elements, then only calls `repositionMarkerEllipses` for main elements (`customData.isMainElement && customData.databaseId`) that have a **complete** pair (`pair?.fill && pair?.ring`) in that map; if no pair exists at all, `repositionedById.size === 0` and the function returns `{ elements, changed: false }` — the original array reference, no new allocation, and `handleChange` performs no `updateScene` call when `changed` is false. Verified deterministically via code reading — a partial pair (only fill or only ring) is also correctly skipped (`if (!pair?.fill || !pair?.ring) continue`). |
| 7 | Marker ellipse identity is preserved across a drag-triggered reposition (D-03): same ids, only x/y/version/versionNonce/updated change. | ✓ VERIFIED | `repositionMarkerEllipses` (`sovereigntyMarkers.ts` lines 298-341) returns `{ ...fillEllipse, x, y, version, versionNonce, updated }` / `{ ...ringEllipse, ... }` — spreads the original element, preserving `id` and all other fields; no `generateElementId()` call in this function. Geometry formula (`fillSize=10`, `ringSize=18`, top-right anchor) is character-for-character identical to `createMarkerEllipses`'s formula (lines 162-170 vs. 307-313), confirming the reposition recomputes the same anchor `createMarkerEllipses` establishes at sync time. |
| 8 | No infinite onChange/updateScene loop, no extra undo/history entries, no duplicate collaboration broadcasts from drag-time repositioning (D-04's non-functional half). | ✓ VERIFIED (static) / ⚠️ needs live confirmation for undo/collab | `handleChange` sets `suppressOnChangeRef.current = true` immediately before `apiRef.current?.updateScene({ elements: repositionedElements }, false)`; the function's first guard (`if (suppressOnChangeRef?.current) { suppressOnChangeRef.current = false; return }`) causes the next re-entrant `onChange` (fired by this programmatic update) to exit immediately without recursing — no loop possible by construction. The `updateScene(..., false)` second-argument matches the existing no-history-entry convention already used elsewhere in the codebase (`DiagramLibrarySidebar.tsx`, `addRelatedElementsService.ts`). The forwarded `onChange`/`broadcastSceneUpdate` calls use `effectiveElements` (the repositioned array) in the same pass, so no duplicate broadcast call is introduced — this is a single invocation, not a second one. Actual absence of extra undo steps and correct behavior under live collaboration still needs a live-browser check (see human_verification), consistent with the SUMMARY's own D4/D5 human-judgment flags. |

**Score:** 6/8 truths verified (2 present-and-wired, behavior-unverified — both requiring a live browser/Docker/Neo4j stack per this project's established UAT pattern).

### Decisions Cross-Referenced (D-01 through D-04)

| Decision | Requirement | Implementation Evidence | Status |
|---|---|---|---|
| D-01 — reload restore path runs the **full** `syncDiagramOnOpen` pipeline (not markers-only shortcut) | `DiagramState.ts` calls `syncDiagramOnOpen(apolloClient, currentScene, { enabled, companyId })` — identical call shape and options object to `handleOpenDiagram` (`DiagramHandlers.ts` line 249). Both paths run `syncDiagramOnOpenSimple` (name/missing-element sync) unconditionally and gate only the sovereignty-marker sub-call inside `syncSovereigntyMarkers`. | ✓ Implemented exactly as decided |
| D-02 — markers live-reposition on **every `onChange`** during drag (not just drag-end) | `ExcalidrawWrapper.tsx`'s `handleChange` calls `repositionAllMarkerEllipses` unconditionally on every invocation (before the `onChange`/broadcast forwarding), which fires on every Excalidraw change event including mid-drag pointer-move events. | ✓ Implemented exactly as decided |
| D-03 — reposition **mutates existing elements in place** (same id, bumped version/versionNonce/updated), not remove-then-recreate | `repositionMarkerEllipses` spreads the original `fillEllipse`/`ringEllipse` objects, changing only `x`/`y`/`version`/`versionNonce`/`updated`; no new `id` generated. `createMarkerEllipses`/`applySovereigntyMarkers` are untouched (confirmed no diff to those functions in the commits). | ✓ Implemented exactly as decided |
| D-04 — reposition only runs for elements that **already carry marker ellipses**, not a lazy check-every-element condition | `repositionAllMarkerEllipses` builds a marker-pair map first, then only processes main elements with a **complete** existing pair; elements without any pair, or with only a partial pair, are skipped with `continue` and incur no `repositionMarkerEllipses` call. | ✓ Implemented exactly as decided |

All four locked decisions (D-01 through D-04) are implemented in the codebase exactly as specified in `02.1-CONTEXT.md` — no deviations found.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `client/src/components/diagrams/state/DiagramState.ts` | Scene-restore effect calls `syncDiagramOnOpen` before `restoreSceneData`, fail-soft try/catch, new `useApolloClient`/`useFeatureFlags` hooks | ✓ VERIFIED | Confirmed present, substantive (27 lines changed, matches plan's exact prescribed shape), and wired (imports used, hook calls present, call site inside the correct effect). |
| `client/src/components/diagrams/utils/sovereigntyMarkers.ts` (new exports `repositionMarkerEllipses`, `repositionAllMarkerEllipses`) | Two new exported functions per plan 02.1-02 | ✓ VERIFIED | Both functions present (lines 298, 355), substantive (full geometry + D-04 pair-completeness logic implemented, not a stub), and exported. `createMarkerEllipses`/`applySovereigntyMarkers`/`fetchSovereigntyMarkersForDiagram`/`syncSovereigntyMarkers` confirmed unchanged. |
| `client/src/components/diagrams/components/ExcalidrawWrapper.tsx` | `handleChange` wired to call `repositionAllMarkerEllipses`, suppress-then-updateScene pattern, `apiRef` in deps | ✓ VERIFIED | Import present (line 17), call inside `handleChange` (line 188), `suppressOnChangeRef.current = true` set before `updateScene` (line 191-193), `apiRef` present in `useCallback` deps array (line 224). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `DiagramState.ts` scene-restore effect | `databaseSyncUtils.ts`'s `syncDiagramOnOpen` | Direct `await` call with `{ enabled: featureFlags.Sovereignty, companyId: selectedCompanyId }` | ✓ WIRED | Call present, options shape identical to `handleOpenDiagram`'s call site; result (`syncedScene`) flows into `restoreSceneData` and then `excalidrawAPI.updateScene`. |
| `ExcalidrawWrapper.tsx`'s `handleChange` | `sovereigntyMarkers.ts`'s `repositionAllMarkerEllipses` | Direct call on every `onChange`, result fed into `updateScene` and forwarded `onChange`/`broadcastSceneUpdate` | ✓ WIRED | Confirmed `effectiveElements` (repositioned array when `changed`) is used for both the forwarded `onChange(effectiveElements, appState)` and `broadcastSceneUpdate(effectiveElements, appState)` calls — collaborators and the unsaved-changes tracker see corrected positions in the same pass, as the plan specified. |

### Requirements Coverage

REQUIREMENTS.md was checked and, as expected per this urgent bugfix phase's insertion into ROADMAP.md, does **not** contain `SOV-RELOAD-SYNC` or `SOV-MARKER-DRAG` — this is not a traceability gap per the task instructions, and is not flagged as orphaned.

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SOV-RELOAD-SYNC | 02.1-01-PLAN.md | F5 reload runs full sync pipeline | Present/wired; runtime behavior needs human confirmation | See Truth 1 |
| SOV-MARKER-DRAG | 02.1-02-PLAN.md | Marker ellipses live-track dragged element | Present/wired; runtime behavior needs human confirmation | See Truth 5 |

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`), warning markers (`TODO`/`HACK`/`PLACEHOLDER`), empty stub implementations, or hardcoded-empty-data patterns were introduced by this phase's changes.

One pre-existing `TODO` was found at `ExcalidrawWrapper.tsx` line 626 (`isViewerRole={false} // TODO: Aus dem Auth-Kontext oder Props holen`) — confirmed via `git show --stat` on both phase commits (`7a9a9f3`, `c7e12a6`) that this line was **not** touched by either commit; it predates this phase and is out of scope.

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `ExcalidrawWrapper.tsx` | 626 | Pre-existing `TODO` (unrelated to this phase) | ℹ️ Info | None — not modified by this phase's commits |

### Automated Verification

- **`cd client && yarn type-check`** — ran directly in this session: **exit 0, no output**. Zero new TypeScript errors across the whole client package (not just the three modified files).
- **Jest tests for the diagrams module** — searched the `client/` workspace for `*.test.ts*` files and for a `test`/`jest` script in `client/package.json`: **none exist**. The client package has no Jest test suite configured (`package.json` scripts are limited to `dev`/`build`/`start`/`lint`/`type-check`/`codegen`/import scripts — no `test` script, no Jest config file, no `__tests__` directories anywhere under `client/src`). This is a pre-existing gap in the codebase's test infrastructure, not something introduced or omitted by this phase — there was no existing relevant Jest test to run.

### Data-Flow Trace (Level 4)

Not applicable in the traditional sense — this phase does not add a component rendering fetched data. The relevant "data flow" is the geometry pipeline itself:

- `DiagramState.ts` reload path: `currentScene` (from localStorage) → `syncDiagramOnOpen` (real GraphQL-backed name/missing-element + sovereignty-marker sync, confirmed non-stub via `fetchElementData`/`fetchSovereigntyMarkersForDiagram` GraphQL calls in `databaseSyncUtils.ts`/`sovereigntyMarkers.ts`) → `restoreSceneData` → `excalidrawAPI.updateScene`. Real data flows; not a static/hardcoded return.
- `ExcalidrawWrapper.tsx` drag path: live `elements` array from Excalidraw's own `onChange` → `repositionAllMarkerEllipses` (pure geometry recompute from the main element's live `x`/`y`/`width`) → `updateScene`. Real, non-hardcoded transform.

### Behavioral Spot-Checks

Skipped — this phase's runtime behavior requires a live browser (Excalidraw canvas rendering, drag events, F5 reload) and a running Docker/Neo4j/GraphQL stack, none of which are available in this session, consistent with the explicit note in the verification request. No CLI-testable entry points exist for these two bugs (no API route, no CLI command, no build artifact to spot-check).

### Probe Execution

No probe scripts (`scripts/*/tests/probe-*.sh`) are declared in either plan or referenced in the SUMMARYs for this phase. Skipped — not applicable.

### Human Verification Required

#### 1. F5/full-page reload restores sovereignty markers

**Test:** With `featureFlags.Sovereignty` enabled and a company selected, open a diagram with visible fill/ring sovereignty markers, let the scene persist to localStorage, then perform a raw browser F5 reload (not the in-app Open dialog, not a manual Ctrl+R resync).
**Expected:** The reloaded diagram shows the same sovereignty markers as before the reload, and the browser's Network tab shows a `sovereigntyMarkers` GraphQL request fired during the reload. Then, with a simulated network/GraphQL failure, reload again and confirm the diagram still restores from localStorage (elements visible, no crash), just without markers.
**Why human:** Requires a live Docker/Neo4j/GraphQL stack, a selected company, and a marker-bearing diagram in a real browser session with Network-tab inspection — not reproducible via static code analysis or a headless type-check in this session.

#### 2. Marker ellipses live-track a dragged main element

**Test:** With `featureFlags.Sovereignty` enabled, drag a single main element that has visible fill/ring markers across the canvas (not a multi-selection). Separately, drag an element with no markers and watch for console errors/lag. Then Ctrl+Z after the marked-element drag. If a second client is available, confirm it sees repositioned markers during collaboration.
**Expected:** Both marker ellipses visually follow the dragged element in real time during the drag (not just snapping into place after release); the unmarked-element drag produces no errors/lag; exactly one undo step reverts the marked-element drag; a second collaborating client sees the repositioned markers.
**Why human:** Real-time visual tracking during an in-progress drag, undo-stack depth, and multi-client collaboration behavior are runtime/visual properties that can only be confirmed in a live running app — not via static analysis, even though the guard logic (no-loop, no-extra-history) was confirmed correct by code inspection.

### Gaps Summary

No gaps were found — every truth and every locked decision (D-01–D-04) is correctly implemented in the codebase, `yarn type-check` is clean, and no anti-patterns were introduced. Two of the eight observable truths (F5-reload marker re-appearance, and live drag-tracking) asserted runtime/visual behavior that could not be exercised via static analysis and were routed to human verification, consistent with the prior Phase 2 (`02-UAT.md`) precedent. Both `02.1-01-SUMMARY.md` and `02.1-02-SUMMARY.md` correctly flagged these same items as `human_judgment: true` rather than silently claiming a pass.

There is also no existing Jest test suite for the diagrams module (or the client package at all) — this is a pre-existing infrastructure gap, not a regression introduced by this phase, and does not block the verdict.

**Update 2026-07-31:** Both human-verification items were confirmed passed by the user against the rebuilt/recreated `client` container (live Docker/Neo4j/GraphQL stack) — see [02.1-UAT.md](./02.1-UAT.md) (2/2 passed, 0 issues). Score raised to 8/8 must-haves verified; phase status closed as `passed`.

---

_Verified: 2026-07-30_
_Verifier: the agent (gsd-verifier)_
