---
phase: 02.3-business-capability-requirement-chain-consistency-compare-pa
verified: 2026-08-07T00:00:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 02.3: Business Capability Requirement-Chain Consistency Verification Report

**Phase Goal:** When a BusinessCapability's own required sovereignty level is weaker than any of its parent BusinessCapabilities' required levels for the same dimension, the system emits a YELLOW self-status finding (`violatingElementType: 'businessCapability'`) that names the child as the violation source. Applies to both the analysis root against its direct parents (D-01) and to descendants against their in-scope parent during subtree recursion. Null-either-side excludes the dimension entirely (D-03/D-04). Multi-parent handling is independent-per-edge (D-02). Reuses the existing `Finding` shape (D-06) and narrowly excepts markers.ts's unconditional GREY-self invariant for this one case (D-05).

**Verified:** 2026-08-07
**Status:** passed
**Re-verification:** No — initial verification (no prior 02.3-VERIFICATION.md existed)

**Traceability note:** This is an urgent INSERTED phase with no dedicated section in `.planning/REQUIREMENTS.md` (confirmed — grep for `02.3|D-01..D-06` in that file returns zero matches). Requirement IDs D-01 through D-06 are defined and tracked exclusively in `02.3-CONTEXT.md`, which was used as the must-haves source for this verification instead of REQUIREMENTS.md.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A root capability weaker than a direct parent's required level produces a YELLOW `Finding` with `violatingElementType='businessCapability'`, correct `requiredLevel`/`actualLevel`/`chainPath=[parentId, childId]` (D-01 root half, D-06) | ✓ VERIFIED | `classifyCapabilityAgainstParent` in [evaluator.ts](../../../server/src/sovereignty/evaluator.ts#L98) implements this exactly; wired into `analyzeBusinessCapability`'s `parentRequiredLevels.flatMap(...)` loop. Tests A/D in `evaluator.test.ts` pass (part of 39/39 suite run). |
| 2 | Descendants are checked against their in-scope parent during subtree recursion, using the immediate parent (not the root) (D-01 descendant half) | ✓ VERIFIED | `analyzeCapabilitySubtree`'s child loop calls `classifyCapabilityAgainstParent(child, { id: chain.rootId, required: chain.required }, [])` — the in-scope `chain` at each recursion level. Test J (grandchild) explicitly proves comparison is against the immediate parent (child), not the root: `requiredLevel === 'MEDIUM'` (child's), not `'HIGH'` (root's). |
| 3 | Multi-parent handling is independent-per-edge — below ANY parent flags, not required to be below ALL, and multiple stricter parents produce multiple independent findings, never collapsed (D-02) | ✓ VERIFIED | `chain.parentRequiredLevels.flatMap(parentEntry => classifyCapabilityAgainstParent(...))` — one call per parent entry, concatenated. Tests K (two independent findings) and L (only-one-stricter produces exactly one) pass. |
| 4 | Null on either side (parent's or child's required level) excludes the dimension entirely — no finding, not even GREY (D-03/D-04) | ✓ VERIFIED | `classifyCapabilityAgainstParent`: `if (parentRequired === null \|\| childRequired === null) continue` — diverges deliberately from `classifyNode`'s GREY-on-null-actual behavior. Tests B, H, I all assert zero findings for the affected dimension. |
| 5 | The new finding reuses the existing `Finding` shape as-is; `ViolatingElementType` gains `'businessCapability'`; no schema change needed since `SovereigntyFinding.violatingElementType` is already loose `String!` (D-06) | ✓ VERIFIED | [types.ts:26-30](../../../server/src/sovereignty/types.ts#L26-L30) — `ViolatingElementType` union now includes `'businessCapability'`; `Finding` interface unchanged (no new fields). `classifyCapabilityAgainstParent` returns `Finding[]`, populating existing fields (`requiredLevel`=parent's, `actualLevel`=child's own, per D-06's explicit repurposing). |
| 6 | markers.ts's previously-unconditional GREY-self invariant is narrowly excepted: a capability with this contradiction finding gets YELLOW selfStatus; every OTHER capability (including diamond-shared ones with only a passthrough reference) still gets GREY (D-05) | ✓ VERIFIED | `markers.ts` computes `selfViolatingIds` once up-front from the full finding set (post-CR-01-fix) and consults it in both the findings-loop and the backfill loop, so per-finding/per-block iteration order can no longer clobber a genuine YELLOW back to GREY. Test E (basic YELLOW), Test F (GREY preserved when no contradiction), and **Test N (diamond-shared capability — CR-01 regression)** all pass. |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `server/src/sovereignty/types.ts` | `ViolatingElementType` extended, `BusinessCapabilityChain.parentRequiredLevels` field | ✓ VERIFIED | Line 26-30 union includes `'businessCapability'`; line 156 `parentRequiredLevels: readonly { id, required }[]` field present with JSDoc. |
| `server/src/sovereignty/evaluator.ts` | `classifyCapabilityAgainstParent`, wired into both `analyzeBusinessCapability` (root) and `analyzeCapabilitySubtree` (descendants) | ✓ VERIFIED | Function exported at line ~98; two call sites confirmed via grep (line ~329 in `analyzeCapabilitySubtree`, line ~337 in `analyzeBusinessCapability`). WR-01 fix: `if (pathVisited.has(child.rootId)) continue` guards the classifier call before it can fire on a cyclic re-entrant edge. |
| `server/src/sovereignty/markers.ts` | Conditional GREY backfill and conditional `isCapability` branch | ✓ VERIFIED | `selfViolatingIds` set computed once from the full `analysis.findings` (not per-finding), consumed by both the main loop and the backfill loop. CR-01 fix present and matches the reviewer's recommended fix verbatim. |
| `server/src/sovereignty/repository.ts` | `fetchBusinessCapabilityChain` fetches root's direct parents' required levels via `isRoot`-gated Cypher, tenant-isolation guarded | ✓ VERIFIED | `isRoot` param (default `false`), OUT-direction `(cap)-[:HAS_PARENT]->(parent:BusinessCapability)-[:OWNED_BY]->(parentCompany:Company)` with `WHERE parent IS NULL OR $isAdmin OR parentCompany.id IN $companyIds` tenant guard (T-02.3-04 mitigation); pre-existing IN-direction child match untouched. |
| `server/src/sovereignty/__tests__/fixtures.ts` | New fixtures for root/descendant/multi-parent/cycle/diamond cases | ✓ VERIFIED | `rootParentContradictionFixture`, `rootParentNoContradictionFixture`, `descendantParentContradictionFixture`, `multiParentBothStricterFixture`, `multiParentOnlyOneStricterFixture`, `capabilityCycleFixture`, `capabilityCycleTriggeringFixture` (WR-01/WR-02 regression fixture), `diamondSharedCapabilityFixture` (CR-01 regression fixture) all present and exported. |
| `server/src/sovereignty/__tests__/evaluator.test.ts`, `markers.test.ts` | Tests A-M plus post-review regression tests | ✓ VERIFIED | 39/39 tests pass across 5 suites, including Test M2 (WR-01 cyclic spurious-finding guard) and Test N (CR-01 diamond-shared capability). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `evaluator.ts analyzeBusinessCapability` | `classifyCapabilityAgainstParent` | `chain.parentRequiredLevels.flatMap(...)` | ✓ WIRED | Confirmed by reading; Tests A/D/K/L exercise this path. |
| `evaluator.ts analyzeCapabilitySubtree` | `classifyCapabilityAgainstParent` | per-child loop, guarded by `pathVisited.has(child.rootId)` | ✓ WIRED | Confirmed by reading; Tests G/H/I/J/M/M2 exercise this path, including the post-review cycle-safety guard. |
| `markers.ts projectMarkers` | `selfViolatingIds` (global, not per-finding) | main findings loop + backfill loop both consult the same precomputed set | ✓ WIRED | Confirmed by reading; Test N (diamond, order-dependent scenario) exercises this and passes. |
| `repository.ts fetchBusinessCapabilityChain` (root call) | Neo4j `HAS_PARENT` OUT edge | `isRoot=true` gated Cypher clause, `loadBusinessCapabilitySupportChain` passes `isRoot: true` | ✓ WIRED | Confirmed by reading; both HAS_PARENT directions present (grep confirms IN unchanged, OUT newly added), tenant guard present. |

### Behavioral Spot-Checks / Test Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full sovereignty suite passes | `cd server && yarn jest src/sovereignty/__tests__/` | 5 suites, 39 tests, all pass | ✓ PASS (independently re-run in this session) |
| Type-check clean | `cd server && yarn tsc --noEmit` | No output, exit 0 | ✓ PASS (independently re-run in this session) |

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|---|---|---|---|---|
| D-01 | 02.3-CONTEXT.md | Root-vs-direct-parents AND descendant-vs-in-scope-parent comparison | ✓ SATISFIED | Truths 1 & 2 verified above |
| D-02 | 02.3-CONTEXT.md | Independent-per-edge multi-parent handling | ✓ SATISFIED | Truth 3, Tests K/L |
| D-03 | 02.3-CONTEXT.md | Null parent-required excludes dimension; cycle-safety preserved | ✓ SATISFIED | Truth 4 (null exclusion), Tests M/M2 (cycle-safety, including post-review WR-01 fix) |
| D-04 | 02.3-CONTEXT.md | Null child-required excludes dimension (not treated as weakest) | ✓ SATISFIED | Truth 4, Tests B/H |
| D-05 | 02.3-CONTEXT.md | Narrow YELLOW-self exception to markers.ts's unconditional GREY invariant | ✓ SATISFIED | Truth 6, Tests E/F/N (including post-review CR-01 fix) |
| D-06 | 02.3-CONTEXT.md | Reuse existing `Finding` shape; extend `ViolatingElementType`; `chainPath=[parentId, childId]` | ✓ SATISFIED | Truth 1/5, `types.ts` union extension, no new DTO |

No orphaned requirements — REQUIREMENTS.md has no 02.3 section to cross-reference against (confirmed empty grep result), consistent with this being an inserted phase whose traceability lives entirely in 02.3-CONTEXT.md.

### Anti-Patterns Found

None found in the current (post-c0f5486) state of the reviewed files. The two issues previously reported by `02.3-REVIEW.md` (CR-01, WR-01) plus WR-02's test-quality gap have been fixed and are each covered by a purpose-built regression test that would fail if the fix were reverted:

- **CR-01** (markers.ts self-violation clobbered by later passthrough finding for diamond-shared capabilities): fixed via a precomputed `selfViolatingIds` set consulted in both loops; regression test `Test N` uses a fixture (`diamondSharedCapabilityFixture`) that reproduces the exact order-dependent scenario the reviewer described (capability violates against `cap-p1`, processed before a non-violating-but-passthrough block for `cap-p2`) and asserts `selfStatus === 'YELLOW'` survives.
- **WR-01** (`classifyCapabilityAgainstParent` call not guarded by cycle-safety `visited` set in `analyzeCapabilitySubtree`): fixed via `if (pathVisited.has(child.rootId)) continue` immediately before both the recursive call and the classifier call; regression test `Test M2` uses a new fixture (`capabilityCycleTriggeringFixture`) specifically engineered so the cyclic re-entrant node's required level (`NONE`) IS weaker than its cyclic "parent" (`LOW`) — the exact condition the original `Test M`/`capabilityCycleFixture` combination failed to exercise (per WR-02) — and asserts no finding attributes a contradiction to the re-entrant edge.
- **WR-02** (Test M gave false confidence because its fixture never triggered the vulnerable path): addressed by adding the count assertion (`expect(result.findings).toHaveLength(1)`) to the original Test M, plus the new Test M2 with a triggering fixture.

The two Info-level items from the review (IN-01: `violatingElementName` falls back to raw `chain.rootId` since `SupportChain` has no `name` field; IN-02: `chainPathPrefix` parameter always called with `[]`; IN-03: an orphaned/unowned parent capability is silently excluded even for admins) remain open as documented, deliberate, low-priority deviations — none are blocking for this phase's goal and none contradict any D-01..D-06 must-have. No debt markers (`TBD`/`FIXME`/`XXX`) found in the modified files.

### Human Verification Required

None. All must-haves are backed by either static/type-level facts (grep-verifiable code shape) or automated tests that were independently re-run in this session (39/39 passing, `tsc --noEmit` clean). No UI/visual, real-time, or external-service behavior is in scope for this phase's must-haves — the D-05 "colored YELLOW fill" consequence is a downstream diagram-rendering concern outside `server/src/sovereignty/*`, and the phase's own scope/artifacts list confines the deliverable to the evaluator/markers/repository/types layer, which is fully exercised by the test suite.

### Gaps Summary

No gaps. All 6 observable truths derived from the phase goal and D-01..D-06 are verified against the current (post-c0f5486) codebase, not merely against SUMMARY.md claims. The code-review-identified Critical (CR-01) and Warning (WR-01, WR-02) issues were independently confirmed fixed by direct code reading (not just trusting the REVIEW.md or SUMMARY.md narrative), and their corresponding regression tests were confirmed present and passing via an independent test run in this session (39/39 tests, 5/5 suites, `tsc --noEmit` clean).

---

_Verified: 2026-08-07_
_Verifier: the agent (gsd-verifier)_
