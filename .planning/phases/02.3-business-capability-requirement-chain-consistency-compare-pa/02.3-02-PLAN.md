---
phase: 02.3
plan: 02
type: execute
wave: 2
depends_on:
  - 02.3-01
files_modified:
  - server/src/sovereignty/evaluator.ts
  - server/src/sovereignty/repository.ts
  - server/src/sovereignty/__tests__/fixtures.ts
  - server/src/sovereignty/__tests__/evaluator.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "For every child capability nested inside a `BusinessCapabilityChain` subtree, if that child's own required level for a dimension is below its immediate parent (already-in-scope `chain.required`), a `Finding` with status='YELLOW', violatingElementType='businessCapability', violatingElementId=<child.rootId>, requiredLevel=<parent.required[dim]>, actualLevel=<child.required[dim]>, chainPath=[..., <parent.rootId>, <child.rootId>] is emitted (D-01 descendant half)."
    - "For a root with MULTIPLE entries in `parentRequiredLevels` where TWO parents both have a stricter required level for the same dimension, exactly TWO independent findings are emitted (one per parent-edge), never one collapsed worst-of finding (D-02 — mirrors walkInfrastructure's per-parent-edge independence)."
    - "For a root with multiple parents where only ONE parent is stricter for a given dimension, exactly ONE finding is emitted for that parent — the root is flagged 'below ANY parent's required level', not required to be below ALL (D-02)."
    - "The Neo4j Cypher query in `fetchBusinessCapabilityChain` fetches the ROOT's own direct parents' four `sovereigntyReq*` scalar columns in the SAME query as the existing childIds/appIds/aiComponentIds fetch, populating `parentRequiredLevels` on the returned `BusinessCapabilityChain`. Descendant `childCapabilities` continue to NOT have parentRequiredLevels populated (D-01 explicit: root-only fetch)."
    - "Cycle-safety contract from Phase 2's D-03 is preserved: `analyzeCapabilitySubtree`'s per-branch visited-set behavior is unchanged; the new descendant-vs-parent check adds no new recursive traversal."
  artifacts:
    - server/src/sovereignty/evaluator.ts (hooks the descendant-vs-parent check into `analyzeCapabilitySubtree`; adds independent-per-parent iteration in `analyzeBusinessCapability`)
    - server/src/sovereignty/repository.ts (extends the existing `fetchBusinessCapabilityChain` Cypher query with an OPTIONAL MATCH to direct parents, decodes their required-level scalar columns, populates `parentRequiredLevels`; only at the top-level root, not on recursive child fetches)
    - server/src/sovereignty/__tests__/fixtures.ts (adds `descendantParentContradictionFixture` for the recursive-subtree case; adds `multiParentContradictionFixture` for the D-02 independent-edge case)
    - server/src/sovereignty/__tests__/evaluator.test.ts (adds descendant + multi-parent tests)
  key_links:
    - "evaluator.ts `analyzeCapabilitySubtree` loop over `chain.childCapabilities` (existing recursion at lines 269-278 in Plan 02.3-01's file state) — the ONE place where descendant-vs-parent finds a parent already in scope for free (no new fetch needed for this case, per D-01)."
    - "repository.ts `fetchBusinessCapabilityChain` — MUST scope the new parent OPTIONAL MATCH to only run at the top-level call, not at each recursive childId call, so descendant capabilities' `parentRequiredLevels` is `[]` (they already get their descendant check via evaluator.ts's in-scope parent, per D-01)."
    - "repository.ts Cypher direction: `(cap)-[:HAS_PARENT]->(parent:BusinessCapability)` is OUT direction (schema.graphql line 78), opposite of the existing `(cap)<-[:HAS_PARENT]-(child)` IN direction (line 79). Getting the arrow wrong reverses parent/child semantics."
---

<objective>
Expand Plan 02.3-01's tracer to cover the two remaining cases in D-01 (descendant-vs-parent inside the recursive subtree walk) and D-02 (multi-parent independent-edge iteration for the root), and land the Neo4j Cypher fetch that actually populates `parentRequiredLevels` from real database data in `repository.ts`. After this plan, the feature works end-to-end from a real GraphQL sovereignty query, not just from hand-built fixtures.

Purpose: turn the fixture-only tracer into a production-wired feature that fires on real data, without changing any of the type contracts or classification logic already frozen in Plan 02.3-01.

Output: (a) `analyzeCapabilitySubtree` emits YELLOW findings for descendants whose required level is below their immediate in-scope parent; (b) `analyzeBusinessCapability` iterates every entry in `parentRequiredLevels` independently (D-02); (c) `fetchBusinessCapabilityChain`'s Cypher query populates `parentRequiredLevels` on the ROOT via an OPTIONAL MATCH + collect map-projection, so a real Neo4j query returns findings for a live parent-vs-child contradiction.
</objective>

<execution_context>
Wave 2 — depends on Plan 02.3-01 which added `parentRequiredLevels` to `BusinessCapabilityChain` and shipped `classifyCapabilityAgainstParent`. This plan REUSES both without changing their signatures.
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md
@.planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-PATTERNS.md
@.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Descendant-vs-parent hook in analyzeCapabilitySubtree + multi-parent independent iteration in analyzeBusinessCapability</name>
  <files>
    server/src/sovereignty/evaluator.ts,
    server/src/sovereignty/__tests__/fixtures.ts,
    server/src/sovereignty/__tests__/evaluator.test.ts
  </files>
  <read_first>
    - server/src/sovereignty/evaluator.ts (post-Plan-02.3-01 state — `analyzeCapabilitySubtree` recursive loop over `chain.childCapabilities`; `walkInfrastructure` per-parent-edge independence pattern lines 101-119 which is the D-02 structural analog; the ROOT-only parent-iteration hook added by Plan 02.3-01 in `analyzeBusinessCapability`)
    - server/src/sovereignty/__tests__/fixtures.ts (post-Plan-02.3-01 state — `rootParentContradictionFixture`, `requirementLevels` helper, `redChainFixture` literal shape to mirror for new descendant fixture)
    - server/src/sovereignty/__tests__/evaluator.test.ts (post-Plan-02.3-01 state — Tests A-D from Plan 02.3-01; existing subtree tests already present in this file)
    - .planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md (D-01 second half: "not just descendants-within-the-subtree checked against a parent already known during the downward walk"; D-02: "flagged if below ANY parent")
    - .planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-PATTERNS.md (Analog 2 for walkInfrastructure per-parent-edge independence; Analog 3 for the analyzeCapabilitySubtree hook point)
    - .planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-CONTEXT.md (D-03 cycle-safety: per-branch visited-set contract must remain untouched)
  </read_first>
  <behavior>
    TDD RED→GREEN. New tests to add in `evaluator.test.ts`:

    - Test G (descendant hook): a `BusinessCapabilityChain` whose `required.resilience = 'HIGH'` and whose `childCapabilities = [childChain]` where `childChain.required.resilience = 'LOW'` (child weaker than parent). `analyzeBusinessCapability(parent)` returns an analysis containing a finding with `status='YELLOW'`, `violatingElementType='businessCapability'`, `violatingElementId=childChain.rootId`, `dimension='resilience'`, `requiredLevel='HIGH'`, `actualLevel='LOW'`, `chainPath` starts with the parent's rootId and ends with the child's rootId.
    - Test H (descendant + null): same as Test G but with `childChain.required.resilience = null`. Expect zero findings for the resilience dimension (D-04 exclusion).
    - Test I (descendant + parent-null): same as Test G but with parent `required.resilience = null`. Expect zero findings for resilience (D-03 exclusion).
    - Test J (grandchild rollup): three-level chain — root → child → grandchild. Grandchild's `required.security` is below the child's `required.security`. `analyzeBusinessCapability(root)` returns a finding naming the grandchild as violatingElementId, comparing grandchild-vs-child (NOT grandchild-vs-root); `chainPath` includes root, child, grandchild in order.
    - Test K (multi-parent independence — D-02, two-parent both-stricter): root has `required.control = 'LOW'` and `parentRequiredLevels = [{ id: 'p1', required: requirementLevels({ control: 'MEDIUM' }) }, { id: 'p2', required: requirementLevels({ control: 'HIGH' }) }]`. Expect exactly TWO findings, both with dimension='control', one with `requiredLevel='MEDIUM'`/`chainPath[0]='p1'` and one with `requiredLevel='HIGH'`/`chainPath[0]='p2'`. No collapsed worst-of.
    - Test L (multi-parent independence — D-02, one-stricter-one-not): root has `required.control = 'MEDIUM'` and `parentRequiredLevels = [{ id: 'p-strict', required: requirementLevels({ control: 'HIGH' }) }, { id: 'p-loose', required: requirementLevels({ control: 'LOW' }) }]`. Expect exactly ONE finding, from `p-strict`, since child MEDIUM < parent HIGH but child MEDIUM > parent LOW.
    - Test M (cycle-safety regression): a `childCapabilities` cycle (child whose grandchild id equals the root's id) — the existing per-branch visited-set contract in `analyzeCapabilitySubtree` must continue to terminate; assert that the analysis completes without stack overflow AND still emits the descendant-vs-parent finding for the non-cyclic edge.
  </behavior>
  <action>
    Inside `analyzeCapabilitySubtree` (currently loops over `chain.childCapabilities` and re-prefixes each nested finding's chainPath): AFTER the recursive `analyzeCapabilitySubtree(child, pathVisited)` call and BEFORE (or after) collecting nested findings, add a call to `classifyCapabilityAgainstParent(child, { id: chain.rootId, required: chain.required }, [])` and push its results into `descendantFindings` (or a sibling local). Per the D-06 chainPath rule, the resulting chainPath produced by `classifyCapabilityAgainstParent` will be `[chain.rootId, child.rootId]` when called with `chainPathPrefix=[]` — that is the correct final path for the direct-parent-in-scope case at this recursion level; the outer re-prefix loop that today prepends `chain.rootId` to nested descendant findings must NOT double-prepend for these new-in-this-scope findings. Either bypass the re-prefix loop for the new findings by pushing them AFTER the loop, or use `[]` chainPathPrefix and skip the re-prefix for these. Verify the resulting chainPath in Test J for the grandchild case starts with root.rootId (not with child.rootId twice).

    In `analyzeBusinessCapability`, the Plan 02.3-01 iteration over `chain.parentRequiredLevels` already implements D-02's independent-per-parent iteration for the ROOT case (each parent entry becomes a separate `classifyCapabilityAgainstParent` call and its findings are concatenated) — verify by writing Tests K and L against the existing loop; if either fails because Plan 02.3-01 collapsed multi-parent iteration into a single call, refactor the loop to be one call per parent entry, mirroring `walkInfrastructure`'s "for (const parent of infra.parentInfrastructure) { findings.push(...walkInfrastructure(parent, ...)) }" pattern from evaluator.ts lines 116-118.

    Do NOT modify the visited-set / cycle-safety contract of `analyzeCapabilitySubtree`. Do NOT add a new recursive walk; descendant-vs-parent piggybacks on the EXISTING recursion by reading the already-in-scope `chain.required` as the parent side.

    In `fixtures.ts`: add `descendantParentContradictionFixture` (three levels for Test J: root with required.security=HIGH, child with required.security=MEDIUM, grandchild with required.security=LOW; each level has empty supportingApplications/supportingAIComponents and `parentRequiredLevels: []`); add `multiParentBothStricterFixture` for Test K; add `multiParentOnlyOneStricterFixture` for Test L; add `capabilityCycleFixture` for Test M (a childCapabilities entry that references back to root via shared rootId). Export all.

    In `evaluator.test.ts`: add Tests G–M described in `<behavior>` using the new fixtures.
  </action>
  <acceptance_criteria>
    - Source: `grep -n "classifyCapabilityAgainstParent" server/src/sovereignty/evaluator.ts` returns TWO or more call sites (one in `analyzeBusinessCapability` from Plan 02.3-01, one in `analyzeCapabilitySubtree` added here).
    - Source: `grep -n "descendantParentContradictionFixture\|multiParentBothStricterFixture\|multiParentOnlyOneStricterFixture\|capabilityCycleFixture" server/src/sovereignty/__tests__/fixtures.ts` returns four export matches.
    - Behavior (Test G): `analyzeBusinessCapability(descendantParentContradictionFixture).findings` contains at least one entry with `violatingElementId === '<child.rootId>'` and `status === 'YELLOW'` and `dimension === 'security'` and `chainPath[0] === '<root.rootId>'`.
    - Behavior (Test J): grandchild's finding names the child as the requirement source — `requiredLevel === 'MEDIUM'` (child's required) and `actualLevel === 'LOW'` (grandchild's required), NOT `requiredLevel === 'HIGH'` (root's) — proving descendant-vs-parent uses the IMMEDIATE parent already in scope, not the root.
    - Behavior (Test K, D-02): `analyzeBusinessCapability(multiParentBothStricterFixture).findings.filter(f => f.dimension === 'control').length === 2`; the two findings' `chainPath[0]` values are `'p1'` and `'p2'` respectively (order-independent).
    - Behavior (Test L, D-02): `analyzeBusinessCapability(multiParentOnlyOneStricterFixture).findings.filter(f => f.dimension === 'control').length === 1`; the single finding's `chainPath[0] === 'p-strict'`.
    - Behavior (Test M, cycle-safety): `analyzeBusinessCapability(capabilityCycleFixture)` returns without stack overflow AND emits the non-cyclic-edge finding, proving the pre-existing per-branch visited-set contract is preserved.
    - Behavior (Tests H, I — null exclusion): D-03/D-04 null-either-side rule holds for the descendant case identically to the root case.
    - Test command: `cd server && yarn jest src/sovereignty/__tests__/evaluator.test.ts` exits 0.
    - Type command: `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/evaluator.test.ts src/sovereignty/__tests__/markers.test.ts src/sovereignty/__tests__/evaluator.cycles.test.ts</automated>
  </verify>
  <done>
    `analyzeCapabilitySubtree` calls `classifyCapabilityAgainstParent` on every recursion level using the already-in-scope parent chain; multi-parent iteration in `analyzeBusinessCapability` produces independent per-parent findings (D-02); all seven new tests (G–M) pass; existing evaluator/markers/cycles tests remain green; cycle-safety contract intact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Neo4j Cypher fetch — populate root's parentRequiredLevels from the database</name>
  <files>
    server/src/sovereignty/repository.ts,
    server/src/sovereignty/__tests__/fixtures.ts
  </files>
  <read_first>
    - server/src/sovereignty/repository.ts (existing `fetchBusinessCapabilityChain` lines 262-355; the exact Cypher shape including `OPTIONAL MATCH (cap)<-[:HAS_PARENT]-(child:BusinessCapability)` for direction contrast; `toMaturityLevel` helper at line 15; `nonNullIds` helper; `NodeCache`/`inFlight` guard at lines 27-52 and 270-273; the row-decode shape at lines 302-317; the recursive childId call at lines 336-343; `loadBusinessCapabilitySupportChain` entry point at lines 367-375)
    - server/src/graphql/schema.graphql (BusinessCapability.parents at line 78: `parents: [BusinessCapability!]! @relationship(type: "HAS_PARENT", direction: OUT)` — this is the OUT direction the new Cypher pattern must use; BusinessCapability.sovereigntyReq* scalar fields — verify exact field names match those already read via `cap.sovereigntyReqStrategicAutonomy` etc. in the existing query)
    - .planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-PATTERNS.md (repository.ts section — the Cypher `collect(DISTINCT parent { .id, .sovereigntyReqStrategicAutonomy, ... }) AS parentRequiredRows` map-projection pattern; the "root-only, not recursive" scoping rule)
    - .planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md (D-01: "only the required levels are needed, not each parent's full support chain"; the-agent's-discretion note: "how the repository fetches root's own direct parent(s) required levels only without pulling their full support chains, are implementation details for planning/research to resolve" — resolved here in favor of inline scalar collect over recursive builder call)
  </read_first>
  <behavior>
    - Behavior N (root populated): calling `loadBusinessCapabilitySupportChain(session, companyIds, isAdmin, rootId)` against a Neo4j graph where `(root)-[:HAS_PARENT]->(parentA)` and `(root)-[:HAS_PARENT]->(parentB)` exist, and both parents have `sovereigntyReqSecurity='HIGH'`, returns a `BusinessCapabilityChain` with `parentRequiredLevels.length === 2`, each entry having `.id` matching parentA/parentB and `.required.security === 'HIGH'`.
    - Behavior O (no parents): a root with zero HAS_PARENT-OUT edges returns `parentRequiredLevels: []`.
    - Behavior P (all parent-required-null): a root whose parents exist but have all four `sovereigntyReq*` columns null returns `parentRequiredLevels` with the parents present, each `.required` having all four fields null (the exclusion happens at evaluator time via D-03, not by omitting the parent here).
    - Behavior Q (recursive scoping): a root with a `childCapabilities` entry — the CHILD's `parentRequiredLevels` in the returned tree is `[]` (root-only fetch, per D-01 explicit scoping). Verified by inspecting `chain.childCapabilities[0].parentRequiredLevels`.
    - Behavior R (existing Cypher unchanged): the existing `appIds`/`aiComponentIds`/`childIds` columns are still returned and decoded identically; the pre-existing test suite in `evaluator.test.ts` and `markers.test.ts` for fixtures built by hand (which do NOT go through the repository) is not affected. If integration tests exercise the repository, they continue to pass.
  </behavior>
  <action>
    Modify the single Cypher query inside `fetchBusinessCapabilityChain` (lines 275-293 today) to add an OPTIONAL MATCH clause that walks OUT-direction HAS_PARENT edges and collects each parent's four `sovereigntyReq*` scalar columns as a map. Specifically: after the existing `OPTIONAL MATCH (cap)<-[:HAS_PARENT]-(child:BusinessCapability)`, add `OPTIONAL MATCH (cap)-[:HAS_PARENT]->(parent:BusinessCapability)`. In the RETURN clause, add one additional column named `parentRequiredRows` computed as `collect(DISTINCT parent { .id, .sovereigntyReqStrategicAutonomy, .sovereigntyReqResilience, .sovereigntyReqSecurity, .sovereigntyReqControl })`. Direction MUST be OUT `(cap)-[:HAS_PARENT]->(parent)` — mirror the schema.graphql `parents` field direction, opposite of the existing `childIds` line's IN direction.

    Update the row-decode `toObject()` type annotation (lines 302-317) to include the new `parentRequiredRows` column typed as an array of `{ id: string | null; sovereigntyReqStrategicAutonomy: string | null; sovereigntyReqResilience: string | null; sovereigntyReqSecurity: string | null; sovereigntyReqControl: string | null }` entries.

    After the existing `required`/`supportingApplications`/`supportingAIComponents`/`childCapabilities` decoding blocks, build a new `parentRequiredLevels` local array by iterating `row.parentRequiredRows`: filter out entries where `id` is null; for each remaining entry, produce `{ id: parentRow.id, required: { strategicAutonomy: toMaturityLevel(parentRow.sovereigntyReqStrategicAutonomy), resilience: toMaturityLevel(parentRow.sovereigntyReqResilience), security: toMaturityLevel(parentRow.sovereigntyReqSecurity), control: toMaturityLevel(parentRow.sovereigntyReqControl) } }`. Include this array in the returned `BusinessCapabilityChain` literal (which today has `rootId`/`rootType`/`required`/`supportingApplications`/`supportingAIComponents`/`childCapabilities`) as the new `parentRequiredLevels` field.

    CRITICAL — scope the population to root-only: the recursive call at lines 336-343 (`fetchBusinessCapabilityChain(session, companyIds, isAdmin, childId, cache, inFlight)`) MUST return a `BusinessCapabilityChain` whose `parentRequiredLevels` is `[]` (not the child's own parents, which for a child inside a subtree recursion would include the current parent and cause confusing double-counting). Two acceptable implementations: (a) add a boolean parameter `isRoot` to `fetchBusinessCapabilityChain` that gates the new OPTIONAL MATCH clause and defaults to false at recursive call sites, only true from `loadBusinessCapabilitySupportChain`; OR (b) always fetch the parents but always override to `parentRequiredLevels: []` when the caller is the internal recursion path. Prefer (a) — cleaner and cheaper — but either satisfies Behavior Q. Document the chosen approach with a JSDoc line near the new parameter or the override.

    In `fixtures.ts`: no new fixture required for this task since the evaluator-side fixtures from Plan 02.3-01 and Task 1 already cover the classification behavior. Only add a fixture if a new evaluator-level test would fail without one; otherwise leave fixtures.ts untouched.

    NO changes to `types.ts` (contract already frozen in Plan 02.3-01), `evaluator.ts` (already handles a populated `parentRequiredLevels` from Plan 02.3-01), `markers.ts`, `graphql/schema.graphql` (loose String! violatingElementType already accepts 'businessCapability'), or `graphql/resolvers.ts` (spreads `...finding` directly).
  </action>
  <acceptance_criteria>
    - Source: `grep -n "HAS_PARENT" server/src/sovereignty/repository.ts` returns at least TWO matches — the pre-existing IN direction for children AND the new OUT direction for parents.
    - Source: `grep -n "parentRequiredRows\|parentRequiredLevels" server/src/sovereignty/repository.ts` returns matches on both the Cypher RETURN clause column name AND the TypeScript field being populated.
    - Source: `grep -c "sovereigntyReqStrategicAutonomy" server/src/sovereignty/repository.ts` (excluding comment lines: `grep -v '^[[:space:]]*//\|^[[:space:]]*\*' server/src/sovereignty/repository.ts | grep -c "sovereigntyReqStrategicAutonomy"`) returns at least 2 (one existing for the root's own required, one new for the parent's required).
    - Source: verify the Cypher direction is OUT for the new pattern — `grep -n "cap)-\[:HAS_PARENT\]->(parent" server/src/sovereignty/repository.ts` returns exactly one match; `grep -n "cap)<-\[:HAS_PARENT\]-(child" server/src/sovereignty/repository.ts` still returns the pre-existing single match (unchanged).
    - Behavior (via unit-testable helper OR type check if no live Neo4j is available): the returned `BusinessCapabilityChain` from the top-level `loadBusinessCapabilitySupportChain` call path has `parentRequiredLevels` populated when parents exist in the DB; the recursive path from within `fetchBusinessCapabilityChain` for a `childId` returns `parentRequiredLevels: []` regardless of whether that child has parents in the DB (Behavior Q, verified by tracing the `isRoot`-style boolean threading or the override behavior).
    - Type command: `cd server && yarn tsc --noEmit` exits 0.
    - Any existing repository/integration test suite (`cd server && yarn jest src/sovereignty/__tests__/`) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/</automated>
  </verify>
  <done>
    `fetchBusinessCapabilityChain`'s Cypher includes an OUT-direction HAS_PARENT match with a `collect(DISTINCT parent { ... })` map-projection populating the ROOT's `parentRequiredLevels`; recursive childId calls return `parentRequiredLevels: []`; `yarn tsc --noEmit` clean; full sovereignty test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GraphQL request → Neo4j Cypher | Existing — `fetchBusinessCapabilityChain` already parametrizes `$rootId`, `$companyIds`, `$isAdmin`; the new OPTIONAL MATCH clause introduces no new user-controlled string interpolation. Company-scoped filtering is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.3-04 | Information Disclosure | new OUT-direction HAS_PARENT match | medium | mitigate | New OPTIONAL MATCH does NOT re-apply the `WHERE $isAdmin OR c.id IN $companyIds` OWNED_BY check on the parent capability — parents from other companies could leak. Mitigation: add `WHERE $isAdmin OR EXISTS { (parent)-[:OWNED_BY]->(pc:Company) WHERE pc.id IN $companyIds }` (or an OPTIONAL MATCH `(parent)-[:OWNED_BY]->(parentCompany:Company)` with a WHERE guard on parentCompany.id) to the parent match so parents outside the caller's company scope are dropped from `parentRequiredRows` before the map-projection collects them. |
| T-02.3-05 | Tampering | Cypher direction confusion | low | mitigate | Wrong direction on the arrow silently swaps parent/child semantics and would produce misleading findings. Mitigation: the acceptance_criteria grep checks explicitly verify BOTH the new OUT-direction and the preserved IN-direction. |
| T-02.3-06 | Denial of Service | recursive fetch cost | low | accept | `parentRequiredRows` is a single scalar collect on a single OPTIONAL MATCH — no extra Neo4j round-trip. Cost is O(direct parents), bounded by data model. |

No new npm/pip installs, no new dependencies — package legitimacy audit not applicable.
</threat_model>

<verification>
- `cd server && yarn tsc --noEmit` clean
- `cd server && yarn jest src/sovereignty/__tests__/` full sovereignty test suite passes
- Manual reading confirms cross-tenant parent leakage mitigation (T-02.3-04) is present in the new OPTIONAL MATCH
- Manual reading confirms recursive `childId` fetches return `parentRequiredLevels: []` (D-01 scoping)
- Manual reading confirms the pre-existing `(cap)<-[:HAS_PARENT]-(child)` IN-direction line is untouched
</verification>

<success_criteria>
- Live Neo4j sovereigntyAnalysis GraphQL queries for a BusinessCapability whose real database has a parent with stricter required levels produce a YELLOW `SovereigntyFinding` with `violatingElementType: 'businessCapability'`.
- Descendant-vs-parent findings surface for every capability inside a subtree without a new Neo4j fetch (uses the already-in-scope `chain.required`).
- Multi-parent independent-edge iteration (D-02) — verified by Tests K and L — behaves identically to `walkInfrastructure`'s multi-parent contract.
- Cycle-safety contract untouched — Test M passes.
- Tenant isolation preserved on the new parent match (T-02.3-04 mitigated).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02.3-02 slice)

- **Cypher column:** `parentRequiredRows` — new RETURN column on `fetchBusinessCapabilityChain`'s single query, produced by `collect(DISTINCT parent { .id, .sovereigntyReqStrategicAutonomy, .sovereigntyReqResilience, .sovereigntyReqSecurity, .sovereigntyReqControl })`.
- **New function parameter (chosen implementation A):** `isRoot: boolean` on `fetchBusinessCapabilityChain`, defaulting to false at recursive call sites, only true from `loadBusinessCapabilitySupportChain` — gates the new OPTIONAL MATCH and enforces D-01's root-only scoping.
- **New evaluator call site:** `classifyCapabilityAgainstParent` invoked from inside `analyzeCapabilitySubtree`'s per-child recursion loop (in addition to Plan 02.3-01's invocation from `analyzeBusinessCapability`).
- **New fixture constants:** `descendantParentContradictionFixture`, `multiParentBothStricterFixture`, `multiParentOnlyOneStricterFixture`, `capabilityCycleFixture` (`server/src/sovereignty/__tests__/fixtures.ts`).
- **New test names:** Tests G–M in `evaluator.test.ts` covering the descendant recursion, multi-parent independence (D-02), null exclusion, and cycle-safety cases.
</artifacts_produced>

<output>
Create `.planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-02-SUMMARY.md` when done.
</output>
