# Phase 02.3: Business capability requirement-chain consistency - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-03
**Phase:** 02.3-business-capability-requirement-chain-consistency-compare-pa
**Areas discussed:** Comparison scope, Multi-parent handling, Null-required semantics, Self-status & marker UI

---

## Comparison Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, always check the root too | New upward fetch of the root's direct parent(s) required levels; a capability's own contradiction-with-parent is visible no matter which capability you open. | ✓ |
| No, only descendants within subtree | Reuses parent required levels already known during the downward walk; the analysis root is never checked against anything above it. | |

**User's choice:** Yes, always check the root too.
**Notes:** User asked for clarification first ("Do we need the 'check root too'?"). Agent explained the concrete case: opening `Corporate Governance` directly (as root) would never see its contradiction with `Strategy` above it unless the root is also checked upward. User confirmed wanting this visible regardless of which capability is opened.

---

## Multi-Parent Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Flag if below ANY parent | Conservative: a contradiction with even one parent is worth flagging. | ✓ |
| Flag if below strictest (max) parent | Mathematically equivalent to "below ANY parent". | |
| Flag only if below ALL parents | Misses cases where child satisfies the toughest parent but violates a looser one. | |

**User's choice:** Flag if below ANY parent.
**Notes:** No further clarification needed.

---

## Null-Required Semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Parent null = skip dimension | Mirrors `classifyNode`'s existing rule — check only fires when the FROM side is explicitly set. | ✓ (parent-null case) |

**User's choice:** Parent null = skip dimension (uncontested, single reasonable option presented).

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, flag as YELLOW | Treats child-null as weakest possible level, mirroring the parent-null case. | |
| No, exclude (child null = no check) | Treats missing-requirement as fundamentally different from "explicitly weaker". | ✓ |

**User's choice:** No, exclude (child null = no check) — diverged from the agent's recommendation ("Yes, flag as YELLOW").
**Notes:** Final rule: comparison only applies when BOTH parent and child have an explicit non-null requirement for that dimension.

---

## Self-Status & Marker UI

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, colored YELLOW fill | Breaks the current UI-SPEC rule that a BusinessCapability fill is always neutral grey; matches phase title's "as a self-status finding" and D-05's carve-out. | ✓ |
| No, fill stays grey; only downstream/list | Keeps fill invariant fully intact; contradiction only surfaces via downstreamStatus and findings list. | |

**User's choice:** Yes, colored YELLOW fill.

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse Finding shape as-is | requiredLevel = parent's required level, actualLevel = child's own required level (repurposed field); violatingElementType extended with 'businessCapability'. | ✓ |
| New distinct finding shape/label | Adds a distinct field/flag or separate finding type for different UI wording. | |

**User's choice:** Reuse Finding shape as-is.

---

## the agent's Discretion

- Exact GraphQL schema/enum wiring for the new `'businessCapability'` violatingElementType value, and how the repository fetches "root's own direct parent(s) required levels only" without pulling their full support chains.
- UI wording for this finding in the existing findings list.

## Deferred Ideas

None — discussion stayed within phase scope.
