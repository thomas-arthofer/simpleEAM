---
phase: 02-canonical-sovereignty-evaluation-ux-diagnostics
plan: 02
type: execute
wave: 2
depends_on: ["02-canonical-sovereignty-evaluation-ux-diagnostics-01"]
files_modified:
  - server/src/sovereignty/markers.ts
  - server/src/sovereignty/graphql/resolvers.ts
  - server/src/graphql/schema.graphql
  - server/src/sovereignty/validation.ts
  - server/src/sovereignty/__tests__/markers.test.ts
  - server/src/sovereignty/__tests__/evaluator.parity.test.ts
autonomous: true
requirements: [SOV-05, SUX-03, SUX-04]

must_haves:
  truths:
    - "For any Application/AIComponent/Infrastructure/BusinessCapability/DataObject node id, sovereigntyMarkers returns both a selfStatus (this element's own achieved-vs-required comparison) and a downstreamStatus (worst status found anywhere below it in the chain) — this is the D-05 fill/ring distinction (SUX-04)."
    - "A BusinessCapability/DataObject node's selfStatus is always GREY (it owns no achieved rating of its own) while its downstreamStatus reflects the worst finding in its supported chain."
    - "The sovereigntyMarkers batch query rejects a request for more nodes than a fixed cap (per Pitfall: unbounded batch = DoS) with a validation error, not a silent partial result."
    - "Invoking the sovereignty evaluator directly (module call) and invoking it through the GraphQL resolver layer for the same fixture id set produce byte-identical Finding/marker output (SOV-05 parity)."
  artifacts:
    - server/src/sovereignty/markers.ts
    - server/src/sovereignty/validation.ts
    - server/src/sovereignty/__tests__/markers.test.ts
    - server/src/sovereignty/__tests__/evaluator.parity.test.ts
  key_links:
    - "markers.ts calls the same analyzeBusinessCapability/analyzeDataObject functions from 02-01's evaluator.ts — no separate marker-only classification logic exists (RESEARCH.md Anti-Pattern 3: one classifier, reused everywhere)."
    - "sovereigntyMarkers's resolver loads its chain via 02-01 Task 2's loadFullSupportChain — the identical loader Query.sovereigntyAnalysis uses — so markers and the detail analysis always see the same chain data, never a second ad hoc query."
    - "sovereigntyMarkers resolver validates its input nodes array with zod (validation.ts) before any Cypher runs, matching the project's existing zod convention in server/src/analytics/routes.ts."
---

<objective>
Expose the D-05 two-marker (self/downstream) projection the diagram layer (plan 02-05) and any future consumer need, as a batched, company-scoped, input-validated `sovereigntyMarkers` GraphQL query built directly on top of 02-01's evaluator — and prove that the resolver path and the direct module-call path agree (closing SOV-05).

Purpose: give the frontend one batch call that returns a fill/ring status pair per element id, instead of re-deriving self/downstream status ad hoc per consumer.
Output: `server/src/sovereignty/markers.ts`, `server/src/sovereignty/validation.ts` (zod schemas), an extended `sovereigntyMarkers` query on `schema.graphql`, and parity + marker Jest suites.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-01-PLAN.md
@.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-CONTEXT.md
@.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-RESEARCH.md
@server/src/sovereignty/evaluator.ts
@server/src/sovereignty/graphql/resolvers.ts
@server/src/analytics/routes.ts
@eam-konzept.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Marker projection module and batch GraphQL query</name>
  <files>server/src/sovereignty/markers.ts, server/src/sovereignty/graphql/resolvers.ts, server/src/graphql/schema.graphql, server/src/sovereignty/__tests__/markers.test.ts</files>
  <read_first>server/src/sovereignty/evaluator.ts (analyzeBusinessCapability/analyzeDataObject/classifyNode from 02-01), server/src/sovereignty/repository.ts (loadFullSupportChain(session, companyIds, isAdmin, rootType, rootId) from 02-01 Task 2 — the only chain loader; do not write a second one), eam-konzept.md §3 (Kern/Ring marker meanings)</read_first>
  <behavior>
    - `projectMarkers(analysis: SovereigntyAnalysis)` returns, for the root itself, `{ id: analysis.rootId, selfStatus: 'GREY', downstreamStatus: analysis.downstreamStatus }` — a BusinessCapability/DataObject root always has `selfStatus: 'GREY'` (D-05: "a BusinessCapability can never get a fill marker").
    - `projectMarkers` returns, for every non-root element that appears anywhere in `analysis.findings[].chainPath`, its own `selfStatus` (RED/YELLOW/GREY/GREEN based on findings whose `violatingElementId` equals that element) and `downstreamStatus` (worst status among findings whose `chainPath` contains that element id at any position at-or-below it).
    - An element that appears in zero findings gets `{ selfStatus: 'GREEN', downstreamStatus: 'GREEN' }` — GREEN is never omitted from the marker map (UI-SPEC: markers must never be suppressed for the compliant case).
  </behavior>
  <action>
Create `server/src/sovereignty/markers.ts` exporting `projectMarkers(analysis: SovereigntyAnalysis): Map&lt;string, { selfStatus: SovereigntyStatus; downstreamStatus: SovereigntyStatus }&gt;` implementing the `<behavior>` rules above by folding over `analysis.findings`, tracking per-element worst-status using the ordering GREEN &lt; GREY &lt; YELLOW &lt; RED (RED always wins, matching eam-konzept.md's "ein Element mit auch nur einem roten Befund gilt als rot" rule) so `downstreamStatus` for any ancestor equals the worst status of any descendant finding. Do not introduce any new comparison/classification logic here — `projectMarkers` only aggregates `Finding[]` already produced by 02-01's `evaluator.ts`.

Add to `server/src/graphql/schema.graphql` (additive): `type SovereigntyMarker { nodeId: ID! selfStatus: SovereigntyStatus! downstreamStatus: SovereigntyStatus! }`, `input SovereigntyMarkerNodeInput { id: ID! type: String! }`, `extend type Query { sovereigntyMarkers(companyId: ID!, rootType: String!, rootId: ID!, nodes: [SovereigntyMarkerNodeInput!]!): [SovereigntyMarker!]! }` — `rootType`/`rootId` identify which `sovereigntyAnalysis` to run (a diagram typically shows descendants of one BusinessCapability or DataObject), `nodes` is the list of on-diagram element ids the client wants markers for.

Extend `server/src/sovereignty/graphql/resolvers.ts` with `Query.sovereigntyMarkers`: reuse the same JWT company-scoping check from Task 1 of 02-01 (reject before querying unless `args.companyId` is in the caller's `company_ids` or the caller has the `admin` role), open a session and call `loadFullSupportChain(session, companyIds, isAdmin, args.rootType, args.rootId)` — the same repository function 02-01 Task 2's `Query.sovereigntyAnalysis` calls, never a separate ad hoc loader — then call `analyzeBusinessCapability(chain)` when `args.rootType === 'businessCapability'` or `analyzeDataObject(chain)` when `args.rootType === 'dataObject'` (mirroring the exact dispatch now in `Query.sovereigntyAnalysis`), call `projectMarkers`, then filter the resulting map down to only the ids present in `args.nodes`, returning `GREEN`/`GREEN` for any requested id that has no marker entry (never omit a requested node from the response).

Create `server/src/sovereignty/__tests__/markers.test.ts` covering all three `<behavior>` cases against 02-01's fixtures, including one explicit "capability ring only" case (a BusinessCapability root has `selfStatus: 'GREY'` even when its `downstreamStatus` is `'RED'`), matching the exact test name used in `02-VALIDATION.md`'s Per-Task Verification Map.
  </action>
  <verify>
    <automated>cd server && yarn jest --testPathPattern=sovereignty/__tests__/markers</automated>
  </verify>
  <done>`markers.test.ts` passes including the capability-ring-only case; `sovereigntyMarkers` is added to `schema.graphql` and wired to a resolver that reuses 02-01's JWT scoping check.</done>
</task>

<task type="auto">
  <name>Task 2: Input validation hardening and resolver/module parity proof</name>
  <files>server/src/sovereignty/validation.ts, server/src/sovereignty/graphql/resolvers.ts, server/src/sovereignty/__tests__/evaluator.parity.test.ts</files>
  <read_first>server/src/analytics/routes.ts lines 1-20 (project's zod usage convention: z.enum, .nullable().optional().transform), 02-RESEARCH.md Security Domain (Cypher injection / unbounded batch threats), 02-RESEARCH.md Pitfall entries on unbounded batch queries</read_first>
  <action>
Create `server/src/sovereignty/validation.ts` using `zod` (already installed, per the `import { z } from 'zod'` convention in `server/src/analytics/routes.ts`): `rootTypeSchema = z.enum(['businessCapability', 'dataObject'])`; `sovereigntyAnalysisArgsSchema` validating `companyId`/`rootId` as non-empty `z.string()` and `rootType` via `rootTypeSchema`; `sovereigntyMarkerNodesSchema = z.array(z.object({ id: z.string().min(1), type: z.string().min(1) })).max(500)` — the 500-node cap directly closes the "unbounded batch marker query = DoS/Information Disclosure" threat named in 02-RESEARCH.md's Security Domain table; parsing throws a `ZodError` (surfaced as a GraphQL error) rather than silently truncating the array.

Modify `server/src/sovereignty/graphql/resolvers.ts`: call `sovereigntyAnalysisArgsSchema.parse(args)` at the top of `Query.sovereigntyAnalysis` and `sovereigntyMarkerNodesSchema.parse(args.nodes)` plus `rootTypeSchema.parse(args.rootType)` at the top of `Query.sovereigntyMarkers`, before the JWT check and before any Cypher runs — reject malformed `rootType`/oversized `nodes` input at the boundary (OWASP ASVS V5 input validation), never pass unvalidated strings into `repository.ts`'s Cypher parameters.

Create `server/src/sovereignty/__tests__/evaluator.parity.test.ts`: build a small in-memory test harness that (a) calls `analyzeBusinessCapability` directly on a fixture chain from `evaluator.test.ts`'s fixtures, and (b) calls `sovereigntyResolvers.Query.sovereigntyAnalysis` with a mocked `context.token` (a hand-built base64url JWT payload granting the fixture's company id) and a stubbed Neo4j session returning the same fixture rows, then asserts the two `Finding[]` outputs are deep-equal — this is the SOV-05 "one canonical module, reachable identically from every call site" proof for the GraphQL path (the Temporal call-site proof is added in plan 02-03).
  </action>
  <verify>
    <automated>cd server && yarn jest --testPathPattern=sovereignty/__tests__/evaluator.parity</automated>
  </verify>
  <done>Oversized `nodes` arrays and malformed `rootType` values are rejected by zod before any Cypher executes; `evaluator.parity.test.ts` passes, proving the resolver path and the direct evaluator call produce identical findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `sovereigntyMarkers` batch resolver | `nodes` array size and shape is fully client-controlled; an unbounded array could force the server to run one Cypher lookup per requested id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Denial of Service / Information Disclosure | `server/src/sovereignty/graphql/resolvers.ts::sovereigntyMarkers` | high | mitigate | `validation.ts`'s `sovereigntyMarkerNodesSchema` caps the batch at 500 nodes and rejects (not truncates) oversized requests before any Cypher runs |
| T-02-05 | Tampering | `server/src/sovereignty/graphql/resolvers.ts::sovereigntyAnalysis`/`sovereigntyMarkers` args | medium | mitigate | `rootType`/`rootId`/`companyId` are zod-validated at the resolver boundary before being passed as Cypher parameters, closing the Cypher-injection pitfall named in 02-RESEARCH.md |

</threat_model>

<verification>
`cd server && yarn jest --testPathPattern=sovereignty` is green including the new markers and parity suites; oversized `sovereigntyMarkers` requests are rejected with a validation error.
</verification>

<success_criteria>
- `sovereigntyMarkers` returns a self/downstream status pair for every requested node id, with BusinessCapability/DataObject roots always GREY-self.
- The batch input is capped and validated before any database access.
- Resolver-path and module-path evaluator calls are proven identical for the same input (SOV-05).
</success_criteria>

<output>
Create `.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-02-SUMMARY.md` when done
</output>
