---
phase: 04
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - server/src/sovereignty/types.ts
  - server/src/sovereignty/evaluator.ts
  - server/src/sovereignty/repository.ts
  - server/src/sovereignty/markers.ts
  - server/src/sovereignty/companyRollup.ts
  - server/src/sovereignty/__tests__/fixtures.ts
  - server/src/sovereignty/__tests__/evaluator.test.ts
  - server/src/sovereignty/__tests__/markers.test.ts
  - server/src/sovereignty/__tests__/companyRollup.test.ts
autonomous: true
requirements:
  - D-04
  - D-05

must_haves:
  truths:
    - "A BusinessProcess whose own required level for a dimension is weaker than any of its `parentProcess`'s required levels for the same dimension produces a Finding with status='YELLOW', violatingElementType='businessProcess', violatingElementId=<process id>, chainPath=[parentId, processId] — mirrors 02.3/03's BusinessCapability treatment exactly (D-04)."
    - "That same BusinessProcess's `analyzeBusinessProcess(...).selfStatus` (and `projectMarkers(...)`'s `selfStatus` for the same id) is three-valued: YELLOW on contradiction, GREEN when at least one dimension was genuinely compared (both sides non-null) against a parentProcess and found consistent, GREY only when nothing is comparable (no parentProcess, or every dimension excluded)."
    - "Multi-parent `parentProcess` edges are evaluated independently (D-02 precedent) — a BusinessProcess below only ONE of two parents' required levels gets exactly one finding naming that parent, not a collapsed worst-of finding."
    - "Null-either-side (parent or child required level) excludes the dimension entirely — no finding, not even GREY."
    - "CRITICAL: a BusinessProcess's genuine YELLOW parent-contradiction finding is NOT silently swallowed to GREEN/GREY by `markers.ts`'s `selfViolatingIds` precompute, which today hardcodes `violatingElementType === 'businessCapability'` — this filter is generalized so it also recognizes `'businessProcess'` self-violations."
    - "No BusinessCapability↔BusinessProcess wiring is introduced anywhere — `analyzeCapabilitySubtree`/`collectOwnFindings`/`BusinessCapabilityChain` are untouched by this plan (D-03)."
  artifacts:
    - server/src/sovereignty/types.ts (ViolatingElementType += 'businessProcess'; BusinessProcessChain gains parentRequiredLevels field)
    - server/src/sovereignty/evaluator.ts (classifyCapabilityAgainstParent gains a violatingElementType param, default 'businessCapability'; analyzeBusinessProcess composes analyzeSupportChain + per-parentProcess classification into a three-valued selfStatus)
    - server/src/sovereignty/repository.ts (loadBusinessProcessSupportChain extended with a root-only HAS_PARENT_PROCESS-OUT OPTIONAL MATCH populating parentRequiredLevels, company-scoped identically to fetchBusinessCapabilityChain's parent clause)
    - server/src/sovereignty/markers.ts (selfViolatingIds filter generalized from a hardcoded 'businessCapability' string check to capabilityIds-membership — the critical bug fix)
    - server/src/sovereignty/companyRollup.ts (loadOwnedIds/analyzeCompanyRollup extended to include BusinessProcess ids, agent's-discretion inclusion per 04-RESEARCH.md Open Question 1, for SOV-05 cross-surface consistency)
    - server/src/sovereignty/__tests__/fixtures.ts (businessProcessParentContradictionFixture, businessProcessParentNoContradictionFixture, businessProcessParentAllExcludedFixture, businessProcessMultiParentFixture)
    - server/src/sovereignty/__tests__/evaluator.test.ts (parent-consistency + comparedCapabilityIds assertions for analyzeBusinessProcess)
    - server/src/sovereignty/__tests__/markers.test.ts (BusinessProcess GREEN/YELLOW/GREY projectMarkers assertions; the explicit selfViolatingIds regression test)
  key_links:
    - "evaluator.ts's classifyCapabilityAgainstParent is the ONE classifier both BusinessCapability and BusinessProcess parent-consistency checks call — generalizing its violatingElementType parameter (default-preserving both existing call sites) is strictly preferred over writing a parallel classifyProcessAgainstParent (04-RESEARCH.md Pattern 2, Anti-Pattern list)."
    - "BusinessProcess's parentProcess check is ROOT-ONLY (mirrors 02.3 D-01's root-half only) — there is no recursive childProcesses achieved-chain subtree to piggyback a descendant-vs-parent check onto, unlike BusinessCapability's childCapabilities recursion (04-RESEARCH.md Pitfall 1, confirmed against 04-DISCUSSION-LOG.md: no multi-hop ancestor walk was ever discussed, only the direct-parent case)."
    - "markers.ts's capabilityIds set (from analysis.capabilityIds, already populated as [chain.rootId] for a BusinessProcess analysis via analyzeSupportChain, Pattern 3) is what the selfViolatingIds fix keys off — no new isBusinessProcess branch, no field rename, this is the single one-line fix the whole plan hinges on."
---

<objective>
Extend `analyzeBusinessProcess` (landed achieved-chain-only in Plan 04-01) with the full 02.3/03 parent-vs-own-required-level tree-consistency check against `parentProcess` (D-04): three-valued GREEN/YELLOW/GREY `selfStatus`, null-exclusion, independent multi-parent evaluation — by generalizing the existing `classifyCapabilityAgainstParent` rather than duplicating it. Then apply the one critical fix this generalization exposes: `markers.ts`'s `selfViolatingIds` precompute today hardcodes `violatingElementType === 'businessCapability'`, which would otherwise silently swallow every BusinessProcess YELLOW self-violation into GREEN or GREY. Finally, extend `companyRollup.ts` to include BusinessProcess ids alongside BusinessCapability/DataObject, for SOV-05 cross-surface consistency (agent's discretion, per 04-RESEARCH.md's explicit Open Question and recommendation).

Purpose: 04-RESEARCH.md confirms BusinessProcess's parent-consistency half is structurally SIMPLER than BusinessCapability's — a root-only upward fetch (like 02.3 D-01's first half), with no recursive `childProcesses` walk needed, since BusinessProcess has no achieved-chain subtree to piggyback the descendant check onto (D-02 established it flat in Plan 04-01). The one real risk is not a missing feature but a silent regression: the hardcoded `'businessCapability'` string in `markers.ts` must be generalized or this entire plan's YELLOW guarantee is unreachable in practice.

Output: `analyzeBusinessProcess` produces YELLOW/GREEN/GREY selfStatus consistent with its `parentProcess` required levels; `projectMarkers()` correctly surfaces BusinessProcess self-violations (the critical fix, with an explicit regression test); `companyRollup.ts` includes BusinessProcess in the company-wide score.
</objective>

<execution_context>
Wave 2 — depends on Plan 04-01, which added `SovereigntyRootType`/`BusinessProcessChain` (achieved-chain shape) and wired `loadBusinessProcessSupportChain`/the GraphQL resolver dispatch. This plan extends both without changing their already-landed shape or signatures.
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md
@.planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md
@.planning/phases/03-business-capability-self-status-green-when-consistent-with-p/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize classifyCapabilityAgainstParent and wire BusinessProcess parent-consistency (three-valued selfStatus)</name>
  <files>
    server/src/sovereignty/types.ts,
    server/src/sovereignty/evaluator.ts,
    server/src/sovereignty/repository.ts,
    server/src/sovereignty/__tests__/fixtures.ts,
    server/src/sovereignty/__tests__/evaluator.test.ts
  </files>
  <read_first>
    - server/src/sovereignty/evaluator.ts (current `classifyCapabilityAgainstParent` signature — `(chain: BusinessCapabilityChain, parentEntry, chainPathPrefix)`, hardcodes `violatingElementType: 'businessCapability'` in its pushed `Finding`; the two existing call sites — `analyzeCapabilitySubtree`'s descendant loop and `analyzeBusinessCapability`'s root parent loop — both must keep working unchanged after the signature grows a parameter; `analyzeBusinessCapability`'s full three-valued `selfStatus`/`comparedCapabilityIds` composition — the exact pattern `analyzeBusinessProcess` now mirrors, minus the `analyzeCapabilitySubtree`/nested-descendant half, which BusinessProcess does not have)
    - server/src/sovereignty/types.ts (current `ViolatingElementType` union — 4 members ending in `'businessCapability'`; current `BusinessProcessChain` interface from Plan 04-01 — achieved-chain-only, no `parentRequiredLevels` yet; `BusinessCapabilityChain.parentRequiredLevels`'s exact field type — the shape `BusinessProcessChain.parentRequiredLevels` mirrors verbatim)
    - server/src/sovereignty/repository.ts (`fetchBusinessCapabilityChain`'s `isRoot`-gated `parentMatchClause`/`parentReturnClause` pattern, and its T-02.3-04 company-scoped `WHERE parent IS NULL OR $isAdmin OR parentCompany.id IN $companyIds` guard on the parent match — `loadBusinessProcessSupportChain` from Plan 04-01, which is NOT recursive, needs the equivalent OPTIONAL MATCH unconditionally since every call is already root-only, no `isRoot` parameter needed)
    - server/src/graphql/schema.graphql (`BusinessProcess.parentProcess: [BusinessProcess!]! @relationship(type: "HAS_PARENT_PROCESS", direction: OUT)` — the exact relationship/direction the new Cypher OPTIONAL MATCH must use)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md (§ Architecture Patterns Pattern 2 — the generalized `classifyCapabilityAgainstParent` signature and the `analyzeBusinessProcess` composition; § Pitfall 1 — why no recursive childProcesses walk or visited-set is needed; § Pitfall 4 — Cartesian-product risk in the new Cypher, mitigated via `collect(DISTINCT ...)`)
    - .planning/phases/02.3-business-capability-requirement-chain-consistency-compare-pa/02.3-CONTEXT.md (D-01, D-02, D-03, D-04, D-06 verbatim — the contract BusinessProcess's check mirrors)
    - .planning/phases/03-business-capability-self-status-green-when-consistent-with-p/03-CONTEXT.md (D-01, D-02 — the three-valued GREEN/YELLOW/GREY `hasRealComparison`/`comparedCapabilityIds` mechanism, already generalized on `SovereigntyAnalysis`/`analyzeSupportChain` — no type change needed there, only the composition in the new `analyzeBusinessProcess`)
  </read_first>
  <behavior>
    TDD RED→GREEN. Tests to add in `evaluator.test.ts` (extending the `describe('analyzeBusinessProcess', ...)` block from Plan 04-01):

    - Test 6 (YELLOW contradiction): a `BusinessProcessChain` whose own `required.security = 'MEDIUM'` and whose `parentRequiredLevels = [{ id: 'proc-parent-strict', required: requirementLevels({ security: 'HIGH' }) }]` produces a `Finding` with `status='YELLOW'`, `violatingElementType='businessProcess'`, `violatingElementId=<process id>`, `dimension='security'`, `requiredLevel='HIGH'`, `actualLevel='MEDIUM'`, `chainPath=['proc-parent-strict', <process id>]`; `analyzeBusinessProcess(chain).selfStatus === 'YELLOW'`.
    - Test 7 (GREEN — compared and consistent): same shape but parent's `security = 'LOW'` (child stricter, or equal) — zero findings; `analyzeBusinessProcess(chain).selfStatus === 'GREEN'`; `comparedCapabilityIds` contains the process id.
    - Test 8 (GREY — no parent at all): `parentRequiredLevels: []` — `selfStatus === 'GREY'`; `comparedCapabilityIds` does NOT contain the process id (mirrors Plan 04-01's Test 4, now explicit about the "no parent" bucket).
    - Test 9 (GREY — parent present, every dimension excluded): one `parentRequiredLevels` entry whose `required` is entirely `null` — `selfStatus === 'GREY'`, distinct from the "no parent" case but same outcome (03-CONTEXT.md D-01's "genuinely nothing compared" bucket).
    - Test 10 (D-02 multi-parent independence): two `parentRequiredLevels` entries, one stricter than the child's own required level, one not — exactly ONE finding, naming only the stricter parent.
    - Test 11 (null-either-side exclusion): parent's or child's required level is `null` for a dimension — zero findings for that dimension, not even GREY.
    - Existing Plan 04-01 tests (Tests 1-5, always-GREY-self on the achieved-chain-only fixtures which all have `parentRequiredLevels: []`) MUST still pass unmodified.

  </behavior>
  <action>
    In `server/src/sovereignty/types.ts`: extend `ViolatingElementType` to include `'businessProcess'` (5th member, alongside the existing `'businessCapability'`). Add `parentRequiredLevels` to `BusinessProcessChain` (landed achieved-chain-only in Plan 04-01), mirroring `BusinessCapabilityChain.parentRequiredLevels`'s exact type verbatim: `readonly { readonly id: string; readonly required: RequirementLevels }[]`. Add a JSDoc line noting this is root-only (every BusinessProcess is independently analyzed as its own root — there is no nested achieved-chain subtree to distinguish "root" from "descendant" for, unlike BusinessCapability).

    In `server/src/sovereignty/evaluator.ts`: change `classifyCapabilityAgainstParent`'s signature to accept a fourth parameter `violatingElementType: ViolatingElementType = 'businessCapability'` (default preserves both existing call sites — `analyzeCapabilitySubtree`'s descendant loop, `analyzeBusinessCapability`'s root parent loop — unchanged). Inside the function, replace the hardcoded `violatingElementType: 'businessCapability'` in the pushed `Finding` with the new parameter. Update the function's own signature to also loosen its first parameter from `chain: BusinessCapabilityChain` to the minimal structural shape `{ readonly rootId: string; readonly required: RequirementLevels }` it actually uses (both `BusinessCapabilityChain` and `BusinessProcessChain` satisfy this shape structurally — no explicit cast needed at either call site). Update the function's doc comment to describe the new parameter and its two call sites (BusinessCapability, BusinessProcess).

    Add a new exported `analyzeBusinessProcess` (replacing Plan 04-01's one-line delegate) that composes `analyzeSupportChain` with the parent-consistency check, mirroring `analyzeBusinessCapability`'s root-parent composition but WITHOUT the `analyzeCapabilitySubtree` nested-descendant half (BusinessProcess has no `childProcesses` achieved-chain subtree, D-02):

    ```
    export function analyzeBusinessProcess(chain: BusinessProcessChain): SovereigntyAnalysis {
      const base = analyzeSupportChain(chain, 'businessProcess')

      const parentResults = chain.parentRequiredLevels.map(parentEntry =>
        classifyCapabilityAgainstParent(chain, parentEntry, [], 'businessProcess')
      )
      const parentContradictionFindings = parentResults.flatMap(r => r.findings)
      const rootHasRealComparison = parentResults.some(r => r.hasRealComparison)
      const combinedFindings = [...base.findings, ...parentContradictionFindings]
      const comparedCapabilityIds = rootHasRealComparison ? [chain.rootId] : []

      const selfStatus: SovereigntyStatus =
        parentContradictionFindings.length > 0 ? 'YELLOW' : rootHasRealComparison ? 'GREEN' : 'GREY'

      return {
        ...base,
        findings: combinedFindings,
        selfStatus,
        downstreamStatus: aggregateDownstreamStatus(combinedFindings),
        comparedCapabilityIds,
      }
    }
    ```

    Do NOT add any recursive `childProcesses` walk, visited-set, or new leaf traversal — every BusinessProcess is analyzed as its own independent root (mirrors DataObject's flat shape), so the "child compared against its immediate parent" case is automatically covered whenever that child is itself queried as a root (04-RESEARCH.md Pitfall 1). Do NOT touch `analyzeCapabilitySubtree`, `collectOwnFindings`, or `BusinessCapabilityChain` (D-03).

    In `server/src/sovereignty/repository.ts`: extend `loadBusinessProcessSupportChain` (from Plan 04-01) with a new `OPTIONAL MATCH (proc)-[:HAS_PARENT_PROCESS]->(parent:BusinessProcess)-[:OWNED_BY]->(parentCompany:Company) WHERE parent IS NULL OR $isAdmin OR parentCompany.id IN $companyIds` clause (mirrors `fetchBusinessCapabilityChain`'s T-02.3-04 company-scoped parent guard exactly — no `isRoot` parameter needed since this function has no recursion, every call is already root-only). Add `collect(DISTINCT parent { .id, .sovereigntyReqStrategicAutonomy, .sovereigntyReqResilience, .sovereigntyReqSecurity, .sovereigntyReqControl }) AS parentRequiredRows` to the RETURN clause. Decode `parentRequiredRows` into `parentRequiredLevels` exactly as `fetchBusinessCapabilityChain` does (filter out entries with a null `id`, map each via `toMaturityLevel`). Include `parentRequiredLevels` in the returned `BusinessProcessChain` literal.

    In `server/src/sovereignty/__tests__/fixtures.ts`: add `businessProcessParentContradictionFixture`, `businessProcessParentNoContradictionFixture`, `businessProcessParentAllExcludedFixture`, `businessProcessMultiParentFixture`, following the exact literal shape of the equivalent BusinessCapability fixtures (`rootParentContradictionFixture`, `rootParentNoContradictionFixture`, `rootParentAllExcludedFixture` — adapt `multiParentOnlyOneStricterFixture`'s shape for the fourth), but `rootType: 'businessProcess'`, `supportingApplications: []`, `supportingAIComponents: []` (no `childCapabilities` field — `BusinessProcessChain` has none). Export all four.

    In `server/src/sovereignty/__tests__/evaluator.test.ts`: add Tests 6-11 described in `<behavior>`, importing the new fixtures.
  </action>
  <reversibility rating="reversible">Additive-only: ViolatingElementType gains a member, classifyCapabilityAgainstParent gains a defaulted parameter (both existing call sites unaffected), BusinessProcessChain gains a field. No existing BusinessCapability/DataObject behavior changes — a missed generalization step fails the TypeScript build immediately (a call site not passing the new param still compiles via the default, so this is purely additive, not a breaking refactor).</reversibility>
  <acceptance_criteria>
    - Source: `grep -n "'businessProcess'" server/src/sovereignty/types.ts` shows matches on `ViolatingElementType`, `SovereigntyRootType`, and `BusinessProcessChain.rootType`.
    - Source: `grep -n "violatingElementType" server/src/sovereignty/evaluator.ts | grep -v "^.*://"` shows `classifyCapabilityAgainstParent`'s signature includes a `violatingElementType` parameter with a default value.
    - Source: `grep -n "classifyCapabilityAgainstParent(chain, parentEntry, \[\], 'businessProcess')" server/src/sovereignty/evaluator.ts` returns a match inside `analyzeBusinessProcess`.
    - Source: `grep -n "HAS_PARENT_PROCESS" server/src/sovereignty/repository.ts` returns a match.
    - Source: `grep -n "parentRequiredRows\|parentRequiredLevels" server/src/sovereignty/repository.ts | grep -c "businessProcess\|proc\."` returns a nonzero count confirming the new clause is inside `loadBusinessProcessSupportChain`, not accidentally duplicated into the BusinessCapability fetch.
    - Behavior (Test 6): `analyzeBusinessProcess(businessProcessParentContradictionFixture).selfStatus === 'YELLOW'`; the finding's `violatingElementType === 'businessProcess'`.
    - Behavior (Test 7): `analyzeBusinessProcess(businessProcessParentNoContradictionFixture).selfStatus === 'GREEN'`.
    - Behavior (Test 9): `analyzeBusinessProcess(businessProcessParentAllExcludedFixture).selfStatus === 'GREY'`.
    - Behavior (Test 10): `analyzeBusinessProcess(businessProcessMultiParentFixture).findings.length === 1`.
    - Existing BusinessCapability tests (all of evaluator.test.ts, not just the BusinessProcess suite) still pass unmodified — proves the `classifyCapabilityAgainstParent` generalization is behavior-preserving for the two existing call sites.
    - Test command: `cd server && yarn jest src/sovereignty/__tests__/evaluator.test.ts` exits 0.
    - Type command: `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/evaluator.test.ts</automated>
  </verify>
  <done>
    `classifyCapabilityAgainstParent` is generalized (no `classifyProcessAgainstParent` duplicate exists anywhere); `analyzeBusinessProcess` produces three-valued GREEN/YELLOW/GREY `selfStatus` against `parentProcess`; `loadBusinessProcessSupportChain` fetches real `parentRequiredLevels` from Neo4j with tenant scoping; Tests 6-11 and all pre-existing evaluator tests pass; `yarn tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CRITICAL FIX — generalize markers.ts's selfViolatingIds filter so BusinessProcess self-violations are not silently swallowed</name>
  <files>
    server/src/sovereignty/markers.ts,
    server/src/sovereignty/__tests__/markers.test.ts
  </files>
  <read_first>
    - server/src/sovereignty/markers.ts (the exact `selfViolatingIds` precompute: `new Set(analysis.findings.filter(f => f.violatingElementType === 'businessCapability').map(f => f.violatingElementId))` — the hardcoded string this task must generalize; confirm `capabilityIds` is declared via `const capabilityIds = new Set(analysis.capabilityIds)` BEFORE this line, so the fix can reference it directly with no reordering; the findings-loop `isCapabilitySelfViolation` usage and the zero-finding backfill loop's `if (selfViolatingIds.has(id)) continue` guard — both consume `selfViolatingIds` and need NO further change once the set itself is generalized)
    - server/src/sovereignty/__tests__/markers.test.ts (existing BusinessCapability YELLOW-self-violation test using `rootParentContradictionFixture`/`diamondSharedCapabilityFixture` — MUST keep passing unmodified after the fix, since `capabilityIds`-membership is a superset-compatible generalization of the old string check, not a narrowing one)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md (§ Critical Finding: `selfViolatingIds` Filter Gap in `markers.ts` — the exact bug, the exact fix (Option B recommended), and why Option B is preferred over merely adding `'businessProcess'` as a second literal to the OR-check)
  </read_first>
  <behavior>
    TDD RED→GREEN. New test in `markers.test.ts`:

    - Test (the critical regression): `projectMarkers(analyzeBusinessProcess(businessProcessParentContradictionFixture)).get(<process id>).selfStatus === 'YELLOW'`. Written and run BEFORE the fix, this test MUST fail (observe it resolve to 'GREEN' or 'GREY' instead, proving the bug is real and reproducible) — record this RED observation, then apply the fix and confirm GREEN→YELLOW flips to the correct value.
    - Existing BusinessCapability YELLOW-self-violation tests (`rootParentContradictionFixture`, `diamondSharedCapabilityFixture`'s precedence case) MUST still pass unmodified — proves the generalized filter is not narrower than the old hardcoded one.
    - New GREEN/GREY tests for BusinessProcess: `projectMarkers(analyzeBusinessProcess(businessProcessParentNoContradictionFixture)).get(<id>).selfStatus === 'GREEN'`; `projectMarkers(analyzeBusinessProcess(businessProcessParentAllExcludedFixture)).get(<id>).selfStatus === 'GREY'`.

  </behavior>
  <action>
    In `server/src/sovereignty/markers.ts`: replace the `selfViolatingIds` precompute's filter predicate from `f.violatingElementType === 'businessCapability'` to `capabilityIds.has(f.violatingElementId)` (Option B from 04-RESEARCH.md's Critical Finding — derive self-violation membership from the same `capabilityIds` set every other branch in this file already keys off, rather than an enumerated `ViolatingElementType` string allow-list that must be remembered and extended by hand every time a new requirement-root type is added). Update the surrounding doc comment (currently explains the diamond-topology full-finding-set-upfront rationale for BusinessCapability specifically) to note this now applies identically to any requirement-root type appearing in `capabilityIds` — BusinessCapability and BusinessProcess today, extensible to a future third type without another code change here.

    Do not change anything else in `markers.ts` — the findings-loop `isCapabilitySelfViolation` check, the zero-finding backfill loop, `comparedIds`, `worseStatus`, and `DEFAULT_MARKER` are all already type-agnostic (04-RESEARCH.md Pattern 3) and require no edits.

    In `server/src/sovereignty/__tests__/markers.test.ts`: add the critical regression test and the two new BusinessProcess GREEN/GREY tests described in `<behavior>`, importing `analyzeBusinessProcess` from `../evaluator` and the new BusinessProcess fixtures from `./fixtures`. Add a one-line comment on the regression test explicitly naming it as the fix for the hardcoded-string gap 04-RESEARCH.md flagged, so a future reader understands why this specific assertion exists.
  </action>
  <acceptance_criteria>
    - Source: `grep -n "violatingElementType === 'businessCapability'" server/src/sovereignty/markers.ts` returns NO match (the hardcoded literal is gone).
    - Source: `grep -n "capabilityIds.has(f.violatingElementId)" server/src/sovereignty/markers.ts` returns a match inside the `selfViolatingIds` precompute.
    - Behavior (the critical fix): `projectMarkers(analyzeBusinessProcess(businessProcessParentContradictionFixture)).get(<process id>)?.selfStatus === 'YELLOW'`.
    - Behavior (regression, unchanged): `projectMarkers(analyzeBusinessCapability(rootParentContradictionFixture)).get(<capability id>)?.selfStatus === 'YELLOW'` still passes; the diamond-topology precedence test still passes.
    - Behavior (new GREEN/GREY for BusinessProcess): both pass as described in `<behavior>`.
    - Test command: `cd server && yarn jest src/sovereignty/__tests__/markers.test.ts` exits 0.
    - Type command: `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/markers.test.ts src/sovereignty/__tests__/evaluator.test.ts</automated>
  </verify>
  <done>
    `markers.ts`'s `selfViolatingIds` filter derives from `capabilityIds` membership instead of a hardcoded `'businessCapability'` string; the critical regression test proves a BusinessProcess YELLOW self-violation now surfaces correctly through `projectMarkers`; all pre-existing BusinessCapability marker tests still pass; `yarn tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Include BusinessProcess in the company-wide sovereignty rollup (SOV-05 cross-surface consistency)</name>
  <files>
    server/src/sovereignty/companyRollup.ts,
    server/src/sovereignty/__tests__/companyRollup.test.ts
  </files>
  <read_first>
    - server/src/sovereignty/companyRollup.ts (`OwnedIds` interface — currently `capabilityIds`/`dataObjectIds` only; `loadOwnedIds`'s Cypher — `OPTIONAL MATCH (c)<-[:OWNED_BY]-(cap:BusinessCapability)` / `(obj:DataObject)` pattern to mirror for `BusinessProcess`; `analyzeCompanyRollup`'s two `for` loops over `capabilityIds`/`dataObjectIds`, each calling `loadFullSupportChain` + the matching `analyze*` function + `pushRequiredScores`/`pushAchievedScores` — the exact pattern the new BusinessProcess loop mirrors)
    - server/src/sovereignty/__tests__/companyRollup.test.ts (`OWNED_IDS` test double / mock shape keyed by companyId — confirm how `loadOwnedIds` is mocked/stubbed in this suite so the new `businessProcessIds` field can be added consistently)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md (§ Pitfall 5 / § Open Questions Question 1 — the explicit discretion call this task resolves: include BusinessProcess in the rollup for SOV-05 consistency, since 04-CONTEXT.md's canonical-refs never mention `companyRollup.ts` and the phase's own "sibling requirement root, evaluated entirely on its own" framing implies parallel, not excluded, treatment)
  </read_first>
  <behavior>
    - Test A: `loadOwnedIds` (or its equivalent Cypher shape) now also returns `businessProcessIds` — a company with 2 owned BusinessProcesses returns both ids.
    - Test B: `analyzeCompanyRollup` for a company owning one BusinessCapability (achieved chain fully compliant) and one BusinessProcess (achieved chain with a RED violation) produces an `achievedSovereigntyScore` reflecting the WORST (min) score across BOTH root types combined — not just the BusinessCapability/DataObject ones from before this task.
    - Test C: a company owning zero BusinessCapabilities/DataObjects/BusinessProcesses still returns `{ expectedSovereigntyScore: null, achievedSovereigntyScore: null, ... }` (unchanged null-when-empty behavior, now provably still holding with a third empty id array).
  </behavior>
  <action>
    In `server/src/sovereignty/companyRollup.ts`: add `businessProcessIds: readonly string[]` to the `OwnedIds` interface. Extend `loadOwnedIds`'s Cypher with `OPTIONAL MATCH (c)<-[:OWNED_BY]-(proc:BusinessProcess)` and add `collect(DISTINCT proc.id) AS businessProcessIds` to the RETURN clause (mirrors the existing `cap`/`obj` collect pattern exactly — use `collect(DISTINCT ...)` to avoid Cartesian-product row multiplication now that three optional matches run in the same query). Decode and filter it identically to `capabilityIds`/`dataObjectIds`.

    In `analyzeCompanyRollup`: destructure `businessProcessIds` from `loadOwnedIds`'s result. Add a third `for` loop mirroring the existing `capabilityIds`/`dataObjectIds` loops: `for (const businessProcessId of businessProcessIds) { const chain = await loadFullSupportChain(session, [companyId], false, 'businessProcess', businessProcessId); if (!chain) continue; pushRequiredScores(chain.required, requiredScores); if (chain.rootType === 'businessProcess') { const analysis = analyzeBusinessProcess(chain); pushAchievedScores(analysis.findings, achievedScores) } }`. Import `analyzeBusinessProcess` alongside the existing `analyzeBusinessCapability`/`analyzeDataObject` import.

    Do not change `pushRequiredScores`/`pushAchievedScores`/`MATURITY_SCORE`/the final score/gap/percent computation — all already type-agnostic over any `RequirementLevels`/`Finding[]` input.

    In `server/src/sovereignty/__tests__/companyRollup.test.ts`: extend the `OWNED_IDS` test double (or equivalent) to include `businessProcessIds` for each company keyed there. Add Tests A-C described in `<behavior>`.
  </action>
  <acceptance_criteria>
    - Source: `grep -n "businessProcessIds" server/src/sovereignty/companyRollup.ts` returns matches in both `OwnedIds` and the Cypher/decode logic.
    - Source: `grep -n "analyzeBusinessProcess" server/src/sovereignty/companyRollup.ts` returns an import match and a call-site match inside the new loop.
    - Behavior (Test B): the combined rollup's `achievedSovereigntyScore` reflects the BusinessProcess's RED finding (a lower score than if BusinessProcess were excluded).
    - Behavior (Test C): a company with zero owned entities of any of the three types still returns `null` scores.
    - Test command: `cd server && yarn jest src/sovereignty/__tests__/companyRollup.test.ts` exits 0.
    - Type command: `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/</automated>
  </verify>
  <done>
    `companyRollup.ts` includes BusinessProcess ids in its owned-entity scan and folds their achieved/required scores into the same min/max company-wide computation as BusinessCapability/DataObject; new tests A-C pass; full sovereignty suite green; `yarn tsc --noEmit` clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GraphQL request → Neo4j Cypher (new HAS_PARENT_PROCESS match) | The new parent-fetch OPTIONAL MATCH introduces a fresh opportunity for cross-tenant leakage if the company-scoping guard is omitted — must mirror `fetchBusinessCapabilityChain`'s T-02.3-04 precedent exactly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-04-04 | Information Disclosure | Cross-tenant `parentProcess` required-level leak | medium | mitigate | The new `OPTIONAL MATCH (proc)-[:HAS_PARENT_PROCESS]->(parent)-[:OWNED_BY]->(parentCompany:Company) WHERE parent IS NULL OR $isAdmin OR parentCompany.id IN $companyIds` clause is copied verbatim from `fetchBusinessCapabilityChain`'s proven-correct parent guard (T-02.3-04) — no new tenant-scoping logic invented. |
| T-04-05 | Tampering | `markers.ts` `selfViolatingIds` generalization | medium | mitigate | The fix narrows to a single, already-reviewed predicate change (`capabilityIds.has(f.violatingElementId)`); guarded by an explicit regression test proving BOTH the new BusinessProcess case AND the pre-existing BusinessCapability case resolve correctly, so a future refactor cannot silently re-narrow the check without failing a test. |
| T-04-06 | Denial of Service | Cypher Cartesian-product risk (multi-OPTIONAL-MATCH) | low | mitigate | Every new/extended Cypher query in this plan (`loadBusinessProcessSupportChain`'s parent clause, `companyRollup.ts`'s `loadOwnedIds`) uses `collect(DISTINCT ...)` on every optional-matched entity, mirroring the pattern already proven correct in `fetchBusinessCapabilityChain` (04-RESEARCH.md Pitfall 4). |

No new external dependencies, no new install commands — package legitimacy audit not applicable.
</threat_model>

<verification>
- `cd server && yarn tsc --noEmit` clean
- `cd server && yarn jest src/sovereignty/__tests__/` full sovereignty suite passes (BusinessCapability/DataObject regressions ruled out + all new BusinessProcess tests pass)
- Manual reading confirms the critical `selfViolatingIds` fix is present and the regression test explicitly proves it (not just implied by a passing suite)
- Manual reading confirms `analyzeCapabilitySubtree`/`collectOwnFindings`/`BusinessCapabilityChain` are untouched (D-03)
- Manual reading confirms no Supplier-related code was added (D-05)
</verification>

<success_criteria>

- `analyzeBusinessProcess`'s `selfStatus` is three-valued (GREEN/YELLOW/GREY) against `parentProcess`, mirroring 02.3/03's BusinessCapability contract exactly, with a structurally simpler root-only implementation (no recursive walk, no visited-set — confirmed unnecessary against 04-DISCUSSION-LOG.md).
- `classifyCapabilityAgainstParent` is generalized, not duplicated — no `classifyProcessAgainstParent` exists anywhere in the codebase after this plan.
- The critical `markers.ts` bug (hardcoded `'businessCapability'` string in `selfViolatingIds`) is fixed and explicitly regression-tested, not just implicitly covered.
- `companyRollup.ts` includes BusinessProcess for SOV-05 cross-surface consistency (agent's-discretion inclusion, documented and tested).
- Zero BusinessCapability/DataObject regressions anywhere in the full sovereignty test suite.

</success_criteria>

<output>
Create `.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-02-SUMMARY.md` when done.
</output>
</output>
