---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/sovereignty/types.ts
  - server/src/sovereignty/evaluator.ts
  - server/src/sovereignty/repository.ts
  - server/src/sovereignty/validation.ts
  - server/src/sovereignty/graphql/resolvers.ts
  - server/src/sovereignty/__tests__/fixtures.ts
  - server/src/sovereignty/__tests__/evaluator.test.ts
autonomous: true
requirements:
  - SOVX-02
  - D-01
  - D-02

must_haves:
  truths:
    - "A `BusinessProcess` is a new independent `SovereigntyRootType` ('businessProcess'), analyzed via a new `analyzeBusinessProcess` entry point that reuses `analyzeSupportChain`/`classifyNode`/`walkApplication` unchanged — no new leaf-node walker is written (D-02)."
    - "A `BusinessProcess` whose own `sovereigntyReq*` required level for a dimension exceeds the achieved level of one of its `supportedByApplications` (or their `hostedOn`/`components` subtree) produces a RED `Finding` exactly like `analyzeDataObject` would for the same achieved-chain shape."
    - "A real GraphQL `sovereigntyAnalysis(rootType: 'businessProcess', ...)`/`sovereigntyMarkers(rootType: 'businessProcess', ...)` query resolves via `loadFullSupportChain`'s new dispatch branch and `analyzeBusinessProcess`, not via a parallel code path."
    - "`sovereigntyAnalysisArgsSchema`'s `rootType` validation accepts `'businessProcess'` before any Cypher runs (T-02-05 boundary check) — an unvalidated `rootType` string never reaches `repository.ts`."
  artifacts:
    - server/src/sovereignty/types.ts (SovereigntyRootType += 'businessProcess'; new BusinessProcessChain interface extending SupportChain, achieved-chain shape only — no parentRequiredLevels yet, added in Plan 04-02)
    - server/src/sovereignty/evaluator.ts (new analyzeBusinessProcess = analyzeSupportChain(chain, 'businessProcess') one-liner, mirrors analyzeDataObject exactly)
    - server/src/sovereignty/repository.ts (new loadBusinessProcessSupportChain achieved-chain fetch mirroring loadDataObjectSupportChain; new dispatch branch in loadFullSupportChain)
    - server/src/sovereignty/validation.ts (rootTypeSchema += 'businessProcess')
    - server/src/sovereignty/graphql/resolvers.ts (3-way rootType dispatch in sovereigntyAnalysis and sovereigntyMarkers resolvers)
    - server/src/sovereignty/__tests__/fixtures.ts (businessProcessGreenFixture, businessProcessRedFixture, businessProcessGreyFixture)
    - server/src/sovereignty/__tests__/evaluator.test.ts (analyzeBusinessProcess achieved-chain suite)
  key_links:
    - "evaluator.ts's analyzeSupportChain is the ONLY classifier analyzeBusinessProcess uses — no parallel achieved-chain implementation is written, matching the codebase's repeated 'one classifier, reused everywhere' rule (RESEARCH.md Anti-Pattern 3)."
    - "repository.ts's loadFullSupportChain dispatcher is the single place resolvers.ts and companyRollup.ts already call — the new 'businessProcess' branch must be added there, not as a second entry point resolvers.ts calls directly."
    - "validation.ts's rootTypeSchema enum gate MUST include 'businessProcess' before resolvers.ts's ternary dispatch is reachable for it — otherwise sovereigntyAnalysisArgsSchema.parse(args) rejects a legitimate businessProcess request with a Zod error before the new code path is ever exercised."
---

<objective>
Stand up `BusinessProcess` as a third, independent `SovereigntyRootType` and prove its achieved-chain evaluation (own `sovereigntyReq*` vs. `supportedByApplications`, walked via the existing `walkApplication`/`hostedOn`/`components` recursion) works end-to-end — from a hand-built fixture through `analyzeBusinessProcess`, then wired to a real Neo4j Cypher fetch and the `sovereigntyAnalysis`/`sovereigntyMarkers` GraphQL resolvers. Parent-consistency (`parentProcess`/`childProcesses`, D-04), the `markers.ts` critical self-violation bug fix, and client UI wiring are out of scope for this plan — Plan 04-02 and 04-03 build on top of what this plan lands.

Purpose: BusinessProcess's achieved-chain half is a near-verbatim structural copy of `analyzeDataObject`/`loadDataObjectSupportChain` (04-RESEARCH.md Pattern 1) — proving this smallest possible slice end-to-end (fixture → evaluator → real Cypher → resolver dispatch) before adding the more novel parent-consistency logic (D-04) catches any wiring mistake on the cheapest surface area, exactly mirroring 02.3-01/02.3-02's tracer-then-expand precedent for the earlier BusinessCapability parent-consistency feature.

Output: `SovereigntyRootType` includes `'businessProcess'`; `analyzeBusinessProcess` classifies a BusinessProcess's own achieved-chain findings; `sovereigntyAnalysis(rootType: 'businessProcess', ...)` resolves against real Neo4j data via a new `loadBusinessProcessSupportChain` fetch.
</objective>

<execution_context>
Wave 1 tracer — no dependencies. Plan 04-02 (Wave 2) depends on the `SovereigntyRootType`/`BusinessProcessChain` type additions here and on the `loadBusinessProcessSupportChain`/resolver dispatch wiring as the entry points its parent-consistency extension builds on.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md
@.planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end tracer — BusinessProcess achieved-chain classification on a hand-built fixture</name>
  <files>
    server/src/sovereignty/types.ts,
    server/src/sovereignty/evaluator.ts,
    server/src/sovereignty/__tests__/fixtures.ts,
    server/src/sovereignty/__tests__/evaluator.test.ts
  </files>
  <read_first>
    - server/src/sovereignty/types.ts (current `SovereigntyRootType = 'businessCapability' | 'dataObject'`; `SupportChain` interface — `rootId`/`required`/`supportingApplications`/`supportingAIComponents`; `DataObjectChain extends SupportChain` — the exact shape `BusinessProcessChain` mirrors)
    - server/src/sovereignty/evaluator.ts (`analyzeDataObject` — the one-line `return analyzeSupportChain(chain, 'dataObject')` delegate this task's `analyzeBusinessProcess` copies verbatim; `analyzeSupportChain` — confirms `capabilityIds: [chain.rootId]`/`comparedCapabilityIds: []`/`selfStatus: 'GREY'` are already type-agnostic, no change needed there)
    - server/src/sovereignty/__tests__/fixtures.ts (`dataObjectChainFixture` — the exact literal shape to mirror; `applicationNode`/`infrastructureNode`/`requirementLevels`/`achievedLevels` helpers — reuse unchanged)
    - server/src/sovereignty/__tests__/evaluator.test.ts (existing `analyzeDataObject`/`describe` block structure and import style — mirror for the new `analyzeBusinessProcess` suite)
    - server/src/graphql/schema.graphql (`BusinessProcess` type, lines ~93-149 — confirms `sovereigntyReqStrategicAutonomy`/`Resilience`/`Security`/`Control` scalar fields exist, `supportedByApplications: [Application!]! @relationship(type: "SUPPORTS", direction: IN)` is the ONLY achieved-chain edge — there is no AIComponent relationship on BusinessProcess, so `supportingAIComponents` is always `[]`)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md (D-01, D-02 verbatim)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md (§ Architecture Patterns Pattern 1; § Code Examples "New types.ts additions")
  </read_first>
  <behavior>
    TDD RED→GREEN. Tests to add in `evaluator.test.ts` (new `describe('analyzeBusinessProcess', ...)` block):

    - Test 1 (RED): a `BusinessProcessChain` whose `required.resilience = 'HIGH'` and whose one `supportingApplications` entry achieves `resilience = 'LOW'` on its `hostedOn` Infrastructure (mirrors `redChainFixture`'s app→infra RED shape) — `analyzeBusinessProcess(chain).findings` contains exactly one `RED` finding on the infrastructure node, `downstreamStatus === 'RED'`.
    - Test 2 (GREY): a `BusinessProcessChain` with one supporting Application that has no achieved values set at all — every dimension with a required value classifies GREY (mirrors `greyChainFixture`).
    - Test 3 (GREEN): a `BusinessProcessChain` where every achieved value meets or exceeds every required value — zero findings, `downstreamStatus === 'GREEN'`.
    - Test 4 (always-GREY self, no parent yet): `analyzeBusinessProcess(chain).selfStatus === 'GREY'` for all three fixtures above (parent-consistency lands in Plan 04-02 — this task must NOT pre-empt that by asserting GREEN/YELLOW anywhere).
    - Test 5 (type-level): `analyzeBusinessProcess(chain).rootType === 'businessProcess'`; `capabilityIds` equals `[chain.rootId]`; `comparedCapabilityIds` equals `[]`.

  </behavior>
  <action>
    In `server/src/sovereignty/types.ts`: change `export type SovereigntyRootType = 'businessCapability' | 'dataObject'` to `export type SovereigntyRootType = 'businessCapability' | 'dataObject' | 'businessProcess'`. Update the type's own doc comment (currently "Requirement roots in Phase 2 scope (D-04 — BusinessProcess is out of scope)") to state BusinessProcess is now in scope per Phase 4 D-01/D-02, superseding the Phase 2 D-04 deferral. Add a new interface directly below `DataObjectChain`:

    ```
    /**
     * A BusinessProcess's own requirements plus the Applications it is
     * directly supported by (D-02 — flat, DataObject-shaped: no nested
     * childProcesses achieved-chain walk, unlike BusinessCapability's
     * childCapabilities rollup). BusinessProcess has no direct AIComponent
     * relationship in schema.graphql, so `supportingAIComponents` is always
     * `[]`. `parentRequiredLevels` (D-04 parent-consistency) is added by
     * Phase 4 Plan 04-02 — this interface stays achieved-chain-only here.
     */
    export interface BusinessProcessChain extends SupportChain {
      readonly rootType: 'businessProcess'
    }
    ```

    Do NOT add a `parentRequiredLevels` field yet — that is Plan 04-02's Task 1, landing alongside the generalized `classifyCapabilityAgainstParent`.

    In `server/src/sovereignty/evaluator.ts`: import `BusinessProcessChain` alongside the existing `DataObjectChain` import. Add, directly below `analyzeDataObject`:

    ```
    /**
     * Analyzes a BusinessProcess's achieved-chain: own requirements vs. the
     * Applications it is directly supported by (D-02). Shares
     * `analyzeSupportChain`/`classifyNode`/`walkApplication` with
     * `analyzeBusinessCapability`/`analyzeDataObject` — no new leaf-node
     * walker is needed. Parent-consistency (`parentProcess`, D-04) lands in
     * a follow-up phase-4 plan.
     */
    export function analyzeBusinessProcess(chain: BusinessProcessChain): SovereigntyAnalysis {
      return analyzeSupportChain(chain, 'businessProcess')
    }
    ```

    Do NOT modify `analyzeSupportChain`, `collectOwnFindings`, `walkApplication`, `walkInfrastructure`, or `walkAIComponent` — all are reused unchanged (D-02).

    In `server/src/sovereignty/__tests__/fixtures.ts`: add three new `BusinessProcessChain` fixtures directly below `dataObjectChainFixture`, following its exact literal shape (`rootType: 'businessProcess'`, `supportingAIComponents: []`): `businessProcessRedFixture` (mirrors `redChainFixture`'s app→infra RED shape — required `resilience: 'HIGH'`, one supporting Application achieving `resilience: 'HIGH'` but hosted on Infrastructure achieving only `LOW`), `businessProcessGreyFixture` (one supporting Application with no achieved values set, required `control: 'MEDIUM'`), `businessProcessGreenFixture` (achieved meets/exceeds required on every dimension, mirrors `greenChainFixture`). Export all three.

    In `server/src/sovereignty/__tests__/evaluator.test.ts`: add the new `describe('analyzeBusinessProcess', ...)` block with Tests 1-5, importing `analyzeBusinessProcess` from `../evaluator` and the three new fixtures from `./fixtures`.
  </action>
  <acceptance_criteria>
    - Source: `grep -n "'businessProcess'" server/src/sovereignty/types.ts` returns matches on both the `SovereigntyRootType` union and the new `BusinessProcessChain` interface's `rootType` field.
    - Source: `grep -n "^export function analyzeBusinessProcess" server/src/sovereignty/evaluator.ts` returns a match.
    - Source: `grep -n "businessProcessRedFixture\|businessProcessGreyFixture\|businessProcessGreenFixture" server/src/sovereignty/__tests__/fixtures.ts` returns three export matches.
    - Behavior (Test 1): `analyzeBusinessProcess(businessProcessRedFixture).findings` has exactly one `RED` entry; `downstreamStatus === 'RED'`.
    - Behavior (Test 3): `analyzeBusinessProcess(businessProcessGreenFixture).findings.length === 0`; `downstreamStatus === 'GREEN'`.
    - Behavior (Test 4): `selfStatus === 'GREY'` on all three fixtures.
    - Test command: `cd server && yarn jest src/sovereignty/__tests__/evaluator.test.ts` exits 0.
    - Type command: `cd server && yarn tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/evaluator.test.ts</automated>
  </verify>
  <done>
    `SovereigntyRootType` includes `'businessProcess'`; `BusinessProcessChain` interface exists (achieved-chain shape only); `analyzeBusinessProcess` delegates to `analyzeSupportChain` unchanged; three new fixtures added; all 5 new tests pass; `yarn tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire BusinessProcess achieved-chain to real Neo4j data and the GraphQL resolver dispatch</name>
  <files>
    server/src/sovereignty/repository.ts,
    server/src/sovereignty/validation.ts,
    server/src/sovereignty/graphql/resolvers.ts
  </files>
  <read_first>
    - server/src/sovereignty/repository.ts (`loadDataObjectSupportChain`, full function — the exact Cypher/decode shape `loadBusinessProcessSupportChain` mirrors; `loadFullSupportChain`'s current `if (rootType === 'businessCapability') { ... } return loadDataObjectSupportChain(...)` two-way dispatch — becomes three-way; `fetchApplication`/`createNodeCache`/`nonNullIds`/`toMaturityLevel` — reuse unchanged)
    - server/src/sovereignty/validation.ts (`rootTypeSchema = z.enum(['businessCapability', 'dataObject'])` — the exact one-line change target; its doc comment referencing "D-04" needs updating to reflect Phase 4)
    - server/src/sovereignty/graphql/resolvers.ts (both `chain.rootType === 'businessCapability' ? analyzeBusinessCapability(chain) : analyzeDataObject(chain)` ternaries — one inside `sovereigntyAnalysis`, one inside `sovereigntyMarkers` — both become three-way dispatches; the `import { analyzeBusinessCapability, analyzeDataObject } from '../evaluator'` line needs `analyzeBusinessProcess` added)
    - server/src/graphql/schema.graphql (`BusinessProcess.company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)` — same `OWNED_BY` company-scoping edge as BusinessCapability/DataObject, confirms the `$isAdmin OR c.id IN $companyIds` pattern applies unchanged)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md (§ Code Examples "BusinessProcess repository fetch" — the achieved-chain portion only; the `parentRequiredLevels`/`parentRequiredRows` half of that example is Plan 04-02's scope, not this task's)
  </read_first>
  <behavior>
    - Behavior A: `loadFullSupportChain(session, companyIds, isAdmin, 'businessProcess', rootId)` against a Neo4j graph where a `BusinessProcess` has one `SUPPORTS`-IN Application returns a `BusinessProcessChain` with `supportingApplications.length === 1` and the correct `required` scalars decoded via `toMaturityLevel`.
    - Behavior B: a `BusinessProcess` not owned by any company in `companyIds` (and caller not admin) returns `null` (mirrors the existing `WHERE $isAdmin OR c.id IN $companyIds` tenant gate).
    - Behavior C: `sovereigntyAnalysisArgsSchema.parse({ companyId, rootType: 'businessProcess', rootId })` succeeds (no Zod throw).
    - Behavior D: the `sovereigntyAnalysis` and `sovereigntyMarkers` resolvers' rootType ternaries route a `chain.rootType === 'businessProcess'` chain to `analyzeBusinessProcess`, never to `analyzeDataObject` (which would silently mis-tag `rootType` in the returned analysis, since `analyzeSupportChain` takes the label as an explicit parameter, not derived from the chain).
  </behavior>
  <action>
    In `server/src/sovereignty/repository.ts`: add a new function `loadBusinessProcessSupportChain` directly below `loadDataObjectSupportChain`, mirroring its exact structure: Cypher `MATCH (proc:BusinessProcess {id: $rootId})-[:OWNED_BY]->(c:Company) WHERE $isAdmin OR c.id IN $companyIds WITH DISTINCT proc OPTIONAL MATCH (proc)<-[:SUPPORTS]-(app:Application) RETURN proc.id AS id, proc.sovereigntyReqStrategicAutonomy AS reqStrategicAutonomy, proc.sovereigntyReqResilience AS reqResilience, proc.sovereigntyReqSecurity AS reqSecurity, proc.sovereigntyReqControl AS reqControl, collect(DISTINCT app.id) AS appIds`. Decode the row exactly like `loadDataObjectSupportChain` does (return `null` on zero records or missing `id`), build `required` via `toMaturityLevel`, fetch each `appId` via the existing `fetchApplication(session, appId, cache, inFlight)` (create a fresh `cache`/`inFlight` pair via `createNodeCache()`/`new Set()` exactly as `loadDataObjectSupportChain` does), and return `{ rootId: row.id, rootType: 'businessProcess', required, supportingApplications, supportingAIComponents: [] }` (BusinessProcess has no AIComponent relation — `supportingAIComponents` is always `[]`, never fetched).

    Update `loadFullSupportChain`'s dispatch from the current two-way `if (rootType === 'businessCapability') { return loadBusinessCapabilitySupportChain(...) } return loadDataObjectSupportChain(...)` to a three-way dispatch: add `if (rootType === 'businessProcess') { return loadBusinessProcessSupportChain(session, companyIds, isAdmin, rootId) }` before the final `dataObject` fallback (or restructure as an explicit `if/else if/else` — either is acceptable as long as all three branches are reachable). Update the function's return type union to include `BusinessProcessChain` alongside `BusinessCapabilityChain | DataObjectChain`, importing `BusinessProcessChain` from `./types`.

    In `server/src/sovereignty/validation.ts`: change `rootTypeSchema = z.enum(['businessCapability', 'dataObject'])` to `z.enum(['businessCapability', 'dataObject', 'businessProcess'])`. Update the preceding doc comment (currently describes "Phase 2 (D-04)") to note Phase 4 D-01/D-02 added `businessProcess`.

    In `server/src/sovereignty/graphql/resolvers.ts`: change the import line to `import { analyzeBusinessCapability, analyzeBusinessProcess, analyzeDataObject } from '../evaluator'`. In BOTH the `sovereigntyAnalysis` resolver and the `sovereigntyMarkers` resolver, change the ternary `chain.rootType === 'businessCapability' ? analyzeBusinessCapability(chain) : analyzeDataObject(chain)` to a three-way form, e.g. `chain.rootType === 'businessCapability' ? analyzeBusinessCapability(chain) : chain.rootType === 'businessProcess' ? analyzeBusinessProcess(chain) : analyzeDataObject(chain)`. Keep every other line in both resolver bodies (auth checks, session handling, `toGraphQLAnalysis`, `resolveMarker` mapping) untouched.

    Do NOT touch `companyRollup.ts` in this task — including BusinessProcess in the company-wide rollup is Plan 04-02's Task 3 (agent's discretion, deferred alongside the parent-consistency work it depends on for meaningful test coverage).
  </action>
  <acceptance_criteria>
    - Source: `grep -n "^async function loadBusinessProcessSupportChain" server/src/sovereignty/repository.ts` returns a match.
    - Source: `grep -n "businessProcess" server/src/sovereignty/repository.ts` returns matches in both the new function and the `loadFullSupportChain` dispatch.
    - Source: `grep -n "'businessProcess'" server/src/sovereignty/validation.ts` returns a match inside `rootTypeSchema`.
    - Source: `grep -n "analyzeBusinessProcess" server/src/sovereignty/graphql/resolvers.ts` returns at least 3 matches (the import line plus one call site in each of the two resolvers).
    - Type command: `cd server && yarn tsc --noEmit` exits 0 (proves `BusinessProcessChain`'s inclusion in `loadFullSupportChain`'s return union type-checks against every existing caller, including `companyRollup.ts` and `resolvers.ts`).
    - Test command: `cd server && yarn jest src/sovereignty/__tests__/` (full existing suite) exits 0 — no BusinessCapability/DataObject regression from the dispatch-signature widening.
  </acceptance_criteria>
  <verify>
    <automated>cd server && yarn tsc --noEmit &amp;&amp; yarn jest src/sovereignty/__tests__/</automated>
  </verify>
  <done>
    `loadBusinessProcessSupportChain` fetches a real BusinessProcess's achieved-chain from Neo4j with the same tenant-scoping guarantee as every other sovereignty fetch; `loadFullSupportChain` dispatches to it; `rootTypeSchema` accepts `'businessProcess'`; both GraphQL resolvers route a `businessProcess` chain to `analyzeBusinessProcess`; `yarn tsc --noEmit` clean; full sovereignty test suite green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GraphQL request → resolver → Neo4j Cypher | Existing boundary, unchanged in shape — `loadBusinessProcessSupportChain` reuses the identical `$isAdmin OR c.id IN $companyIds` tenant-scoping WHERE clause every other sovereignty fetch already applies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-04-01 | Tampering | Cypher injection via unvalidated `rootType` | medium | mitigate | `rootTypeSchema`'s zod enum is extended to include `'businessProcess'` and continues to reject any other string before `sovereigntyAnalysisArgsSchema.parse(args)` returns — no hand-rolled string check is introduced anywhere in the new code (T-02-05 precedent). |
| T-04-02 | Information Disclosure | Cross-tenant BusinessProcess achieved-chain leak | low | mitigate | `loadBusinessProcessSupportChain`'s Cypher reuses the exact `$isAdmin OR c.id IN $companyIds` WHERE clause on `(proc)-[:OWNED_BY]->(c:Company)` already proven correct for BusinessCapability/DataObject — no new tenant-scoping logic invented. |
| T-04-03 | Denial of Service | `supportedByApplications` walk cost | low | accept | Reuses `walkApplication`'s existing per-fetch memoization (`NodeCache`/`inFlight`) and evaluator-side visited-set cycle guard unchanged — no new unbounded traversal introduced. |

No new external dependencies, no new install commands — package legitimacy audit not applicable.
</threat_model>

<verification>
- `cd server && yarn tsc --noEmit` clean
- `cd server && yarn jest src/sovereignty/__tests__/` full sovereignty suite passes (existing BusinessCapability/DataObject tests unaffected + new `analyzeBusinessProcess` suite passes)
- Manual reading confirms `analyzeCapabilitySubtree`/`collectOwnFindings`/`classifyCapabilityAgainstParent` were NOT touched (D-03 — BusinessCapability's downstream chain stays unwired from BusinessProcess this plan)
- Manual reading confirms no Supplier-related code was added (D-05)
</verification>

<success_criteria>

- `analyzeBusinessProcess` correctly classifies a hand-built achieved-chain fixture (RED/GREY/GREEN) identically to `analyzeDataObject`'s existing behavior on the equivalent shape.
- A real `sovereigntyAnalysis(rootType: 'businessProcess', ...)` GraphQL query resolves via `loadBusinessProcessSupportChain` + `analyzeBusinessProcess`, never via `analyzeDataObject` mislabeling.
- `rootTypeSchema` accepts `'businessProcess'` at the validation boundary before any Cypher runs.
- Zero changes to `markers.ts`, `companyRollup.ts`, `BusinessCapability`'s own analysis path, or any client file — all deferred to Plan 04-02/04-03.

</success_criteria>

<output>
Create `.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-01-SUMMARY.md` when done.
</output>
</output>
