---
phase: 04
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - client/src/graphql/sovereigntyDetail.ts
  - client/src/components/sovereignty/SovereigntyProcessView.tsx
  - client/src/components/sovereignty/types.ts
  - client/src/components/sovereignty/SovereigntyEntityDialog.tsx
  - client/src/app/[lang]/sovereignty/page.tsx
  - client/src/components/diagrams/utils/sovereigntyMarkers.ts
  - client/messages/de.json
  - client/messages/en.json
autonomous: false
requirements:
  - D-01
  - D-04

must_haves:
  truths:
    - "A user can see a list of BusinessProcesses on the `/sovereignty` page with a per-process self/downstream StatusChip and a findings panel, in a new third tab alongside the existing Capabilities/Data tabs."
    - "Clicking a BusinessProcess chip or a finding's violating-element chip (when it names a BusinessProcess) opens the existing SovereigntyEntityDialog in edit mode using the already-existing BusinessProcessForm/GET_BUSINESS_PROCESSES/UPDATE_BUSINESS_PROCESS — no new form is built."
    - "A BusinessProcess diagram element on an Excalidraw canvas, with featureFlags.Sovereignty enabled, gets the same fill/ring two-marker treatment as a BusinessCapability/DataObject element — it is not silently skipped by isMarkerRoot's allow-list."
  artifacts:
    - client/src/graphql/sovereigntyDetail.ts (GET_SOVEREIGNTY_BUSINESS_PROCESSES_LIST, mirrors GET_SOVEREIGNTY_DATA_OBJECTS_LIST)
    - client/src/components/sovereignty/SovereigntyProcessView.tsx (new file, structural copy of SovereigntyDataView.tsx with rootType:'businessProcess')
    - client/src/components/sovereignty/types.ts (EntityType += 'businessprocess')
    - client/src/components/sovereignty/SovereigntyEntityDialog.tsx (new entity.type === 'businessprocess' branch)
    - client/src/app/[lang]/sovereignty/page.tsx (ViewType += 'processes', third Tab, conditional SovereigntyProcessView render)
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (DiagramElement.customData.elementType union += 'businessProcess'; isMarkerRoot allow-list += 'businessProcess')
  key_links:
    - "SovereigntyProcessView.tsx reuses GET_SOVEREIGNTY_ANALYSIS unchanged (already accepts an arbitrary rootType: String!) — only the list query and the two i18n-driven labels differ from SovereigntyDataView.tsx, per 04-RESEARCH.md Pattern 4."
    - "sovereigntyMarkers.ts's isMarkerRoot allow-list is the single gate that determines whether a BusinessProcess diagram element ever issues a sovereigntyMarkers query at all — missing this entry fails silently (no error, no marker), not loudly, so the human-verify checkpoint explicitly re-checks it on a live diagram."
    - "client/src/graphql/businessProcess.ts already exports GET_BUSINESS_PROCESSES/UPDATE_BUSINESS_PROCESS and BusinessProcessForm.tsx already exists — SovereigntyEntityDialog.tsx's new branch wires existing CRUD infrastructure, it does not create any."
---

<objective>
Give BusinessProcess the same client-side sovereignty surfaces BusinessCapability and DataObject already have: a detail-view tab on the `/sovereignty` page (list + per-item findings/status card), an entity-edit dialog branch reusing the existing `BusinessProcessForm`, and diagram-marker eligibility so a BusinessProcess element on an Excalidraw canvas renders fill/ring markers like every other requirement root. No new backend work — this plan is pure client wiring against the `analyzeBusinessProcess`/`loadBusinessProcessSupportChain`/generalized-`markers.ts` backend landed in Plans 04-01/04-02.

Purpose: 04-RESEARCH.md Pattern 4 confirms `SovereigntyDataView.tsx` is already a fully generic list+card pattern — BusinessProcess's flat, non-nested shape (D-02) matches DataObject's view shape exactly, not BusinessCapability's tree-shaped view, so the lowest-diff, most-consistent choice is a structural copy plus a third tab, not a new route or a new tree-rendering component.

Output: `/sovereignty` page has a working "Processes" tab; a BusinessProcess entity can be opened and edited from a sovereignty finding chip; BusinessProcess diagram elements participate in the marker sync pipeline.
</objective>

<execution_context>
Wave 3 — depends on Plan 04-02, which landed `analyzeBusinessProcess`'s full parent-consistency behavior and the `markers.ts` critical fix. This plan only reads GraphQL fields already exposed by the backend (`sovereigntyAnalysis`/`sovereigntyMarkers` are already untyped `rootType: String!`, no schema change needed).
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md
@.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SovereigntyProcessView + third "Processes" tab on the /sovereignty page</name>
  <files>
    client/src/graphql/sovereigntyDetail.ts,
    client/src/components/sovereignty/SovereigntyProcessView.tsx,
    client/src/components/sovereignty/types.ts,
    client/src/app/[lang]/sovereignty/page.tsx,
    client/messages/de.json,
    client/messages/en.json
  </files>
  <read_first>
    - client/src/components/sovereignty/SovereigntyDataView.tsx (full file — `DataObjectAnalysisCard`, `FindingsPanel`, `StatusChip`, `VIOLATING_ELEMENT_TYPE_MAP`, the top-level component's `GET_SOVEREIGNTY_DATA_OBJECTS_LIST` query + `rootType: 'dataObject'` + `onEntityClick({ id, type: 'dataobject' })` — the exact structure to copy)
    - client/src/graphql/sovereigntyDetail.ts (`GET_SOVEREIGNTY_DATA_OBJECTS_LIST`, `GET_SOVEREIGNTY_ANALYSIS` — the latter needs ZERO changes, already accepts `rootType: String!`)
    - client/src/components/sovereignty/types.ts (`EntityType` union — 5 members ending `'infrastructure'`)
    - client/src/app/[lang]/sovereignty/page.tsx (full file — `ViewType = 'capabilities' | 'data'`, the `<Tabs>`/`<Tab>` structure, the `activeView === 'data' && <SovereigntyDataView .../>` conditional render — the exact pattern the third tab mirrors)
    - client/messages/de.json (`sovereigntyDetail` block, lines ~4068-4090 — `capabilityView`/`dataView` label keys, `entityTypes` sub-object — the new `processView` label and `entityTypes.businessprocess` key go here)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-RESEARCH.md (§ Architecture Patterns Pattern 4 — the exact reuse-not-new-route recommendation)
  </read_first>
  <action>
    In `client/src/graphql/sovereigntyDetail.ts`: add `GET_SOVEREIGNTY_BUSINESS_PROCESSES_LIST`, mirroring `GET_SOVEREIGNTY_DATA_OBJECTS_LIST` exactly: `query GetSovereigntyBusinessProcessesList($where: BusinessProcessWhere) { businessProcesses(where: $where) { id name } }`.

    Create `client/src/components/sovereignty/SovereigntyProcessView.tsx` as a structural copy of `SovereigntyDataView.tsx`: rename `DataObjectListItem`→`BusinessProcessListItem`, `DataObjectAnalysisCard`→`BusinessProcessAnalysisCard`, `SovereigntyDataViewProps`→`SovereigntyProcessViewProps`; replace the `GET_SOVEREIGNTY_DATA_OBJECTS_LIST` import/usage with `GET_SOVEREIGNTY_BUSINESS_PROCESSES_LIST`; replace `rootType: 'dataObject'` with `rootType: 'businessProcess'` in the per-item `GET_SOVEREIGNTY_ANALYSIS` call; replace `onEntityClick({ id: dataObject.id, type: 'dataobject' })` with `type: 'businessprocess'`; replace the `data?.dataObjects ?? []` destructure with `data?.businessProcesses ?? []`; keep `FindingsPanel`/`StatusChip`/`VIOLATING_ELEMENT_TYPE_MAP`/`STATUS_COLORS` identical (these are already generic — findings from a BusinessProcess analysis reference `application`/`infrastructure`/`businessProcess` violating-element types, all already mappable). Use the `t('processView...')`-prefixed i18n keys added below instead of the `dataView...` keys `SovereigntyDataView.tsx` uses (e.g. `noCompanySelected` stays shared, but any data-object-specific copy like `noDataObjects` needs a `noBusinessProcesses` equivalent).

    In `client/src/components/sovereignty/types.ts`: add `'businessprocess'` to the `EntityType` union (6th member).

    In `client/src/app/[lang]/sovereignty/page.tsx`: change `type ViewType = 'capabilities' | 'data'` to `'capabilities' | 'data' | 'processes'`. Import `SovereigntyProcessView` from `'@/components/sovereignty/SovereigntyProcessView'`. Add a third `<Tab value="processes" label={t('processView')} />` after the existing `data` tab. Add `{activeView === 'processes' && <SovereigntyProcessView onEntityClick={setSelectedEntity} />}` alongside the existing conditional renders.

    In `client/messages/de.json` and `client/messages/en.json`'s `sovereigntyDetail` block: add `"processView": "Prozessansicht"` (de) / `"Process View"` (en) alongside the existing `capabilityView`/`dataView` keys; add `"businessprocess": "Geschäftsprozess"` (de) / `"Business Process"` (en) to the `entityTypes` sub-object; add a `"noBusinessProcesses"` key mirroring `"noDataObjects"`'s wording pattern, substituting "Geschäftsprozesse"/"Business Processes" for "Datenobjekte"/"Data Objects".
  </action>
  <verify>
    <automated>cd client && yarn tsc --noEmit</automated>
  </verify>
  <done>
    `/sovereignty` page has a working third "Processes" tab rendering `SovereigntyProcessView`, which lists BusinessProcesses and shows per-item self/downstream status + findings via the unchanged `GET_SOVEREIGNTY_ANALYSIS` query; `EntityType` includes `'businessprocess'`; new i18n keys present in both `de.json`/`en.json`; `yarn tsc --noEmit` clean in the client package.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SovereigntyEntityDialog BusinessProcess branch + diagram marker eligibility</name>
  <files>
    client/src/components/sovereignty/SovereigntyEntityDialog.tsx,
    client/src/components/diagrams/utils/sovereigntyMarkers.ts
  </files>
  <read_first>
    - client/src/components/sovereignty/SovereigntyEntityDialog.tsx (full file — the existing `application`/`dataobject`/`infrastructure`/`aicomponent`/`capability` branches: each pairs a `useLazyQuery(GET_X)` fetch triggered from the `useEffect` `switch (entity.type)`, an `entity.type === 'x'` conditional at the bottom rendering the matching `<XForm .../>`, and a `handleXSubmit` building the mutation `input` object — the exact 3-part pattern (fetch trigger, submit handler, render branch) the new BusinessProcess branch mirrors)
    - client/src/graphql/businessProcess.ts (`GET_BUSINESS_PROCESSES`, `UPDATE_BUSINESS_PROCESS` — already exist, confirm their exact field/argument shape before wiring `useLazyQuery`/`useMutation` calls)
    - client/src/components/business-processes/BusinessProcessForm.tsx (confirm its prop signature — `onSubmit`/`data`/mode — to match the existing `ApplicationForm`/`DataObjectForm` call-site shape in `SovereigntyEntityDialog.tsx`)
    - client/src/components/diagrams/utils/sovereigntyMarkers.ts (`DiagramElement.customData.elementType` union — 7 members ending `'applicationInterface'`; `isMarkerRoot` — currently `element.customData?.elementType === 'businessCapability' || element.customData?.elementType === 'dataObject'`)
    - .planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-CONTEXT.md (the-agent's-discretion note on diagram marker gating — confirms reusing the existing `featureFlags.Sovereignty` gate, no new mechanism)
  </read_first>
  <action>
    In `client/src/components/sovereignty/SovereigntyEntityDialog.tsx`: add a `useLazyQuery(GET_BUSINESS_PROCESSES)` fetch (mirrors `fetchDataObject`'s call shape) and a `useMutation(UPDATE_BUSINESS_PROCESS)` (mirrors `updateDataObject`), importing both from `@/graphql/businessProcess`. In the `useEffect`'s `switch (entity.type)`, add a `case 'businessprocess':` branch that fetches by id (mirror the `dataobject`/`infrastructure` case's exact query-variable shape — confirm from `GET_BUSINESS_PROCESSES`'s actual arguments during implementation whether it takes a `where: { id: { eq: entity.id } }` filter like DataObject or a bare `id` argument like Application/Infrastructure). Add a `handleBusinessProcessSubmit` function mirroring `handleDataObjectSubmit`'s shape (destructure form values, build an `input` object, call the mutation with `{ variables: { id: businessProcessData.id, input } }`). Add `entity.type === 'businessprocess'` to the dialog's "is data loaded" boolean check (alongside the existing `application`/`dataobject`/`aicomponent`/`infrastructure` checks). Add a final render branch: `if (entity.type === 'businessprocess') { return <BusinessProcessForm ... onSubmit={handleBusinessProcessSubmit} data={businessProcessData} ... /> }`, importing `BusinessProcessForm` from `@/components/business-processes/BusinessProcessForm`.

    In `client/src/components/diagrams/utils/sovereigntyMarkers.ts`: add `'businessProcess'` to the `DiagramElement.customData.elementType` union (8th member). Change `isMarkerRoot`'s condition from `element.customData?.elementType === 'businessCapability' || element.customData?.elementType === 'dataObject'` to also include `|| element.customData?.elementType === 'businessProcess'`.
  </action>
  <verify>
    <automated>cd client && yarn tsc --noEmit</automated>
  </verify>
  <done>
    Clicking a BusinessProcess chip anywhere in the sovereignty UI opens `SovereigntyEntityDialog` in edit mode with `BusinessProcessForm`, submitting via the already-existing `UPDATE_BUSINESS_PROCESS` mutation; `isMarkerRoot` recognizes `'businessProcess'` diagram elements; `yarn tsc --noEmit` clean in the client package.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify BusinessProcess sovereignty UI end-to-end (Processes tab, entity dialog, diagram markers)</name>
  <what-built>
    A third "Processes" tab on `/sovereignty` listing BusinessProcesses with self/downstream status chips and a findings panel; a BusinessProcess entity-edit dialog reachable from any sovereignty finding chip; BusinessProcess diagram elements now eligible for fill/ring sovereignty markers on an Excalidraw canvas (gated behind the existing `featureFlags.Sovereignty` flag).
  </what-built>
  <how-to-verify>
    1. With `featureFlags.Sovereignty` enabled for the test company, open `/sovereignty` and confirm a "Processes" tab appears alongside "Capabilities"/"Data".
    2. Click into the Processes tab: confirm at least one BusinessProcess with a configured `sovereigntyReq*` value and a supporting Application renders a self/downstream StatusChip pair and (if non-compliant) a findings panel.
    3. Click a finding's violating-element chip or the BusinessProcess's own name chip — confirm the entity dialog opens with the correct BusinessProcess data pre-filled, and a save round-trips via `UPDATE_BUSINESS_PROCESS` without error.
    4. Open a diagram containing at least one BusinessProcess element; force a resync (Ctrl+R or reload); confirm the BusinessProcess element gets an inner fill ellipse and outer ring ellipse rendered, matching a BusinessCapability element's marker treatment on the same canvas.
    5. Confirm a BusinessProcess with a `parentProcess` whose required level is stricter than its own renders a YELLOW self-fill (not GREY) on both the diagram marker and the `/sovereignty` Processes tab's StatusChip, for the exact same BusinessProcess.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during manual verification.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → GraphQL (list/analysis/mutation queries) | Existing boundary, unchanged in shape — every new query/mutation this plan wires (`GET_SOVEREIGNTY_BUSINESS_PROCESSES_LIST`, `GET_SOVEREIGNTY_ANALYSIS` with `rootType: 'businessProcess'`, `GET_BUSINESS_PROCESSES`/`UPDATE_BUSINESS_PROCESS`) already exists or reuses an already-validated resolver boundary from Plans 04-01/04-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-04-07 | Information Disclosure | `SovereigntyProcessView`'s list query | low | accept | `GET_SOVEREIGNTY_BUSINESS_PROCESSES_LIST` reuses the existing `useCompanyWhere` company-scoping filter pattern identically to `SovereigntyDataView.tsx` — no new server-side trust boundary introduced, this is a client query shape copy only. |
| T-04-08 | Tampering | `isMarkerRoot` allow-list omission | low | accept | Purely a client-side rendering-eligibility gate; omitting it fails silently (no marker rendered) rather than insecurely — no security impact, only a UX gap, which is why the human-verify checkpoint explicitly re-checks it on a live diagram. |

No new external dependencies, no new install commands — package legitimacy audit not applicable.
</threat_model>

<verification>
- `cd client && yarn tsc --noEmit` clean
- Manual human-verify checkpoint (Task 3) confirms the third tab, entity dialog, and diagram marker eligibility all work end-to-end against real data produced by Plans 04-01/04-02's backend
- Manual reading confirms no new GraphQL schema, resolver, or server-side file was touched by this plan (all backend work already landed in Plans 04-01/04-02)
</verification>

<success_criteria>

- `/sovereignty`'s third "Processes" tab lists BusinessProcesses and reuses `GET_SOVEREIGNTY_ANALYSIS` unchanged.
- A BusinessProcess is editable from any sovereignty finding chip via the already-existing `BusinessProcessForm`/`UPDATE_BUSINESS_PROCESS` — no new form or mutation was created.
- BusinessProcess diagram elements are marker-eligible (`isMarkerRoot` allow-list updated) and visibly render fill/ring markers matching a BusinessCapability element's treatment for the same YELLOW/GREEN/GREY selfStatus.
- Zero server-side files touched by this plan.

</success_criteria>

<output>
Create `.planning/phases/04-extend-sovereignty-hierarchy-checks-to-other-ea-element-type/04-03-SUMMARY.md` when done.
</output>
</output>
