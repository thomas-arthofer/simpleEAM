---
phase: quick/260731-et9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/diagrams/dialogs/LinkRelatedElementDialog.tsx
autonomous: true
requirements: []
---

<objective>
Change the default arrow type from "sharp" to "elbow" when users link related elements via the context menu "Add Related" action.

Purpose: Improve diagram readability by using elbow arrows (right-angle connectors) instead of straight sharp arrows as the default for related element connections.

Output: Users see elbow arrows by default when linking elements through the "Add Related" dialog.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.github/copilot-instructions.md

# Key codebase facts (verified by orchestrator):
# - LinkRelatedElementDialog is the ONLY active "Add Related" dialog in production use
# - AddRelatedElementsDialog.tsx and AddRelatedElementsDialogNew.tsx are dead code (not imported anywhere)
# - Service files (relatedElementsCreationService, addRelatedElementsService, multiHopRelatedElementsService) 
#   are only used by the dead dialog code, NOT by the live LinkRelatedElementDialog
# - Valid ArrowType values: 'sharp' | 'curved' | 'elbow'
</context>

<tasks>

<task type="auto">
  <name>Change default arrow type to elbow in LinkRelatedElementDialog</name>
  <files>client/src/components/diagrams/dialogs/LinkRelatedElementDialog.tsx</files>
  <action>
In LinkRelatedElementDialog.tsx at line 282, change the hardcoded default arrow type from 'sharp' to 'elbow' in the createArrowBetweenElements call.

This is the ONLY change needed — the service files (relatedElementsCreationService, addRelatedElementsService, multiHopRelatedElementsService) are dead code, only referenced by unused legacy dialogs (AddRelatedElementsDialog, AddRelatedElementsDialogNew), so they should NOT be touched.

Locate the createArrowBetweenElements call around line 282:
```
const arrowElement = createArrowBetweenElements({
  sourceElement,
  targetElement,
  arrowType: 'sharp',  // ← Change this to 'elbow'
  position: arrowPosition,
  reverseArrow: false,
  totalArrows: 1,
  arrowIndex: 0,
  arrowGap: 'medium',
})
```

Change only the arrowType value. Do NOT touch:
- The dropdown options/labels (keep "Sharp Arrow" text in UI if present)
- Any user-selectable arrow type configuration
- The dead code files (AddRelatedElementsDialog*.tsx, *Service.ts files)
  </action>
  <verify>
    <automated>
grep -n "arrowType: 'elbow'" client/src/components/diagrams/dialogs/LinkRelatedElementDialog.tsx | grep -c "line 282"
    </automated>
  </verify>
  <done>
- LinkRelatedElementDialog.tsx line 282 now has `arrowType: 'elbow'` instead of `'sharp'`
- File compiles without TypeScript errors
- No other files modified (dead code left untouched)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure UI default value change, no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ET9-01 | None | UI default value | low | accept | No security impact - cosmetic default change only |
</threat_model>

<verification>
Manual verification after deployment:
1. Open a diagram in edit mode
2. Right-click a linked element (e.g., BusinessCapability with DB backing)
3. Select "Add Related" from context menu
4. Click any related element in the dialog
5. Verify the newly created arrow uses elbow/right-angle style, not straight sharp arrow
</verification>

<success_criteria>
- Default arrow type for "Add Related" action is 'elbow'
- Change is surgical (single line in single file)
- No dead code modified
- TypeScript compilation succeeds
- User can still manually select other arrow types if desired (this task only changes the default, not available options)
</success_criteria>

<output>
Create `.planning/quick/260731-et9-default-arrow-type-in-add-related-elemen/260731-et9-SUMMARY.md` when done
</output>
