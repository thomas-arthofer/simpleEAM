"""
JWT type for Keycloak-based authentication
"""
type JWT @jwt {
  sub: String!
  preferred_username: String
  email: String
  roles: [String!]! @jwtClaim(path: "realm_access.roles")
  company_ids: [String!] @jwtClaim(path: "company_ids")
}

extend schema @authentication

"""
Business Capability – represents a business capability within Enterprise Architecture Management
"""
type BusinessCapability
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE]
        where: {
          AND: [
            { jwt: { roles: { includes: "company-admin" } } }
            { node: { id_IN: "$jwt.company_ids" } }
          ]
        }
      }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  maturityLevel: Int
  status: CapabilityStatus!
  type: CapabilityType
  businessValue: Int
  sequenceNumber: Int
  tags: [String!]
  sovereigntyReqStrategicAutonomy: SovereigntyMaturity
  sovereigntyReqStrategicAutonomyRationale: String
  sovereigntyReqResilience: SovereigntyMaturity
  sovereigntyReqResilienceRationale: String
  sovereigntyReqSecurity: SovereigntyMaturity
  sovereigntyReqSecurityRationale: String
  sovereigntyReqControl: SovereigntyMaturity
  sovereigntyReqControlRationale: String
  sovereigntyReqWeight: Float
  introductionDate: Date
  endDate: Date
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  parents: [BusinessCapability!]! @relationship(type: "HAS_PARENT", direction: OUT)
  children: [BusinessCapability!]! @relationship(type: "HAS_PARENT", direction: IN)
  supportedByApplications: [Application!]! @relationship(type: "SUPPORTS", direction: IN)
  supportedByAIComponents: [AIComponent!]! @relationship(type: "SUPPORTS", direction: IN)
  supportedByBusinessProcesses: [BusinessProcess!]! @relationship(type: "SUPPORTS", direction: IN)
  relatedDataObjects: [DataObject!]! @relationship(type: "RELATED_TO", direction: OUT)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

"""
BusinessProcess – represents a business process within Enterprise Architecture Management
"""
type BusinessProcess
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  processType: ProcessType!
  status: ProcessStatus!
  maturityLevel: Int
  category: String
  tags: [String!]
  sovereigntyReqStrategicAutonomy: SovereigntyMaturity
  sovereigntyReqStrategicAutonomyRationale: String
  sovereigntyReqResilience: SovereigntyMaturity
  sovereigntyReqResilienceRationale: String
  sovereigntyReqSecurity: SovereigntyMaturity
  sovereigntyReqSecurityRationale: String
  sovereigntyReqControl: SovereigntyMaturity
  sovereigntyReqControlRationale: String
  sovereigntyReqWeight: Float
  bpmnXml: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  parentProcess: [BusinessProcess!]! @relationship(type: "HAS_PARENT_PROCESS", direction: OUT)
  childProcesses: [BusinessProcess!]! @relationship(type: "HAS_PARENT_PROCESS", direction: IN)
  supportsCapabilities: [BusinessCapability!]! @relationship(type: "SUPPORTS", direction: OUT)
  supportedByApplications: [Application!]! @relationship(type: "SUPPORTS", direction: IN)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

# GEA# elements (GraphQL names cannot include #, so GEA_ is used as a safe prefix)

"""
GEA_Vision – represents a vision within General Enterprise Architecture
"""
type GEA_Vision
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  visionStatement: String
  timeHorizon: String
  year: Date!
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  supportsMissions: [GEA_Mission!]!
    @relationship(type: "SUPPORTS", direction: OUT, properties: "GEARelationshipProperties")
  supportedByGoals: [GEA_Goal!]!
    @relationship(type: "OPERATIONALIZES", direction: IN, properties: "GEARelationshipProperties")
  supportedByValues: [GEA_Value!]!
    @relationship(type: "SUPPORTS", direction: IN, properties: "GEARelationshipProperties")
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
}

"""
GEA_Mission – represents a mission within General Enterprise Architecture
"""
type GEA_Mission
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  purposeStatement: String
  keywords: [String!]
  year: Date!
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  supportedByVisions: [GEA_Vision!]!
    @relationship(type: "SUPPORTS", direction: IN, properties: "GEARelationshipProperties")
  supportedByValues: [GEA_Value!]!
    @relationship(type: "SUPPORTS", direction: IN, properties: "GEARelationshipProperties")
  supportedByGoals: [GEA_Goal!]!
    @relationship(type: "SUPPORTS", direction: IN, properties: "GEARelationshipProperties")
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
}

"""
GEA_Value – represents a value within General Enterprise Architecture
"""
type GEA_Value
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  valueStatement: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  supportsMissions: [GEA_Mission!]!
    @relationship(type: "SUPPORTS", direction: OUT, properties: "GEARelationshipProperties")
  supportsVisions: [GEA_Vision!]!
    @relationship(type: "SUPPORTS", direction: OUT, properties: "GEARelationshipProperties")
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
}

"""
GEA_Goal – represents a goal within General Enterprise Architecture
"""
type GEA_Goal
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  goalStatement: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  operationalizesVisions: [GEA_Vision!]!
    @relationship(type: "OPERATIONALIZES", direction: OUT, properties: "GEARelationshipProperties")
  supportsMissions: [GEA_Mission!]!
    @relationship(type: "SUPPORTS", direction: OUT, properties: "GEARelationshipProperties")
  supportsValues: [GEA_Value!]!
    @relationship(type: "SUPPORTS", direction: OUT, properties: "GEARelationshipProperties")
  achievedByStrategies: [GEA_Strategy!]!
    @relationship(type: "ACHIEVES", direction: IN, properties: "GEARelationshipProperties")
  advancedByTransformations: [Transformation!]! @relationship(type: "ADVANCES_GOAL", direction: IN)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
}

"""
GEA_Strategy – represents a strategy within General Enterprise Architecture
"""
type GEA_Strategy
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  achievesGoals: [GEA_Goal!]!
    @relationship(type: "ACHIEVES", direction: OUT, properties: "GEARelationshipProperties")
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
}

"""
Transformation – represents a planned or ongoing transformation of the enterprise architecture
"""
type Transformation
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  status: TransformationStatus!
  targetDate: Date
  startDate: Date
  completionDate: Date
  priority: TransformationPriority
  rationale: String
  expectedOutcome: String
  tags: [String!]
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  sourceArchitecture: [Architecture!]! @relationship(type: "TRANSFORMS_FROM", direction: OUT)
  targetArchitectures: [Architecture!]! @relationship(type: "TRANSFORMS_TO", direction: OUT)
  goals: [GEA_Goal!]! @relationship(type: "ADVANCES_GOAL", direction: OUT)
  impactsCapabilities: [BusinessCapability!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
  impactsApplications: [Application!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
  impactsAIComponents: [AIComponent!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
  impactsDataObjects: [DataObject!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
  impactsInterfaces: [ApplicationInterface!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
  impactsInfrastructure: [Infrastructure!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
  impactsBusinessProcesses: [BusinessProcess!]!
    @relationship(type: "IMPACTS", direction: OUT, properties: "TransformationImpactProperties")
}

"""
AiRun – orchestration and review state for AI generated drafts
"""
type AiRun @node {
  id: ID! @id
  companyId: String!
  prompt: String!
  objective: String
  useCase: String!
  status: String!
  approvalStatus: String
  workflowId: String
  initiatedBy: String
  resultSummary: String
  statusMessage: String
  draftPayload: String
  errorMessage: String
  approvedBy: String
  approvedAt: DateTime
  rejectedBy: String
  rejectedAt: DateTime
  startedAt: DateTime
  completedAt: DateTime
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  auditEvents: [AiRunAuditEvent!]! @relationship(type: "FOR_RUN", direction: IN)
}

"""
AiRunAuditEvent – immutable audit trail entries for AI run approval actions
"""
type AiRunAuditEvent @node {
  id: ID! @id
  runId: String!
  action: String!
  actor: String!
  comment: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  run: [AiRun!]! @relationship(type: "FOR_RUN", direction: OUT)
}

"""
AgentConfig – persisted configuration for backend agents, managed globally by administrators
"""
type AgentConfig
  @node
  @authorization(
    validate: [
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
  ) {
  id: ID! @id
  agentKey: String!
  displayName: String!
  description: String
  systemPrompt: String!
  temperature: Float
  topP: Float
  maxTokens: Int
  isEnabled: Boolean!
  configVersion: Int!
  metadataJson: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
}

"""
GEA relationship properties for relationships between Vision, Mission, Value, Goal and Strategy.
Score range and meaning:
+3 Strong support
+2 Support
+1 Weak support / resonance
 0 Neutral
-1 Slight contradiction
-2 Significant conflict
-3 Direct contradiction
"""
type GEARelationshipProperties @relationshipProperties {
  score: Int!
}

"""
Transformation impact properties for relationships between transformations and impacted elements
"""
type TransformationImpactProperties @relationshipProperties {
  action: TransformationImpactAction!
  notes: String
}

"""
Person – represents an individual within the organization
"""
type Person
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { companies: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
      { operations: [CREATE, UPDATE], where: { node: { email: { eq: "$jwt.email" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { companies: { some: { id_IN: "$jwt.company_ids" } } } }
            { node: { email: { eq: "$jwt.email" } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  firstName: String!
  lastName: String!
  email: String
  department: String
  role: String
  phone: String
  avatarUrl: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  companies: [Company!]! @relationship(type: "EMPLOYED_BY", direction: OUT)
  ownedCapabilities: [BusinessCapability!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedApplications: [Application!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedDataObjects: [DataObject!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedArchitectures: [Architecture!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedDiagrams: [Diagram!]! @relationship(type: "CREATED_BY", direction: IN)
  ownedInfrastructure: [Infrastructure!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedInterfaces: [ApplicationInterface!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedAIComponents: [AIComponent!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedBusinessProcesses: [BusinessProcess!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAVisions: [GEA_Vision!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAMissions: [GEA_Mission!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAValues: [GEA_Value!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAGoals: [GEA_Goal!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAStrategies: [GEA_Strategy!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedTransformations: [Transformation!]! @relationship(type: "OWNED_BY", direction: IN)
  createdAnalyticsReports: [AnalyticsReport!]! @relationship(type: "CREATED_BY", direction: IN)
  createdAnalyticsReportFolders: [ReportFolder!]! @relationship(type: "CREATED_BY", direction: IN)
}

"""
ReportFolder - organizes analytics reports within a company
"""
type ReportFolder
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
            { node: { createdBy: { some: { email: { eq: "$jwt.email" } } } } }
          ]
        }
      }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  createdBy: [Person!]! @relationship(type: "CREATED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  parent: [ReportFolder!]! @relationship(type: "HAS_CHILD_FOLDER", direction: IN)
  children: [ReportFolder!]! @relationship(type: "HAS_CHILD_FOLDER", direction: OUT)
  reports: [AnalyticsReport!]! @relationship(type: "IN_FOLDER", direction: IN)
}

"""
AnalyticsReport - represents a saved analytics report definition
"""
type AnalyticsReport
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
            { node: { createdBy: { some: { email: { eq: "$jwt.email" } } } } }
          ]
        }
      }
    ]
    filter: [
      {
        where: {
          OR: [
            {
              AND: [{ jwt: { roles: { includes: "admin" } } }, { node: { isPublic: { eq: true } } }]
            }
            {
              AND: [
                { jwt: { roles: { includes: "admin" } } }
                { node: { createdBy: { some: { email: { eq: "$jwt.email" } } } } }
              ]
            }
            {
              AND: [
                { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
                { node: { isPublic: { eq: true } } }
              ]
            }
            {
              AND: [
                { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
                { node: { createdBy: { some: { email: { eq: "$jwt.email" } } } } }
              ]
            }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  isPublic: Boolean!
  elementType: String!
  chartType: String!
  dimension: String!
  secondDimension: String
  measure: String!
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  createdBy: [Person!]! @relationship(type: "CREATED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  folder: [ReportFolder!]! @relationship(type: "IN_FOLDER", direction: OUT)
}

"""
Supplier – represents a supplier or vendor providing products or services
"""
type Supplier
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  supplierType: SupplierType!
  status: SupplierStatus!
  address: String
  phone: String
  email: String
  website: String
  primaryContactPerson: String
  contractStartDate: Date
  contractEndDate: Date
  annualSpend: Float
  riskClassification: RiskClassification
  strategicImportance: StrategicImportance
  performanceRating: Int
  complianceCertifications: [String!]
  sovereigntyAchStrategicAutonomy: SovereigntyMaturity
  sovereigntyAchStrategicAutonomyEvidence: String
  sovereigntyAchResilience: SovereigntyMaturity
  sovereigntyAchResilienceEvidence: String
  sovereigntyAchSecurity: SovereigntyMaturity
  sovereigntyAchSecurityEvidence: String
  sovereigntyAchControl: SovereigntyMaturity
  sovereigntyAchControlEvidence: String
  lastSovereigntyAssessmentAt: DateTime
  tags: [String!]
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  providesApplications: [Application!]! @relationship(type: "PROVIDED_BY", direction: IN)
  supportsApplications: [Application!]! @relationship(type: "SUPPORTED_BY", direction: IN)
  maintainsApplications: [Application!]! @relationship(type: "MAINTAINED_BY", direction: IN)
  providesAIComponents: [AIComponent!]! @relationship(type: "PROVIDED_BY", direction: IN)
  supportsAIComponents: [AIComponent!]! @relationship(type: "SUPPORTED_BY", direction: IN)
  maintainsAIComponents: [AIComponent!]! @relationship(type: "MAINTAINED_BY", direction: IN)
  providesInfrastructure: [Infrastructure!]! @relationship(type: "PROVIDED_BY", direction: IN)
  hostsInfrastructure: [Infrastructure!]! @relationship(type: "HOSTED_BY", direction: IN)
  maintainsInfrastructure: [Infrastructure!]! @relationship(type: "MAINTAINED_BY", direction: IN)
  developsSoftwareProducts: [SoftwareProduct!]! @relationship(type: "DEVELOPED_BY", direction: IN)
  providesSoftwareProducts: [SoftwareProduct!]! @relationship(type: "PROVIDED_BY", direction: IN)
  maintainsSoftwareProducts: [SoftwareProduct!]! @relationship(type: "MAINTAINED_BY", direction: IN)
  manufacturesHardwareProducts: [HardwareProduct!]!
    @relationship(type: "MANUFACTURED_BY", direction: IN)
  providesHardwareProducts: [HardwareProduct!]! @relationship(type: "PROVIDED_BY", direction: IN)
  maintainsHardwareProducts: [HardwareProduct!]! @relationship(type: "MAINTAINED_BY", direction: IN)
}

"""
Application – represents a business application within Enterprise Architecture Management
"""
type Application
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  status: ApplicationStatus!
  criticality: CriticalityLevel!
  timeCategory: TimeCategory
  sevenRStrategy: SevenRStrategy
  technologyStack: [String!]
  version: String
  hostingEnvironment: String
  vendor: String
  sbom: String
  costs: Float
  sovereigntyAchStrategicAutonomy: SovereigntyMaturity
  sovereigntyAchStrategicAutonomyEvidence: String
  sovereigntyAchResilience: SovereigntyMaturity
  sovereigntyAchResilienceEvidence: String
  sovereigntyAchSecurity: SovereigntyMaturity
  sovereigntyAchSecurityEvidence: String
  sovereigntyAchControl: SovereigntyMaturity
  sovereigntyAchControlEvidence: String
  lastSovereigntyAssessmentAt: DateTime
  introductionDate: Date
  endOfLifeDate: Date
  planningDate: Date
  endOfUseDate: Date
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  parents: [Application!]! @relationship(type: "HAS_PARENT_APPLICATION", direction: OUT)
  components: [Application!]! @relationship(type: "HAS_PARENT_APPLICATION", direction: IN)
  predecessors: [Application!]! @relationship(type: "SUCCESSOR_OF", direction: OUT)
  successors: [Application!]! @relationship(type: "SUCCESSOR_OF", direction: IN)
  supportsCapabilities: [BusinessCapability!]! @relationship(type: "SUPPORTS", direction: OUT)
  supportsBusinessProcesses: [BusinessProcess!]! @relationship(type: "SUPPORTS", direction: OUT)
  usesDataObjects: [DataObject!]! @relationship(type: "USES", direction: OUT)
  isDataSourceFor: [DataObject!]! @relationship(type: "DATA_SOURCE", direction: IN)
  sourceOfInterfaces: [ApplicationInterface!]!
    @relationship(type: "INTERFACE_SOURCE", direction: OUT)
  targetOfInterfaces: [ApplicationInterface!]!
    @relationship(type: "INTERFACE_TARGET", direction: OUT)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  implementsPrinciples: [ArchitecturePrinciple!]!
    @relationship(type: "IMPLEMENTS_PRINCIPLE", direction: OUT)
  hostedOn: [Infrastructure!]! @relationship(type: "HOSTED_ON", direction: OUT)
  usesSoftwareProducts: [SoftwareProduct!]!
    @relationship(type: "USES_SOFTWARE_PRODUCT", direction: OUT)
  softwareVersions: [SoftwareVersion!]! @relationship(type: "USES_SOFTWARE_VERSION", direction: OUT)
  usesAIComponents: [AIComponent!]! @relationship(type: "USED_BY", direction: OUT)
  providedBy: [Supplier!]! @relationship(type: "PROVIDED_BY", direction: OUT)
  supportedBy: [Supplier!]! @relationship(type: "SUPPORTED_BY", direction: OUT)
  maintainedBy: [Supplier!]! @relationship(type: "MAINTAINED_BY", direction: OUT)
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

"""
ApplicationInterface – represents an interface between applications
"""
type ApplicationInterface
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  interfaceType: InterfaceType!
  protocol: InterfaceProtocol
  version: String
  status: InterfaceStatus!
  introductionDate: Date
  endOfLifeDate: Date
  planningDate: Date
  endOfUseDate: Date
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  predecessors: [ApplicationInterface!]!
    @relationship(type: "SUCCESSOR_OF_INTERFACE", direction: OUT)
  successors: [ApplicationInterface!]! @relationship(type: "SUCCESSOR_OF_INTERFACE", direction: IN)
  sourceApplications: [Application!]! @relationship(type: "INTERFACE_SOURCE", direction: IN)
  targetApplications: [Application!]! @relationship(type: "INTERFACE_TARGET", direction: IN)
  dataObjects: [DataObject!]! @relationship(type: "TRANSFERS", direction: OUT)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

"""
DataObjectRelationship – properties for relationships between DataObjects
"""
type DataObjectRelationship @relationshipProperties {
  name: String!
}

"""
DataObject – represents a business data object within Enterprise Architecture Management
"""
type DataObject
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  classification: DataClassification!
  format: String
  sovereigntyReqStrategicAutonomy: SovereigntyMaturity
  sovereigntyReqStrategicAutonomyRationale: String
  sovereigntyReqResilience: SovereigntyMaturity
  sovereigntyReqResilienceRationale: String
  sovereigntyReqSecurity: SovereigntyMaturity
  sovereigntyReqSecurityRationale: String
  sovereigntyReqControl: SovereigntyMaturity
  sovereigntyReqControlRationale: String
  sovereigntyReqWeight: Float
  introductionDate: Date
  endOfLifeDate: Date
  planningDate: Date
  endOfUseDate: Date
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  dataSources: [Application!]! @relationship(type: "DATA_SOURCE", direction: OUT)
  usedByApplications: [Application!]! @relationship(type: "USES", direction: IN)
  relatedToCapabilities: [BusinessCapability!]! @relationship(type: "RELATED_TO", direction: IN)
  transferredInInterfaces: [ApplicationInterface!]! @relationship(type: "TRANSFERS", direction: IN)
  usedForTrainingAI: [AIComponent!]! @relationship(type: "TRAINED_WITH", direction: IN)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  relatedDataObjects: [DataObject!]!
    @relationship(
      type: "RELATED_TO_DATA_OBJECT"
      direction: OUT
      properties: "DataObjectRelationship"
    )
  inverseRelatedDataObjects: [DataObject!]!
    @relationship(
      type: "RELATED_TO_DATA_OBJECT"
      direction: IN
      properties: "DataObjectRelationship"
    )
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

"""
Infrastructure – represents an infrastructure component within Enterprise Architecture Management
"""
type Infrastructure
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  infrastructureType: InfrastructureType!
  status: InfrastructureStatus!
  vendor: String
  version: String
  capacity: String
  location: String
  ipAddress: String
  operatingSystem: String
  specifications: String
  maintenanceWindow: String
  costs: Float
  sovereigntyAchStrategicAutonomy: SovereigntyMaturity
  sovereigntyAchStrategicAutonomyEvidence: String
  sovereigntyAchResilience: SovereigntyMaturity
  sovereigntyAchResilienceEvidence: String
  sovereigntyAchSecurity: SovereigntyMaturity
  sovereigntyAchSecurityEvidence: String
  sovereigntyAchControl: SovereigntyMaturity
  sovereigntyAchControlEvidence: String
  lastSovereigntyAssessmentAt: DateTime
  introductionDate: Date
  endOfLifeDate: Date
  planningDate: Date
  endOfUseDate: Date
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  parentInfrastructure: [Infrastructure!]!
    @relationship(type: "HAS_PARENT_INFRASTRUCTURE", direction: OUT)
  childInfrastructures: [Infrastructure!]!
    @relationship(type: "HAS_PARENT_INFRASTRUCTURE", direction: IN)
  hostsApplications: [Application!]! @relationship(type: "HOSTED_ON", direction: IN)
  hostsAIComponents: [AIComponent!]! @relationship(type: "HOSTED_ON", direction: IN)
  usesSoftwareProducts: [SoftwareProduct!]!
    @relationship(type: "USES_SOFTWARE_PRODUCT", direction: OUT)
  usesHardwareProducts: [HardwareProduct!]!
    @relationship(type: "USES_HARDWARE_PRODUCT", direction: OUT)
  softwareVersions: [SoftwareVersion!]! @relationship(type: "USES_SOFTWARE_VERSION", direction: OUT)
  hardwareVersions: [HardwareVersion!]! @relationship(type: "USES_HARDWARE_VERSION", direction: OUT)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  providedBy: [Supplier!]! @relationship(type: "PROVIDED_BY", direction: OUT)
  hostedBy: [Supplier!]! @relationship(type: "HOSTED_BY", direction: OUT)
  maintainedBy: [Supplier!]! @relationship(type: "MAINTAINED_BY", direction: OUT)
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

"""
SoftwareProduct – normalized software product entity for technology lifecycle management
"""
type SoftwareProduct
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  productFamily: [ProductFamily!]! @relationship(type: "HAS_PRODUCT_FAMILY", direction: OUT)
  lifecycleStatus: LifecycleStatus
  isActive: Boolean
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByApplications: [Application!]! @relationship(type: "USES_SOFTWARE_PRODUCT", direction: IN)
  usedByInfrastructure: [Infrastructure!]!
    @relationship(type: "USES_SOFTWARE_PRODUCT", direction: IN)
  versions: [SoftwareVersion!]! @relationship(type: "HAS_SOFTWARE_VERSION", direction: OUT)
  developedBy: [Supplier!]! @relationship(type: "DEVELOPED_BY", direction: OUT)
  providedBy: [Supplier!]! @relationship(type: "PROVIDED_BY", direction: OUT)
  maintainedBy: [Supplier!]! @relationship(type: "MAINTAINED_BY", direction: OUT)
}

"""
SoftwareVersion – version metadata for a software product
"""
type SoftwareVersion
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  version: String
  releaseChannel: String
  isLts: Boolean
  supportTier: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  softwareProduct: [SoftwareProduct!]! @relationship(type: "HAS_SOFTWARE_VERSION", direction: IN)
  lifecycleRecords: [LifecycleRecord!]! @relationship(type: "HAS_LIFECYCLE_RECORD", direction: OUT)
  usedByApplications: [Application!]! @relationship(type: "USES_SOFTWARE_VERSION", direction: IN)
  usedByInfrastructure: [Infrastructure!]!
    @relationship(type: "USES_SOFTWARE_VERSION", direction: IN)
  sbomDocuments: [SbomDocument!]! @relationship(type: "HAS_SBOM", direction: OUT)
}

"""
HardwareProduct – normalized hardware product entity for technology lifecycle management
"""
type HardwareProduct
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  productFamily: [ProductFamily!]! @relationship(type: "HAS_PRODUCT_FAMILY", direction: OUT)
  lifecycleStatus: LifecycleStatus
  isActive: Boolean
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByInfrastructure: [Infrastructure!]!
    @relationship(type: "USES_HARDWARE_PRODUCT", direction: IN)
  versions: [HardwareVersion!]! @relationship(type: "HAS_HARDWARE_VERSION", direction: OUT)
  manufacturedBy: [Supplier!]! @relationship(type: "MANUFACTURED_BY", direction: OUT)
  providedBy: [Supplier!]! @relationship(type: "PROVIDED_BY", direction: OUT)
  maintainedBy: [Supplier!]! @relationship(type: "MAINTAINED_BY", direction: OUT)
}

"""
HardwareVersion – version/model metadata for a hardware product
"""
type HardwareVersion
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  version: String
  releaseChannel: String
  supportTier: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  hardwareProduct: [HardwareProduct!]! @relationship(type: "HAS_HARDWARE_VERSION", direction: IN)
  lifecycleRecords: [LifecycleRecord!]! @relationship(type: "HAS_LIFECYCLE_RECORD", direction: OUT)
  usedByInfrastructure: [Infrastructure!]!
    @relationship(type: "USES_HARDWARE_VERSION", direction: IN)
}

"""
ProductFamily – shared family taxonomy for software and hardware products
"""
type ProductFamily
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [UPDATE, DELETE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { jwt: { roles: { includes: "architect" } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  category: ProductFamilyCategory!
  type: ProductFamilyType!
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  softwareProducts: [SoftwareProduct!]! @relationship(type: "HAS_PRODUCT_FAMILY", direction: IN)
  hardwareProducts: [HardwareProduct!]! @relationship(type: "HAS_PRODUCT_FAMILY", direction: IN)
}

"""
SbomDocument – Software Bill of Materials evidence document
"""
type SbomDocument
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  format: String
  version: String
  source: String
  sourceUrl: String
  generatedAt: DateTime
  tool: String
  digest: String
  storageReference: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  softwareVersions: [SoftwareVersion!]! @relationship(type: "HAS_SBOM", direction: IN)
}

"""
LifecycleRecord – lifecycle metadata for software/hardware versions
"""
type LifecycleRecord
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  gaDate: Date
  mainstreamSupportEndDate: Date
  extendedSupportEndDate: Date
  eosDate: Date
  eolDate: Date
  lifecycleStatus: LifecycleStatus
  source: String
  sourceUrl: String
  sourceConfidence: Float
  lastValidatedAt: DateTime
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  softwareVersions: [SoftwareVersion!]! @relationship(type: "HAS_LIFECYCLE_RECORD", direction: IN)
  hardwareVersions: [HardwareVersion!]! @relationship(type: "HAS_LIFECYCLE_RECORD", direction: IN)
}

"""
AIComponent – represents an AI component within Enterprise Architecture Management
"""
type AIComponent
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  aiType: AIComponentType!
  model: String
  version: String
  status: AIComponentStatus!
  accuracy: Float
  trainingDate: Date
  lastUpdated: Date
  provider: String
  license: String
  costs: Float
  sovereigntyAchStrategicAutonomy: SovereigntyMaturity
  sovereigntyAchStrategicAutonomyEvidence: String
  sovereigntyAchResilience: SovereigntyMaturity
  sovereigntyAchResilienceEvidence: String
  sovereigntyAchSecurity: SovereigntyMaturity
  sovereigntyAchSecurityEvidence: String
  sovereigntyAchControl: SovereigntyMaturity
  sovereigntyAchControlEvidence: String
  lastSovereigntyAssessmentAt: DateTime
  tags: [String!]
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  usedByOrganisations: [Organisation!]! @relationship(type: "USED_BY", direction: OUT)
  trainedWithDataObjects: [DataObject!]! @relationship(type: "TRAINED_WITH", direction: OUT)
  usedByApplications: [Application!]! @relationship(type: "USED_BY", direction: IN)
  supportsCapabilities: [BusinessCapability!]! @relationship(type: "SUPPORTS", direction: OUT)
  implementsPrinciples: [ArchitecturePrinciple!]!
    @relationship(type: "IMPLEMENTS_PRINCIPLE", direction: OUT)
  hostedOn: [Infrastructure!]! @relationship(type: "HOSTED_ON", direction: OUT)
  providedBy: [Supplier!]! @relationship(type: "PROVIDED_BY", direction: OUT)
  supportedBy: [Supplier!]! @relationship(type: "SUPPORTED_BY", direction: OUT)
  maintainedBy: [Supplier!]! @relationship(type: "MAINTAINED_BY", direction: OUT)
  partOfArchitectures: [Architecture!]! @relationship(type: "CONTAINS", direction: IN)
  depictedInDiagrams: [Diagram!]! @relationship(type: "DEPICTED_IN", direction: OUT)
  impactedByTransformations: [Transformation!]!
    @relationship(type: "IMPACTS", direction: IN, properties: "TransformationImpactProperties")
}

"""
Company – represents an organization within Enterprise Architecture Management
"""
type Company
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { id_IN: "$jwt.company_ids" } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [{ jwt: { roles: { includes: "admin" } } }, { node: { id_IN: "$jwt.company_ids" } }]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  address: String
  website: String
  industry: String
  size: CompanySize
  primaryColor: String
  secondaryColor: String
  font: String
  diagramFont: String
  logo: String
  features: String
  llmUrl: String
  llmModel: String
  llmKey: String
  strategicAutonomyPriority: Int
  resiliencePriority: Int
  securityPriority: Int
  controlPriority: Int
  expectedSovereigntyScore: Float
  achievedSovereigntyScore: Float
  sovereigntyGap: Float
  sovereigntyScorePercent: Float
  sovereigntyScoreStatus: String
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  organisations: [Organisation!]! @relationship(type: "BELONGS_TO", direction: IN)
  ownedCapabilities: [BusinessCapability!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedApplications: [Application!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedDataObjects: [DataObject!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedInterfaces: [ApplicationInterface!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedInfrastructure: [Infrastructure!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedAIComponents: [AIComponent!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedBusinessProcesses: [BusinessProcess!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAVisions: [GEA_Vision!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAMissions: [GEA_Mission!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAValues: [GEA_Value!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAGoals: [GEA_Goal!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedGEAStrategies: [GEA_Strategy!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedTransformations: [Transformation!]! @relationship(type: "OWNED_BY", direction: IN)
  employees: [Person!]! @relationship(type: "EMPLOYED_BY", direction: IN)
  ownedArchitectures: [Architecture!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedDiagrams: [Diagram!]! @relationship(type: "OWNED_BY", direction: IN)
  analyticsReports: [AnalyticsReport!]! @relationship(type: "OWNED_BY", direction: IN)
  analyticsReportFolders: [ReportFolder!]! @relationship(type: "OWNED_BY", direction: IN)
  ownedArchitecturePrinciples: [ArchitecturePrinciple!]!
    @relationship(type: "OWNED_BY", direction: IN)
  ownedSuppliers: [Supplier!]! @relationship(type: "OWNED_BY", direction: IN)
}

"""
Organisation – represents an organizational unit within the company
"""
type Organisation
  @node
  @authorization(
    validate: [
      { operations: [CREATE], where: { jwt: { roles: { includes: "architect" } } } }
      {
        operations: [UPDATE, DELETE]
        where: {
          AND: [
            { jwt: { roles: { includes: "architect" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  type: OrganisationType!
  level: Int
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "BELONGS_TO", direction: OUT)
  parentOrganisation: [Organisation!]!
    @relationship(type: "HAS_PARENT_ORGANISATION", direction: OUT)
  childOrganisations: [Organisation!]! @relationship(type: "HAS_PARENT_ORGANISATION", direction: IN)
  usedCapabilities: [BusinessCapability!]! @relationship(type: "USED_BY", direction: IN)
  usedApplications: [Application!]! @relationship(type: "USED_BY", direction: IN)
  usedDataObjects: [DataObject!]! @relationship(type: "USED_BY", direction: IN)
  usedInterfaces: [ApplicationInterface!]! @relationship(type: "USED_BY", direction: IN)
  usedInfrastructure: [Infrastructure!]! @relationship(type: "USED_BY", direction: IN)
  usedAIComponents: [AIComponent!]! @relationship(type: "USED_BY", direction: IN)
  usedBusinessProcesses: [BusinessProcess!]! @relationship(type: "USED_BY", direction: IN)
}

"""
Possible status values for a business capability
"""
enum CapabilityStatus {
  ACTIVE
  PLANNED
  RETIRED
}

"""
Types of business capabilities
"""
enum CapabilityType {
  STRATEGIC
  OPERATIONAL
  SUPPORT
}

"""
Possible status values for a business process
"""
enum ProcessStatus {
  ACTIVE
  PLANNED
  RETIRED
}

"""
Types of business processes
"""
enum ProcessType {
  CORE
  SUPPORT
  MANAGEMENT
}

"""
Possible status values for an application
"""
enum ApplicationStatus {
  ACTIVE
  IN_DEVELOPMENT
  RETIRED
}

"""
Criticality levels for applications
"""
enum CriticalityLevel {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

"""
TIME categories for strategic application classification
"""
enum TimeCategory {
  TOLERATE
  INVEST
  MIGRATE
  ELIMINATE
}

"""
7R strategies for cloud migration and application portfolio management
"""
enum SevenRStrategy {
  RETIRE
  RETAIN
  REHOST
  REPLATFORM
  REFACTOR
  REARCHITECT
  REPLACE
}

enum LifecycleStatus {
  SUPPORTED
  APPROACHING_EOS
  APPROACHING_EOL
  EOS
  EOL
  UNSUPPORTED
}

"""
Product family type distinguishing software and hardware families
"""
enum ProductFamilyType {
  HARDWARE
  SOFTWARE
}

"""
Fixed product family categories (i18n labels are resolved in the client)
"""
enum ProductFamilyCategory {
  COMPUTE_END_USER_DEVICES
  NETWORK_DEVICES
  STORAGE_DATA_INFRASTRUCTURE
  SECURITY_HARDWARE
  PERIPHERALS_OUTPUT_DEVICES
  DATA_CENTER_INFRASTRUCTURE
  OPERATING_PLATFORM_SOFTWARE
  INFRASTRUCTURE_PLATFORM_SOFTWARE
  CLOUD_PLATFORM_SERVICES
  SECURITY_SOFTWARE
  MANAGEMENT_OPERATIONS_SOFTWARE
  END_USER_COLLABORATION_SOFTWARE
  DEVELOPMENT_ENGINEERING_SOFTWARE
  BUSINESS_ENTERPRISE_APPLICATIONS
}

"""
Data classification for business data objects
"""
enum DataClassification {
  PUBLIC
  INTERNAL
  CONFIDENTIAL
  STRICTLY_CONFIDENTIAL
}

"""
Interface types for application integrations
"""
enum InterfaceType {
  API # synchrone Service-Aufrufe
  FILE # File-basierter Austausch
  DATABASE # direkter DB-Zugriff
  MESSAGE_QUEUE # asynchrone Messaging-Systeme
  EVENT_STREAM # Event-getriebene Streaming-Systeme
  BATCH # zeitgesteuerte Verarbeitungen
  UI # Mensch-zu-System Schnittstellen
  IDENTITY # AuthN/AuthZ/Directory
  INTEGRATION_PLATFORM # z.B. ESB, iPaaS
  OTHER
}

"""
Protocols used by application interfaces
"""
enum InterfaceProtocol {
  # API / Web
  REST
  SOAP
  GRAPHQL
  GRPC
  ODATA

  # Transport
  HTTP
  HTTPS

  # File Transfer
  FTP
  SFTP
  FTPS
  SCP
  NFS
  SMB
  WEBDAV

  # Messaging / Streaming
  AMQP
  JMS
  KAFKA
  MQTT

  # Mail / Directory
  SMTP
  IMAP
  POP3
  LDAP
  LDAPS

  # Database Connectivity
  JDBC
  ODBC

  # Vendor / Specific
  ORACLE
  SAP_IDOC
  EDI
  AS2

  # Security / Identity
  OAUTH2
  OPENID_CONNECT
  SAML

  OTHER
}

"""
Status values for application interfaces
"""
enum InterfaceStatus {
  ACTIVE
  IN_DEVELOPMENT
  OUT_OF_SERVICE
  PLANNED
  DEPRECATED
}

"""
Supplier types
"""
enum SupplierType {
  SOFTWARE_VENDOR
  SERVICE_PROVIDER
  CONSULTANT
  HARDWARE_VENDOR
  CLOUD_PROVIDER
}

"""
Supplier status values
"""
enum SupplierStatus {
  ACTIVE
  POTENTIAL
  INACTIVE
  BLOCKED
}

"""
Risk classification for suppliers
"""
enum RiskClassification {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

"""
Strategic importance of suppliers
"""
enum StrategicImportance {
  LOW
  MEDIUM
  HIGH
  STRATEGIC
}

"""
Sovereignty maturity level for requirement and achieved evaluations
"""
enum SovereigntyMaturity {
  NONE
  LOW
  MEDIUM
  HIGH
  VERY_HIGH
}

"""
Architecture – represents an architecture within Enterprise Architecture Management
"""
type Architecture
  @node
  @authorization(
    validate: [
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  timestamp: DateTime!
  domain: ArchitectureDomain!
  type: ArchitectureType!
  tags: [String!]
  expectedSovereigntyScore: Float
  achievedSovereigntyScore: Float
  sovereigntyGap: Float
  sovereigntyScorePercent: Float
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  containsCapabilities: [BusinessCapability!]! @relationship(type: "CONTAINS", direction: OUT)
  containsBusinessProcesses: [BusinessProcess!]! @relationship(type: "CONTAINS", direction: OUT)
  containsApplications: [Application!]! @relationship(type: "CONTAINS", direction: OUT)
  containsDataObjects: [DataObject!]! @relationship(type: "CONTAINS", direction: OUT)
  containsInterfaces: [ApplicationInterface!]! @relationship(type: "CONTAINS", direction: OUT)
  containsInfrastructure: [Infrastructure!]! @relationship(type: "CONTAINS", direction: OUT)
  containsAIComponents: [AIComponent!]! @relationship(type: "CONTAINS", direction: OUT)
  containsGEAVisions: [GEA_Vision!]! @relationship(type: "CONTAINS", direction: OUT)
  containsGEAMissions: [GEA_Mission!]! @relationship(type: "CONTAINS", direction: OUT)
  containsGEAValues: [GEA_Value!]! @relationship(type: "CONTAINS", direction: OUT)
  containsGEAGoals: [GEA_Goal!]! @relationship(type: "CONTAINS", direction: OUT)
  containsGEAStrategies: [GEA_Strategy!]! @relationship(type: "CONTAINS", direction: OUT)
  sourceForTransformations: [Transformation!]! @relationship(type: "TRANSFORMS_FROM", direction: IN)
  targetForTransformations: [Transformation!]! @relationship(type: "TRANSFORMS_TO", direction: IN)
  parentArchitecture: [Architecture!]! @relationship(type: "PART_OF", direction: OUT)
  childArchitectures: [Architecture!]! @relationship(type: "PART_OF", direction: IN)
  diagrams: [Diagram!]! @relationship(type: "BELONGS_TO", direction: IN)
  appliedPrinciples: [ArchitecturePrinciple!]!
    @relationship(type: "APPLIES_PRINCIPLE", direction: OUT)
}

"""
Diagram – represents an Excalidraw diagram
"""
type Diagram
  @node
  @authorization(
    validate: [
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  title: String!
  description: String
  diagramType: DiagramType
  diagramJson: String! # Contains the Excalidraw diagram as a JSON string
  diagramPng: String # Contains the diagram PNG image encoded as base64
  diagramPngDark: String # Contains the dark-mode diagram PNG image encoded as base64
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  architecture: [Architecture!]! @relationship(type: "BELONGS_TO", direction: OUT)
  creator: [Person!]! @relationship(type: "CREATED_BY", direction: OUT)
  containsCapabilities: [BusinessCapability!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsBusinessProcesses: [BusinessProcess!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsApplications: [Application!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsDataObjects: [DataObject!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsInterfaces: [ApplicationInterface!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsInfrastructure: [Infrastructure!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsAIComponents: [AIComponent!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsGEAVisions: [GEA_Vision!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsGEAMissions: [GEA_Mission!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsGEAValues: [GEA_Value!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsGEAGoals: [GEA_Goal!]! @relationship(type: "DEPICTED_IN", direction: IN)
  containsGEAStrategies: [GEA_Strategy!]! @relationship(type: "DEPICTED_IN", direction: IN)
}

"""
ArchitecturePrinciple – represents an architecture principle within Enterprise Architecture Management
"""
type ArchitecturePrinciple
  @node
  @authorization(
    validate: [
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "architect" } } } }
      { operations: [CREATE, UPDATE, DELETE], where: { jwt: { roles: { includes: "admin" } } } }
    ]
    filter: [
      {
        where: {
          OR: [
            { jwt: { roles: { includes: "admin" } } }
            { node: { company: { some: { id_IN: "$jwt.company_ids" } } } }
          ]
        }
      }
    ]
  ) {
  id: ID! @id
  name: String!
  description: String
  category: PrincipleCategory!
  priority: PrinciplePriority!
  rationale: String
  implications: String
  tags: [String!]
  isActive: Boolean!
  createdAt: DateTime! @timestamp(operations: [CREATE])
  updatedAt: DateTime @timestamp(operations: [UPDATE])
  company: [Company!]! @relationship(type: "OWNED_BY", direction: OUT)
  owners: [Person!]! @relationship(type: "OWNED_BY", direction: OUT)
  appliedInArchitectures: [Architecture!]! @relationship(type: "APPLIES_PRINCIPLE", direction: IN)
  implementedByApplications: [Application!]!
    @relationship(type: "IMPLEMENTS_PRINCIPLE", direction: IN)
  implementedByAIComponents: [AIComponent!]!
    @relationship(type: "IMPLEMENTS_PRINCIPLE", direction: IN)
}

"""
Architecture domains
"""
enum ArchitectureDomain {
  BUSINESS
  DATA
  APPLICATION
  TECHNOLOGY
  SECURITY
  INTEGRATION
  ENTERPRISE
}

"""
Architecture types
"""
enum ArchitectureType {
  CURRENT_STATE
  FUTURE_STATE
  TRANSITION
  CONCEPTUAL
}

"""
Transformation lifecycle status values
"""
enum TransformationStatus {
  IDEA
  OPTION
  PLANNING
  IMPLEMENTING
  DONE
}

"""
Priority values for transformations
"""
enum TransformationPriority {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

"""
Impact action values for transformation relationships
"""
enum TransformationImpactAction {
  CREATED
  MODIFIED
  REMOVED
}

"""
Diagram types for Excalidraw diagrams
"""
enum DiagramType {
  ARCHITECTURE
  APPLICATION_LANDSCAPE
  CAPABILITY_MAP
  DATA_FLOW
  PROCESS
  NETWORK
  INTEGRATION_ARCHITECTURE
  SECURITY_ARCHITECTURE
  CONCEPTUAL
  OTHER
}

"""
Categories for architecture principles
"""
enum PrincipleCategory {
  BUSINESS
  DATA
  APPLICATION
  TECHNOLOGY
  SECURITY
  INTEGRATION
  GOVERNANCE
  COMPLIANCE
  PERFORMANCE
  SCALABILITY
  RELIABILITY
  MAINTAINABILITY
  INTEROPERABILITY
  REUSABILITY
  FLEXIBILITY
  COST_OPTIMIZATION
}

"""
Priority levels for architecture principles
"""
enum PrinciplePriority {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

"""
Infrastructure types
"""
enum InfrastructureType {
  CLOUD_DATACENTER
  ON_PREMISE_DATACENTER
  KUBERNETES_CLUSTER
  VIRTUALIZATION_CLUSTER
  VIRTUAL_MACHINE
  CONTAINER_HOST
  PHYSICAL_SERVER
  IOT_GATEWAY
  IOT_PLATFORM
}

"""
Status values for infrastructure components
"""
enum InfrastructureStatus {
  ACTIVE
  INACTIVE
  MAINTENANCE
  PLANNED
  DECOMMISSIONED
  UNDER_CONSTRUCTION
}

"""
Company sizes
"""
enum CompanySize {
  STARTUP
  SMALL
  MEDIUM
  LARGE
  ENTERPRISE
  MULTINATIONAL
}

"""
Organization types
"""
enum OrganisationType {
  DIVISION
  DEPARTMENT
  TEAM
  PROJECT
  FUNCTION
  SUBSIDIARY
  BRANCH
  UNIT
}

"""
AI component types
"""
enum AIComponentType {
  MACHINE_LEARNING_MODEL
  DEEP_LEARNING_MODEL
  NATURAL_LANGUAGE_PROCESSING
  COMPUTER_VISION
  RECOMMENDATION_ENGINE
  PREDICTIVE_ANALYTICS
  CHATBOT
  VOICE_ASSISTANT
  DECISION_SUPPORT_SYSTEM
  AUTOMATION_ENGINE
  GENERATIVE_AI
  AGENTIC_AI
  RAG
  OTHER
}

"""
Status values for AI components
"""
enum AIComponentStatus {
  IN_DEVELOPMENT
  TRAINING
  TESTING
  DEPLOYED
  ACTIVE
  DEPRECATED
  RETIRED
}

"""
Canonical sovereignty analysis (server/src/sovereignty/) — additive-only extension.
Backed by a custom resolver (server/src/sovereignty/graphql/resolvers.ts), NOT by
@neo4j/graphql's generated CRUD, so it runs outside the @authorization directive
pipeline and re-implements the same JWT company/role check manually (see T-02-01
in .planning/phases/02-canonical-sovereignty-evaluation-ux-diagnostics/02-01-PLAN.md).
"""
enum SovereigntyStatus {
  RED
  YELLOW
  GREY
  GREEN
}

"""
Sovereignty evaluation dimension — mirrors SovereigntyDimension in
server/src/sovereignty/types.ts.
"""
enum SovereigntyDimensionEnum {
  STRATEGIC_AUTONOMY
  RESILIENCE
  SECURITY
  CONTROL
}

"""
A single sovereignty chain violation: names the violating element, dimension,
required/actual maturity, and the chain path from the requirement root down to
the violating element (SOV-04).
"""
type SovereigntyFinding {
  violatingElementId: ID!
  violatingElementType: String!
  violatingElementName: String!
  dimension: SovereigntyDimensionEnum!
  status: SovereigntyStatus!
  requiredLevel: SovereigntyMaturity
  actualLevel: SovereigntyMaturity
  chainPath: [String!]!
}

"""
Canonical sovereignty analysis result for a BusinessCapability or DataObject
root. `selfStatus` is always GREY for a root (D-05) — it owns no achieved
rating of its own.
"""
type SovereigntyAnalysis {
  rootId: ID!
  rootType: String!
  findings: [SovereigntyFinding!]!
  selfStatus: SovereigntyStatus!
  downstreamStatus: SovereigntyStatus!
}

type Query {
  sovereigntyAnalysis(companyId: ID!, rootType: String!, rootId: ID!): SovereigntyAnalysis!
}
